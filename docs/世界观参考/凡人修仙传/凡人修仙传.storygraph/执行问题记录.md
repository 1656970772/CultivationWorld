# StoryGraph 全量执行问题记录

执行对象：

- 小说源：`E:\AI_Projects\CultivationWorld\docs\世界观参考\凡人修仙传\凡人修仙传.txt`
- 模板目录：`E:\AI_Projects\CultivationWorld\docs\世界观参考\模板`
- Graph 目录：`E:\AI_Projects\CultivationWorld\docs\世界观参考\凡人修仙传\凡人修仙传.storygraph`

## 已记录问题

1. `execute-complete` 当前实现包含 MVP 假设：只 claim agent batches，并未真实等待或派发 agent 产物。为避免把 prepared/claimed 误报为完成，本次改用显式 `prepare-stage1`、agent 分片产物、`ingest`、`merge`、`validate` 流程。
2. 技能配置中引用 `template-requirements.schema.json`，但当前 skill 目录没有独立 `schemas/` 目录；schema 约束实际散落在 Python validation 代码中。已通过 `template_requirements.py` 确认分片必需字段。
3. 默认 chunk 策略把 `凡人修仙传.txt` 切成 483 个 chunk，Stage 1 lane extraction 默认每个 agent 处理 2 个 chunk，共 242 个 lane 批次；全量 agent 调度规模较大，后续会按配置窗口推进并记录吞吐或阻塞。

4. template requirements 分片 agent 反馈：intermediate/template-requirements-parts/ 目录在分片写入前未预创建，agent 需要自行创建父目录。当前已由分片 agent 临时创建，建议后续 prepare/dispatch 阶段预创建该目录。

5. multi-agent 已完成 agent 如果不显式 close，仍占用线程并发上限；派发 batch-0007 时出现 gent thread limit reached。当前临时处理：关闭 batch-0001 至 batch-0006 的已完成 agent 后继续派发。

6. batch-0007 worker 反馈：PowerShell 单行 Python 检查命令受引号转义干扰，需要改用更简单的 PowerShell JSON 解析或临时脚本。当前未影响产物，但增加了诊断时间。

7. template requirements refinement 的输出目录 intermediate/template-requirements-refinement/ 在 pass-1 claim 后仍未预创建；worker 需要自行创建父目录或会在写入时遇到路径不存在。

8. 首批 lane extraction worker 启动后 5 分钟内未产生 intermediate/lane-outputs 文件；正文抽取阶段明显比 template requirements/refinement 慢。当前处理：不重派同一 batch，避免晚到输出覆盖，继续等待并检查落盘状态。

9. batch-0004 worker 反馈：PowerShell/Python -c 多层引号曾被吞噬，验证脚本第一次未正确执行；临时改用环境变量传递脚本解决。该问题会拖慢批量 agent 自检。
10. batch-0004 worker 反馈：chunk 文件实际换行可能为 CRLF，而 task packet 的 source_range 使用 LF 口径；抽取时必须按 LF 口径定位，并额外确认 source_range 落在 packet 范围内且能支撑事实。

11. lane extraction 首批中部分 worker 长时间未落盘：batch-0002、0003、0005 从 13:51 左右派发后超过 20 分钟仍缺少输出。当前处理：继续等待一轮；若仍无输出，将按 skill 要求关闭旧 agent 后按同一 task packet 重派，避免晚到输出覆盖。

12. batch-0003 worker 反馈：PowerShell 行内换行传给 python -c 时触发 SyntaxError；改用 here-string 后通过。与前述引号问题同属 Windows 命令承载大段脚本不稳定。

13. batch-0008 worker 反馈：为计算 offset 临时写过系统 TEMP 脚本，随后已删除；最终产物只剩目标 lane output。建议后续沉淀一个纯内存 offset 计算命令或项目小工具，减少批量抽取时的流程摩擦。
14. batch-0008 worker 再次遇到 PowerShell/Python 单行换行转义失败，改用 python -c exec(...) 通过；该问题已重复出现。

15. batch-0007 worker 反馈：chunk-0013.txt / chunk-0014.txt 与 task packet 的 source_range 存在可观测错位。worker 为满足校验只抽取数值上落在 packet 范围内的证据，并在输出的 structured_failures / uncertainties 中记录。该问题可能影响后续证据精度，若同类边界继续出现，应升级为 chunk text 与 source_range 的 boundary inventory 检查。

16. batch-0005 worker 反馈：PowerShell 单行中文引号导致 Python 命令解析失败，临时改用 Node REPL 定位 offset；同批还发现 chunk 文本长度与 packet 范围轻微延伸，按 task packet source_range 严格裁剪证据范围。
17. batch-0002 对应 chunk-0003/0004 输出已落盘并通过主校验，但关闭 agent 时工具状态仍显示 running，随后收到 shutdown 通知；说明 subagent 完成通知可能滞后于文件落盘。当前处理：以落盘文件合同校验和 claim-agent-batches 状态为准，避免阻塞。

18. 针对多次 chunk/source_range 边界反馈，主 agent 做了轻量 boundary inventory：遍历 483 个 chunk，比较 coverage/chunk-ledger.json 的 source_range 长度与 chunk 文件 LF 归一化长度，结果 mismatches=0。结论：切块账本本身未发现系统性长度错位；此前问题更可能来自 CRLF/LF 显示口径或 agent offset 计算方式。

19. batch-0009 worker 再次反馈 PowerShell 多层中文引号导致临时脚本解析失败，改用 Node REPL 计算 offset。该问题已多次出现，建议后续将 source_range/offset 校验固定成项目脚本，而不是每个 agent 临时拼命令。
20. batch-0014 worker 再次反馈：PowerShell 单行脚本引号与 CRLF/LF 偏移口径容易误导 source_range 计算；临时处理为统一 LF normalization 后定位证据。建议后续把“chunk offset 计算前统一转换为 LF 口径”沉淀为项目抽取流程规则或工具。
21. 主 agent 追加第 20 条记录时也触发了 PowerShell 引号/中文正文参数拆分问题：Add-Content 报 “A positional parameter cannot be found that accepts argument 'chunk'”。已改用 here-string 临时处理。
22. batch-0011 worker 反馈：lane output 目标父目录原本不存在，agent 需要自行创建父目录后写入指定输出。这与前面 template requirements/refinement 输出目录未预创建属于同类问题，建议 prepare/claim 阶段预创建所有 expected output 父目录。
23. batch-0017 worker 反馈：PowerShell `python -c` 嵌套中文路径和引号时两次转义失败，临时改用 Node REPL 与 forward-slash Python 单行命令完成校验。建议考虑更具体规则：复杂校验脚本优先使用 Node REPL、here-string 或临时脚本文件，不要硬塞进一行 PowerShell。
24. batch-0016 worker 再次反馈：chunk 文件为 CRLF、task packet 为 LF 口径，需要 LF 规范化后计算偏移并反查 excerpt；PowerShell 单行 Python 校验脚本也再次受到中文/f-string 引号冲突影响，临时改用更短命令和 Node REPL。该问题已重复出现，建议沉淀固定的 LF 偏移反查脚本或项目校验工具。
25. 主 agent 尝试读取 `subagent-excute` 技能时使用了错误路径 `C:\Users\Administrator\.codex\skills\.system\subagent-excute\SKILL.md`，实际文件不在该位置；当前不影响 StoryGraph 执行，但说明技能文件路径不应靠猜测，应优先按技能清单给出的 source locator 读取。
26. batch-0018 worker 反馈：PowerShell `foreach { ... } | ConvertTo-Json` 写法在当前 shell 中两次触发 “empty pipe element” 解析错误；临时改为先收集数组再管道输出。建议后续补充 PowerShell 写法规范，避免复杂脚本在一行里管道脚本块。
27. batch-0019 worker 再次反馈：PowerShell `python -c` 混合中文、f-string 和多层引号时两次转义失败，临时改用 Node REPL 和更简单 Python 表达式绕过。该问题已多轮重复，应优先沉淀固定校验脚本/Node REPL 工作流。
28. 主 agent 独立校验发现 batch-0019 严重问题：worker 汇报 `validate_lane_output` 通过，但主校验显示 chunk-0037 和 chunk-0038 大量 `source_range_outside_chunk`。初步判断：输出使用了 chunk 内局部偏移，而 validator 需要 task packet 的绝对 source_range 口径。已按修复闭环要求启动新的 repair agent，先复现 probe 再修复这两个输出。另一个风险：`claim-agent-batches` 可能只根据 expected output 文件存在将 batch 标记 completed，不能替代 lane contract 校验。
29. 主 agent 独立校验发现 batch-0020 与 batch-0019 同类严重问题：worker 汇报通过，但 chunk-0039/0040 大量 `source_range_outside_chunk`，应为局部偏移误用。此类边界问题已重复出现，已升级为 boundary inventory：检查外部输入入口、chunk 文件读取、路径/offset 规范化、worker 校验命令、claim 状态等边界；暂停继续派发新 lane，先修复已发现错误并加强后续 prompt。
30. boundary inventory 当前结果：主 agent 对已落盘的 48 个 lane output 全量运行 `validate_lane_output(..., chunk_source_range=packet.source_range)`，仅 chunk-0039/0040 失败，错误均为 `source_range_outside_chunk`，与 batch-0019 同类。已暂停继续派发新 lane，启动新的 repair agent 修复 batch-0020，并要求后续抽取 prompt 强化“source_range 必须是小说源绝对偏移，不是 chunk 内局部偏移”。
31. batch-0022 worker 反馈：PowerShell 一行脚本嵌套 f-string 引号时被截断，第一次自定义闭合检查未跑起来；改用 Node 读文件检查后完成验证。该问题与前述复杂校验脚本承载问题同类。
32. batch-0020 repair 完成后主 agent 复验：chunk-0039/0040 的局部偏移已转换为绝对 source_range，`validate_lane_output` 通过。batch-0024 worker 反馈：PowerShell `python -c` 转义与 CRLF/LF 口径仍拖慢校验；建议将“CRLF->LF 后计算 StoryGraph source_range，并回切 excerpt 校验”固化成项目级工具或规则。
33. batch-0028 worker 反馈：task packet 的质量规则路径写作 `references/stage1-extraction-agent-quality-rules.md`，按 graph dir 相对路径查找时不存在；worker 临时改读 StoryGraph skill 源目录中的同名文件。建议后续在 prepare/graph dir 中复制 references，或在 task packet 中写入可解析的绝对 skill source 路径，避免每个 agent 重复定位。
34. batch-0025 worker 反馈：新版 prompt 的“source_range 必须是小说源绝对偏移”约束生效，`validate_lane_output(... chunk_source_range=packet.source_range)` 通过；但 PowerShell 多层引号仍两次截断验证脚本，临时改用内存 base64 脚本执行。
35. batch-0026 worker 反馈：新版绝对偏移 prompt 生效，source_range/source_location.source_range 均按 packet 起点加 LF 局部偏移计算并通过 validator；PowerShell 大段 Python 校验仍需 base64 绕过，建议封成本地固定命令。
36. batch-0030 worker 反馈：packet 指向 graph 内质量规则文件不存在，且 `CultivationWorld\scripts\storygraph.py` 不存在；validator 实际需要从 StoryGraph skill 源或真实工具路径导入执行。建议后续统一在 task packet 中提供可执行 validator/quality-rules 的绝对路径，避免各 agent 自行猜测。
37. batch-0034 worker 反馈：PowerShell 单行 Python 引号转义失败、Node REPL 变量复用导致一次无写入重试；临时处理为使用隔离作用域/更稳妥脚本形态完成校验和写入。该问题与前述“复杂校验脚本不适合硬塞进一行 PowerShell”同类，建议后续固化为固定校验脚本或约定使用 here-string/临时脚本。
38. batch-0035 worker 反馈：验证时 PowerShell 多层引号再次导致命令解析失败，同时需要避开 `python -` 在 PowerShell 5.1 下可能注入 BOM 的问题；临时改用 here-string 传给 `python -c` 完成校验。该问题已持续拖慢 lane extraction 验证效率。
39. batch-0036 至 batch-0039 在 CLI 状态中显示 running，但 `agent-dispatch-state.json` 未记录 agent_id/心跳，且主控等待多轮未见目标输出落盘；临时处理为向对应 worker 发送状态同步并要求继续完成。建议后续 claim/spawn 时把 agent_id、最近心跳、失败原因写入调度状态，便于主控判断是正常运行、卡住还是漏派。
40. batch-0036 至 batch-0039 worker 经状态同步后仍未落盘目标输出，主控关闭四个停滞 agent 并准备以新的 worker 接管同一写入范围。当前调度状态仍显示 running，说明 CLI running 状态不能单独作为真实执行依据；主控需以文件存在性和 validator 结果为最终验收。
41. batch-0036 至 batch-0039 第二轮接管 worker 在较长等待后仍无目标输出落盘，说明停滞不是单个 agent 偶发问题。临时处理：关闭第二轮接管 worker，改为单 chunk 探针并直接提供更明确的读取/写入上下文；后续需评估是否是多 agent 调度吞吐、prompt 过重或 agent 启动上下文导致的长时间无产出。
42. 单 chunk 探针 `chunk-0071` 在直接提供 packet、source_range、chunk 正文和写入路径后仍未在 3 分钟内落盘，说明停滞不只是 batch 粒度或路径查找问题，可能涉及 worker 启动、技能上下文或调度吞吐。临时处理：关闭探针 agent，下一轮改用显式 storygraph skill 入口和更短 prompt 测试。
43. 主 agent 试图用临时 Python 追加第 42 条记录时，再次因 PowerShell here-string / Python 字符串转义组合写错触发 `SyntaxError: unexpected character after line continuation character`，未写入成功。已改回 `apply_patch` 记录；这进一步证明大段中文/多层脚本不适合临时塞入一行命令。
44. batch-0037 worker 反馈：chunk 文件为 CRLF，但 task packet 的 `source_range` 使用 LF 口径；若直接按原文件字符数计算绝对偏移，会导致 range 超出 packet 范围。临时处理为先将 chunk text 规范化为 LF，再按 `packet.source_range[0] + local_offset` 生成范围，并用 validator 复验通过。该问题已多次出现，建议沉淀为固定 offset 校准工具。
45. batch-0036 worker 反馈：PowerShell here-string 喂 `python -` 触发 BOM 语法错误；另一次 sanity 脚本使用 `$pid` 变量名撞到 PowerShell 只读变量。临时处理为绕开 `python -` 并更换变量名后重跑验证。建议固定“临时脚本写文件/`python -c`/Node REPL”的安全模板，并避免使用 PowerShell 内置只读变量名。
46. batch-0039 worker 反馈：PowerShell 5.1 使用 `python -` 读取 stdin 临时脚本时再次触发 BOM 首字符语法错误；临时处理为改用 `python -c`/Node 写盘绕开，正式 JSON 经主控验证为 UTF-8 no BOM。该问题与前述 PowerShell 临时脚本承载问题同类。
47. batch-0038 worker 反馈：质量规则仍需 fallback 到 StoryGraph skill 源目录；第一次 `python -c` 验证命令被 PowerShell 换行转义绊住，后改用 here-string/短脚本完成验证。产物经主控验证通过。
48. batch-0045 worker 反馈：PowerShell 传 `python -c` 多行脚本时第一次被 `\n` 转义绊住；临时改用 PowerShell here-string 传入代码后验证通过。产物为 chunk-0089/0090，不是重复提取，主控复验通过。
49. batch-0041 worker 反馈：PowerShell 中文路径与嵌套 f-string 引号两次拖慢临时校验脚本，临时改用 Node REPL 做 offset/引用闭合检查。建议将 StoryGraph lane 输出闭合检查与 excerpt 回切检查沉淀为固定脚本，避免每批重复处理命令行转义问题。
50. batch-0051 worker 反馈：PowerShell `New-Item` 在其环境中不接受 `-LiteralPath`，且复杂 Python 一行命令被嵌套引号截断；临时用 .NET 创建目录，并用 PowerShell here-string 传校验脚本绕过。chunk-0102 仅为卷标题，按 completed 空抽取并记录 rejected candidate，主控复验通过。
51. batch-0046 worker 反馈：PowerShell here-string 直接喂 `python -` 再次触发 BOM 注入问题，且目标 lane output 父目录不存在需创建。临时改用 `python -c`/Node，并只创建必要输出目录；主控复验 chunk-0091/0092 通过。
52. batch-0048 worker 反馈：PowerShell 5.1 用 here-string 管道喂 `python -` 触发 BOM 注入，长中文片段在 PowerShell 引号中也较脆；临时改用 Node REPL 在内存中计算与写入，避免创建临时脚本文件。主控复验 chunk-0095/0096 通过。
53. batch-0049 worker 反馈：Node REPL 持久会话保留上一次偏移计算的 `fs` 绑定，导致第一次写入脚本未执行；临时确认未写文件后重置 REPL 并重跑完成。建议后续 Node REPL 生成产物前避免复用顶层 `const` 名，或先 reset/使用局部作用域。
54. batch-0047 worker 反馈：PowerShell + `python -c` 的多层引号和换行转义反复拖慢自检脚本执行，临时改用 PowerShell here-string 和字符串拼接绕开。建议沉淀固定本地校验工具，覆盖 packet 读取、lane validator、excerpt 回切、引用闭合检查。
55. batch-0052 worker 反馈：PowerShell here-string 喂给 `python -` 再次触发 BOM 问题；临时改用 PowerShell 原生定位与无 stdin 的 Python 命令处理，未写临时文件。主控复验 chunk-0103/0104 通过。
56. batch-0054 worker 反馈：目标输出父目录原先不存在，需要随目标文件创建；PowerShell `python -c` 引号截断导致检查命令重跑。临时按现有 Windows/PowerShell 规则规避；主控复验 chunk-0107/0108 通过。
57. batch-0055 worker 反馈：PowerShell 5.1 用 here-string 直接喂 `python -` 会注入 BOM，触发 `U+FEFF` 语法错误；临时改成 `python -c` 执行内存脚本绕开。主控复验 chunk-0109/0110 通过。
58. batch-0056 worker 反馈：验证时一次 PowerShell/Python 单行换行转义报错、一次 Node REPL 持久绑定名冲突；临时用 Node 块作用域和直接复查绕过。主控复验 chunk-0111/0112 通过。
59. batch-0058 worker 反馈：两个目标输出父目录一开始不存在；临时只创建目标 JSON 的必要父目录并写入指定文件。建议后续允许 lane 输出父目录缺失时创建目标父目录，避免批量抽取反复卡住。
60. batch-0059 worker 反馈：PowerShell 一行 Python 处理中文引号、感叹号和多行字符串时容易被引号解析绊住，临时改用 Node REPL 做 offset 计算和写盘。建议沉淀“证据片段查找 + 绝对 offset 校验”固定工具。
61. batch-0061 worker 反馈：早期 requirements 汇总阶段曾误写 `%TEMP%\\sg_req_summary.py` 临时脚本，后续已停止使用临时落盘；本轮图目录内只写入目标 lane 输出及必要目录。chunk-0122 中“那两个帮会”按质量规则保守处理为 uncertainty，未硬回填具体势力名。
62. batch-0060 worker 反馈：PowerShell 中 `python -c` 多行转义第一次失败，临时改用 PowerShell here-string 作为 `-c` 参数解决。主控复验 chunk-0119/0120 通过。
63. batch-0063 worker 反馈：第一次验证命令被 PowerShell 内联引号转义干扰，后来改用临时验证脚本跑通。产物本身无问题，主控复验 chunk-0125/0126 通过。
64. batch-0062 worker 反馈：PowerShell 多层引号让一次 Node 正则检查误报 excerpt mismatch，后改用 Node REPL 直接复查通过。建议复杂校验脚本避免塞进 PowerShell 单行，优先用独立校验脚本或 REPL。
65. batch-0064 worker 反馈：PowerShell/Python 多层引号对一次只读探查命令造成语法截断，临时改用 Node REPL 做 JSON/offset 处理。建议沉淀“读取 packet + 计算 LF offset + validate_lane_output + BOM 检查”的固定只读校验命令。
66. batch-0067 worker 反馈：graph dir 相对质量规则文件仍不存在，需 fallback 到 StoryGraph skill 源目录；PowerShell 引号和 Node REPL 跨次执行作用域各踩一次，临时改用 `ConvertFrom-Json` 和 `globalThis` 绑定绕过。主控复验 chunk-0133/0134 通过。
67. batch-0065 worker 反馈：PowerShell 对 `python -c` 多行脚本和中文路径/反斜杠引号组合解析反复报错，临时改用 Node `spawnSync` 直接传 Python 脚本参数绕开 shell 引号。主控复验 chunk-0129/0130 通过。
68. batch-0066 worker 反馈：图谱目录内相对质量规则不存在，需 fallback 到 StoryGraph skill 源目录；PowerShell 调 `python -c` 时多行 `\n` 转义会被当作字面字符导致 `SyntaxError`，临时改成单行 Python 命令和 Node REPL 检查绕过。主控复验 chunk-0131/0132 通过。
69. batch-0068 worker 反馈：PowerShell 下多行 `python -c` 引号容易出错，Node REPL 又会保留失败脚本的顶层变量，导致两次脚本层重跑；临时通过拆小命令和重置 REPL 处理。主控复验 chunk-0135/0136 通过。
70. batch-0070 worker 反馈：输出目录原本不存在，需要创建目标父目录；PowerShell 内联 Python 遇到中文片段和多层引号时两次截断脚本，临时改用 Node REPL 做只读 offset/闭合检查。主控复验 chunk-0139/0140 通过。
71. batch-0071 worker 反馈：PowerShell 直接把 here-string 传给 `python -c` 时会剥掉脚本里的双引号，第一次生成脚本因此 `SyntaxError` 且未落盘；临时改成把脚本放入环境变量再由 Python `exec` 执行。主控复验 chunk-0141/0142 通过。
72. batch-0072 worker 反馈：graph dir 下相对质量规则不存在，需 fallback 到 StoryGraph skill 源目录；PowerShell 单行中文引号转义报错，临时改用 Node REPL 做 offset 计算。主控复验 chunk-0143/0144 通过。
73. batch-0073 worker 反馈：两个 lane-output 父目录原本不存在，且一次性 PowerShell 生成命令触发 Windows 命令行长度限制；临时改用 Node REPL 分段生成，没有写临时脚本，只创建目标路径父目录。主控复验 chunk-0145/0146 通过。
74. batch-0069 worker 反馈：graph dir 内相对质量规则不存在，需 fallback 到 StoryGraph skill 源规则文件；两个目标输出目录原本不存在，因此只创建目标路径所需目录并写入指定 JSON。主控复验 chunk-0137/0138 通过。
75. batch-0075 worker 反馈：校验时 PowerShell here-string 通过 stdin 喂给 `python -` 触发开头 BOM，导致一次 `U+FEFF` 语法错误；临时改用 `python -c` 规避。主控复验 chunk-0149/0150 通过。
76. batch-0074 worker 反馈：graph dir 相对质量规则路径不存在，已读取 fallback 规则文件；主控复验 chunk-0147/0148 通过。
77. batch-0078 worker 反馈：PowerShell 多行 `python -c` 仍容易被换行/反斜杠截断，临时改用 Node REPL 做 offset 与 JSON 生成，并用单行 Python 做 validator。主控复验 chunk-0155/0156 通过。
78. batch-0076 worker 反馈：PowerShell/Python 多层引号导致一次只读自检命令截断，临时改用 Node REPL 完成校验。主控复验 chunk-0151/0152 通过。
79. batch-0077 worker 反馈：目标父目录原本不存在；校验时 PowerShell 多层引号导致脚本参数被剥引号，且 Node REPL 变量重声明，临时改用单引号字面量和新变量名处理。主控复验 chunk-0153/0154 通过。
80. batch-0079 worker 反馈：PowerShell 的 `python -c` 多行转义连续绊了两次，临时改成 Node 回读检查和单行 Python 校验。主控复验 chunk-0157/0158 通过。
81. batch-0081 worker 反馈：完整生成脚本直接塞进 PowerShell 时触发 Windows 命令长度限制，临时改用 Node REPL 写入 JSON，再用短 Python 校验命令验证。主控复验 chunk-0161/0162 通过。
82. batch-0080 worker 反馈：PowerShell/Python 多层引号和 Node REPL 变量持久化导致只读探查/生成脚本重跑一次，临时改用 LF 口径 Node 定位与重置 REPL 处理。主控复验 chunk-0159/0160 通过。
83. batch-0084 worker 自检覆盖官方 validator、UTF-8 no BOM、evidence excerpt 回切、source range 绝对偏移边界与引用闭合，主控复验 chunk-0167/0168 通过。
84. batch-0083 worker 反馈：一开始 offset probe 使用过 `%TEMP%` 临时脚本，后续正式生成与验证收紧到目标 JSON，不再创建辅助文件；chunk-0166 首次生成因原文细差在写盘前失败，重置后用精确 excerpt 重建并通过验证。
85. batch-0086 worker 反馈：chunk 文本实际是 CRLF，需先归一为 LF 后计算局部 offset，再加 packet 起点写入小说源绝对偏移。主控复验 chunk-0171/0172 通过。
86. batch-0082 worker 反馈：两条 evidence 起初用了“摘关键句但跳过中间句”的非连续 quote，导致 range 映射检查不干净；已改成连续原文摘录并重算 offset。建议固定检查 evidence quote 等于 source_range 映射文本。
87. batch-0085 worker 反馈：PowerShell 5.1 的 `New-Item` 不支持 `-LiteralPath`，创建目标父目录时第一次命令失败，随后用 `[System.IO.Directory]::CreateDirectory(...)` 临时处理。主控复验 chunk-0169/0170 通过。
88. batch-0087 worker 反馈：PowerShell 5.1 用 here-string 喂 `python -` 触发 BOM 导致 Python 报 `U+FEFF`，后续 `python -c` 里字面 `\n` 也踩引号问题；临时改用 Node 生成文件、Python 单行表达式校验。主控复验 chunk-0173/0174 通过。
89. batch-0088 worker 反馈：PowerShell 单行 `python -c` 在长中文字符串和引号组合下发生转义截断，临时改用 Node REPL 做 offset 定位和写入。chunk-0175 仅抽取 1 node/1 evidence 但 validator 通过，主控复验 chunk-0175/0176 通过。
90. batch-0091 worker 反馈：质量规则使用 fallback 到 StoryGraph skill 源目录；主控复验 chunk-0181/0182 通过。
91. batch-0089 worker 反馈：PowerShell 内联 `python -c` 在 f-string/引号嵌套处截断了两次只读自检命令，临时改用 Node REPL 完成同等检查。主控复验 chunk-0177/0178 通过。
92. batch-0090 worker 反馈：图谱目录内质量规则相对路径不存在，按要求读取 StoryGraph skill 源目录 fallback；主控复验 chunk-0179/0180 通过。
93. batch-0093 worker 反馈：graph dir 下相对质量规则路径不存在，按要求读取 fallback；PowerShell 跑复杂 Python 闭合检查 one-liner 时又被多层引号截断，临时改用 Node REPL 完成只读检查。主控复验 chunk-0185/0186 通过。
94. batch-0094 worker 反馈：PowerShell 的 `python -c` 多行转义导致一次语法报错，临时改用单行 Python 与 Node REPL 完成校验；另有一个临时 offset 脚本已清理。主控复验 chunk-0187/0188 通过。
95. batch-0092 worker 反馈：PowerShell 对中文弯引号和多层脚本字符串解析踩坑，offset 查询改用 Node REPL 后稳定完成。主控复验 chunk-0183/0184 通过。
96. batch-0096 worker 反馈：PowerShell here-string 直接管道给 `python -` 触发 BOM 语法错误，另一次 Python `-c` 多层引号转义导致脚本解析失败；临时改为 PowerShell 变量传给 `python -c` 和 Node REPL 计算 offset。主控复验 chunk-0191/0192 通过。
97. batch-0095 worker 反馈：第一次验证把多行 Python 直接塞进 `python -c`，PowerShell 转义导致语法错误；已改用 here-string 传参解决。主控复验 chunk-0189/0190 通过。
98. batch-0098 worker 反馈：PowerShell here-string 直接喂 `python -` 时触发 UTF-8 BOM 首字符问题，导致 Python 报 `U+FEFF`；临时改用 Node REPL 做 offset 计算和写盘。主控复验 chunk-0195/0196 通过。
99. batch-0099 worker 反馈：PowerShell 单行 Python 在长中文文本与引号组合下发生截断，临时改用 Node REPL 与 here-string 完成定位、写盘和校验。主控复验 chunk-0197/0198 通过。
100. batch-0101 worker 反馈：PowerShell 对 `foreach { ... } | ...` 和多行 `python -c` 转义较挑剔，第一次验证命令解析失败；临时改成先收集数组再管道，并使用无换行 `python -c` 完成验证。主控复验 chunk-0201/0202 通过。
101. batch-0103 worker 反馈：目标父目录原本不存在，仅创建指定输出父目录；验证脚本中 PowerShell/Python 字节串转义踩坑，临时改用 `bytes([239,187,191])` 处理 BOM 检查。主控复验 chunk-0205/0206 通过。
102. batch-0102 worker 反馈：第一次用 PowerShell 单行脚本计算 offset 时，被中文引号和长字符串解析绊住，命令失败但未写文件；随后改用 Node REPL 内存计算完成。主控复验 chunk-0203/0204 通过。
103. batch-0105 worker 反馈：graph dir 下相对质量规则文件不存在，按要求读取 StoryGraph skill 源目录 fallback；目标 lane output 父目录原本不存在，仅创建指定父目录。主控复验 chunk-0209/0210 通过。
104. batch-0100 worker 反馈：第一次大补丁修 `chunk-0200` 时上下文没命中，随后改用结构化 JSON 读写，只重写目标输出文件本身并重新跑完整校验。主控复验 chunk-0199/0200 通过。
105. batch-0104 worker 反馈：graph dir 下相对质量规则文件不存在，使用 StoryGraph skill 源规则 fallback；验证过程中遇到 REPL 变量名冲突和 PowerShell 行内 Python 引号截断，临时用唯一变量名和 REPL 检查处理。主控复验 chunk-0207/0208 通过。
106. batch-0106 worker 反馈：graph dir 下相对质量规则路径不存在，按要求读取 StoryGraph skill 源目录 fallback；目标输出父目录原本不存在，仅创建指定输出所需目录。主控复验 chunk-0211/0212 通过。
107. batch-0107 worker 反馈：graph dir 下相对质量规则文件不存在，使用 StoryGraph skill 源目录 fallback；目标输出目录原本不存在，仅创建指定路径所需目录；偏移核验临时 probe 已清理。主控复验 chunk-0213/0214 通过。
108. batch-0109 worker 反馈：graph dir 内相对质量规则路径不存在，已读取 fallback；首次内存生成时模板映射漏了 `炼器师`，闭合检查在写盘前拦住并补齐；目标父目录原本不存在，仅创建指定目录。主控复验 chunk-0217/0218 通过。
109. batch-0108 worker 反馈：graph dir 下质量规则相对路径不存在，读取 fallback；验证脚本第一次被 PowerShell / `python -c` 的 f-string 引号转义绊住，改字符串拼接通过；Node REPL 有一次持久变量重名，未写盘，重跑后成功。主控复验 chunk-0215/0216 通过。
110. batch-0110 worker 反馈：`chunk-0219` 初次自检发现 1 条 evidence range 跨进下一段半句，已收紧到正确绝对范围并重跑通过；另有一次 PowerShell `python -c` 多行转义失败，改用 here-string/Node 做只读检查。主控复验 chunk-0219/0220 通过。
111. batch-0111 worker 反馈：图谱目录下质量规则相对路径不存在，使用 fallback；生成过程中有两处 evidence 摘录一开始截短，被写前 quote 回切自检拦下，修正为完整原文片段后才写盘。主控复验 chunk-0221/0222 通过。
112. batch-0112 worker 反馈：graph dir 内相对质量规则不存在，读取 fallback；校验脚本中途遇到一次 PowerShell 引号转义问题，改用变量传入后通过，未影响产物。主控复验 chunk-0223/0224 通过。
113. batch-0113 worker 反馈：两个目标父目录原本不存在，仅创建目标文件所需父目录；PowerShell 内联 Python 遇到中文正文和嵌套引号时两次解析失败，随后改用 Node REPL 和简化 Python 命令处理。主控复验 chunk-0225/0226 通过。
114. batch-0114 worker 反馈：graph dir 下相对质量规则路径不存在，按要求读取 fallback；两个目标输出目录原本不存在，仅创建必要目录并写入指定 JSON。主控复验 chunk-0227/0228 通过。
115. batch-0115 worker 反馈：graph dir 下相对质量规则不存在，读取 fallback；PowerShell 在 requirements 摘要和统计命令中遇到引号/管道问题，改用更稳的读取方式完成校验。主控复验 chunk-0229/0230 通过。
116. batch-0120 worker 反馈：一次 `python -c` 查询被 PowerShell 引号转义绊住，改用 PowerShell JSON 解析继续；自检时发现两条证据覆盖可更严谨，已在允许文件内补强证据范围。主控复验 chunk-0239/0240 通过。
117. batch-0118 worker 反馈：graph dir 下相对质量规则路径不存在，读取 fallback；PowerShell 多层引号截断了一次 Python 校验命令，临时改用 Node 直接调用 `python -c` 完成官方 validator。主控复验 chunk-0235/0236 通过。
118. batch-0119 worker 反馈：目标输出目录原本不存在，仅为落盘创建对应目录；验证阶段 PowerShell 调 Python 多行 `-c` 两次被引号/换行转义绊住，临时改用 PowerShell here-string 传给 `python -c` 后通过。主控复验 chunk-0237/0238 通过。
119. batch-0121 worker 反馈：graph dir 下未发现相对质量规则文件，按要求读取 StoryGraph skill 源目录 fallback；两个目标父目录原本不存在，仅为指定输出路径创建父目录。主控复验 chunk-0241/0242 通过。
120. batch-0116 worker 反馈：PowerShell here-string 直接喂 `python -` 时触发 UTF-8 BOM 问题，改用 `python -c` 并设置 `PYTHONDONTWRITEBYTECODE=1`；两个 lane output 父目录原本不存在，仅创建目标文件所需目录。主控复验 chunk-0231/0232 通过。
121. batch-0117 worker 反馈：图谱目录下没有找到 task packet 指向的相对质量规则文件，读取 fallback；第一次用 PowerShell 长 `python -c` 触发 Windows `206 文件名或扩展名太长`，未产生写入，随后改用 Node REPL 生成并用官方 validator 独立复验。主控复验 chunk-0233/0234 通过。
122. batch-0122 worker 反馈：图谱相对质量规则路径不存在，读取 fallback；PowerShell 单行 Python 在中文路径和复杂引号下触发转义问题，临时改用 Node REPL 做结构化 JSON 解析、写入和 offset 校验。主控复验 chunk-0243/0244 通过。
123. batch-0124 worker 反馈：图谱目录下未找到 task packet 指向的相对质量规则文件，读取 fallback；PowerShell 多行验证脚本转义不顺，最终用只读 `python -c` 复验输出。主控复验 chunk-0247/0248 通过。
124. batch-0127 worker 反馈：目标目录原本不存在，仅创建目标文件所需父目录；验证时 PowerShell 5.1 的 `python -` / 多层引号会注入 BOM 或吞掉 f-string 引号，临时改用 Node 调用 `python -c` 执行官方 validator。主控复验 chunk-0253/0254 通过。
125. batch-0125 worker 反馈：目标父目录原本不存在，为写入指定文件创建对应目录；PowerShell 多层引号导致一次 `python -c` 解析失败，临时改用 base64 包装脚本执行，后续校验顺利。主控复验 chunk-0249/0250 通过。
126. batch-0123 worker 反馈：PowerShell 传超长生成脚本触发 Windows 命令行长度限制，未写临时脚本文件，改用 Node REPL 执行生成逻辑，只写两个目标 JSON；graph dir 下相对规则路径不存在，读取 skill fallback 规则。主控复验 chunk-0245/0246 通过。
127. batch-0126 worker 反馈：PowerShell 一行 Python 容易被中文/引号/反斜杠组合截断；临时改用 Node REPL 做 offset/JSON 写入，并用 PowerShell here-string 调官方 Python validator。主控复验 chunk-0251/0252 通过。
128. batch-0128 worker 反馈：PowerShell 当前环境的 `New-Item` 不支持 `-LiteralPath` 参数，创建目标父目录时受阻；临时改用 `[System.IO.Directory]::CreateDirectory()` 按原始路径创建目录。主控复验 chunk-0255/0256 通过。
129. batch-0129 worker 反馈：目标输出父目录原本不存在，写入时创建对应父目录；第一次验证命令被 PowerShell 多行 `python -c` 引号解析干扰，未进入 validator，随后改用 base64 包装验证脚本后通过。主控复验 chunk-0257/0258 通过。
130. batch-0130 worker 反馈：图谱目录内未找到质量规则相对路径，读取 fallback；PowerShell 直接传多行 `python -c` 时会吞掉代码里的引号，临时改用环境变量传 Python 校验代码。主控复验 chunk-0259/0260 通过。
131. batch-0131 worker 反馈：graph dir 下相对质量规则文件不存在，读取 fallback；两个目标输出父目录原本不存在，为写入指定 `run-001.json` 创建对应父目录。主控复验 chunk-0261/0262 通过。
132. batch-0132 worker 反馈：图目录内 `references/stage1-extraction-agent-quality-rules.md` 不存在，读取 fallback；第一次用 PowerShell 管道喂 `python -` 计算 offset 时触发 PowerShell 5.1 BOM 注入问题，临时改用 Node REPL 只读计算 offset。主控复验 chunk-0263/0264 通过。
133. batch-0134 worker 反馈：PowerShell 单行 `python -c` 处理中文长字符串和换行时反复触发转义语法错误，临时改用 Node REPL 做 offset 定位和自定义校验；两个输出父目录原本不存在，仅创建指定父目录。主控复验 chunk-0267/0268 通过。
134. batch-0135 worker 反馈：graph dir 下相对质量规则文件不存在，读取 fallback；PowerShell 多层引号在一次只读深度校验脚本里截断 Python 命令，临时改用 Node REPL 做只读校验，没有写入额外文件。主控复验 chunk-0269/0270 通过。
135. batch-0133 worker 反馈：graph dir 内相对质量规则文件不存在，读取 fallback；两个目标父目录原本不存在，仅创建这两个输出文件所需父目录。主控复验 chunk-0265/0266 通过。
136. batch-0137 worker 反馈：graph dir 下相对质量规则不存在，读取 fallback；PowerShell 单次写入 JSON 时命令过长，改用 Node 直接写 UTF-8 no BOM；自检发现 `chunk-0274` 中“青凝镜压制小钟”的边最初语义指向不准，已补本 chunk 的“遮天钟/小钟”节点并修正后通过验证。主控复验 chunk-0273/0274 通过。
137. batch-0136 worker 反馈：graph dir 下相对质量规则文件不存在，读取 fallback；目标父目录原本不存在，仅创建对应 `chunk-0271/chunk-0272` 的输出目录；自查时发现并修正 `chunk-0271` 一条“魔道六宗”节点证据不够贴合的问题。主控复验 chunk-0271/0272 通过。
138. batch-0139 worker 反馈：graph dir 下相对质量规则不存在，读取 fallback；两个目标父目录原本不存在，仅创建目标文件所需最小父目录；PowerShell 超长中文 `python -c` 曾被引号截断，改用 Node REPL 做 offset 与自检。主控复验 chunk-0277/0278 通过。
139. batch-0140 worker 反馈：两个输出父目录原本不存在，仅创建允许路径父目录并写入目标文件；一次 PowerShell 单行 Python 闭合检查被引号截断，改用 Node 完成同等检查。主控复验 chunk-0279/0280 通过。
140. batch-0141 worker 反馈：目标父目录原本不存在，仅创建两个指定输出文件所需父目录；一条初始 evidence 摘录跨句匹配失败，已拆成可精确回切的短摘录后写入。主控复验 chunk-0281/0282 通过。
141. batch-0142 worker 反馈：输出目录原先不存在，仅创建目标文件所需目录层级；第一次用 PowerShell 单行脚本计算 offset 时被长字符串引号截断，临时改用 Node REPL 完成 offset 计算和写盘。主控复验 chunk-0283/0284 通过。
142. batch-0138 worker 反馈：graph dir 下质量规则相对路径不存在，读取 fallback；目标父目录原本不存在，仅创建目标 output 目录；PowerShell 环境里 `New-Item -LiteralPath` 不可用，改用 `.NET Directory.CreateDirectory()`，另有一次大段中文 `python -c` 引号转义失败，改用 Node REPL 做 offset 计算。主控复验 chunk-0275/0276 通过。
143. batch-0143 worker 反馈：质量规则使用 fallback；父目录原本不存在，写入指定文件时创建对应 `comprehensive_extraction` 目录；PowerShell 5.1 用 here-string 直接喂 `python -` 触发 BOM 注入，导致 Python 报 `U+FEFF`，改用 `python -c` 和 Node REPL 生成/校验。主控复验 chunk-0285/0286 通过。
144. batch-0145 worker 反馈：PowerShell 对长中文 `python -c` 多层引号不稳定，两个只读/检查探针被引号解析打断，未写入额外文件；临时改用 Node REPL 完成 offset 和回切检查。主控复验 chunk-0289/0290 通过。
145. batch-0144 worker 反馈：目标输出父目录原先不存在，写指定文件时创建对应父目录；第一次生成在写盘前因一个 evidence quote 与原文不完全匹配被拦住，修正后再写入；后续 Python `-c` 自检被 PowerShell 嵌套引号影响，改用 Node 做只读自检。主控复验 chunk-0287/0288 通过。
146. batch-0146 worker 反馈：图谱目录下 `references/stage1-extraction-agent-quality-rules.md` 不存在，读取全局 fallback；写盘脚本最初被 PowerShell `python -c` 多层引号拆坏，前两次失败都发生在写入前，后改用 Node REPL 生成并用官方 validator 复验。主控复验 chunk-0291/0292 通过。
147. batch-0147 worker 反馈：PowerShell here-string 管道喂 `python -` 触发 BOM 注入，改用 `python -c` / Node 做只读校验；当前环境 `New-Item` 不支持 `-LiteralPath`，用 `.NET Directory.CreateDirectory()` 只创建两个目标父目录。主控复验 chunk-0293/0294 通过。
148. batch-0148 worker 反馈：graph dir 下没有相对质量规则，读取 fallback；两个目标输出目录原本不存在，为写入指定文件创建对应目录；一次 Node REPL 同名变量声明冲突，未落盘，随后用隔离作用域重跑完成。主控复验 chunk-0295/0296 通过。
149. batch-0149 worker 反馈：目标父目录原本不存在，仅创建两个输出文件所需父目录；第一次尝试用 PowerShell 内联生成时撞到 Windows 命令行长度限制，改用 Node 写文件，再用短 Python 命令调用官方 validator 复核。主控复验 chunk-0297/0298 通过。
150. batch-0151 worker 反馈：目标 lane-output 目录原本不存在，仅创建目标文件所需父目录；两次 PowerShell `python -c` 多行转义导致检查命令失败，产物未受影响，改用纯单行官方 validator 和只读 Node 脚本完成验证。主控复验 chunk-0301/0302 通过。
151. batch-0150 worker 反馈：graph dir 下相对质量规则文件不存在，读取 fallback；两个目标父目录原本不存在，为写入指定文件创建对应父目录，未修改其他 worker 文件。主控复验 chunk-0299/0300 通过。
152. batch-0154 worker 反馈：目标输出父目录原先不存在，写入时创建对应父目录；PowerShell 内嵌 Python 做自定义校验时遇到多层引号截断，临时改用 Node 做同等校验。主控复验 chunk-0307/0308 通过。
153. batch-0152 worker 反馈：目标父目录原本不存在，仅创建输出路径所需目录；初稿模板名写成不存在的 `邪修行为分析`，二次模板名校验发现后修正为 requirements 中真实的 `邪修分析`；一次 PowerShell `python -c` 对 bytes literal 的引号转义导致验证脚本报错，临时改用 `bytes.fromhex()`。主控复验 chunk-0303/0304 通过。
154. batch-0153 worker 反馈：packet 指向的 graph dir 相对质量规则不存在，读取 StoryGraph skill fallback；PowerShell 5.1 的 here-string 喂 `python -` 触发 BOM/转义问题，改用 `python -c` 和 Node REPL 完成校验与写入。主控复验 chunk-0305/0306 通过。
155. batch-0155 worker 反馈：目标父目录原本不存在，仅创建两个输出文件所需父目录；一开始用 PowerShell 内联长中文摘录计算偏移时被引号转义绊住，后改用 Node REPL 做偏移搜索和写盘。主控复验 chunk-0309/0310 通过。
156. batch-0157 worker 反馈：graph dir 内未找到相对质量规则文件，读取 fallback；两个目标输出父目录原本不存在，仅创建目标文件所需父目录并写入 JSON，未修改其他 worker 文件。主控复验 chunk-0313/0314 通过。
157. batch-0156 worker 反馈：两个 lane-output 父目录原本不存在，仅创建对应目录后落盘目标 `run-001.json`；offset 探针第一次被 PowerShell 多层引号绊住，改用 Node REPL 读取和计算，未产生额外文件。主控复验 chunk-0311/0312 通过。
158. batch-0158 worker 反馈：两个目标输出目录原先不存在，生成时创建这两个输出文件父目录；验证阶段 PowerShell 向 `python -c` 传多行脚本时吞双引号，已改用 Python 单引号字符串重跑验证，产物未因此改动。主控复验 chunk-0315/0316 通过。
159. batch-0159 worker 反馈：目标 lane-output 子目录原本不存在，写入时按两个目标路径创建父目录；一次只读 offset probe 被 PowerShell 多层引号绊住，改用 Node REPL 只读计算偏移，产物不受影响。主控复验 chunk-0317/0318 通过。
160. batch-0160 worker 反馈：graph dir 下相对质量规则文件不存在，按要求读取 skill fallback 质量规则；两个目标输出父目录原本不存在，仅为指定输出路径创建必要父目录。主控复验 chunk-0319/0320 通过。
161. batch-0162 worker 反馈：graph dir 下相对质量规则文件不存在，按要求读取 skill fallback 质量规则；第一次 validator 命令因 PowerShell 到 `python -c` 的换行转义失败，改用 here-string 重跑通过，产物未受影响。主控复验 chunk-0323/0324 通过。
162. batch-0161 worker 反馈：graph dir 下相对质量规则路径不存在，按要求读取 skill fallback 质量规则；两个目标父目录原本不存在，写入时只创建对应 chunk 的 `comprehensive_extraction` 目录。主控复验 chunk-0321/0322 通过。
163. batch-0163 worker 反馈：graph dir 下没有 `references/stage1-extraction-agent-quality-rules.md`，按要求读取 skill fallback 质量规则；两个目标父目录原本不存在，仅创建这两个目标文件所需父目录。主控复验 chunk-0325/0326 通过。
164. batch-0165 worker 反馈：图谱目录下未找到相对质量规则文件，按要求读取 skill fallback 质量规则；一次 PowerShell 调 `python -c` 的多层引号导致自检命令失败，改用 here-string 解决；定向 `git status` 只显示目标 JSON 新增。主控复验 chunk-0329/0330 通过。
165. batch-0164 worker 反馈：质量规则在 graph dir 相对路径未找到，按要求读取 skill fallback 质量规则；一次误用 here-string 喂 `python -` 触发 BOM 语法错误，随后改用 `python -c`；Node REPL 因持久变量重复声明阻塞一次，已重置/局部作用域处理。主控复验 chunk-0327/0328 通过。
166. batch-0166 worker 反馈：质量规则图谱相对路径不存在，按要求读取 skill fallback 质量规则；PowerShell `New-Item` 不接受 `-LiteralPath`，改用 .NET `Directory.CreateDirectory`；曾用临时 `.py` 文件跑验证，不符合严格写范围，临时文件已删除，后续改用 `python -c` 或 REPL。主控复验 chunk-0331/0332 通过。
167. batch-0167 worker 反馈：两个目标父目录原本不存在，仅创建指定输出路径所需目录；PowerShell `New-Item -LiteralPath` 在当前环境不可用，改用 .NET `CreateDirectory()`；`python -c` 多行转义容易出错，偏移和回切自检改用 Node REPL。主控复验 chunk-0333/0334 通过。
168. batch-0169 worker 反馈：无影响结果的阻塞问题；Node REPL 做 offset 校验时有一次临时函数名复用报错，换名后继续；未写入额外文件。主控复验 chunk-0337/0338 通过。
169. batch-0168 worker 反馈：graph dir 下相对质量规则文件不存在，按要求读取 skill fallback 质量规则；Node REPL 有一次重复变量声明、PowerShell 有一次 `python -c` 引号解析问题，均未影响产物，已改用隔离变量和 Node 只读复核绕过。主控复验 chunk-0335/0336 通过。
170. batch-0172 worker 反馈：graph dir 下质量规则相对路径不存在，按要求读取 skill fallback 质量规则；两个目标输出目录原本不存在，仅创建目标 JSON 所需父目录；PowerShell 内联中文脚本有一次引号解析失败，未写文件，随后改用 Node 局部执行完成验证。主控复验 chunk-0343/0344 通过。
171. batch-0171 worker 反馈：一次只读 requirements 探查命令被 PowerShell / Python `-c` 换行转义触发 `SyntaxError`，未写文件；随后改用单行表达式和 Node REPL 内存脚本处理。主控复验 chunk-0341/0342 通过。
172. batch-0170 worker 反馈：PowerShell 对 `python -c` 的多层引号在中文路径场景下两次打坏验证脚本参数，拖慢流程；临时改用 Node REPL 的 `spawnSync('python', ['-c', code])` 参数数组调用官方 validator，未创建临时脚本文件。主控复验 chunk-0339/0340 通过。
173. batch-0173 worker 反馈：graph dir 下相对质量规则路径未找到，按要求读取 skill fallback 质量规则；目标输出目录原本不存在，写入时仅为两个指定 JSON 创建必要父目录。主控复验 chunk-0345/0346 通过。
174. batch-0175 worker 反馈：graph dir 内未找到相对质量规则文件，按要求读取 fallback 规则；目标父目录原本不存在，仅创建两个目标 JSON 的父目录；一次 PowerShell/Python 单行引号被截断，临时改成更朴素命令继续读取。主控复验 chunk-0349/0350 通过。
175. batch-0174 worker 反馈：graph dir 下相对质量规则不存在，按要求读取 skill fallback；PowerShell `python -c` 引号/命令长度限制拖慢流程，临时改用 Node REPL 直接写指定两个 JSON，未创建临时脚本；一次“定神符” evidence 偏移被额外 quote 回切检查抓到并修正，复验通过。主控复验 chunk-0347/0348 通过。
176. batch-0176 worker 反馈：graph dir 下相对质量规则路径不存在，按要求读取 skill fallback；两个 lane-output 父目录原本不存在，写入目标 JSON 时创建对应父目录；未创建临时脚本或其他落盘文件。主控复验 chunk-0351/0352 通过。
177. batch-0177 worker 反馈：两个目标父目录原本不存在，仅创建指定 JSON 所需父目录并写入目标文件；未创建临时脚本或其他落盘文件。主控复验 chunk-0353/0354 通过。
178. batch-0179 worker 反馈：官方 validator 只做基础 contract/range 校验，不检查语义闭合和 quote 回切，worker 补做自定义检查；两处初稿表达过宽，已在写入文件内修正：chunk-0357 补“中年妇人筑基后期”精确证据，chunk-0358 将“曲魂炼尸身份”从不合适自指 edge 移到不确定性记录。主控复验 chunk-0357/0358 通过。
179. batch-0180 worker 反馈：图谱内相对质量规则不存在，按要求读取 skill fallback；两个目标输出父目录原本不存在，仅为指定 JSON 创建必要父目录；PowerShell 单行 `python -c` 打印 requirements categories 时出现引号截断，临时改用 Node REPL 做只读 JSON/offset 处理，未创建临时脚本或其他落盘文件。主控复验 chunk-0359/0360 通过。
180. batch-0178 worker 反馈：graph dir 下相对质量规则不存在，按要求读取 skill fallback；目标父目录原本不存在，仅创建两个目标 JSON 所需父目录；PowerShell 长脚本命令长度限制和 `python -c` 多层引号改写导致生成/验证各一次无效尝试，临时用 Node REPL 生成 JSON、PowerShell 环境变量传 base64 内存脚本给 Python 做 validator。主控复验 chunk-0355/0356 通过。
181. batch-0182 worker 反馈：两个目标输出目录原本不存在，写入指定 JSON 时创建对应父目录；未写其他文件；一次 PowerShell `python -c` 多行转义问题，改用 here-string 作为 `python -c` 参数后验证通过；自检时将 chunk-0364 未命名“秘术”从实体节点降级为不确定性，避免过度类型化。主控复验 chunk-0363/0364 通过。
182. batch-0183 worker 反馈：graph dir 下相对质量规则不存在，按要求读取 skill fallback；PowerShell 一行 `python -c` 遇到中文弯引号时参数转义截断，导致两次只读探针失败，后改用 PowerShell here-string 写入环境变量再执行 `python -c`，未创建临时脚本；两个目标 lane-output 目录原本不存在，仅为允许 JSON 创建必要父目录。主控复验 chunk-0365/0366 通过。
183. batch-0181 worker 反馈：PowerShell `python -c` 单行里嵌中文引号时出现一次转义语法错误，后改用 PowerShell here-string 作为 `python -c` 参数；Node REPL 因保留上次顶层变量名阻塞一次，重置 REPL 后继续；未创建临时脚本文件。主控复验 chunk-0361/0362 通过。
184. batch-0185 worker 反馈：图谱目录内相对质量规则未找到，按要求读取 skill fallback；两个目标父目录原本不存在，为指定 `run-001.json` 创建对应父目录；两次 PowerShell/Python 单行引号转义读辅助信息失败，已改用 here-string 参数临时处理，未落盘。主控复验 chunk-0369/0370 通过。
185. batch-0184 worker 反馈：两个目标输出目录原本不存在，仅创建写入指定 JSON 所需父目录；验证过程中 PowerShell 对长 `python -c` 代码的引号处理反复剥内部引号，临时改用 Node REPL 和环境变量传代码执行，未创建临时脚本。主控复验 chunk-0367/0368 通过；chunk-0368 体量很小但 validator 通过，含 1 条 evidence、1 条 uncertainty、1 条 rejected candidate。
186. batch-0186 worker 反馈：图谱目录下没有 `scripts/storygraph.py`，按 skill 源路径运行官方 validator；两个目标父目录原本不存在，为指定文件路径创建对应目录；未创建临时脚本或其他落盘文件；一次 PowerShell 多层引号截断 `python -c`，改用 Node REPL 内存执行规避。主控复验 chunk-0371/0372 通过。
187. batch-0187 worker 反馈：两个目标父目录原本不存在，为写入指定 JSON 创建对应输出目录；未写入临时脚本或其他文件；验证时 PowerShell 到 `python -c` 的引号传递出现一次语法问题，随后改用 UTF-8 base64 环境变量传脚本完成验证。主控复验 chunk-0373/0374 通过。
188. batch-0188 worker 反馈：两个目标父目录原先不存在，仅创建这两个目标输出目录以写入指定 JSON；未创建临时脚本或其他落盘文件；验证过程中 PowerShell 长中文内嵌 Python 出现引号传递问题，临时改用 Node REPL 和环境变量承载 Python 验证代码，避免写临时文件。主控复验 chunk-0375/0376 通过。
189. batch-0189 worker 反馈：graph dir 下未发现相对质量规则文件，按要求读取 skill fallback；目标父目录原本不存在，仅为两个指定输出路径创建父目录；未创建临时脚本或其他落盘文件；PowerShell 包装多行 `python -c` 时两次引号转义失败，改用 here-string 参数重跑验证。主控复验 chunk-0377/0378 通过。
190. batch-0190 worker 反馈：质量规则 graph dir 相对路径不存在，按要求读取 skill fallback；两个目标父目录原先不存在，仅创建目标文件所需父目录；未创建临时脚本或其他产物。主控复验 chunk-0379/0380 通过。
191. batch-0191 worker 反馈：graph dir 下质量规则相对路径不存在，按要求读取 skill fallback；PowerShell 一行 `python -c` 多行转义再次被截断，拖慢一次验证；改用 Node REPL 做只读 offset 计算，并用 PowerShell here-string 作为 `python -c` 参数完成官方 validator；未创建临时脚本或其他落盘文件。主控复验 chunk-0381/0382 通过。
192. batch-0192 worker 反馈：目标父目录原本不存在，仅创建两个输出文件所需父目录；未创建临时脚本、日志或其他落盘文件；一次额外闭合检查的 `python -c` 被 PowerShell 引号截断，改用 Node REPL 做只读复核，官方 validator 不受影响。主控复验 chunk-0383/0384 通过。
193. batch-0193 worker 反馈：graph dir 下质量规则相对路径不存在，按要求读取 skill fallback；两个目标父目录原本不存在，为写入指定 JSON 创建对应父目录，未创建临时脚本或其他落盘文件。主控复验 chunk-0385/0386 通过。
194. batch-0194 worker 反馈：graph dir 下质量规则相对路径不存在，按要求读取 skill fallback；两个目标父目录原本不存在，仅为指定 JSON 创建对应父目录并落盘；未创建临时脚本或其他文件；PowerShell 多行 `python -c` 传参会吞双引号，前两次只读摘要命令失败，之后改用单引号 + 环境变量传路径处理。主控复验 chunk-0387/0388 通过。
195. batch-0195 worker 反馈：graph dir 下质量规则相对路径不存在，按要求读取 skill fallback；两个目标父目录原先不存在，仅创建写入目标 JSON 所需目录结构；未落临时脚本或其他文件。主控复验 chunk-0389/0390 通过。
196. batch-0196 worker 反馈：PowerShell 多层引号会把 `python -c` 内的 f-string 双引号吃掉，导致前两次验证命令语法错误；改用 base64 内存传参执行验证脚本，未创建临时脚本文件。主控复验 chunk-0391/0392 通过。
197. batch-0198 worker 反馈：一次 PowerShell + `python -c` 的多层引号把额外检查脚本截断成 `SyntaxError`，未写任何临时文件；随后改用 Node REPL 内存脚本完成同等检查。主控复验 chunk-0395/0396 通过。
198. batch-0199 worker 反馈：目标父目录原本不存在，写入时仅创建两个目标 JSON 必需父目录；验证命令第一次被 PowerShell/f-string 引号转义绊住，已改成无 f-string 写法重跑通过。主控复验 chunk-0397/0398 通过。
199. batch-0201 worker 反馈：PowerShell 内联 `python -c` 的复杂 f-string 引号在一次验证打印处踩转义坑，改成 `%` 格式化后重跑通过；没有落盘临时脚本；目标输出父目录原先不存在，仅为两个目标文件创建必要父目录。主控复验 chunk-0401/0402 通过。
200. batch-0197 worker 反馈：两个目标父目录原本不存在，写入目标 JSON 时创建必要目录；未创建临时脚本或其他落盘文件；验证阶段 PowerShell 向 `python -c` 传多层引号时两次截断脚本，最后用 here-string 转 base64 环境变量执行绕过。主控复验 chunk-0393/0394 通过。
201. batch-0200 worker 反馈：graph dir 下相对质量规则文件未找到，按要求读取 skill fallback；PowerShell/Python 一行引号和 Node REPL 顶层变量复用各阻塞一次，均未产生临时落盘文件；后续改用 PowerShell here-string + `python -c` 和 Node 局部作用域处理。主控复验 chunk-0399/0400 通过。
202. batch-0203 worker 反馈：graph dir 下相对质量规则文件不存在，按要求读取 skill fallback；目标 lane-output 目录原本不存在，仅创建放置两个允许 JSON 所需父目录；未创建临时脚本或其他落盘文件。主控复验 chunk-0405/0406 通过。
203. batch-0202 worker 反馈：目标父目录原本不存在，写入指定 JSON 时创建对应父目录；PowerShell 直接传多行 `python -c` 时吞掉引号，验证改用内存 base64 传参完成，未创建临时脚本或其他落盘文件。主控复验 chunk-0403/0404 通过。
204. batch-0206 worker 反馈：目标父目录原本不存在，写入指定 JSON 时创建两个必要父目录；PowerShell 长中文 `python -c` 参数两次吃引号，改用 Node REPL 和环境变量执行验证代码，未创建临时脚本或其他落盘文件。主控复验 chunk-0411/0412 通过。
205. batch-0204 worker 反馈：graph dir 下质量规则相对路径不存在，按要求读取 skill fallback；PowerShell 5.1 对 `python -c` 多层引号会吞内层引号，Node REPL 长脚本也有一次语法阻塞；改用 Base64 包装 Python 内存脚本绕过，未创建临时脚本文件。主控复验 chunk-0407/0408 通过。
206. batch-0205 worker 反馈：PowerShell 对长内联 Python 的引号处理会拆坏脚本，前两次只读 probe 失败但未落盘；改用 Node REPL 调 Python validator 避开 quoting 问题；父目录原本不存在，仅创建两个目标文件所需目录。主控复验 chunk-0409/0410 通过。
207. batch-0207 worker 反馈：图谱目录相对质量规则不存在，按要求读取 skill fallback；目标目录原本不存在，仅创建对应必要目录；PowerShell 传多行 `python -c` 时会把包含双引号/f-string 的脚本参数拆坏，第一次验证脚本报自身语法错误；临时改用 Node REPL 做生成与写入，并把最终 Python 验证脚本改成不依赖双引号/f-string 的形式。主控复验 chunk-0413/0414 通过。
208. batch-0208 worker 反馈：最初用 PowerShell 内联生成时撞到 Windows 命令行长度限制，命令未执行且未写入文件；随后改用 Node REPL 内存代码生成，未创建临时脚本文件；两个目标父目录原本不存在，仅创建目标 JSON 所需父目录。主控复验 chunk-0415/0416 通过。
209. batch-0209 worker 反馈：目标父目录一开始不存在，写入时仅创建两个目标 JSON 所需父目录；未创建临时脚本文件；Windows 命令行长度限制和 PowerShell 对 `python -c` 中双引号的干扰造成小阻滞，已改用 Node REPL 内存生成、Python 内存验证绕开。主控复验 chunk-0417/0418 通过。
210. batch-0210 worker 反馈：目标父目录原本不存在，仅创建写入两个指定 JSON 所需父目录；PowerShell 多层引号会剥掉 `python -c` 脚本里的引号，第一次官方校验命令失败，后续改用环境变量传递内存脚本完成校验，未创建临时脚本文件。主控复验 chunk-0419/0420 通过。
211. batch-0211 worker 反馈：质量规则读取使用 skill fallback；一开始用 `python -c` 承载完整生成脚本触发 Windows 命令行长度限制，未落盘文件；随后改用 Node REPL 在进程内生成和写入，避免创建临时脚本文件；目标目录原本不存在，为写入两个指定 JSON 创建对应目录。主控复验 chunk-0421/0422 通过。
212. batch-0212 worker 反馈：目标 chunk-0423/chunk-0424 lane-output 父目录原先不存在，写入目标 JSON 时创建对应父目录；没有创建临时脚本文件或其他落盘文件。主控复验 chunk-0423/0424 通过。
213. batch-0215 worker 反馈：graph dir 下相对质量规则不存在，按要求读取 skill fallback；两个目标 lane-output 父目录原本不存在，写入时仅创建两个目标 JSON 所需目录；没有创建临时脚本或其他落盘文件。主控复验 chunk-0429/0430 通过。
214. batch-0213 worker 反馈：graph dir 下相对质量规则不存在，按要求读取 skill fallback；目标 lane-output 父目录原本不存在，写入时仅创建两个目标 JSON 所需目录；PowerShell 多行 `python -c` 引号展开会吞掉内层引号，改用 PowerShell 原生读取和 Node 局部作用域生成；生成前曾捕获 evidence 引用闭合问题并在写盘前中止，修正后验证通过。主控复验 chunk-0425/0426 通过。
215. batch-0218 worker 反馈：graph dir 下相对质量规则不存在，按要求读取 skill fallback；两个目标 lane-output 父目录原本不存在，写入时仅创建两个目标 JSON 所需目录；PowerShell 直接传多行 Python 时出现引号吞噬问题，改用环境变量传递 `python -c` 脚本处理。主控复验 chunk-0435/0436 通过。
216. batch-0214/0216/0217 的完成通知未按顺序回到主线程，但官方 `claim-agent-batches` 已将状态推进到 completed，且目标 JSON 均已落盘；主控按调度状态和实际文件双重盘点后关闭对应 agent，并复验 chunk-0427/0428、0431/0432、0433/0434 全部通过。
217. batch-0219 worker 反馈：两个目标 lane-output 父目录原本不存在，写入时仅创建两个目标 JSON 所需目录；只读 Python 探查命令被 PowerShell 多层换行转义绊住，后续改用 Node REPL 和 PowerShell 内存 base64 脚本完成验证；未创建临时脚本或其他落盘文件。主控复验 chunk-0437/0438 通过。
218. batch-0220 worker 反馈：graph dir 下相对质量规则不存在，按要求读取 skill fallback；PowerShell 包装 `python -c` 多行脚本时两次因换行/`exec` 引号失败，改用环境变量承载脚本重跑；未创建临时脚本或其他落盘文件。主控复验 chunk-0439/0440 通过。
219. batch-0223 worker 反馈：目标 lane-output 父目录原本不存在，写入时仅创建两个目标 JSON 所需目录；PowerShell 对 `python -c` 多行验证脚本的引号处理会剥离 f-string 引号，后改用 Base64 环境变量传入内存脚本完成验证；未创建临时脚本文件。主控复验 chunk-0445/0446 通过。
220. batch-0224 worker 反馈：第一次用 PowerShell `python -c` 直接传长脚本时撞到 Windows 命令行长度限制；随后验证脚本又遇到一次 PowerShell 引号重解析问题；临时改为 Node REPL 生成文件、用环境变量传短 Python validator 脚本验证；未创建临时脚本文件。主控复验 chunk-0447/0448 通过。
221. batch-0221 worker 反馈：graph dir 内相对质量规则不存在，按要求读取 skill fallback；目标父目录原本不存在，为写入两个指定 JSON 创建对应目录；中途遇到一次 PowerShell 大段中文 `python -c` 引号问题、一次 Node REPL 顶层常量重名，均未写入文件，后改用局部作用域完成。主控复验 chunk-0441/0442 通过。
222. batch-0222 完成通知未按顺序回到主线程，但官方状态已为 completed，且目标 JSON 已落盘；主控按调度状态和实际文件双重盘点后关闭对应 agent，并复验 chunk-0443/0444 通过。
223. batch-0226 worker 反馈：graph dir 内相对质量规则文件不存在，按要求读取 skill fallback；目标父目录原本不存在，为写入两个授权 JSON 创建必要目录；未创建临时脚本文件。主控复验 chunk-0451/0452 通过。
224. batch-0225 worker 反馈：目标父目录原本不存在，仅创建两个目标文件所需父目录；PowerShell 的 `python -c` 多行转义仍容易截断只读探查命令，临时改用 Node REPL 做内存解析、offset 计算和回切校验；建议后续将“读取 packet + LF offset 换算 + 官方 validator + evidence 回切 + BOM 检查”沉淀为固定只读校验命令或项目规则。主控复验 chunk-0449/0450 通过。
225. batch-0227 worker 反馈：质量规则读取了指定 fallback 路径；两个目标文件原先不存在，写入时仅生成对应目标 JSON；未创建临时脚本或其他落盘文件。主控复验 chunk-0453/0454 通过。
226. batch-0229 worker 反馈：目标 `comprehensive_extraction` 目录原本不存在，写入时创建两个目标父目录；未创建临时脚本或其他落盘文件；PowerShell 对 `python -c` 多行转义不友好，第一次只读探查触发 `SyntaxError`，后续改用 Node REPL 做 JSON/offset 处理，官方 validator 用 `python -c` 单行完成。主控复验 chunk-0457/0458 通过。
227. batch-0228 worker 反馈：图谱目录内没有相对质量规则文件，按要求读取 skill fallback；两个目标输出目录原本不存在，因此写目标 JSON 时创建必要父目录；没有创建临时脚本文件或其他产物。主控复验 chunk-0455/0456 通过。
228. batch-0231 完成通知未按顺序回到主线程，但官方状态已为 completed，且目标 JSON 已落盘；主控按调度状态和实际文件双重盘点后关闭对应 agent，并复验 chunk-0461/0462 通过。
229. batch-0232 worker 反馈：两个目标父目录原本不存在，仅创建两个目标 JSON 必需父目录并写入指定文件；第一次 Python validator 命令被 PowerShell 引号截断，后改用 here-string 传入环境变量执行；未创建临时脚本文件。主控复验 chunk-0463/0464 通过。
230. batch-0230 worker 反馈：PowerShell 5.1 对长 `python -c` 多行脚本的引号处理以及环境变量长度限制拖慢流程；临时改用 Node REPL 承载长脚本，并在 Node 内调用官方 Python `validate_lane_output`；目标父目录原先不存在，写出时只创建两个允许产物所需父目录；未创建临时脚本文件。主控复验 chunk-0459/0460 通过。
231. batch-0234 worker 反馈：目标父目录原本不存在，写入时创建两个必要父目录；PowerShell 向 `python -c` 传多行校验脚本时吞掉部分引号，第一次验证命令语法错误；临时改用内存 base64 包装执行，未落盘；建议后续沉淀为固定项目级只读校验命令。主控复验 chunk-0467/0468 通过。
232. batch-0236 完成通知未按顺序回到主线程，但官方状态已为 completed，且目标 JSON 已落盘；主控按调度状态和实际文件双重盘点后关闭对应 agent，并复验 chunk-0471/0472 通过。
233. batch-0233 worker 反馈：完整生成脚本太长，直接 `python -c` 撞到 Windows 命令长度限制；随后改用 Node REPL 内存执行写盘，未创建临时脚本文件；验证时还遇到 PowerShell native command 对双引号参数处理异常，改成 Python 单引号脚本后通过。主控复验 chunk-0465/0466 通过。
234. batch-0237 worker 反馈：目标父目录原本不存在，仅创建两个目标 JSON 所需目录；未创建临时脚本或其他落盘文件；过程中 PowerShell 内联 Python 被中文正文/引号转义绊住一次，后改用 Node REPL 局部作用域完成 offset 计算和写盘；建议由用户决定是否沉淀“长中文正文 offset 计算优先用 Node REPL/避免一行 Python 嵌套引号”为规则。主控复验 chunk-0473/0474 通过。
235. batch-0235 worker 反馈：目标父目录原本不存在，写入时创建对应父目录；PowerShell 命令长度限制导致第一次长脚本未执行，后改用 Node REPL 向 Python 传入内存脚本处理；自检发现一次 evidence 编号映射错误并在写盘前修正。主控复验 chunk-0469/0470 通过。
236. batch-0240 worker 反馈：两个目标父目录原本不存在，仅创建写入两个指定 JSON 所必需父目录；未创建临时脚本文件或其他落盘文件；第一次验证命令因 PowerShell/Python 内联引号组合报语法错，后改成保守字符串拼接重跑。主控复验 chunk-0479/0480 通过。
237. batch-0239 worker 反馈：两个目标父目录原本不存在，仅创建输出文件所需目录；PowerShell 的长 `python -c` 换行转义造成一次只读检查失败，临时改用 Node REPL 做 BOM 和 evidence 回切校验；未创建临时脚本或辅助文件。主控复验 chunk-0477/0478 通过。
238. batch-0238 完成通知未按顺序回到主线程，但目标 JSON 已落盘；主控按实际文件复验 chunk-0475/0476 通过，并关闭对应 agent。
239. batch-0242 worker 反馈：graph dir 下相对质量规则文件不存在，按要求读取 skill fallback；PowerShell 的 `python -c` 嵌套引号在中文路径下出现截断，临时改用 Node REPL 生成/校验，并由 Node 参数数组调用官方 Python validator；目标父目录原本不存在，因此只创建写入该 JSON 所需父目录。主控复验 chunk-0483 通过。
240. batch-0241 worker 反馈：目标父目录原本不存在，写入时创建必要父目录；没有创建临时脚本或其它落盘文件；第一次尝试用单条 PowerShell `python -c` 生成时撞到 Windows 命令长度限制，随后改用 Node REPL 内存执行生成逻辑，再用短 Python 命令跑官方 validator。主控复验 chunk-0481/0482 通过；lane extraction 483/483 全部落盘无缺失。
241. `merge-stage1` 首次失败：局部 lane validator 通过，但 canonical graph 校验要求 `supports_templates[].status`，大量 worker 产物仅有 `requirement_status`；另有少量 `needs_review` requirement status、4 个非对象 support、33 个空 supports、1 个 confidence 被误写成 `['绿液']`。主控做机械结构规范化：补 `status`、将 `needs_review` 映射为 `partial`、移除非对象 support、按同 chunk 常见 support 补空 supports、将坏 confidence 改为同记录 support confidence/`EXTRACTED`；不改实体、关系、事件、证据正文。规范化后 483 个 lane-output 全量 `validate_lane_output` 通过。
242. `merge-stage1` 第二次失败：canonical schema 的真实 requirement status 枚举是 `covered / needs_review / not_found_in_source`，上一轮 `partial` 不合法；同时暴露出部分 evidence 使用 `id` 而非 `evidence_id`、部分 `sg-*` id 不符合 canonical 前缀、部分 edge 只有 `relation_type` 缺 `edge_type`、chunk-0379/0380 evidence 的 `fact_summary` 不是字符串。主控做第二轮机械结构规范化：`partial` 改回 `needs_review`，统一 evidence/node/edge/event canonical id 并同步本 chunk 引用，`relation_type` 映射到 `edge_type`，非字符串 evidence `fact_summary` 用 excerpt 派生；不改事实正文。规范化后 483 个 lane-output 全量 `validate_lane_output` 通过。
243. `merge-stage1` 第三次失败仅剩 chunk-0183/0184 的 14 个 `event_unknown_node`：这些 events 的 `participants` 是 `{node_id, role}` 对象列表，而 canonical schema 只接受 node id 字符串列表。主控将 `participants` 扁平化为 node id 列表，并把原对象列表保留到 `participant_roles`；规范化后 483 个 lane-output 全量 `validate_lane_output` 通过。

### 244. Stage1 validate-graph 发现 coverage 派生账本陈旧
- 时间：2026-06-24 01:48
- 现象：`validate-graph` 在 canonical graph 已生成后仍报告 `requirements_readiness_count_mismatch`、`requirements_readiness_template_mismatch`、大量 `chunk_not_completed:*:pending_agent_outputs`、`graph_coverage_evidence_mismatch`、`agent_run_not_completed`，以及 refinement task packet 缺少 `source_path` / `relevant_template_requirements` / `lane_contract`。
- 判断：不是重复抽取缺页；483 个 lane outputs 已存在且通过 lane validator。问题出在 `coverage/*.json` 与 `coverage/agent-run-ledger.json` 仍保留 prepare 阶段快照，没有按合并后的 canonical graph 回写。
- 处理：从 `graphify-out/graph.json`、`requirements/template-requirements.json` 和现有 lane output 路径机械重建 `coverage/chunk-ledger.json`、`coverage/evidence-index.json`、`coverage/template-readiness.json`、`coverage/agent-run-ledger.json`；补全 3 个 template-requirements-refinement task packet 的校验字段。备份位置：`intermediate/repair-backups/coverage-rebuild-20260624-014810`。

### 245. Stage1 validate-graph 通过但保留未审查警告
- 时间：2026-06-24 01:49
- 命令：`python C:\Users\Administrator\.codex\skills\storygraph\scripts\storygraph.py validate-graph --graph-dir E:\AI_Projects\CultivationWorld\docs\世界观参考\凡人修仙传\凡人修仙传.storygraph`
- 结果：`ok: true`，`errors: []`；剩余 `warnings: ["review_not_performed"]`，`review_status: "unreviewed_usable"`。
- 判断：Stage1 canonical graph 与 coverage/ledger 结构验证已通过；后续继续执行 Stage2，不在此处停止。

### 246. Stage2 全量模板执行完成
- 时间：2026-06-24 03:20
- 范围：`prepare-stage2 --selection all` 生成 37 个模板 batch；按 Stage2 agent-driven 规则逐模板生成 `intermediate/stage2/extraction-records/<模板名>/run-001.json`。
- 调度：官方 Stage2 并发窗口为 6；主控持续滚动 claim，避免重复派发，最终 `completed_count: 37`、`in_flight_count: 0`、`pending_count: 0`。
- 汇入与渲染：`ingest-stage2` 返回 `record_count: 37`；`render-stage2` 返回 `rendered_count: 37`，草稿输出位于 `drafts/*.md`。
- 验证：`validate-stage2` 返回 `ok: true, errors: []`；最终复跑 `validate-graph` 仍为 `ok: true, errors: []`，仅保留 `review_not_performed` warning / `review_status: unreviewed_usable`。
- 图谱计数：nodes 7134，edges 4664，events 3088，evidence 7499。

### 247. Stage2 执行中反复出现的流程阻滞
- 时间：2026-06-24 03:20
- PowerShell/Windows 多层引号问题：多名 Stage2 worker 在长 `python -c`、含中文/换行/管道符/双引号脚本中遇到参数被吞、换行转义失败或 `|` 被误解析，临时处理方式包括改用 Node REPL、临时 `.py`、base64/环境变量传脚本或 PowerShell here-string。
- PowerShell 兼容性问题：个别 worker 遇到 `New-Item -LiteralPath` / `Split-Path -LiteralPath -Parent` 兼容性差，临时使用 `.NET Directory.CreateDirectory()` 创建必要父目录。
- Stage2 task packet 的 `evidence_ids` 为空：各模板 agent 需从 `graphify-out/graph.json` 和 `coverage/evidence-index.json` 反查 `supports_templates[].template_name`，这是可行的但明显拖慢模板生成。
- evidence record 字段不完全一致：部分 evidence 缺显式 `chunk_id` 或缺展开 `source_excerpt`，worker 临时从 `evidence:chunk-XXXX:*` ID、`source_range`、graph 标签和 evidence_id 做保守回退，不补造无证细节。
- 建议由用户决策是否沉淀规则：复杂 JSON/中文大脚本优先固定小脚本或 Node REPL，不塞长一行 PowerShell；Stage2 生成器可考虑把 template evidence ids/chunk_id/source_excerpt 派生写入 task packet，减少每个 worker 重复反查。
