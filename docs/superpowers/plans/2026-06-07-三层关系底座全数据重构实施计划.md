# 三层关系底座全数据重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立可运行的“个体-群体-势力”三层关系底座，以全数据规则打穿追杀令链，并让旧关系 API 退为兼容门面。

**Architecture:** 新关系底座采用端口、仓储、解释器、策略注册表和门面组合。领域代码只发布标准关系事件或读取信号；`LedgerRepository` 持有三层账本，`RelationImpactEngine` 解释配置写账本，`RelationshipSignalProvider` 解释配置输出 AI 信号。

**Tech Stack:** JavaScript ES modules、JSON 数据配置、Node.js 工具测试、现有 WorldEngine / NPC GOAP / Job-Toil。

---

## 文件结构

新增文件：

- `apps/game/js/engine/relationship/ledger-repository.js`：三层账本仓储，处理 key、默认值、mark/tag stacking、snapshot/load、decay/expire/consume。
- `apps/game/js/engine/relationship/expression-evaluator.js`：无副作用表达式解释器，提供 `clamp/min/max/abs/round/floor/ceil/scale/hasTag/hasMark/markWeight/markCount`。
- `apps/game/js/engine/relationship/selector-resolver.js`：selector 解析器，支持 `$actor.id`、`$target.id`、`$target.factionId`、`$event.witnesses`、`$ledger.marks[type=x]`。
- `apps/game/js/engine/relationship/effect-operators.js`：core、potential、marks、tags、consume mark 的 effect operator 注册表。
- `apps/game/js/engine/relationship/impact-engine.js`：事件匹配、互斥组、selector 展开、表达式计算、账本写入 pipeline。
- `apps/game/js/engine/relationship/relation-event-emitter.js`：旧事件名与显式 hook 到标准 `RelationEvent` 的适配器。
- `apps/game/js/engine/relationship/signal-provider.js`：按 signal rules 输出 `facts/gates/modifiers/targetPreferences/traces`。
- `apps/game/tools/test-relationship-platform.mjs`：新三层关系底座单元测试。
- `apps/game/tools/test-relationship-wanted-chain.mjs`：追杀令链信号与 NPC 接入测试。
- `apps/game/data/relationships/dictionaries/marks.json`：mark 字典。
- `apps/game/data/relationships/dictionaries/tags.json`：tag 字典。
- `apps/game/data/relationships/dictionaries/signal-keys.json`：signal key 白名单。
- `apps/game/data/relationships/dictionaries/relation-event-types.json`：事件类型白名单。
- `apps/game/data/relationships/dictionaries/group-types.json`：稳定 group 类型。
- `apps/game/data/relationships/schemas/ledgers.json`：账本字段、范围和默认值。
- `apps/game/data/relationships/event-hooks/legacy-events.json`：旧事件名到标准事件的 hook。
- `apps/game/data/relationships/impact-rules/combat.json`：击杀、伤害、血仇、通缉影响规则。
- `apps/game/data/relationships/impact-rules/social.json`：救命恩、人情、背叛影响规则。
- `apps/game/data/relationships/impact-rules/faction.json`：发布/撤销通缉与势力声望规则。
- `apps/game/data/relationships/signal-rules/wanted-chain.json`：追杀令链信号规则。
- `apps/game/data/relationships/groups/groups.json`：第一版稳定组织来源。

修改文件：

- `apps/game/js/engine/world/relationship-system.js`：改为新系统门面，保留旧 API 兼容投影。
- `apps/game/js/engine/world/relationship-init.js`：改为写 tag/mark 或调用门面，不再依赖旧边为单一真相源。
- `apps/game/js/engine/npc/relationship.js`：`addGrudge/addGratitude/topGrudge/topGratitude` 委托新 mark 语义。
- `apps/game/js/engine/npc/npc-goals.js`：从 `SignalProvider` 读取 wanted/bloodFeud/lifeDebt 信号生成候选 Goal 或调制现有复仇链。
- `apps/game/js/engine/npc/npc-utility.js`：把 relationship modifiers 合入 Goal modulators。
- `apps/game/js/engine/world/tick-manager.js`：`_applyRelationEvent` 改为发布标准事件，`_resolveRevengeTarget` 读取 wanted/bloodFeud 信号。
- `apps/game/js/engine/world/services/world-context-builder.js`：在 worldContext 暴露 `relationshipSignals` 和标准 `emitRelationEvent` 端口。
- `apps/game/js/core/config-loader.js`：显式加载 `apps/game/data/relationships/` 新 JSON。
- `docs/data/data-config-rules.md`：新增 `data/relationships/` 目录说明。
- `docs/architecture/file-structure.md`：补充 `engine/relationship/` 和数据目录。
- `docs/README.md`：补充实施计划导航。

---

### Task 1: 写新平台 RED 测试

**Files:**
- Create: `apps/game/tools/test-relationship-platform.mjs`
- Create: `apps/game/tools/test-relationship-wanted-chain.mjs`

- [ ] **Step 1: 写账本、impact、signal 的失败测试**

```javascript
#!/usr/bin/env node
import { readFileSync } from 'node:fs';
import { fileURLToPath, pathToFileURL } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const load = (p) => JSON.parse(readFileSync(resolve(GAME_ROOT, p), 'utf-8'));
const imp = (p) => import(pathToFileURL(resolve(GAME_ROOT, p)).href);

const { RelationshipSystem } = await imp('js/engine/world/relationship-system.js');

const relationshipConfig = {
  schemas: { ledgers: load('data/relationships/schemas/ledgers.json') },
  dictionaries: {
    marks: load('data/relationships/dictionaries/marks.json'),
    tags: load('data/relationships/dictionaries/tags.json'),
    signals: load('data/relationships/dictionaries/signal-keys.json'),
    eventTypes: load('data/relationships/dictionaries/relation-event-types.json'),
    groupTypes: load('data/relationships/dictionaries/group-types.json'),
  },
  eventHooks: [load('data/relationships/event-hooks/legacy-events.json')],
  impactRules: [
    load('data/relationships/impact-rules/combat.json'),
    load('data/relationships/impact-rules/social.json'),
    load('data/relationships/impact-rules/faction.json'),
  ],
  signalRules: [load('data/relationships/signal-rules/wanted-chain.json')],
  groups: load('data/relationships/groups/groups.json'),
};

let failed = 0;
const assert = (condition, message) => {
  if (!condition) { console.error('  FAIL:', message); failed++; }
  else console.log('  OK:', message);
};

console.log('1) combat.kill public writes faction wanted ledger');
{
  const rs = new RelationshipSystem({ enabled: true, platform: relationshipConfig });
  rs.handleEvent({
    id: 'evt_kill_1',
    type: 'combat.kill',
    actor: { id: 'npc_killer', type: 'npc', factionId: 'sect_001', roleRank: 2 },
    target: { id: 'npc_victim', type: 'npc', factionId: 'sect_002', roleRank: 4 },
    visibility: 'public',
    witness: { count: 6 },
    day: 12,
    source: { type: 'action', id: 'act_npc_job_hunt_enemy' },
  });

  const ledger = rs.getFactionReputation('sect_002', 'npc_killer');
  assert(ledger && ledger.core.reputation < 0, '公开击杀降低目标势力对凶手的声望');
  assert(ledger && ledger.marks.some(m => m.type === 'wantedOrder' && m.weight >= 50), '公开击杀写入 wantedOrder mark');
  assert(rs.stats().byLayer.faction === 1, '势力账本统计为 1');
}

console.log('2) mark stacking max/add/instance works');
{
  const rs = new RelationshipSystem({ enabled: true, platform: relationshipConfig });
  rs.addMark({ layer: 'faction', factionId: 'sect_001', subjectId: 'npc_a', type: 'wantedOrder', weight: 40, day: 1 });
  rs.addMark({ layer: 'faction', factionId: 'sect_001', subjectId: 'npc_a', type: 'wantedOrder', weight: 75, day: 2 });
  rs.addMark({ layer: 'individual', subjectId: 'npc_b', objectId: 'npc_a', type: 'favorDebt', weight: 10, day: 2 });
  rs.addMark({ layer: 'individual', subjectId: 'npc_b', objectId: 'npc_a', type: 'favorDebt', weight: 15, day: 3 });

  const factionLedger = rs.getFactionReputation('sect_001', 'npc_a');
  const individualLedger = rs.getIndividualRelation('npc_b', 'npc_a');
  assert(factionLedger.marks.filter(m => m.type === 'wantedOrder').length === 1, 'wantedOrder 使用 max stacking 保留单条');
  assert(factionLedger.marks.find(m => m.type === 'wantedOrder').weight === 75, 'wantedOrder 保留最高权重');
  assert(individualLedger.marks.filter(m => m.type === 'favorDebt').length === 2, 'favorDebt 使用 instance stacking 保留多条');
}

console.log('3) snapshot/load preserves ledgers');
{
  const rs = new RelationshipSystem({ enabled: true, platform: relationshipConfig });
  rs.addTag({ layer: 'individual', subjectId: 'npc_a', objectId: 'npc_b', type: 'sameSect', day: 1 });
  rs.addMark({ layer: 'individual', subjectId: 'npc_a', objectId: 'npc_c', type: 'bloodFeud', weight: 80, day: 1 });
  const snap = rs.snapshot();
  const restored = new RelationshipSystem({ enabled: true, platform: relationshipConfig });
  restored.loadFrom(snap);
  assert(restored.getIndividualRelation('npc_a', 'npc_b').tags.some(t => t.type === 'sameSect'), 'tag 恢复');
  assert(restored.getIndividualRelation('npc_a', 'npc_c').marks.some(m => m.type === 'bloodFeud'), 'mark 恢复');
}

if (failed === 0) { console.log('\\n三层关系平台单元测试全部通过'); process.exit(0); }
console.error(`\\n三层关系平台单元测试失败：${failed} 项`);
process.exit(1);
```

- [ ] **Step 2: 写追杀令链失败测试**

```javascript
#!/usr/bin/env node
import { readFileSync } from 'node:fs';
import { fileURLToPath, pathToFileURL } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const load = (p) => JSON.parse(readFileSync(resolve(GAME_ROOT, p), 'utf-8'));
const imp = (p) => import(pathToFileURL(resolve(GAME_ROOT, p)).href);

const { RelationshipSystem } = await imp('js/engine/world/relationship-system.js');

function loadRelationshipPlatform() {
  return {
    schemas: { ledgers: load('data/relationships/schemas/ledgers.json') },
    dictionaries: {
      marks: load('data/relationships/dictionaries/marks.json'),
      tags: load('data/relationships/dictionaries/tags.json'),
      signals: load('data/relationships/dictionaries/signal-keys.json'),
      eventTypes: load('data/relationships/dictionaries/relation-event-types.json'),
      groupTypes: load('data/relationships/dictionaries/group-types.json'),
    },
    eventHooks: [load('data/relationships/event-hooks/legacy-events.json')],
    impactRules: [
      load('data/relationships/impact-rules/combat.json'),
      load('data/relationships/impact-rules/social.json'),
      load('data/relationships/impact-rules/faction.json'),
    ],
    signalRules: [load('data/relationships/signal-rules/wanted-chain.json')],
    groups: load('data/relationships/groups/groups.json'),
  };
}

let failed = 0;
const assert = (condition, message) => {
  if (!condition) { console.error('  FAIL:', message); failed++; }
  else console.log('  OK:', message);
};

console.log('1) wantedOrder produces hunt signals for faction member');
{
  const rs = new RelationshipSystem({ enabled: true, platform: loadRelationshipPlatform() });
  rs.addMark({ layer: 'faction', factionId: 'sect_001', subjectId: 'npc_wanted', type: 'wantedOrder', weight: 80, day: 1 });
  const signals = rs.getSignals({
    actor: { id: 'npc_hunter', factionId: 'sect_001' },
    target: { id: 'npc_wanted' },
    contextType: 'action',
    actionId: 'act_npc_job_hunt_enemy',
  });
  assert(signals.facts.isWantedByFaction === true, '输出 isWantedByFaction fact');
  assert(signals.gates.canTriggerWantedHunt === true, '输出 canTriggerWantedHunt gate');
  assert(signals.modifiers.huntWeight > 1, '通缉提高 huntWeight');
  assert(signals.targetPreferences.revengeTargetBonus >= 80, '通缉提高复仇目标偏好');
}

console.log('2) lifeDebt softens hunt signal and allows warning/intercede facts');
{
  const rs = new RelationshipSystem({ enabled: true, platform: loadRelationshipPlatform() });
  rs.addMark({ layer: 'faction', factionId: 'sect_001', subjectId: 'npc_wanted', type: 'wantedOrder', weight: 80, day: 1 });
  rs.addMark({ layer: 'individual', subjectId: 'npc_hunter', objectId: 'npc_wanted', type: 'lifeDebt', weight: 70, day: 1 });
  const signals = rs.getSignals({
    actor: { id: 'npc_hunter', factionId: 'sect_001' },
    target: { id: 'npc_wanted' },
    contextType: 'action',
    actionId: 'act_npc_job_hunt_enemy',
  });
  assert(signals.facts.hasLifeDebt === true, '救命恩输出 hasLifeDebt fact');
  assert(signals.modifiers.huntWeight < 3, '救命恩降低追杀权重');
  assert(signals.gates.canIntercede === true, '救命恩允许求情/放水信号');
}

console.log('3) bloodFeud adds personal hunt preference beyond faction wanted');
{
  const rs = new RelationshipSystem({ enabled: true, platform: loadRelationshipPlatform() });
  rs.addMark({ layer: 'individual', subjectId: 'npc_hunter', objectId: 'npc_enemy', type: 'bloodFeud', weight: 90, day: 1 });
  const signals = rs.getSignals({
    actor: { id: 'npc_hunter', factionId: 'sect_001' },
    target: { id: 'npc_enemy' },
    contextType: 'action',
    actionId: 'act_npc_job_hunt_enemy',
  });
  assert(signals.facts.hasBloodFeud === true, '输出 hasBloodFeud fact');
  assert(signals.modifiers.huntWeight > 2, '血仇提高 huntWeight');
  assert(signals.targetPreferences.revengeTargetBonus >= 90, '血仇提高目标偏好');
}

if (failed === 0) { console.log('\\n追杀令链关系信号测试全部通过'); process.exit(0); }
console.error(`\\n追杀令链关系信号测试失败：${failed} 项`);
process.exit(1);
```

- [ ] **Step 3: 运行 RED 测试并确认失败原因正确**

Run:

```powershell
node apps/game/tools/test-relationship-platform.mjs
node apps/game/tools/test-relationship-wanted-chain.mjs
```

Expected: 两个测试都失败，失败点为 `data/relationships/...` 文件不存在或 `RelationshipSystem` 尚无 `handleEvent/getSignals/addMark/addTag`。

### Task 2: 新增全数据关系配置

**Files:**
- Create all JSON files under `apps/game/data/relationships/`
- Modify: `docs/data/data-config-rules.md`

- [ ] **Step 1: 新增 schema 与字典配置**

写入 `schemas/ledgers.json`，核心范围固定为 `-100..100` 或 `0..100`，默认值为 0：

```json
{
  "layers": {
    "individual": {
      "core": {
        "affinity": { "min": -100, "max": 100, "default": 0 },
        "trust": { "min": -100, "max": 100, "default": 0 }
      }
    },
    "group": {
      "core": {
        "identity": { "min": -100, "max": 100, "default": 0 },
        "trust": { "min": -100, "max": 100, "default": 0 }
      }
    },
    "faction": {
      "core": {
        "reputation": { "min": -100, "max": 100, "default": 0 },
        "credit": { "min": -100, "max": 100, "default": 0 }
      }
    }
  },
  "potential": {
    "expectedPotential": { "min": 0, "max": 100, "default": 0 },
    "confidence": { "min": 0, "max": 100, "default": 0 },
    "strategicValue": { "min": 0, "max": 100, "default": 0 },
    "threatValue": { "min": 0, "max": 100, "default": 0 },
    "investmentIntent": { "min": 0, "max": 100, "default": 0 },
    "suppressionIntent": { "min": 0, "max": 100, "default": 0 }
  }
}
```

写入 `dictionaries/marks.json`，至少包含 `wantedOrder/favorDebt/lifeDebt/bloodFeud/betrayal/contributionMark/disciplineViolation/blacklist/protectedAsset/recruitmentInterest/strategicThreat`，并为 `wantedOrder` 使用 `stacking:"max"`，`favorDebt` 使用 `stacking:"instance"`。

- [ ] **Step 2: 新增第一版 rules**

`impact-rules/combat.json` 必须包含：

```json
{
  "rules": [
    {
      "id": "combat_kill_public_wanted_order",
      "priority": 80,
      "exclusiveGroup": "combat_kill_public_severity",
      "match": {
        "eventType": "combat.kill",
        "visibility": ["public", "group"],
        "conditions": [
          { "expr": "actor.id != target.id" },
          { "expr": "target.factionId != null" }
        ]
      },
      "effects": [
        {
          "layer": "faction",
          "faction": "$target.factionId",
          "subject": "$actor.id",
          "changes": {
            "core.reputation": { "delta": { "expr": "-clamp(target.roleRank * 8 + witness.count * 2, 20, 80)" } },
            "marks": [
              {
                "type": "wantedOrder",
                "weight": { "expr": "clamp(target.roleRank * 12 + witness.count * 2, 20, 100)" }
              }
            ]
          }
        }
      ]
    }
  ]
}
```

`signal-rules/wanted-chain.json` 必须包含 `wanted_hunt_signal`、`life_debt_soften_hunt`、`blood_feud_hunt_signal` 三条规则，输出 `isWantedByFaction`、`hasLifeDebt`、`hasBloodFeud`、`canTriggerWantedHunt`、`canIntercede`、`huntWeight`、`revengeTargetBonus`。

- [ ] **Step 3: 运行 JSON 解析验证**

Run:

```powershell
node -e "const fs=require('fs'); const path=require('path'); for (const f of fs.readdirSync('apps/game/data/relationships',{recursive:true})) if (f.endsWith('.json')) JSON.parse(fs.readFileSync(path.join('apps/game/data/relationships',f),'utf8')); console.log('relationship json ok')"
```

Expected: 输出 `relationship json ok`。

### Task 3: 实现账本仓储与解释器

**Files:**
- Create: `apps/game/js/engine/relationship/ledger-repository.js`
- Create: `apps/game/js/engine/relationship/expression-evaluator.js`
- Create: `apps/game/js/engine/relationship/selector-resolver.js`
- Create: `apps/game/js/engine/relationship/effect-operators.js`
- Create: `apps/game/js/engine/relationship/impact-engine.js`
- Create: `apps/game/js/engine/relationship/signal-provider.js`

- [ ] **Step 1: 实现 `LedgerRepository`**

实现 API：

```javascript
export class LedgerRepository {
  constructor({ schema, dictionaries } = {})
  getIndividual(subjectId, objectId, opts = {})
  getGroup(groupId, subjectId, opts = {})
  getFaction(factionId, subjectId, opts = {})
  addMark(ref, mark)
  addTag(ref, tag)
  applyCoreDelta(ref, path, delta)
  findLedgers(query)
  tick(day)
  removeEntity(entityId)
  snapshot()
  loadFrom(snapshot)
  stats()
}
```

`addMark` 必须按字典 `stacking` 处理：`unique/max/add/refresh/instance`。`applyCoreDelta` 必须按 schema clamp。

- [ ] **Step 2: 实现表达式、selector 和 effect operator**

`ExpressionEvaluator.evaluate(input, context)` 使用 `new Function` 但只暴露白名单上下文和函数，不把 `globalThis/window/process` 暴露给表达式。`SelectorResolver.resolve(selector, context)` 只解析 `$` 路径和简单 `marks[type=x]` 过滤。

- [ ] **Step 3: 实现 `RelationImpactEngine` 与 `RelationshipSignalProvider`**

`RelationImpactEngine.apply(event)` 流程：

```text
normalize event -> match rules -> resolve exclusiveGroup -> expand selector -> get ledger -> run effect operators -> append trace
```

`RelationshipSignalProvider.getSignals(context)` 合并规则输出时：

```text
facts/gates：布尔 true 优先；
modifiers：同 key 相乘，未命中默认 1；
targetPreferences：同 key 相加，未命中默认 0；
traces：记录 ruleId、ledgerKey、outputs。
```

- [ ] **Step 4: 运行 RED 测试到 GREEN**

Run:

```powershell
node apps/game/tools/test-relationship-platform.mjs
node apps/game/tools/test-relationship-wanted-chain.mjs
```

Expected: 两个测试通过。

### Task 4: 改造关系系统门面与旧 API 兼容

**Files:**
- Modify: `apps/game/js/engine/world/relationship-system.js`
- Modify: `apps/game/js/engine/world/relationship-init.js`
- Modify: `apps/game/js/engine/npc/relationship.js`
- Modify: `apps/game/js/engine/world/services/monster-respawn-service.js`

- [ ] **Step 1: 改造 `RelationshipSystem` facade**

保留旧 API：

```javascript
addEdge(fromId, toId, type, opts = {})
applyEvent(eventType, fromId, toId, opts = {})
getEdge(fromId, toId, type)
edgesFrom(fromId)
edgesOfType(fromId, type)
topEdgeOfType(fromId, type)
allEdges()
removeEntity(entityId)
tick()
snapshot()
loadFrom(snapshot)
```

兼容投影规则：

| 旧 type | 新语义 |
|---------|--------|
| `grudge/enemy/beast_grudge/territory_threat` | individual mark `bloodFeud` 或 `resourceGrudge` |
| `gratitude/benefactor` | individual mark `lifeDebt` 或 `favorDebt` |
| `same_sect/ally/master/disciple/dao_companion/pack_member/pack_leader` | individual tag |

`edgesOfType` 从账本 mark/tag 投影旧 edge 数组，调用点能继续读取 `toId/type/strength`。

- [ ] **Step 2: 改造 `RelationshipGraph`**

绑定模式下：

```javascript
addGrudge(actorId, amount) -> relationshipSystem.addMark({ layer:'individual', subjectId: ownerId, objectId: actorId, type:'bloodFeud', weight: amount })
addGratitude(actorId, amount) -> relationshipSystem.addMark({ layer:'individual', subjectId: ownerId, objectId: actorId, type:'lifeDebt', weight: amount })
topGrudge/topGratitude -> 使用 topEdgeOfType 兼容投影
```

- [ ] **Step 3: 运行旧兼容测试**

Run:

```powershell
node apps/game/tools/test-relationship.mjs
node apps/game/tools/test-relationship-goals.mjs
node apps/game/tools/test-master-disciple.mjs
```

Expected: 旧兼容测试通过；若旧测试断言旧边作为单一真相源，更新断言为新账本投影语义。

### Task 5: 接入配置加载、worldContext 和 NPC 信号消费

**Files:**
- Modify: `apps/game/js/core/config-loader.js`
- Modify: `apps/game/js/engine/world-engine.js`
- Modify: `apps/game/js/engine/world/tick-manager.js`
- Modify: `apps/game/js/engine/world/services/world-context-builder.js`
- Modify: `apps/game/js/engine/npc/npc-goals.js`
- Modify: `apps/game/js/engine/npc/npc-utility.js`

- [ ] **Step 1: `config-loader` 显式加载 relationship configs**

新增 `relationshipPlatform` 返回字段，结构与测试中的 `relationshipConfig.platform` 一致。

- [ ] **Step 2: `WorldEngine` 构造新系统**

把构造从：

```javascript
this.relationshipSystem = new RelationshipSystem(this._balanceConfig.relationship);
```

改为：

```javascript
this.relationshipSystem = new RelationshipSystem({
  ...this._balanceConfig.relationship,
  platform: configs.relationshipPlatform || null,
});
```

- [ ] **Step 3: worldContext 暴露端口**

`WorldContextBuilder.build()` 增加：

```javascript
emitRelationEvent(event) { return host.relationshipSystem?.handleEvent?.(event); },
relationshipSignals: host.relationshipSystem?.signals || null,
getRelationshipSignals(input) { return host.relationshipSystem?.getSignals?.(input) || null; }
```

- [ ] **Step 4: NPC 复仇目标读取 wanted/bloodFeud 信号**

`TickManager._resolveRevengeTarget(entity)` 在旧 grudge 后追加：

```javascript
const factionId = entity.state?.get?.('factionId') || null;
const wanted = this.relationshipSystem?.topWantedTargetForFaction?.(factionId, entity.id);
if (!targetId && wanted?.targetId) targetId = wanted.targetId;
```

`topWantedTargetForFaction` 必须跳过 actor 对目标有高 `lifeDebt` 且无 `bloodFeud` 的对象，避免救命恩对象被无脑追杀。

- [ ] **Step 5: Utility 合入 relationship modifiers**

`decorateGoalConsiderations` 对 `act_npc_job_hunt_enemy` 相关目标读取 `worldContext.getRelationshipSignals`。当信号有 `modifiers.huntWeight` 时向 goal 添加：

```javascript
goal.addModulator({ label: 'relationship.huntWeight', mult: signals.modifiers.huntWeight });
```

- [ ] **Step 6: 运行接入测试**

Run:

```powershell
node apps/game/tools/test-relationship-wanted-chain.mjs
node apps/game/tools/test-job-config-load.mjs
node apps/game/tools/test-job-action-planning.mjs
node apps/game/tools/test-revenge.mjs
```

Expected: 全部通过。

### Task 6: 文档、真实模拟和发布前验证

**Files:**
- Modify: `docs/data/data-config-rules.md`
- Modify: `docs/architecture/file-structure.md`
- Modify: `docs/README.md`
- Create: `docs/superpowers/reports/2026-06-07-三层关系底座全数据重构验证.md`

- [ ] **Step 1: 更新文档导航和数据目录规则**

`docs/data/data-config-rules.md` 增加 `relationships/` 目录树和字段规则；`docs/architecture/file-structure.md` 增加 `apps/game/js/engine/relationship/`；`docs/README.md` 增加实施计划和验证报告链接。

- [ ] **Step 2: 运行关系与 AI 回归**

Run:

```powershell
node apps/game/tools/test-relationship-platform.mjs
node apps/game/tools/test-relationship-wanted-chain.mjs
node apps/game/tools/test-relationship.mjs
node apps/game/tools/test-relationship-goals.mjs
node apps/game/tools/test-master-disciple.mjs
node apps/game/tools/test-revenge.mjs
node apps/game/tools/test-job-config-load.mjs
node apps/game/tools/test-job-action-planning.mjs
```

Expected: 全部通过。

- [ ] **Step 3: 运行真实模拟观察**

Run:

```powershell
node apps/game/tools/simulate-analysis.mjs 120
```

Expected: 报告中记录真实行为统计，至少观察关系账本非空、通缉信号样本、复仇/追杀相关事件样本。不得用指纹或哈希作为正确性证据。

- [ ] **Step 4: 写验证报告**

验证报告必须包含：

```markdown
# 三层关系底座全数据重构验证

> 最后更新：2026-06-07

## 验证命令

## 单元测试结果

## 追杀令链观察

## 真实模拟样本

## 结论

## 剩余风险
```

### Task 7: 合并主干并发布 GitHub

**Files:**
- Git branch: `codex/three-layer-relationship-data-rebuild`
- Trunk branch: `master`

- [ ] **Step 1: 检查工作区和提交**

Run:

```powershell
git -c core.quotePath=false status --short
git add apps/game docs
git commit -m "feat: 重构三层关系全数据底座"
```

Expected: commit 成功。

- [ ] **Step 2: 合并回 master**

Run:

```powershell
git switch master
git merge --no-ff codex/three-layer-relationship-data-rebuild -m "merge: 三层关系全数据底座"
```

Expected: merge 成功，无冲突。

- [ ] **Step 3: GitHub 发布前验证**

Run:

```powershell
gh auth status
git remote -v
git status --short --branch
```

Expected: `gh auth status` 已登录，当前分支为 `master`，工作区干净。

- [ ] **Step 4: 使用 GitHub API 同步远端**

按项目规则，发布时使用 GitHub API 路径。若本地 merge commit 已完成，优先用 `gh api repos/1656970772/CultivationWorld/git/refs/heads/master` 校验远端，再按需要更新远端 ref。完成后验证：

```powershell
git fetch origin master
git status --short --branch
git log --oneline --decorate -5
```

Expected: `master` 与 `origin/master` 跟踪正常，远端包含本次 merge commit。

---

## 自审

- 规格覆盖：计划覆盖新三层账本、全数据配置、事件 hook、impact 规则、signal provider、旧 API 空壳兼容、追杀令链、真实模拟验证和 GitHub 发布。
- 占位扫描：本计划不包含 `TBD` 或空泛的“以后实现”；延后项均在规格文档中说明，不作为本轮任务。
- 类型一致性：计划统一使用 `RelationshipSystem.handleEvent/getSignals/addMark/addTag`、`LedgerRepository`、`RelationImpactEngine`、`RelationshipSignalProvider`、`wantedOrder/lifeDebt/bloodFeud`。
