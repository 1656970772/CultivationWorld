# 修为数值化与突破重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 最后更新：2026-06-07

**Goal:** 将 NPC 修炼进度彻底改为数值修为，移除旧 0..1 比例字段，并用统一接口收口闭关、历练、小层和突破成功/失败结算。

**Architecture:** `numeric-cultivation.js` 成为修为规则唯一入口，负责需求读取、数值累加、小层推导、闭关递减、突破判定与成功/失败字段结算。运行时状态、GOAP、UI、报告和工具只读写 `cultivation`、`experienceCultivation`、`totalCultivation`、`qi`、`rankStage`，不再输出或依赖 `cultivationProgress`、`insight`、`totalProgress`、`cultivationProgressRatio`。本轮不扩展 `ranks.json` 到 12 境界，不实现功法感悟、天劫、心魔、材料消耗链或境界跌落。

**Tech Stack:** ESM JavaScript、JSON 数据配置、NPCState/NPCEntity/Need/GOAP、Job/Toil、WorldEngine 快照、前端状态面板、Node 工具测试、真实多种子模拟、Markdown 文档。

---

## Scope Check

本规格涉及多个调用层，但它们共享同一目标：删除旧比例字段并把修为数值作为唯一真相源。可以作为一个实施计划推进，按以下可独立提交任务拆分：

1. 先建立统一修为接口和 focused tests。
2. 再改配置与状态初始化，阻断旧字段继续产生。
3. 然后接入闭关、历练和突破结算。
4. 再清理 GOAP、Utility、UI、工具和文档。
5. 最后跑 focused tests 与真实模拟验证。

## File Structure

### Core Runtime

- Modify: `apps/game/js/engine/npc/numeric-cultivation.js`
  修为规则唯一入口，删除比例迁移函数，新增小层、递减、成功/失败结算。
- Modify: `apps/game/js/engine/npc/npc-state.js`
  初始化数值修为字段，删除旧比例字段同步逻辑，`toGOAPState()` 输出数值修为辅助键。
- Modify: `apps/game/js/engine/npc/services/cultivation-service.js`
  闭关收益改为数值修为，按 `cultivation / cultivationRequired` 递减。
- Modify: `apps/game/js/engine/npc/cultivation-experience.js`
  只增加 `experienceCultivation`，不再同步旧 `insight`。
- Modify: `apps/game/js/engine/npc/npc-lifecycle.js`
  突破成功/失败调用统一接口，失败真气 50%、修为 20%、重伤 3。
- Modify: `apps/game/js/engine/npc/npc-entity.js`
  删除闭关 cap 前置注入；刷新派生状态时读取数值修为短板。

### AI And Behavior

- Modify: `apps/game/js/engine/abstract/need.js`
  增量目标支持数值修为：`stepRatioOf`、`maxOf`。
- Modify: `apps/game/js/engine/npc/npc-needs.js`
  fallback 修炼/破境评估器改读数值修为。
- Modify: `apps/game/data/needs/npc-needs.json`
  `need_npc_cultivation`、破境丹、猎妖资源、晋升等规则改读数值字段。
- Modify: `apps/game/data/balance/utility.json`
  Utility 输入从旧比例字段改为 `totalCultivation`、`cultivation`、`nextCultivationRequired` 或新派生键。
- Modify: `apps/game/data/balance/obsession.json`
  执念目标不再使用 `totalProgress`。
- Modify: `apps/game/js/engine/npc/dynamic-goals.js`、`apps/game/js/engine/npc/interrupt-policy.js`
  动态目标动机和打断策略改用数值修为完成度。
- Modify: `apps/game/js/engine/npc/actions/combat-actions.js`、`apps/game/js/engine/npc/toils/cultivation-toils.js`
  游历产出从 `insight` 改成 `experienceCultivation`。
- Modify: `apps/game/js/engine/npc/actions/relationship-actions.js`、`apps/game/js/engine/npc/toils/social-toils.js`、`apps/game/js/engine/npc/npc-goals.js`
  师傅传功从 `insightGain` 改为历练修为/点化修为收益。
- Modify: `apps/game/data/balance/relationship.json`
  `teachDisciple.insightGain` 改为 `experienceCultivationGain`。

### Data And UI

- Modify: `apps/game/data/definitions/ranks.json`
  `cultivationRequired` 与 `qiRequired` 同步。
- Modify: `apps/game/data/balance/cultivation.json`
  删除/废弃 `cultivationCap`，新增 `stageThresholds`、失败保留比例和重伤配置。
- Modify: `apps/game/js/engine/world-engine.js`
  快照移除旧比例字段，输出数值修为、下一境界需求、小层。
- Modify: `apps/game/js/ui/follow-entity-status.js`
  跟随面板只显示真气、修炼修为、历练修为、总修为和小层。
- Modify: `apps/game/tools/report.html`、`apps/game/tools/simulate-analysis.mjs`、`apps/game/tools/trace-genius.mjs`
  报告和分析工具改用数值修为。

### Tests And Docs

- Replace: `apps/game/tools/test-numeric-cultivation-migration.mjs`
  改为 `test-numeric-cultivation.mjs` 或保留文件名但测试纯数值接口。
- Modify: existing tests under `apps/game/tools/` that assert old fields.
- Create: `apps/game/tools/test-cultivation-config-numeric.mjs`
- Create: `apps/game/tools/test-numeric-cultivation-runtime-cleanup.mjs`
- Create: `docs/superpowers/reports/2026-06-07-修为数值化与突破重构验证.md`
- Modify: `docs/README.md`
- Modify: `docs/data-models/ranks.md`
- Modify: `docs/worldbuilding/wiki/rules/travel-and-risk.md`
- Modify: `docs/data/data-config-rules.md`
- Modify: `docs/decisions/adr-051-numeric-cultivation-and-job-action-migration.md`

---

## Task 1: 统一修为接口与 focused tests

**Files:**
- Modify: `apps/game/js/engine/npc/numeric-cultivation.js`
- Replace: `apps/game/tools/test-numeric-cultivation-migration.mjs`

- [ ] **Step 1: Replace the old migration test with numeric-only tests**

Replace `apps/game/tools/test-numeric-cultivation-migration.mjs` with:

```js
#!/usr/bin/env node

let failed = 0;
function assert(cond, msg) {
  if (!cond) {
    console.error('  FAIL:', msg);
    failed++;
  } else {
    console.log('  OK:', msg);
  }
}
function equal(actual, expected, msg) {
  assert(Object.is(actual, expected), `${msg} (${actual} === ${expected})`);
}
function close(actual, expected, msg, epsilon = 1e-9) {
  assert(Math.abs(actual - expected) <= epsilon, `${msg} (${actual} ~= ${expected})`);
}

const mod = await import(new URL('../js/engine/npc/numeric-cultivation.js', import.meta.url).href);

const ranks = [
  { id: 'mortal', order: 0, category: 'mortal' },
  { id: 'qi_refining', name: '炼气', order: 20, category: 'cultivation', cultivationRequired: 50, qiRequired: 50 },
  { id: 'foundation_building', name: '筑基', order: 40, category: 'cultivation', cultivationRequired: 500, qiRequired: 500 },
];

function entity(initial = {}) {
  const data = new Map(Object.entries({
    rankId: 'mortal',
    rankName: '凡人',
    rankStage: null,
    cultivation: 0,
    experienceCultivation: 0,
    totalCultivation: 0,
    qi: 0,
    injuryLevel: 0,
    ...initial,
  }));
  return {
    _ranksData: ranks,
    _cultivationConfig: {
      minCultivationRatio: 0.3,
      stageThresholds: { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 },
      breakthrough: {
        failureQiRetention: 0.5,
        failureCultivationRetention: 0.2,
        failureInjuryLevel: 3,
      },
    },
    state: {
      get: (k) => data.get(k),
      set: (k, v) => data.set(k, v),
    },
    data,
  };
}

console.log('1) next rank and requirements');
{
  const e = entity();
  equal(mod.nextCultivationRank(e, ranks).id, 'qi_refining', 'next rank from mortal is qi_refining');
  equal(mod.getCultivationRequired(e, ranks), 50, 'cultivation required mirrors qiRequired');
  equal(mod.getTotalCultivation(e), 0, 'initial total cultivation is 0');
}

console.log('2) add cultivation and experience');
{
  const e = entity();
  equal(mod.addCultivation(e, ranks, 12), 12, 'addCultivation returns total');
  equal(e.data.get('cultivation'), 12, 'cultivation increased');
  equal(e.data.get('experienceCultivation'), 0, 'experience unchanged');
  equal(e.data.get('totalCultivation'), 12, 'total syncs after cultivation');
  equal(mod.addExperienceCultivation(e, ranks, 8), 20, 'addExperienceCultivation returns new total');
  equal(e.data.get('experienceCultivation'), 8, 'experience increased');
  equal(e.data.get('totalCultivation'), 20, 'total syncs after experience');
}

console.log('3) rank stage thresholds use total cultivation');
{
  const e = entity({ cultivation: 10, experienceCultivation: 5, totalCultivation: 15 });
  equal(mod.computeRankStage(e, ranks, { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 }), 'middle', '15/50 enters middle');
  e.state.set('experienceCultivation', 18);
  mod.syncTotalCultivation(e, ranks);
  equal(mod.refreshRankStage(e, ranks, { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 }), 'late', '30/50 enters late');
  e.state.set('experienceCultivation', 36);
  mod.syncTotalCultivation(e, ranks);
  equal(mod.refreshRankStage(e, ranks, { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 }), 'perfection', '46/50 enters perfection');
}

console.log('4) cultivation gain decays from cultivation only');
{
  const cfg = { cultivationDecayK: 2.5 };
  const fresh = entity({ cultivation: 0, experienceCultivation: 45, totalCultivation: 45 });
  const rooted = entity({ cultivation: 25, experienceCultivation: 0, totalCultivation: 25 });
  close(mod.computeCultivationGain(fresh, ranks, 10, cfg), 10, 'experience does not slow retreat cultivation');
  const expected = 10 * Math.exp(-2.5 * (25 / 50));
  close(mod.computeCultivationGain(rooted, ranks, 10, cfg), expected, 'cultivation slows retreat cultivation');
}

console.log('5) breakthrough gates');
{
  const e = entity({ cultivation: 14, experienceCultivation: 36, totalCultivation: 50, qi: 50 });
  assert(mod.canAttemptBreakthrough(e, ranks, { minCultivationRatio: 0.3 }) === false, 'needs 30% cultivation root');
  e.state.set('cultivation', 15);
  e.state.set('experienceCultivation', 35);
  mod.syncTotalCultivation(e, ranks);
  assert(mod.canAttemptBreakthrough(e, ranks, { minCultivationRatio: 0.3 }) === true, 'can break through with total, root cultivation, and qi');
}

console.log('6) breakthrough success clears cultivation and spends qi');
{
  const e = entity({ cultivation: 20, experienceCultivation: 30, totalCultivation: 50, qi: 60 });
  const next = mod.nextCultivationRank(e, ranks);
  mod.applyBreakthroughSuccess(e, next, { qiRequired: next.qiRequired });
  equal(e.data.get('rankId'), 'qi_refining', 'rankId updated');
  equal(e.data.get('rankName'), '炼气', 'rankName updated');
  equal(e.data.get('rankStage'), 'early', 'new rank starts early');
  equal(e.data.get('cultivation'), 0, 'cultivation cleared');
  equal(e.data.get('experienceCultivation'), 0, 'experience cleared');
  equal(e.data.get('totalCultivation'), 0, 'total cleared');
  equal(e.data.get('qi'), 10, 'qi spent');
}

console.log('7) breakthrough failure retention and injury');
{
  const e = entity({ cultivation: 30, experienceCultivation: 20, totalCultivation: 50, qi: 80, injuryLevel: 1 });
  mod.applyBreakthroughFailure(e, { breakthrough: { failureQiRetention: 0.5, failureCultivationRetention: 0.2, failureInjuryLevel: 3 } });
  equal(e.data.get('qi'), 40, 'qi retains 50%');
  close(e.data.get('cultivation'), 6, 'cultivation retains 20%');
  close(e.data.get('experienceCultivation'), 4, 'experience retains 20%');
  close(e.data.get('totalCultivation'), 10, 'total recomputed after failure');
  equal(e.data.get('injuryLevel'), 3, 'injury raised to heavy wound');
}

console.log('8) old ratio helpers are gone');
for (const name of ['syncNumericCultivationFromRatios', 'syncProgressRatiosFromNumeric', 'migrateProgressToNumericCultivation', 'nextCultivationRequired']) {
  equal(typeof mod[name], 'undefined', `${name} is removed`);
}

if (failed > 0) {
  console.error(`\nNumeric cultivation tests failed: ${failed}`);
  process.exit(1);
}

console.log('\nNumeric cultivation tests passed');
```

- [ ] **Step 2: Run the test and verify it fails on old API**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-numeric-cultivation-migration.mjs
```

Expected: FAIL because `getCultivationRequired`, `computeRankStage`, `computeCultivationGain`, `applyBreakthroughSuccess`, or `applyBreakthroughFailure` is not defined.

- [ ] **Step 3: Replace `numeric-cultivation.js` with the unified numeric service**

Replace `apps/game/js/engine/npc/numeric-cultivation.js` with:

```js
function readState(entity, key, fallback = null) {
  const value = typeof entity?.state?.get === 'function'
    ? entity.state.get(key)
    : entity?.state?.[key];
  return value ?? fallback;
}

function writeState(entity, key, value) {
  if (typeof entity?.state?.set === 'function') {
    entity.state.set(key, value);
    return;
  }
  if (entity?.state) entity.state[key] = value;
}

function numeric(value, fallback = 0) {
  const n = Number(value);
  return Number.isFinite(n) ? n : fallback;
}

export function nextCultivationRank(entityOrRankId, ranks = []) {
  const rankId = typeof entityOrRankId === 'string'
    ? entityOrRankId
    : entityOrRankId?.state?.get?.('rankId');
  const current = ranks.find(r => r.id === rankId);
  const currentOrder = current?.order ?? 0;
  return ranks
    .filter(r => r.category === 'cultivation' || Number.isFinite(r.cultivationRequired) || Number.isFinite(r.qiRequired))
    .sort((a, b) => (a.order || 0) - (b.order || 0))
    .find(r => (r.order || 0) > currentOrder) || null;
}

export function getCultivationRequired(entityOrRankId, ranks = []) {
  const next = nextCultivationRank(entityOrRankId, ranks);
  return numeric(next?.cultivationRequired, numeric(next?.qiRequired, 0));
}

export function getTotalCultivation(entity) {
  const existing = readState(entity, 'totalCultivation', null);
  if (existing != null) return numeric(existing, 0);
  return numeric(readState(entity, 'cultivation', 0)) + numeric(readState(entity, 'experienceCultivation', 0));
}

export function syncTotalCultivation(entity) {
  const total = numeric(readState(entity, 'cultivation', 0)) + numeric(readState(entity, 'experienceCultivation', 0));
  writeState(entity, 'totalCultivation', total);
  return total;
}

export function cultivationCompletion(entity, ranks = []) {
  const required = getCultivationRequired(entity, ranks);
  if (required <= 0) return 0;
  return getTotalCultivation(entity) / required;
}

export function cultivationRootCompletion(entity, ranks = []) {
  const required = getCultivationRequired(entity, ranks);
  if (required <= 0) return 0;
  return numeric(readState(entity, 'cultivation', 0)) / required;
}

export function stageThresholds(config = {}) {
  return config.stageThresholds || { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 };
}

export function computeRankStage(entity, ranks = [], thresholds = {}) {
  const rankId = readState(entity, 'rankId', 'mortal');
  if (rankId === 'mortal') return null;
  const t = { early: 0, middle: 0.25, late: 0.6, perfection: 0.9, ...thresholds };
  const completion = cultivationCompletion(entity, ranks);
  if (completion >= t.perfection) return 'perfection';
  if (completion >= t.late) return 'late';
  if (completion >= t.middle) return 'middle';
  return 'early';
}

export function refreshRankStage(entity, ranks = [], thresholds = {}) {
  const stage = computeRankStage(entity, ranks, thresholds);
  writeState(entity, 'rankStage', stage);
  return stage;
}

export function addCultivation(entity, ranks = [], gain = 0, config = {}) {
  const amount = numeric(gain);
  if (amount > 0) {
    writeState(entity, 'cultivation', numeric(readState(entity, 'cultivation', 0)) + amount);
  }
  const total = syncTotalCultivation(entity);
  refreshRankStage(entity, ranks, stageThresholds(config));
  return total;
}

export function addExperienceCultivation(entity, ranks = [], gain = 0, config = {}) {
  const amount = numeric(gain);
  if (amount > 0) {
    writeState(entity, 'experienceCultivation', numeric(readState(entity, 'experienceCultivation', 0)) + amount);
  }
  const total = syncTotalCultivation(entity);
  refreshRankStage(entity, ranks, stageThresholds(config));
  return total;
}

export function computeCultivationGain(entity, ranks = [], baseGain = 0, config = {}) {
  const required = getCultivationRequired(entity, ranks);
  const completion = required > 0 ? numeric(readState(entity, 'cultivation', 0)) / required : 0;
  const decayK = numeric(config.cultivationDecayK, 2.5);
  return Math.max(0, numeric(baseGain, 0)) * Math.exp(-decayK * Math.max(0, completion));
}

export function canAttemptBreakthrough(entity, ranks = [], cultivationConfig = {}) {
  const next = nextCultivationRank(entity, ranks);
  if (!next) return false;
  const required = numeric(next.cultivationRequired, numeric(next.qiRequired, 0));
  const total = getTotalCultivation(entity);
  const cultivation = numeric(readState(entity, 'cultivation', 0));
  const qi = numeric(readState(entity, 'qi', 0));
  const minRatio = cultivationConfig.minCultivationRatio ?? 0.3;
  return total >= required
    && cultivation >= required * minRatio
    && qi >= numeric(next.qiRequired, 0);
}

export function applyBreakthroughSuccess(entity, nextRank, opts = {}) {
  if (!nextRank) return;
  const qiRequired = numeric(opts.qiRequired, numeric(nextRank.qiRequired, 0));
  const currentQi = numeric(readState(entity, 'qi', 0));
  writeState(entity, 'rankId', nextRank.id);
  writeState(entity, 'rankName', nextRank.name);
  writeState(entity, 'rankStage', nextRank.id === 'mortal' ? null : 'early');
  writeState(entity, 'cultivation', 0);
  writeState(entity, 'experienceCultivation', 0);
  writeState(entity, 'totalCultivation', 0);
  writeState(entity, 'qi', currentQi - qiRequired);
}

export function applyBreakthroughFailure(entity, cultivationConfig = {}) {
  const cfg = cultivationConfig.breakthrough || cultivationConfig || {};
  const qiRetention = numeric(cfg.failureQiRetention, 0.5);
  const cultivationRetention = numeric(cfg.failureCultivationRetention, 0.2);
  const injuryLevel = numeric(cfg.failureInjuryLevel, 3);
  writeState(entity, 'qi', Math.floor(numeric(readState(entity, 'qi', 0)) * qiRetention));
  writeState(entity, 'cultivation', numeric(readState(entity, 'cultivation', 0)) * cultivationRetention);
  writeState(entity, 'experienceCultivation', numeric(readState(entity, 'experienceCultivation', 0)) * cultivationRetention);
  syncTotalCultivation(entity);
  writeState(entity, 'injuryLevel', Math.max(numeric(readState(entity, 'injuryLevel', 0)), injuryLevel));
}
```

- [ ] **Step 4: Run the numeric service test**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-numeric-cultivation-migration.mjs
```

Expected: PASS and output `Numeric cultivation tests passed`.

- [ ] **Step 5: Commit Task 1**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/js/engine/npc/numeric-cultivation.js' 'apps/game/tools/test-numeric-cultivation-migration.mjs'; git commit -m "refactor: centralize numeric cultivation rules"
```

Expected: commit succeeds.

---

## Task 2: 配置数据改为纯数值修为

**Files:**
- Modify: `apps/game/data/definitions/ranks.json`
- Modify: `apps/game/data/balance/cultivation.json`
- Create: `apps/game/tools/test-cultivation-config-numeric.mjs`

- [ ] **Step 1: Write the config validation test**

Create `apps/game/tools/test-cultivation-config-numeric.mjs`:

```js
#!/usr/bin/env node
import { readFileSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const load = (path) => JSON.parse(readFileSync(resolve(GAME_ROOT, path), 'utf-8'));

let failed = 0;
function assert(cond, msg) {
  if (!cond) {
    console.error('  FAIL:', msg);
    failed++;
  } else {
    console.log('  OK:', msg);
  }
}

const ranks = load('data/definitions/ranks.json');
const cult = load('data/balance/cultivation.json');

console.log('1) cultivationRequired mirrors qiRequired');
for (const rank of ranks.filter(r => Number.isFinite(r.qiRequired))) {
  assert(rank.cultivationRequired === rank.qiRequired, `${rank.id} cultivationRequired equals qiRequired`);
}

console.log('2) no cultivationCap hard limit remains');
assert(!Object.prototype.hasOwnProperty.call(cult, 'cultivationCap'), 'cultivationCap removed from cultivation.json');

console.log('3) stage thresholds are configured');
assert(cult.stageThresholds?.early === 0, 'early threshold is 0');
assert(cult.stageThresholds?.middle === 0.25, 'middle threshold is 0.25');
assert(cult.stageThresholds?.late === 0.6, 'late threshold is 0.6');
assert(cult.stageThresholds?.perfection === 0.9, 'perfection threshold is 0.9');

console.log('4) failure punishment configured');
assert(cult.breakthrough?.failureQiRetention === 0.5, 'failure keeps 50% qi');
assert(cult.breakthrough?.failureCultivationRetention === 0.2, 'failure keeps 20% cultivation');
assert(cult.breakthrough?.failureInjuryLevel === 3, 'failure causes heavy wound level 3');

console.log('5) comments no longer describe progress/insight/cap semantics');
const text = readFileSync(resolve(GAME_ROOT, 'data/balance/cultivation.json'), 'utf-8');
for (const forbidden of ['cultivationProgress', 'insight', 'totalProgress', 'cultivationCap']) {
  assert(!text.includes(forbidden), `cultivation.json does not mention ${forbidden}`);
}

if (failed > 0) {
  console.error(`\nCultivation config numeric tests failed: ${failed}`);
  process.exit(1);
}

console.log('\nCultivation config numeric tests passed');
```

- [ ] **Step 2: Run config test and verify it fails**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-cultivation-config-numeric.mjs
```

Expected: FAIL because `cultivationRequired` differs from `qiRequired` and `cultivationCap` still exists.

- [ ] **Step 3: Update `ranks.json` requirements**

Modify `apps/game/data/definitions/ranks.json` so each cultivation rank with `qiRequired` has matching `cultivationRequired`:

```json
{
  "id": "qi_refining",
  "cultivationRequired": 50,
  "qiRequired": 50
}
```

```json
{
  "id": "foundation_building",
  "cultivationRequired": 500,
  "qiRequired": 500
}
```

```json
{
  "id": "golden_core",
  "cultivationRequired": 5000,
  "qiRequired": 5000
}
```

```json
{
  "id": "nascent_soul",
  "cultivationRequired": 50000,
  "qiRequired": 50000
}
```

```json
{
  "id": "spirit_transformation",
  "cultivationRequired": 500000,
  "qiRequired": 500000
}
```

- [ ] **Step 4: Update `cultivation.json` numeric semantics**

Modify `apps/game/data/balance/cultivation.json`:

- Remove the top-level `cultivationCap` object.
- Keep `cultivationDecayK`, but change its comment to:

```json
"_cultivationDecayKComment": "闭关修为递减系数。每次闭关修为增量 = 基础修为收益 × e^(-k × cultivation / cultivationRequired)：闭关根基越厚，继续闭关越慢；历练修为不参与递减。"
```

- Change `_minCultivationRatioComment` to:

```json
"_minCultivationRatioComment": "突破所需总修为中，闭关修为 cultivation 的最低占比。默认 0.3 表示至少 30% 修为必须来自闭关/修炼场/丹药等直接修炼，不能完全依靠历练修为突破。"
```

- Add top-level `stageThresholds` after `minCultivationRatio`:

```json
"stageThresholds": {
  "_comment": "境界小层由 totalCultivation / cultivationRequired 推导：0-25% 初期，25-60% 中期，60-90% 后期，90%+ 圆满。",
  "early": 0,
  "middle": 0.25,
  "late": 0.6,
  "perfection": 0.9
}
```

- In `breakthrough`, set/add:

```json
"failureQiRetention": 0.5,
"failureCultivationRetention": 0.2,
"failureInjuryLevel": 3
```

- Rewrite `qiPerProgress._comment` so it says `qiPerProgress` is a temporary legacy name for “真气随本次闭关修为收益同步增长的倍率”; do not mention `cultivationProgress` or `cultivationCap`.

- Rewrite `actions.explore` comments and field names in a later task; for this config task, remove comments that mention `insight` as the main semantic.

- [ ] **Step 5: Run config test**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-cultivation-config-numeric.mjs
```

Expected: PASS and output `Cultivation config numeric tests passed`.

- [ ] **Step 6: Run numeric service test again**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-numeric-cultivation-migration.mjs
```

Expected: PASS.

- [ ] **Step 7: Commit Task 2**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/data/definitions/ranks.json' 'apps/game/data/balance/cultivation.json' 'apps/game/tools/test-cultivation-config-numeric.mjs'; git commit -m "data: convert cultivation config to numeric values"
```

Expected: commit succeeds.

---

## Task 3: NPCState 与快照删除旧比例字段

**Files:**
- Modify: `apps/game/js/engine/npc/npc-state.js`
- Modify: `apps/game/js/engine/world-engine.js`
- Create: `apps/game/tools/test-numeric-cultivation-runtime-cleanup.mjs`
- Modify: `apps/game/tools/test-world-snapshot-npc-status-fields.mjs`

- [ ] **Step 1: Write runtime cleanup test**

Create `apps/game/tools/test-numeric-cultivation-runtime-cleanup.mjs`:

```js
#!/usr/bin/env node
import { readFileSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const read = (path) => readFileSync(resolve(GAME_ROOT, path), 'utf-8');

let failed = 0;
function assert(cond, msg) {
  if (!cond) {
    console.error('  FAIL:', msg);
    failed++;
  } else {
    console.log('  OK:', msg);
  }
}

const bannedRuntimeFields = ['cultivationProgress', 'insight', 'totalProgress', 'cultivationProgressRatio', 'cultivationCap'];

console.log('1) npc-state no longer initializes or syncs old fields');
const npcState = read('js/engine/npc/npc-state.js');
for (const field of bannedRuntimeFields) {
  assert(!npcState.includes(field), `npc-state.js does not mention ${field}`);
}
for (const field of ['cultivation', 'experienceCultivation', 'totalCultivation', 'rankStage']) {
  assert(npcState.includes(field), `npc-state.js contains ${field}`);
}

console.log('2) world snapshot no longer outputs old fields');
const worldEngine = read('js/engine/world-engine.js');
for (const field of bannedRuntimeFields) {
  assert(!worldEngine.includes(field), `world-engine.js does not mention ${field}`);
}
for (const field of ['cultivation', 'experienceCultivation', 'totalCultivation', 'nextCultivationRequired', 'rankStage']) {
  assert(worldEngine.includes(field), `world-engine.js snapshot includes ${field}`);
}

console.log('3) no ratio migration functions remain in runtime');
const numeric = read('js/engine/npc/numeric-cultivation.js');
for (const name of ['syncNumericCultivationFromRatios', 'syncProgressRatiosFromNumeric', 'migrateProgressToNumericCultivation']) {
  assert(!numeric.includes(name), `${name} removed`);
}

if (failed > 0) {
  console.error(`\nNumeric cultivation runtime cleanup tests failed: ${failed}`);
  process.exit(1);
}

console.log('\nNumeric cultivation runtime cleanup tests passed');
```

- [ ] **Step 2: Run cleanup test and verify it fails**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-numeric-cultivation-runtime-cleanup.mjs
```

Expected: FAIL because `npc-state.js` and `world-engine.js` still mention old fields.

- [ ] **Step 3: Update `npc-state.js` imports**

Modify `apps/game/js/engine/npc/npc-state.js` imports:

```js
import {
  getCultivationRequired,
  syncTotalCultivation,
  refreshRankStage,
} from './numeric-cultivation.js';
import { normalizeRankStage } from './cultivator-combat-attributes.js';
```

Remove the import of `nextCultivationRequired`.

- [ ] **Step 4: Replace initial cultivation state**

In `NPCState` constructor, replace the initial progress block with numeric initialization:

```js
const initialCultivationRequired = getCultivationRequired(initialRankId, ranksData || []);
const initialCultivation = random.next() * initialCultivationRequired * 0.3;
```

In `super({ ... })`, keep:

```js
cultivation: initialCultivation,
experienceCultivation: 0,
totalCultivation: initialCultivation,
qi: 0,
rankStage: normalizeRankStage(npcConfig.rankStage, initialRankId),
```

Remove these keys entirely:

```js
cultivationProgress
insight
cultivationProgressRatio
totalProgress
```

- [ ] **Step 5: Replace sync methods in `npc-state.js`**

Delete `_syncTotalProgress()`, `_isCultivationSyncKey()`, and `_syncCultivationFields()`.

Replace `set()` and `setMany()` with:

```js
set(key, value) {
  super.set(key, value);
  if (this._isNumericCultivationKey(key)) {
    this._syncNumericCultivation();
  }
}

setMany(updates) {
  super.setMany(updates);
  if (Object.keys(updates || {}).some(k => this._isNumericCultivationKey(k))) {
    this._syncNumericCultivation();
  }
}

_isNumericCultivationKey(key) {
  return key === 'cultivation'
    || key === 'experienceCultivation'
    || key === 'rankId';
}

_syncNumericCultivation() {
  syncTotalCultivation(this);
  refreshRankStage(this, this._ranksData || [], { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 });
}
```

Do not widen the `NPCState` constructor for cultivation config. Use the default thresholds in `NPCState`; runtime systems that already hold config should call `refreshRankStage()` with configured thresholds after they update cultivation values.

- [ ] **Step 6: Replace `toGOAPState()`**

Replace `toGOAPState()` with:

```js
toGOAPState() {
  const flat = super.toGOAPState();
  const required = getCultivationRequired(this, this._ranksData || []);
  const total = this.get('totalCultivation') || 0;
  flat.nextCultivationRequired = required;
  flat.cultivationShortfall = Math.max(0, required - total);
  flat.cultivationRootShortfall = Math.max(0, required * 0.3 - (this.get('cultivation') || 0));
  flat.canBreakthroughByCultivation = total >= required && (this.get('cultivation') || 0) >= required * 0.3;
  return flat;
}
```

Do not add a new ratio field to GOAP state.

- [ ] **Step 7: Update `world-engine.js` snapshot fields**

In `_npcStatusFields(n)`, remove `cultivationCap`, `retreatCultivationCap`, `insight`, `totalProgress`, `maxInsight`.

Return these fields:

```js
const nextRank = this._nextCultivationRank(rankId);
const nextCultivationRequired = nextRank?.cultivationRequired || nextRank?.qiRequired || null;
const cultivation = n.state.get('cultivation') || 0;
const experienceCultivation = n.state.get('experienceCultivation') || 0;
const totalCultivation = n.state.get('totalCultivation') || (cultivation + experienceCultivation);

return {
  hp: n.state.get('hp') || 0,
  maxHp: n.state.get('maxHp') || 0,
  injuryLevel: n.state.get('injuryLevel') || 0,
  lifeRatio: n.state.get('lifeRatio') || 0,
  cultivation,
  experienceCultivation,
  totalCultivation,
  nextCultivationRequired,
  rankStage: n.state.get('rankStage') || null,
  actionRemaining: n.state.get('actionRemaining') || 0,
  minCultivationRatio,
  nextRankName: nextRank?.name || null,
  nextQiRequired: nextRank?.qiRequired || null,
  ...
};
```

Keep the existing quest and talent fields after the snippet.

- [ ] **Step 8: Update snapshot test expectations**

Modify `apps/game/tools/test-world-snapshot-npc-status-fields.mjs`:

- Remove assertions for `npc.insight`, `npc.totalProgress`, `npc.cultivationCap`, `npc.retreatCultivationCap`, `npc.maxInsight`.
- Add:

```js
assert.equal(typeof npc.cultivation, 'number', '包含修炼修为');
assert.equal(typeof npc.experienceCultivation, 'number', '包含历练修为');
assert.equal(typeof npc.totalCultivation, 'number', '包含总修为');
assert.ok('rankStage' in npc, '包含境界小层');
assert.ok('nextCultivationRequired' in npc, '包含突破所需修为');
```

- [ ] **Step 9: Run runtime cleanup and snapshot tests**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-numeric-cultivation-runtime-cleanup.mjs; node tools/test-world-snapshot-npc-status-fields.mjs
```

Expected: both tests PASS.

- [ ] **Step 10: Commit Task 3**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/js/engine/npc/npc-state.js' 'apps/game/js/engine/world-engine.js' 'apps/game/tools/test-numeric-cultivation-runtime-cleanup.mjs' 'apps/game/tools/test-world-snapshot-npc-status-fields.mjs'; git commit -m "refactor: remove progress fields from npc state"
```

Expected: commit succeeds.

---

## Task 4: 闭关与修炼场改用数值修为收益

**Files:**
- Modify: `apps/game/js/engine/npc/services/cultivation-service.js`
- Modify: `apps/game/js/engine/npc/toils/cultivation-toils.js`
- Modify: `apps/game/js/engine/npc/npc-entity.js`
- Modify: `apps/game/tools/test-monster-hunt-job.mjs`
- Create: `apps/game/tools/test-cultivation-service-numeric.mjs`

- [ ] **Step 1: Write cultivation service numeric test**

Create `apps/game/tools/test-cultivation-service-numeric.mjs`:

```js
#!/usr/bin/env node
import { runCultivation } from '../js/engine/npc/services/cultivation-service.js';

let failed = 0;
function assert(cond, msg) {
  if (!cond) {
    console.error('  FAIL:', msg);
    failed++;
  } else {
    console.log('  OK:', msg);
  }
}

function mkEntity({ cultivation = 0, experienceCultivation = 0, qi = 0 } = {}) {
  const data = new Map(Object.entries({
    rankId: 'mortal',
    cultivation,
    experienceCultivation,
    totalCultivation: cultivation + experienceCultivation,
    qi,
    lowSpiritStone: 0,
  }));
  return {
    staticData: { name: '测试修士' },
    state: { get: (k) => data.get(k), set: (k, v) => data.set(k, v) },
    inventory: { getAmount: () => 0, remove: () => {} },
    data,
  };
}

function world() {
  return {
    rng: { next: () => 0.5 },
    ranksData: [
      { id: 'mortal', order: 0, category: 'mortal' },
      { id: 'qi_refining', name: '炼气', order: 20, category: 'cultivation', cultivationRequired: 50, qiRequired: 50 },
    ],
    balanceConfig: {
      cultivation: {
        cultivationSpeed: { mortal: 0.1 },
        spiritStoneCost: { mortal: 0 },
        qiBaseGain: { mortal: 0 },
        qiPerProgress: { mortal: 1 },
        cultivationDecayK: 2.5,
        stageThresholds: { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 },
        speedVariance: { min: 1, max: 1 },
      },
    },
  };
}

const fresh = mkEntity({ cultivation: 0, experienceCultivation: 45 });
const freshResult = runCultivation(fresh, world(), { duration: 1 });
assert(fresh.data.get('cultivation') > 0, 'fresh cultivation gains numeric cultivation');
assert(fresh.data.get('experienceCultivation') === 45, 'experience cultivation is untouched by retreat');
assert(fresh.data.get('totalCultivation') === fresh.data.get('cultivation') + 45, 'total syncs');
assert(fresh.data.get('cultivationProgress') == null, 'old cultivationProgress is not written');
assert(fresh.data.get('insight') == null, 'old insight is not written');
assert(typeof freshResult.cultivationGain === 'number', 'result exposes cultivationGain');
assert(typeof freshResult.cultivationDecay === 'number', 'result exposes cultivationDecay');

const rooted = mkEntity({ cultivation: 25, experienceCultivation: 0 });
const rootedResult = runCultivation(rooted, world(), { duration: 1 });
assert(rootedResult.cultivationGain < freshResult.cultivationGain, 'higher cultivation root decays retreat gain');
assert(rootedResult.qiGain > 0, 'qi still grows from cultivation gain');

if (failed > 0) {
  console.error(`\nCultivation service numeric tests failed: ${failed}`);
  process.exit(1);
}

console.log('\nCultivation service numeric tests passed');
```

- [ ] **Step 2: Run service test and verify it fails**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-cultivation-service-numeric.mjs
```

Expected: FAIL because `runCultivation` still writes `cultivationProgress`.

- [ ] **Step 3: Update imports in `cultivation-service.js`**

Replace:

```js
import { syncNumericCultivationFromRatios } from '../numeric-cultivation.js';
```

with:

```js
import {
  addCultivation,
  computeCultivationGain,
  getCultivationRequired,
} from '../numeric-cultivation.js';
```

- [ ] **Step 4: Replace progress/cap gain calculation**

In `runCultivation`, replace the cap/progress block:

```js
const capMap = cult.cultivationCap || {};
const cap = capMap[rankId] ?? 1.0;
const decayK = cult.cultivationDecayK ?? 2.5;
const current = entity.state.get('cultivationProgress') || 0;
const decayFactor = Math.exp(-decayK * Math.min(1, current / Math.max(cap, 1e-6)));
const effectiveGain = progressGain * decayFactor;
const newProgress = Math.min(current + effectiveGain, cap);
const qiPerProgressMap = cult.qiPerProgress || {};
let progressQi = (qiPerProgressMap[rankId] ?? 0) * (newProgress - current);
entity.state.set('cultivationProgress', newProgress);
```

with:

```js
const required = getCultivationRequired(entity, worldContext.ranksData || []);
const baseCultivationGain = progressGain * required;
const cultivationGain = computeCultivationGain(entity, worldContext.ranksData || [], baseCultivationGain, cult);
const rootBefore = entity.state.get('cultivation') || 0;
addCultivation(entity, worldContext.ranksData || [], cultivationGain, cult);
const rootAfter = entity.state.get('cultivation') || 0;
const cultivationDelta = rootAfter - rootBefore;
const qiPerProgressMap = cult.qiPerProgress || {};
let progressQi = (qiPerProgressMap[rankId] ?? 0) * cultivationDelta;
const decayFactor = baseCultivationGain > 0 ? cultivationGain / baseCultivationGain : 0;
```

Keep `progressGain` only as an internal base rate if useful; do not return old progress fields.

- [ ] **Step 5: Replace dao companion dual cultivation block**

Replace the block that reads and writes `cultivationProgress` with:

```js
const baseDualGain = speed * days * dualBonus * required;
const dualGain = computeCultivationGain(entity, worldContext.ranksData || [], baseDualGain, cult);
const beforeDual = entity.state.get('cultivation') || 0;
addCultivation(entity, worldContext.ranksData || [], dualGain, cult);
const afterDual = entity.state.get('cultivation') || 0;
qiGain += (qiPerProgressMap[rankId] ?? 0) * (afterDual - beforeDual) * qiMultiplier;
```

- [ ] **Step 6: Remove ratio sync call and update return payload**

Remove:

```js
syncNumericCultivationFromRatios(entity, worldContext.ranksData || []);
```

Return:

```js
return {
  success: true,
  cultivationGain,
  cultivation: entity.state.get('cultivation') || 0,
  totalCultivation: entity.state.get('totalCultivation') || 0,
  cultivationDecay: decayFactor,
  qiGain,
  qi: currentQi + qiGain,
  stoneConsumed: consumed,
  techniqueId: techniqueId || null,
  techniqueBreakthroughBonus,
  companionBonusApplied,
  description: `${entity.staticData.name} ${descriptionPrefix}，消耗${consumed}灵石，修为+${cultivationGain.toFixed(1)}，真气+${qiGain.toFixed(1)}`,
};
```

- [ ] **Step 7: Remove cap precondition injection from `npc-entity.js`**

In `apps/game/js/engine/npc/npc-entity.js`:

- Delete `_applyCultivationCapPreconditions(actions)`.
- Delete `refreshCultivationCapPreconditions()`.
- Remove the call to `this._applyCultivationCapPreconditions(actions);` from `_initActions`.
- In `npc-lifecycle.js` later, remove calls to `entity.refreshCultivationCapPreconditions()`.
- Update comments mentioning cap to describe numeric decay instead.

- [ ] **Step 8: Update toil assertion in `test-monster-hunt-job.mjs`**

Replace:

```js
assert(cultivateNpc.state.get('cultivationProgress') > 0, 'toil_cultivate 增加修炼进度');
```

with:

```js
assert(cultivateNpc.state.get('cultivation') > 0, 'toil_cultivate 增加修炼修为');
```

- [ ] **Step 9: Run service and affected toil test**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-cultivation-service-numeric.mjs; node tools/test-monster-hunt-job.mjs
```

Expected: both tests PASS.

- [ ] **Step 10: Commit Task 4**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/js/engine/npc/services/cultivation-service.js' 'apps/game/js/engine/npc/npc-entity.js' 'apps/game/tools/test-cultivation-service-numeric.mjs' 'apps/game/tools/test-monster-hunt-job.mjs'; git commit -m "refactor: make retreat cultivation numeric"
```

Expected: commit succeeds.

---

## Task 5: 游历、历练和师徒点化改用 experienceCultivation

**Files:**
- Modify: `apps/game/js/engine/npc/cultivation-experience.js`
- Modify: `apps/game/js/engine/npc/toils/cultivation-toils.js`
- Modify: `apps/game/js/engine/npc/actions/combat-actions.js`
- Modify: `apps/game/js/engine/npc/actions/relationship-actions.js`
- Modify: `apps/game/js/engine/npc/toils/social-toils.js`
- Modify: `apps/game/js/engine/npc/npc-goals.js`
- Modify: `apps/game/data/balance/cultivation.json`
- Modify: `apps/game/data/balance/relationship.json`
- Modify: `apps/game/tools/test-cultivation-experience-gain.mjs`
- Modify: `apps/game/tools/test-master-disciple.mjs`

- [ ] **Step 1: Update `cultivation-experience.js` to pass config to addExperienceCultivation**

Change:

```js
const totalCultivation = addExperienceCultivation(entity, worldContext?.ranksData || [], gain);
```

to:

```js
const totalCultivation = addExperienceCultivation(
  entity,
  worldContext?.ranksData || [],
  gain,
  worldContext?.balanceConfig?.cultivation || {},
);
```

- [ ] **Step 2: Rename explore config fields**

In `apps/game/data/balance/cultivation.json`, inside `actions.explore`:

- Rename `insightMin` to `experienceCultivationMin`.
- Rename `insightMax` to `experienceCultivationMax`.
- Rename each `insightMultiplier` to `experienceCultivationMultiplier`.
- Rewrite comments and descriptions to say “历练修为”, not “感悟/insight”.

Example:

```json
"experienceCultivationMin": 1,
"experienceCultivationMax": 3,
"fortuneEvents": [
  { "id": "minor_insight", "name": "略有感悟", "weight": 30, "experienceCultivationMultiplier": 1.0, "qiMultiplier": 1.0 }
]
```

Use numeric values in the same rough scale as `cultivationRequired === qiRequired`; do not use 0..1 ratios.

- [ ] **Step 3: Update explore Toil executor**

In `apps/game/js/engine/npc/toils/cultivation-toils.js`, replace the old `insight` block with:

```js
const expMin = exploreCfg.experienceCultivationMin ?? 1;
const expMax = exploreCfg.experienceCultivationMax ?? 3;
const baseExperience = expMin + worldContext.rng.next() * (expMax - expMin);
const directExperienceGain = baseExperience * (event.experienceCultivationMultiplier ?? 1.0);
const directExperience = addExperienceCultivation(
  entity,
  worldContext.ranksData || [],
  directExperienceGain,
  cult,
);
```

Add import:

```js
import { addExperienceCultivation } from '../numeric-cultivation.js';
```

Update context patch fields:

```js
experienceCultivationGain: Number(directExperienceGain.toFixed(4)),
totalCultivation: directExperience,
```

Update description:

```js
description: `${entity.staticData?.name || entity.name || entity.id} 游历归来：${event.name}，历练修为+${directExperienceGain.toFixed(1)}、真气+${qiGain}`,
```

- [ ] **Step 4: Update legacy Explore action executor**

In `apps/game/js/engine/npc/actions/combat-actions.js`, apply the same conversion as Step 3:

- Import `addExperienceCultivation`.
- Read `experienceCultivationMin/Max`.
- Apply `experienceCultivationMultiplier`.
- Write `experienceCultivationGain` and `totalCultivation`.
- Do not read or write `insight`.

- [ ] **Step 5: Update relationship teaching config**

In `apps/game/data/balance/relationship.json`, replace:

```json
"insightGain": 0.12
```

with:

```json
"experienceCultivationGain": 12
```

Rewrite the `_comment` to say “师傅点化给徒弟历练修为/道路感悟数值”, not `insight`.

- [ ] **Step 6: Update relationship teaching executors**

In both `apps/game/js/engine/npc/actions/relationship-actions.js` and `apps/game/js/engine/npc/toils/social-toils.js`, replace:

```js
const insightGain = teachCfg.insightGain ?? 0.12;
const cur = disciple.state.get('insight') || 0;
disciple.state.set('insight', cur + insightGain);
```

with:

```js
const experienceCultivationGain = teachCfg.experienceCultivationGain ?? 12;
addExperienceCultivation(
  disciple,
  worldContext.ranksData || disciple._ranksData || [],
  experienceCultivationGain,
  worldContext.balanceConfig?.cultivation || {},
);
```

Add imports where needed:

```js
import { addExperienceCultivation } from '../numeric-cultivation.js';
```

or for `actions/relationship-actions.js`:

```js
import { addExperienceCultivation } from '../numeric-cultivation.js';
```

Use the correct relative path for each file.

- [ ] **Step 7: Update `npc-goals.js` disciple progress checks**

Replace calculations like:

```js
const dProg = (disciple.state?.get('cultivationProgress') || 0) + (disciple.state?.get('insight') || 0);
```

with:

```js
const required = nextCultivationRank(disciple, disciple._ranksData || [])?.cultivationRequired
  || nextCultivationRank(disciple, disciple._ranksData || [])?.qiRequired
  || 1;
const dProg = (disciple.state?.get('totalCultivation') || 0) / Math.max(1, required);
```

Import `nextCultivationRank` from `./numeric-cultivation.js`.

- [ ] **Step 8: Update tests**

In `apps/game/tools/test-cultivation-experience-gain.mjs`:

- Remove assertions that `insight` or `totalProgress` sync.
- Assert `experienceCultivation` and `totalCultivation`.

Use:

```js
assert(entity.state.get('experienceCultivation') === result.gain, 'experienceCultivation gains numeric cultivation');
assert(entity.state.get('totalCultivation') === 10 + result.gain, 'totalCultivation syncs after experience gain');
assert(entity.state.get('insight') == null, 'legacy insight is not written');
```

In `apps/game/tools/test-master-disciple.mjs`:

- Replace `insight` setup with `experienceCultivation`.
- Replace expected gain with `experienceCultivationGain`.
- Assert `disc.state.get('experienceCultivation')` increases.

- [ ] **Step 9: Run experience and teaching tests**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-cultivation-experience-gain.mjs; node tools/test-master-disciple.mjs
```

Expected: both tests PASS.

- [ ] **Step 10: Commit Task 5**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/js/engine/npc/cultivation-experience.js' 'apps/game/js/engine/npc/toils/cultivation-toils.js' 'apps/game/js/engine/npc/actions/combat-actions.js' 'apps/game/js/engine/npc/actions/relationship-actions.js' 'apps/game/js/engine/npc/toils/social-toils.js' 'apps/game/js/engine/npc/npc-goals.js' 'apps/game/data/balance/cultivation.json' 'apps/game/data/balance/relationship.json' 'apps/game/tools/test-cultivation-experience-gain.mjs' 'apps/game/tools/test-master-disciple.mjs'; git commit -m "refactor: convert insight gains to experience cultivation"
```

Expected: commit succeeds.

---

## Task 6: 突破成功/失败结算接入统一接口

**Files:**
- Modify: `apps/game/js/engine/npc/npc-lifecycle.js`
- Modify: `apps/game/tools/test-quest-reward-economy.mjs`
- Create: `apps/game/tools/test-breakthrough-numeric-outcomes.mjs`
- Modify: `apps/game/tools/test-npc-combat-attributes-runtime.mjs`

- [ ] **Step 1: Write breakthrough outcome test**

Create `apps/game/tools/test-breakthrough-numeric-outcomes.mjs`:

```js
#!/usr/bin/env node
import { tryBreakthrough } from '../js/engine/npc/npc-lifecycle.js';

let failed = 0;
function assert(cond, msg) {
  if (!cond) {
    console.error('  FAIL:', msg);
    failed++;
  } else {
    console.log('  OK:', msg);
  }
}
function entity({ roll = 0, cultivation = 15, experienceCultivation = 35, qi = 50, injuryLevel = 0 } = {}) {
  const data = new Map(Object.entries({
    rankId: 'mortal',
    rankName: '凡人',
    rankStage: null,
    cultivation,
    experienceCultivation,
    totalCultivation: cultivation + experienceCultivation,
    qi,
    injuryLevel,
    ageDays: 0,
    maxAgeDays: 36000,
    breakthroughAidBonus: 0,
  }));
  return {
    id: 'npc_test',
    name: '测试修士',
    _ranksData: [
      { id: 'mortal', name: '凡人', order: 0, category: 'mortal' },
      { id: 'qi_refining', name: '炼气', order: 20, category: 'cultivation', cultivationRequired: 50, qiRequired: 50, lifespan: { baseYears: 110, varianceYears: 0 } },
    ],
    _cultivationConfig: {
      minCultivationRatio: 0.3,
      stageThresholds: { early: 0, middle: 0.25, late: 0.6, perfection: 0.9 },
      breakthrough: {
        successRates: { mortal_to_qi_refining: 0.5 },
        failureQiRetention: 0.5,
        failureCultivationRetention: 0.2,
        failureInjuryLevel: 3,
        agePenaltyThreshold: 0.8,
        agePenaltyMultiplier: 0.7,
      },
    },
    _rng: { next: () => roll },
    state: { get: (k) => data.get(k), set: (k, v) => data.set(k, v) },
    data,
    refreshCombatAttributesOnBreakthrough() { data.set('combatRefreshed', true); },
    tryBreakthroughFullHeal() {},
    _rollBreakthroughPathOrder() { data.set('pathRolled', true); },
  };
}

const success = entity({ roll: 0.1, cultivation: 15, experienceCultivation: 35, qi: 60 });
tryBreakthrough(success);
assert(success.data.get('rankId') === 'qi_refining', 'success updates rank');
assert(success.data.get('rankStage') === 'early', 'success starts early');
assert(success.data.get('cultivation') === 0, 'success clears cultivation');
assert(success.data.get('experienceCultivation') === 0, 'success clears experience');
assert(success.data.get('totalCultivation') === 0, 'success clears total');
assert(success.data.get('qi') === 10, 'success spends qi');
assert(success.data.get('combatRefreshed') === true, 'success refreshes combat');
assert(success._breakthroughInfo.success === true, 'success info recorded');

const failure = entity({ roll: 0.9, cultivation: 20, experienceCultivation: 30, qi: 80, injuryLevel: 1 });
tryBreakthrough(failure);
assert(failure.data.get('rankId') === 'mortal', 'failure keeps rank');
assert(failure.data.get('qi') === 40, 'failure keeps 50% qi');
assert(failure.data.get('cultivation') === 4, 'failure keeps 20% cultivation');
assert(failure.data.get('experienceCultivation') === 6, 'failure keeps 20% experience');
assert(failure.data.get('totalCultivation') === 10, 'failure recomputes total');
assert(failure.data.get('injuryLevel') === 3, 'failure causes heavy wound');
assert(failure._breakthroughInfo.success === false, 'failure info recorded');

if (failed > 0) {
  console.error(`\nBreakthrough numeric outcome tests failed: ${failed}`);
  process.exit(1);
}

console.log('\nBreakthrough numeric outcome tests passed');
```

- [ ] **Step 2: Run breakthrough test and verify it fails**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-breakthrough-numeric-outcomes.mjs
```

Expected: FAIL because `npc-lifecycle.js` still writes old fields and old failure values.

- [ ] **Step 3: Update lifecycle imports**

In `apps/game/js/engine/npc/npc-lifecycle.js`, import:

```js
import {
  applyBreakthroughFailure,
  applyBreakthroughSuccess,
  canAttemptBreakthrough,
  nextCultivationRank,
  syncTotalCultivation,
} from './numeric-cultivation.js';
```

Remove use of old ratio-specific helpers.

- [ ] **Step 4: Update success branch**

Replace the manual success field writes:

```js
entity.state.set('rankId', nextRank.id);
...
entity.state.set('totalProgress', 0);
entity.state.set('qi', currentQi - qiRequired);
```

with:

```js
applyBreakthroughSuccess(entity, nextRank, { qiRequired });
```

Do not write `cultivationProgressRatio`, `cultivationProgress`, `insight`, or `totalProgress`.

Remove:

```js
entity.refreshCultivationCapPreconditions();
```

because cap no longer exists.

- [ ] **Step 5: Update failure branch**

Replace the entire failure branch reset logic with:

```js
const qiBefore = currentQi;
const cultivationBefore = entity.state.get('cultivation') || 0;
const experienceBefore = entity.state.get('experienceCultivation') || 0;
applyBreakthroughFailure(entity, entity._cultivationConfig);
entity._breakthroughInfo = {
  npcId: entity.id,
  npcName: entity.name,
  fromRank: currentRank.name,
  targetRank: nextRank.name,
  success: false,
  qiLost: qiBefore - (entity.state.get('qi') || 0),
  cultivationLost: cultivationBefore - (entity.state.get('cultivation') || 0),
  experienceCultivationLost: experienceBefore - (entity.state.get('experienceCultivation') || 0),
  injuryLevel: entity.state.get('injuryLevel') || 0,
  aidBonus,
};
```

- [ ] **Step 6: Update lifecycle comments**

Rewrite top comments to say:

```js
 * 突破判定：总修为、最低闭关修为、真气三门槛达标后按成功率掷骰。
 * 成功后：消耗真气，清零本境界修为，进入下一境界初期。
 * 失败后：真气保留 50%，两类修为保留 20%，伤势至少为 3。
```

- [ ] **Step 7: Update quest reward economy breakthrough test**

In `apps/game/tools/test-quest-reward-economy.mjs`, replace setup:

```js
realNpc.state.set('cultivationProgress', 1.0);
realNpc.state.set('insight', 0);
```

with:

```js
realNpc.state.set('cultivation', realNpc._ranksData.find(r => r.id === 'qi_refining').cultivationRequired);
realNpc.state.set('experienceCultivation', 0);
realNpc.state.set('totalCultivation', realNpc.state.get('cultivation'));
```

Replace progress assertions with numeric cultivation assertions.

- [ ] **Step 8: Update combat attributes runtime test**

In `apps/game/tools/test-npc-combat-attributes-runtime.mjs`, keep the existing assertion that success resets `rankStage` and refreshes combat attributes, but update source search snippets to no longer expect `refreshCultivationCapPreconditions`.

- [ ] **Step 9: Run breakthrough-related tests**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-breakthrough-numeric-outcomes.mjs; node tools/test-quest-reward-economy.mjs; node tools/test-npc-combat-attributes-runtime.mjs
```

Expected: all PASS.

- [ ] **Step 10: Commit Task 6**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/js/engine/npc/npc-lifecycle.js' 'apps/game/tools/test-breakthrough-numeric-outcomes.mjs' 'apps/game/tools/test-quest-reward-economy.mjs' 'apps/game/tools/test-npc-combat-attributes-runtime.mjs'; git commit -m "refactor: apply numeric breakthrough outcomes"
```

Expected: commit succeeds.

---

## Task 7: GOAP、Utility、动态目标改用数值修为

**Files:**
- Modify: `apps/game/js/engine/abstract/need.js`
- Modify: `apps/game/js/engine/npc/npc-needs.js`
- Modify: `apps/game/data/needs/npc-needs.json`
- Modify: `apps/game/data/balance/utility.json`
- Modify: `apps/game/data/balance/obsession.json`
- Modify: `apps/game/js/engine/npc/dynamic-goals.js`
- Modify: `apps/game/js/engine/npc/interrupt-policy.js`
- Modify: `apps/game/tools/test-interrupt-policy.mjs`
- Modify: `apps/game/tools/test-utility-divergence.mjs`
- Modify: `apps/game/tools/test-obsession.mjs`

- [ ] **Step 1: Extend incremental goal resolution for numeric cultivation**

In `apps/game/js/engine/abstract/need.js`, update `_resolveIncrementalGoals`:

```js
const base = entityState.get(cond.incrementOf) || 0;
const stepBase = typeof cond.stepRatioOf === 'string'
  ? (entityState.get(cond.stepRatioOf) || 0)
  : 1;
const step = typeof cond.stepRatio === 'number'
  ? stepBase * cond.stepRatio
  : (typeof cond.step === 'number' ? cond.step : 0.05);
const max = typeof cond.maxOf === 'string'
  ? (entityState.get(cond.maxOf) || Infinity)
  : (typeof cond.max === 'number' ? cond.max : Infinity);
const value = Math.min(base + step, max);
```

Keep old `step/max` behavior for non-cultivation goals.

- [ ] **Step 2: Update `need_npc_cultivation` config**

In `apps/game/data/needs/npc-needs.json`, replace `need_npc_cultivation` old progress text and goal:

```json
"goalState": {
  "totalCultivation": {
    "op": "gte",
    "incrementOf": "totalCultivation",
    "stepRatioOf": "nextCultivationRequired",
    "stepRatio": 0.01,
    "maxOf": "nextCultivationRequired"
  },
  "qiBelowNextRank": { "op": "eq", "value": false }
}
```

Rules:

```json
{ "_comment": "缺总修为：数值修为未达下一境界需求。", "condition": { "key": "cultivationShortfall", "op": "gt", "value": 0 }, "priorityBoost": 20, "urgencyBoost": 10 },
{ "_comment": "缺闭关根基：闭关修为低于最低占比。", "condition": { "key": "cultivationRootShortfall", "op": "gt", "value": 0 }, "priorityBoost": 10, "urgencyBoost": 8 }
```

Keep `qiBelowNextRank` rule.

- [ ] **Step 3: Update other need rules**

In `apps/game/data/needs/npc-needs.json`:

- `need_npc_breakthrough_aid`: replace `totalProgress >= 0.85` with `rankStage == perfection` and `canBreakthroughByCultivation == true` rules.
- `need_npc_hunt_resources`: replace `totalProgress >= 0.7` with two `rankStage` rules, one for `late` and one for `perfection`.
- `need_npc_ambition`: replace `cultivationProgress >= 0.6` with two `rankStage` rules, one for `late` and one for `perfection`.

Use two explicit rules instead of adding a new `in` or ratio operator:

```json
{ "condition": { "key": "rankStage", "op": "eq", "value": "late" }, "priorityBoost": 20, "urgencyBoost": 10 },
{ "condition": { "key": "rankStage", "op": "eq", "value": "perfection" }, "priorityBoost": 35, "urgencyBoost": 18 }
```

- [ ] **Step 4: Update fallback evaluators in `npc-needs.js`**

In `NPCCultivationEvaluator`, read:

```js
const totalCultivation = entityState.get('totalCultivation') || 0;
const nextRequired = entityState.get('nextCultivationRequired') || 1;
const shortfall = Math.max(0, nextRequired - totalCultivation);
const completion = nextRequired > 0 ? totalCultivation / nextRequired : 0;
```

Return goal:

```js
goalState: {
  totalCultivation: {
    op: 'gte',
    value: Math.min(totalCultivation + nextRequired * 0.01, nextRequired),
  },
  qiBelowNextRank: { op: 'eq', value: false },
},
satisfied: shortfall <= 0 && !qiBelowNextRank,
```

In `NPCBreakthroughEvaluator`, use `rankStage === 'perfection'` and `canBreakthroughByCultivation`.

- [ ] **Step 5: Update utility and obsession data**

In `apps/game/data/balance/utility.json`, replace input keys:

- `totalProgress` -> `cultivationShortfall` or `totalCultivation` depending on curve.
- `cultivationProgress` -> `cultivation` or `rankStage`.

For simple first pass, use:

```json
"inputKey": "cultivationShortfall"
```

and invert curves where needed so larger shortfall means stronger cultivation need.

In `apps/game/data/balance/obsession.json`, replace goal states like:

```json
"goalState": { "totalProgress": { "op": "gte", "value": 90 } }
```

with numeric or stage goals:

```json
"goalState": { "rankStage": { "op": "eq", "value": "perfection" } }
```

for this migration. Do not introduce `totalProgress`.

- [ ] **Step 6: Update dynamic goals and interrupt policy**

In `dynamic-goals.js`, replace total progress motive:

```js
const total = Number(getStateValue(entity, 'totalCultivation', 0)) || 0;
const required = Number(getStateValue(entity, 'nextCultivationRequired', 0)) || 1;
const cultivationDrive = clamp(total / Math.max(1, required), 0, 1);
```

Return:

```js
dao: Math.max(ambition, cultivationDrive),
```

In `interrupt-policy.js`, replace total progress reads with numeric completion using `totalCultivation` and `nextCultivationRequired`.

- [ ] **Step 7: Update tests**

In `apps/game/tools/test-interrupt-policy.mjs`, replace fake state:

```js
state: { get: (key) => ({ totalCultivation: 96, nextCultivationRequired: 100, injuryLevel: 0 }[key] ?? null) },
```

In `apps/game/tools/test-utility-divergence.mjs`, replace `cultivationProgress/totalProgress` helper with `cultivation/totalCultivation/nextCultivationRequired/rankStage`.

In `apps/game/tools/test-obsession.mjs`, use `rankStage` or `totalCultivation` goals instead of `totalProgress`.

- [ ] **Step 8: Run AI tests**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-interrupt-policy.mjs; node tools/test-utility-divergence.mjs; node tools/test-obsession.mjs
```

Expected: all PASS.

- [ ] **Step 9: Commit Task 7**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/js/engine/abstract/need.js' 'apps/game/js/engine/npc/npc-needs.js' 'apps/game/data/needs/npc-needs.json' 'apps/game/data/balance/utility.json' 'apps/game/data/balance/obsession.json' 'apps/game/js/engine/npc/dynamic-goals.js' 'apps/game/js/engine/npc/interrupt-policy.js' 'apps/game/tools/test-interrupt-policy.mjs' 'apps/game/tools/test-utility-divergence.mjs' 'apps/game/tools/test-obsession.mjs'; git commit -m "refactor: drive ai from numeric cultivation"
```

Expected: commit succeeds.

---

## Task 8: UI、报告和调试工具清理旧字段

**Files:**
- Modify: `apps/game/js/ui/follow-entity-status.js`
- Modify: `apps/game/tools/test-follow-entity-status-ui.mjs`
- Modify: `apps/game/tools/simulate-analysis.mjs`
- Modify: `apps/game/tools/trace-genius.mjs`
- Modify: `apps/game/tools/report.html`
- Modify: other `apps/game/tools/*.mjs` tests found by search

- [ ] **Step 1: Update follow status panel**

In `apps/game/js/ui/follow-entity-status.js`:

- Rename `totalProgressStatus(ratio)` to `cultivationStatus(current, required)`.
- Remove fallbacks from `cultivationCap`, `cultivationProgress`, and `insight`.
- Compute:

```js
const nextCultivationRequired = num(entity.nextCultivationRequired);
const cultivation = num(entity.cultivation) ?? 0;
const experienceCultivation = num(entity.experienceCultivation) ?? 0;
const totalCultivation = num(entity.totalCultivation) ?? (cultivation + experienceCultivation);
```

Rows:

```js
row('小层', text(entity.rankStageName || entity.rankStage, '无')),
boundedRow('真气', entity.qi, entity.nextQiRequired, { status: qiStatus }),
boundedRow('修炼修为', cultivation, nextCultivationRequired),
boundedRow('历练修为', experienceCultivation, nextCultivationRequired),
boundedRow('总修为', totalCultivation, nextCultivationRequired, { status: cultivationStatus }),
```

- [ ] **Step 2: Update follow UI test fixtures**

In `apps/game/tools/test-follow-entity-status-ui.mjs`, remove fixture keys:

```js
cultivationProgress
cultivationCap
insight
totalProgress
```

Use:

```js
cultivation: 15000,
experienceCultivation: 42000,
totalCultivation: 57000,
nextCultivationRequired: 50000,
rankStage: 'perfection',
```

Assert output contains “修炼修为”, “历练修为”, “总修为”, and does not contain “闭关进度” or “感悟”.

- [ ] **Step 3: Update analysis and trace tools**

In `simulate-analysis.mjs`:

- Replace `cultivationProgress` snapshots with `totalCultivation` and `nextCultivationRequired`.
- Replace `progress` output label with `cultivation`.
- Keep summary charts numeric.

In `trace-genius.mjs`:

- Replace header:

```js
`真气${s.qi}|修为${s.totalCultivation}/${s.nextCultivationRequired}(修${s.cultivation}+历${s.experienceCultivation})`
```

- Remove `progress`, `insight`, `total` fields.

In `report.html`:

- Labels should say “平均修为 & 累计突破”, “总修为/所需修为”, “修炼修为”, “历练修为”.
- Remove “进度” and “感悟” chart labels where they refer to old fields.

- [ ] **Step 4: Search and update remaining test fixtures**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; rg -n --glob '!docs/**' --glob '!node_modules/**' "cultivationProgress|insight|totalProgress|cultivationProgressRatio|cultivationCap" apps
```

For every remaining app result:

- If it is executable runtime code, convert it to numeric fields.
- If it is a test fixture, update the fixture.
- If it is an ADR/doc string embedded in comments, rewrite the comment.
- Do not leave old fields in `apps/`.

- [ ] **Step 5: Run UI/tool tests**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-follow-entity-status-ui.mjs; node tools/test-numeric-cultivation-runtime-cleanup.mjs
```

Expected: both PASS and app search from Step 4 returns no old field names.

- [ ] **Step 6: Commit Task 8**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/js/ui/follow-entity-status.js' 'apps/game/tools'; git commit -m "refactor: update cultivation ui and tools"
```

Expected: commit succeeds.

---

## Task 9: 文档同步

**Files:**
- Modify: `docs/README.md`
- Modify: `docs/data-models/ranks.md`
- Modify: `docs/worldbuilding/wiki/rules/travel-and-risk.md`
- Modify: `docs/data/data-config-rules.md`
- Modify: `docs/decisions/adr-051-numeric-cultivation-and-job-action-migration.md`
- Create: `docs/superpowers/reports/2026-06-07-修为数值化与突破重构验证.md`

- [ ] **Step 1: Update `docs/data-models/ranks.md`**

Change “当前规则” to state:

```markdown
- `cultivationRequired` 与 `qiRequired` 当前保持相同数值；前者是突破所需修为，后者是突破所需真气。
- 修为字段为数值资源：`cultivation`、`experienceCultivation`、`totalCultivation`。
- 旧 `cultivationProgress`、`insight`、`totalProgress`、`cultivationProgressRatio` 已从运行时业务语义中移除。
```

- [ ] **Step 2: Rewrite `travel-and-risk.md`**

Replace old “突破进度双源” section with:

```markdown
### 修为双源

- `cultivation`：闭关、修炼场、丹药等直接修炼获得的修炼修为。
- `experienceCultivation`：游历、任务、斩妖、动态事件、机会点、PvP、外出社交等获得的历练修为。
- `totalCultivation = cultivation + experienceCultivation`，达到下一境界 `cultivationRequired` 后才可尝试突破。
- 突破还要求 `cultivation >= cultivationRequired * minCultivationRatio`，默认最低闭关修为占比为 30%。
- 闭关没有硬上限，但收益按 `e^(-cultivationDecayK * cultivation / cultivationRequired)` 递减。
- 历练修为更快，但伴随受伤、死亡、任务失败、妖兽袭击或资源损失风险。
```

Add failure section:

```markdown
### 突破失败

突破失败后真气保留 50%，修炼修为和历练修为各保留 20%，伤势至少提升到 `injuryLevel = 3`。突破材料若已消耗则全部浪费；材料消耗链另行设计。
```

- [ ] **Step 3: Update `data-config-rules.md`**

Replace old numeric cultivation compatibility text with:

```markdown
| `cultivation` | number | 闭关、修炼场、丹药等直接修炼获得的修炼修为 |
| `experienceCultivation` | number | 游历、任务、斩妖、事件等获得的历练修为 |
| `totalCultivation` | number | 两类修为之和，突破修为门槛使用 |
| `rankStage` | string|null | 由 `totalCultivation / cultivationRequired` 推导的小层 |
```

State that `cultivationProgress`、`insight`、`totalProgress`、`cultivationProgressRatio` are removed from runtime semantics.

- [ ] **Step 4: Update ADR-051**

Append a section:

```markdown
## 2026-06-07 修订：纯数值修为

后续重构移除了 `cultivationProgress`、`insight`、`totalProgress`、`cultivationProgressRatio` 兼容字段。运行时只保留 `cultivation`、`experienceCultivation`、`totalCultivation`、`qi`、`rankStage`。闭关硬上限 `cultivationCap` 被取消，闭关收益改为随 `cultivation / cultivationRequired` 递减。突破失败保留 50% 真气、20% 修为，并造成重伤。
```

- [ ] **Step 5: Create validation report skeleton**

Create `docs/superpowers/reports/2026-06-07-修为数值化与突破重构验证.md`:

```markdown
# 修为数值化与突破重构验证

> 最后更新：2026-06-07

## Focused Tests

| 命令 | 结果 | 观察 |
|------|------|------|
| `node tools/test-numeric-cultivation-migration.mjs` | 待执行 | 统一修为接口 |
| `node tools/test-cultivation-config-numeric.mjs` | 待执行 | 配置纯数值化 |
| `node tools/test-numeric-cultivation-runtime-cleanup.mjs` | 待执行 | 旧比例字段移除 |
| `node tools/test-cultivation-service-numeric.mjs` | 待执行 | 闭关递减 |
| `node tools/test-breakthrough-numeric-outcomes.mjs` | 待执行 | 突破成功/失败 |

## Full Simulation

| 命令 | 结果 | 观察 |
|------|------|------|
| `node tools/simulate-analysis.mjs 500` | 待执行 | 闭关/历练/突破分布 |
| `node tools/simulate-analysis.mjs 1000` | 待执行 | 失败重伤与长期节奏 |

## Notes

- 不使用哈希、指纹或单一摘要值自证。
- 以真实行为数据观察突破次数、失败重伤、闭关/历练/任务分布和死亡风险。
```

- [ ] **Step 6: Update README navigation**

Add the plan/report under `docs/README.md` key documents:

```markdown
| 修为数值化与突破重构 | `superpowers/specs/2026-06-07-修为数值化与突破重构-design.md`、`superpowers/plans/2026-06-07-修为数值化与突破重构实施计划.md`、`superpowers/reports/2026-06-07-修为数值化与突破重构验证.md` |
```

- [ ] **Step 7: Run docs old-field audit**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; rg -n --glob '!docs/世界观参考/**' "cultivationProgress|insight|totalProgress|cultivationProgressRatio|cultivationCap" docs
```

Expected: remaining hits are only historical ADRs explicitly describing old behavior, the new design spec’s deletion list, or migration notes. Current wiki/data-model/system docs must not describe old fields as active.

- [ ] **Step 8: Commit Task 9**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'docs/README.md' 'docs/data-models/ranks.md' 'docs/worldbuilding/wiki/rules/travel-and-risk.md' 'docs/data/data-config-rules.md' 'docs/decisions/adr-051-numeric-cultivation-and-job-action-migration.md' 'docs/superpowers/reports/2026-06-07-修为数值化与突破重构验证.md'; git commit -m "docs: update numeric cultivation semantics"
```

Expected: commit succeeds.

---

## Task 10: 全局旧字段审计与真实模拟验证

**Files:**
- Modify as needed: any remaining app files found by search
- Modify: `docs/superpowers/reports/2026-06-07-修为数值化与突破重构验证.md`

- [ ] **Step 1: Run app old-field audit**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; rg -n --glob '!docs/**' --glob '!node_modules/**' --glob '!dist/**' --glob '!build/**' "cultivationProgress|insight|totalProgress|cultivationProgressRatio|cultivationCap" apps
```

Expected: no output. If output remains, convert the file to numeric fields and rerun.

- [ ] **Step 2: Run focused tests**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-numeric-cultivation-migration.mjs; node tools/test-cultivation-config-numeric.mjs; node tools/test-numeric-cultivation-runtime-cleanup.mjs; node tools/test-cultivation-service-numeric.mjs; node tools/test-cultivation-experience-gain.mjs; node tools/test-breakthrough-numeric-outcomes.mjs; node tools/test-world-snapshot-npc-status-fields.mjs; node tools/test-follow-entity-status-ui.mjs; node tools/test-monster-hunt-job.mjs; node tools/test-quest-reward-economy.mjs; node tools/test-master-disciple.mjs; node tools/test-interrupt-policy.mjs; node tools/test-utility-divergence.mjs; node tools/test-obsession.mjs
```

Expected: all PASS.

- [ ] **Step 3: Run 500-day simulation**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/simulate-analysis.mjs 500
```

Expected:

- Simulation completes without runtime errors.
- Report includes numeric cultivation fields.
- Breakthrough attempts and failures, if any, show numeric cultivation loss and heavy injury.
- Behavior distribution includes cultivation, travel, quest, hunt, or event actions; no old `progress/insight` diagnostics.

- [ ] **Step 4: Run 1000-day simulation**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/simulate-analysis.mjs 1000
```

Expected:

- Simulation completes without runtime errors.
- NPCs are not blocked by missing old fields.
- High-risk experience sources correlate with injuries/deaths or failed outcomes.
- Pure retreat cultivation slows down through observed lower `cultivationGain` near high `cultivation`.

- [ ] **Step 5: Update validation report with real outputs**

Open `docs/superpowers/reports/2026-06-07-修为数值化与突破重构验证.md` and replace each `待执行` result with:

- `通过` or `失败`
- Command date
- Important observed counts from the tool output, such as breakthrough successes/failures, heavy wounds, deaths, and action distribution

Do not record hashes or fingerprints.

- [ ] **Step 6: Run diff check**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git diff --check
```

Expected: no whitespace errors.

- [ ] **Step 7: Commit Task 10**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git -c core.quotePath=false status --short
```

Then stage only files listed in this plan and modified by this task, for example:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; git add -- 'apps/game/tools/test-numeric-cultivation-runtime-cleanup.mjs' 'docs/superpowers/reports/2026-06-07-修为数值化与突破重构验证.md'; git commit -m "test: verify numeric cultivation refactor"
```

Expected: commit succeeds. Before committing, inspect `git -c core.quotePath=false status --short` and only stage files that belong to this refactor.

---

## Final Verification Commands

Run after all tasks:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld'; rg -n --glob '!docs/世界观参考/**' "cultivationProgress|insight|totalProgress|cultivationProgressRatio|cultivationCap" apps docs
```

Expected: no `apps/` hits. `docs/` hits only in historical ADRs, migration notes, or the design spec’s deletion list.

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/test-numeric-cultivation-migration.mjs; node tools/test-cultivation-config-numeric.mjs; node tools/test-numeric-cultivation-runtime-cleanup.mjs; node tools/test-cultivation-service-numeric.mjs; node tools/test-cultivation-experience-gain.mjs; node tools/test-breakthrough-numeric-outcomes.mjs; node tools/test-world-snapshot-npc-status-fields.mjs; node tools/test-follow-entity-status-ui.mjs; node tools/test-monster-hunt-job.mjs; node tools/test-quest-reward-economy.mjs; node tools/test-master-disciple.mjs; node tools/test-interrupt-policy.mjs; node tools/test-utility-divergence.mjs; node tools/test-obsession.mjs
```

Expected: all tests PASS.

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Set-Location -LiteralPath 'E:\AI_Projects\CultivationWorld\apps\game'; node tools/simulate-analysis.mjs 500; node tools/simulate-analysis.mjs 1000
```

Expected: both simulations complete and produce real behavior observations for the validation report.

## Handoff Notes

- Use `superpowers:subagent-driven-development` for implementation when possible. Suggested subagent boundaries:
  1. Tasks 1-2: numeric service and config.
  2. Tasks 3-4: state, snapshot, retreat cultivation.
  3. Tasks 5-7: experience, teaching, AI.
  4. Tasks 8-10: UI/tools/docs/verification.
- Main agent must review each subagent patch and run the final verification commands.
- Do not use hashes, fingerprints, or golden outputs as correctness evidence.
- Do not expand `ranks.json` to 12 runtime realms in this refactor.
