# 提交前收尾修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 最后更新：2026-06-06

**Goal:** 在正式提交 NPC 战斗智能闭环与世界观参考补充这一版前，清理提交边界、确认可再生报告文件策略，并补齐最终提交验证记录。

**Architecture:** 本计划不新增业务机制，只处理版本发布前的仓库卫生和验收证据。逻辑代码、数据配置、世界观参考文档保持现状；提交前只调整忽略规则、提交范围和验证记录，避免把本地工具缓存或临时工作树带入版本。

**Tech Stack:** Git、PowerShell、Node.js ESM 验证脚本、现有 `apps/game/tools` 模拟报告工具。

---

## 当前检查结论

2026-06-06 已完成的提交前检查：

- `apps/game` 关键脚本组已通过，覆盖 Reaction、GOAP 风险分支、路线风险、统一战斗、斩妖任务、历练修为、数值修为迁移、Job/Toil 配置、JobAction 规划、Job 中断恢复、经济 Toil、妖兽资源闭环、跟随状态 UI。
- `node apps/game/tools/verify-reaction.mjs --days=50 --seeds=303` 已通过；50 天内观察到 34 次反应行为，19 个触发反应的 NPC 均恢复正常行为。
- `node tools/simulate-analysis.mjs 900 --seed=303` 已通过；耗时约 6 分 1 秒，报告中观察到猎妖接取 278、猎妖完成 24、猎妖失败 0、任务斩妖死亡 24、妖兽死亡 29。
- 默认长程模拟报告里 `reactionSamples` 与 `assistSamples` 仍为空；Reaction 已由独立激活脚本覆盖，助攻真实长程样本仍需后续平衡观察，不作为本次提交阻断。

提交前仍需处理的阻断项：

- `.playwright-cli/` 是本地浏览器检查产物，当前未跟踪，不能提交。
- `.worktrees/` 是本地工作树目录，当前未跟踪，不能提交。
- `apps/game/tools/report-data.js` 是模拟生成报告，已在 `.gitignore` 声明为可再生输出；需要从 Git 索引移除并保留本地文件，之后由 `.gitignore` 防止重新误加入提交。900 天模拟刷新后曾产生约 19532 行新增、5978 行删除的大 diff。本次提交采用“不提交报告快照，只在验证报告记录关键指标”的策略。

## Task 1: 忽略本地工具目录

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: 在 `.gitignore` 加入本地工具目录**

在 “Local screenshots and visual QA outputs.” 或 “Code intelligence / analysis caches” 附近加入：

```gitignore
# Local browser/test helper state.
.playwright-cli/
.worktrees/
```

- [ ] **Step 2: 验证目录不再出现在未跟踪列表**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git -c core.quotePath=false status --short -- .playwright-cli .worktrees .gitignore
```

Expected:

```text
 M .gitignore
```

`.playwright-cli/` 和 `.worktrees/` 不应再以 `??` 显示。

## Task 2: 排除可再生报告快照

**Files:**
- Index: `apps/game/tools/report-data.js`
- Inspect: `.gitignore`

- [ ] **Step 1: 从索引移除可再生报告快照**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git rm --cached -- apps/game/tools/report-data.js
```

Expected:

```text
rm 'apps/game/tools/report-data.js'
```

该命令只移除 Git 索引记录，保留本地工作区文件内容。

- [ ] **Step 2: 确认可再生报告只作为本地验收产物**

本次提交不包含 `apps/game/tools/report-data.js` 的巨大报告快照，只在 `docs/superpowers/reports/2026-06-06-NPC战斗智能闭环验证.md` 记录关键指标。该文件从索引移除后，后续由 `.gitignore` 保护，避免 `git add -u`、`git commit -a` 或显式 add 误带入报告快照：

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
Test-Path -LiteralPath 'apps\game\tools\report-data.js'
git -c core.quotePath=false status --short -- apps/game/tools/report-data.js
git ls-files -- apps/game/tools/report-data.js
```

Expected:

```text
True
D  apps/game/tools/report-data.js
```

`git ls-files` 无输出；工作区保留本地文件，且不会以未跟踪文件重新出现。

- [ ] **Step 3: 确认报告摘要已经覆盖可再生输出**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
Select-String -LiteralPath "docs\superpowers\reports\2026-06-06-NPC战斗智能闭环验证.md" -Pattern "900 天报告摘要|huntQuestAccepted|huntQuestCompleted|questHuntDeaths|reactionSamples|assistSamples"
```

Expected: 输出包含 900 天报告摘要表，并记录 `huntQuestAccepted=278`、`huntQuestCompleted=24`、`questHuntDeaths=24`、`reactionSamples=0`、`assistSamples=0`。

## Task 3: 提交前最终验证

**Files:**
- Test: `apps/game/tools/test-reaction-combat-intelligence.mjs`
- Test: `apps/game/tools/test-combat-intelligence-jobs.mjs`
- Test: `apps/game/tools/test-combat-risk-branch-planning.mjs`
- Test: `apps/game/tools/test-combat-route-risk.mjs`
- Test: `apps/game/tools/test-combat-encounter-unification.mjs`
- Test: `apps/game/tools/test-monster-hunt-job.mjs`
- Test: `apps/game/tools/test-cultivation-experience-gain.mjs`
- Test: `apps/game/tools/test-numeric-cultivation-migration.mjs`
- Test: `apps/game/tools/test-npc-action-job-migration.mjs`
- Test: `apps/game/tools/test-job-config-load.mjs`
- Test: `apps/game/tools/test-job-action-planning.mjs`
- Test: `apps/game/tools/test-job-interrupt-resume.mjs`
- Test: `apps/game/tools/test-economy-toils.mjs`
- Test: `apps/game/tools/test-monster-resource-loop.mjs`
- Test: `apps/game/tools/test-follow-entity-status-ui.mjs`
- Modify: `docs/superpowers/reports/2026-06-06-NPC战斗智能闭环验证.md`

- [ ] **Step 1: 运行脚本级回归**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
$ErrorActionPreference = "Stop"
Set-Location -LiteralPath "E:\AI_Projects\CultivationWorld\apps\game"
$tests = @(
  "test-reaction-combat-intelligence.mjs",
  "test-combat-intelligence-jobs.mjs",
  "test-combat-risk-branch-planning.mjs",
  "test-combat-route-risk.mjs",
  "test-combat-encounter-unification.mjs",
  "test-monster-hunt-job.mjs",
  "test-cultivation-experience-gain.mjs",
  "test-numeric-cultivation-migration.mjs",
  "test-npc-action-job-migration.mjs",
  "test-job-config-load.mjs",
  "test-job-action-planning.mjs",
  "test-job-interrupt-resume.mjs",
  "test-economy-toils.mjs",
  "test-monster-resource-loop.mjs",
  "test-follow-entity-status-ui.mjs"
)
foreach ($test in $tests) {
  Write-Host "`n>>> node tools/$test"
  node "tools/$test"
  if ($LASTEXITCODE -ne 0) { throw "FAILED: $test" }
}
```

Expected: 全部脚本退出码为 0。

- [ ] **Step 2: 运行 Reaction 激活验证**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
Set-Location -LiteralPath "E:\AI_Projects\CultivationWorld"
node apps/game/tools/verify-reaction.mjs --days=50 --seeds=303
```

Expected: 输出 “四层 AI 反应层验证通过”，并观察到至少一次 Reaction 行为。

- [ ] **Step 3: 运行 900 天真实模拟**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
Set-Location -LiteralPath "E:\AI_Projects\CultivationWorld\apps\game"
node tools/simulate-analysis.mjs 900 --seed=303
```

Expected: 退出码为 0，`apps/game/tools/report-data.js` 中能观察到 `huntQuestAccepted`、`huntQuestCompleted`、`questHuntDeaths` 与 `questProgressSamples`。

- [ ] **Step 4: 将最终验证结果同步到验证报告**

如果 Step 1-3 的输出与当前报告一致，只更新“正式审查结论”前后的验证时间说明，不改指标；如果指标不同，按真实输出更新 900 天报告摘要表。报告必须继续保留以下边界说明：默认主模拟 `reactionSamples=0` 由独立 Reaction 激活脚本覆盖，真实长程 `assistSamples=0` 仍是后续平衡观察项。

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git -c core.quotePath=false diff -- docs/superpowers/reports/2026-06-06-NPC战斗智能闭环验证.md
```

Expected: diff 只包含最终验证记录或真实指标修正，不引入未验证的结论。

## Task 4: 暂存提交范围

**Files:**
- Modify: staged file set only

- [ ] **Step 1: 查看最终状态**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git -c core.quotePath=false status --short
```

Expected:

输出中不应包含 `.playwright-cli/` 或 `.worktrees/`。

- [ ] **Step 2: 按边界暂存**

推荐暂存范围：

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git add -- .gitignore AGENTS.md .cursor/rules/project-rules.md apps/game docs
git rm --cached -- apps/game/tools/report-data.js
```

Expected:

`apps/game/tools/report-data.js` 作为 staged deletion 保留在索引变更中，本地文件仍存在；后续由 `.gitignore` 保护，不再提交可再生报告快照内容。

- [ ] **Step 3: 复核暂存内容**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git -c core.quotePath=false diff --cached --name-status
```

Expected:

确认输出只包含业务代码、配置、测试、ADR、计划、规格、报告与世界观参考文档；不包含 `.playwright-cli/`、`.worktrees/`，并且 `apps/game/tools/report-data.js` 只以删除索引记录的形式出现，不包含报告快照内容。

- [ ] **Step 4: 复核提交摘要**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
git -c core.quotePath=false diff --cached --stat
```

Expected: 摘要能清楚反映本次提交范围；若世界观参考文档、战斗智能重构、提交前收尾修复混在同一暂存集里，优先拆分为多个提交，避免审查边界过大。

## Self-Review

- Spec coverage: 本计划覆盖提交前发现的三个实际风险：本地工具目录、可再生报告快照、最终验证与暂存范围。
- Placeholder scan: 无 `TBD`、`TODO`、`implement later` 或未定义任务。
- Scope control: 不要求修改业务逻辑；若最终验证再次失败，应另开针对失败脚本的修复计划，不把调试过程混入本收尾计划。
