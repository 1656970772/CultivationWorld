# NPC 效用评分公式升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 NPC Utility 选目标层从裸乘法评分升级为“门槛 + 基础欲望 + 加权效用 + 收益风险分离”公式，同时保持默认关闭态不改变现有行为入口。

**Architecture:** `Goal` 继续作为 Utility 目标评分载体，新增评分上下文承载风险、收益和评分参数；`Consideration` 继续负责把输入映射到 `[0,1]`，新增权重与 floor 参与加权几何平均；`npc-utility` 负责把 `goalRisk`、`expectedValue` 和人格/情绪修正后的风险权重写入目标评分上下文。GOAP、Job/Toil 和具体执行器不参与本次改动。

**Tech Stack:** JavaScript ES modules, Node.js test scripts under `apps/game/tools`, JSON config under `apps/game/data`, Markdown docs.

---

## File Structure

- Modify: `apps/game/js/engine/abstract/goal.js`
  - Responsibility: 统一计算 Goal 最终评分，新增 `setScoreContext()` / `getScoreContext()`，把 consideration 聚合从乘积改为加权几何平均。
- Modify: `apps/game/js/engine/abstract/consideration.js`
  - Responsibility: 继续计算单个 consideration 的 `[0,1]` 值，新增 `floor` 字段并规范化 `weight`。
- Modify: `apps/game/js/engine/npc/npc-utility.js`
  - Responsibility: 在 Utility 激活时计算 `goalRisk`、`expectedValue`、风险权重和收益权重，并写入 Goal 评分上下文；移除旧的线性 `riskAversion` modulator。
- Modify: `apps/game/data/config/ai-config.json`
  - Responsibility: 在 `npc.utility.score` 中集中存放评分公式参数。
- Modify: `apps/game/tools/test-utility.mjs`
  - Responsibility: 单元验证新评分公式、floor、偏置夹取、收益放大和风险压制。
- Modify: `apps/game/tools/test-utility-divergence.mjs`
  - Responsibility: 激活态验证风险、情绪、收益仍能产生 NPC 目标分化。
- Verify only: `apps/game/tools/test-obsession.mjs`
  - Responsibility: 确认执念乘子仍与新评分公式兼容。
- Verify only: `apps/game/tools/simulate-analysis.mjs`
  - Responsibility: 默认关闭态与激活态真实模拟观察。
- Modify: `docs/README.md`
  - Responsibility: 记录本实施计划与验证报告的文档导航。

---

### Task 1: 写失败测试覆盖新 Goal 评分公式

**Files:**
- Modify: `apps/game/tools/test-utility.mjs`

- [ ] **Step 1: 更新测试标题注释**

Replace the top coverage comment in `apps/game/tools/test-utility.mjs` with:

```js
/**
 * Utility 考量层单元测试（ADR-020 / ADR-021 / NPC效用评分公式升级）。
 * 覆盖：
 *   1) Consideration 各响应曲线在 [0,1] 范围且单调性正确
 *   2) Goal.score 新公式：加权几何平均、floor、偏置夹取、收益放大、风险压制
 *   3) 默认态：无 considerations / modulators / scoreContext 时 score === priority
 *   4) 派生输入(timeValue)从 derived 表读取
 *   5) npc-utility 把 goalRisk / expectedValue 写入 Goal 评分上下文
 *   6) 随机扰动（上头）：命中时 Goal 分数按 mult 放大且受偏置上限保护
 *   7) 路径偏好：explore_first 时探索类目标获得 deltaPriority 加成
 *
 * 用法：node tools/test-utility.mjs
 */
```

- [ ] **Step 2: 替换 `Goal.score` 测试块**

Replace the current block starting at `console.log('2) Goal.score 乘法语义');` and ending before `console.log('3) 派生输入 timeValue');` with:

```js
console.log('2) Goal.score 新公式语义');
{
  const g = new Goal({ id: 'g1', priority: 80, urgency: 0 });
  assert(g.score() === 80, '无考量因素、无调制项、无评分上下文时 score===priority');

  const c1 = new Consideration({ id: 'a', inputKey: 'x', curve: CurveType.LINEAR, params: { slope: 1 } });
  const c2 = new Consideration({ id: 'b', inputKey: 'y', curve: CurveType.LINEAR, params: { slope: 1 } });
  g.evaluateConsiderations([c1, c2], stateOf({ x: 0.5, y: 0.5 }), {});
  assert(approx(g.score(), 80 * 0.5), '两个 0.5 consideration 使用几何平均，score=80×0.5=40');

  const weighted = new Goal({ id: 'gWeighted', priority: 100, urgency: 0 });
  const cw1 = new Consideration({ id: 'w1', inputKey: 'x', weight: 3, curve: CurveType.LINEAR, params: { slope: 1 } });
  const cw2 = new Consideration({ id: 'w2', inputKey: 'y', weight: 1, curve: CurveType.LINEAR, params: { slope: 1 } });
  weighted.evaluateConsiderations([cw1, cw2], stateOf({ x: 0.25, y: 1 }), {});
  const expectedWeightedMean = Math.exp((3 * Math.log(0.25) + Math.log(1)) / 4);
  assert(approx(weighted.score(), 100 * expectedWeightedMean), 'consideration.weight 参与加权几何平均');

  const floored = new Goal({ id: 'gFloor', priority: 100, urgency: 0 });
  const cf = new Consideration({ id: 'floor', inputKey: 'x', floor: 0.2, curve: CurveType.LINEAR, params: { slope: 1 } });
  floored.evaluateConsiderations([cf], stateOf({ x: 0 }), {});
  assert(approx(floored.score(), 20), 'consideration.floor=0.2 防止软因素把目标压成 0');

  const modded = new Goal({ id: 'gMod', priority: 70, urgency: 0 });
  modded.modulators.push({ label: 'obsession', deltaPriority: 0, mult: 1.5 });
  assert(approx(modded.score(), 105), '普通 modulator.mult 保持乘法放大：70×1.5=105');

  const capped = new Goal({ id: 'gCap', priority: 50, urgency: 0 });
  capped.modulators.push({ label: 'tooLarge', deltaPriority: 0, mult: 10 });
  capped.setScoreContext({ scoreConfig: { minBiasMult: 0.25, maxBiasMult: 3 } });
  assert(approx(capped.score(), 150), 'biasMult 按 maxBiasMult=3 夹取：50×3=150');

  const rewarded = new Goal({ id: 'gReward', priority: 60, urgency: 0 });
  rewarded.setScoreContext({ expectedValue: 0.5, scoreConfig: { rewardWeight: 0.5 } });
  assert(approx(rewarded.score(), 75), 'rewardMult=1+0.5×0.5，score=60×1.25=75');

  const risky = new Goal({ id: 'gRisk', priority: 60, urgency: 0 });
  risky.setScoreContext({ goalRisk: 0.5, riskWeight: 1 });
  assert(approx(risky.score(), 40), 'riskMult=1/(1+1×0.5)，score=60/1.5=40');
}
```

- [ ] **Step 3: Run test to verify it fails**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-utility.mjs
```

Expected: FAIL. At least one failure should mention `g.setScoreContext is not a function` or the old multiplication result `20` instead of the new expected `40`.

- [ ] **Step 4: Commit failing test**

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git add -- apps/game/tools/test-utility.mjs; git commit -m "test: specify npc utility score formula"
```

Expected: commit succeeds and only `apps/game/tools/test-utility.mjs` is included.

---

### Task 2: 实现 Goal 加权评分上下文

**Files:**
- Modify: `apps/game/js/engine/abstract/goal.js`
- Modify: `apps/game/js/engine/abstract/consideration.js`
- Test: `apps/game/tools/test-utility.mjs`

- [ ] **Step 1: Add scoring helpers to `goal.js`**

Insert this block after `GoalSource`:

```js
const DEFAULT_SCORE_CONFIG = Object.freeze({
  minBiasMult: 0.25,
  maxBiasMult: 3,
  defaultConsiderationFloor: 0.05,
  rewardWeight: 0.5,
  riskWeight: 1,
});

function clamp(value, min, max) {
  const n = Number(value);
  if (!Number.isFinite(n)) return min;
  return n < min ? min : n > max ? max : n;
}

function clamp01(value) {
  return clamp(value, 0, 1);
}

function positiveNumber(value, fallback) {
  const n = Number(value);
  return Number.isFinite(n) && n >= 0 ? n : fallback;
}
```

- [ ] **Step 2: Add score context field in `Goal` constructor**

In `Goal.constructor`, after `_considerationTrace`, add:

```js
    /**
     * 评分上下文（NPC 效用评分公式升级）。
     * 默认 null 表示仅按 priority / modulators / considerations 计算；
     * npc-utility 在 Utility 激活态写入 expectedValue、goalRisk 与评分参数。
     * @type {null|Object}
     */
    this._scoreContext = null;
```

- [ ] **Step 3: Replace `evaluateConsiderations` trace mapping**

Replace the current `this._considerationTrace = this.considerations.map(...)` block with:

```js
    this._considerationTrace = this.considerations.map(c => ({
      id: c.id,
      value: c.evaluate(entityState, worldContext, derived),
      weight: c.weight,
      floor: c.floor,
    }));
```

- [ ] **Step 4: Replace `_considerationProduct()` with weighted mean**

Replace `_considerationProduct()` with:

```js
  /** 考量因素加权几何平均（无考量因素时为 1）。 */
  _considerationMean(defaultFloor = DEFAULT_SCORE_CONFIG.defaultConsiderationFloor) {
    if (!this._considerationTrace || this._considerationTrace.length === 0) return 1;

    let weightedLog = 0;
    let weightSum = 0;
    const fallbackFloor = clamp01(defaultFloor);

    for (const t of this._considerationTrace) {
      const weight = positiveNumber(t.weight, 1);
      if (weight <= 0) continue;

      const floor = t.floor == null ? fallbackFloor : clamp01(t.floor);
      const safeValue = Math.max(clamp01(t.value), floor);
      weightedLog += weight * Math.log(safeValue);
      weightSum += weight;
    }

    if (weightSum <= 0) return 1;
    return Math.exp(weightedLog / weightSum);
  }
```

- [ ] **Step 5: Add score context methods**

Insert these methods before `score()`:

```js
  /**
   * 写入评分上下文。由 Utility 层注入收益、风险和评分参数。
   * @param {Object} [context]
   * @returns {Goal}
   */
  setScoreContext(context = {}) {
    const config = {
      ...DEFAULT_SCORE_CONFIG,
      ...(context.scoreConfig || {}),
    };

    const hardGate = context.hardGate == null ? 1 : clamp01(context.hardGate);
    const expectedValue = clamp01(context.expectedValue ?? 0);
    const goalRisk = positiveNumber(context.goalRisk, 0);
    const rewardWeight = positiveNumber(context.rewardWeight ?? config.rewardWeight, DEFAULT_SCORE_CONFIG.rewardWeight);
    const riskWeight = positiveNumber(context.riskWeight ?? config.riskWeight, DEFAULT_SCORE_CONFIG.riskWeight);

    this._scoreContext = {
      hardGate,
      expectedValue,
      goalRisk,
      rewardWeight,
      riskWeight,
      scoreConfig: {
        minBiasMult: positiveNumber(config.minBiasMult, DEFAULT_SCORE_CONFIG.minBiasMult),
        maxBiasMult: positiveNumber(config.maxBiasMult, DEFAULT_SCORE_CONFIG.maxBiasMult),
        defaultConsiderationFloor: clamp01(config.defaultConsiderationFloor),
        rewardWeight,
        riskWeight,
      },
    };

    if (this._scoreContext.scoreConfig.maxBiasMult < this._scoreContext.scoreConfig.minBiasMult) {
      const min = this._scoreContext.scoreConfig.maxBiasMult;
      const max = this._scoreContext.scoreConfig.minBiasMult;
      this._scoreContext.scoreConfig.minBiasMult = min;
      this._scoreContext.scoreConfig.maxBiasMult = max;
    }

    return this;
  }

  /**
   * 返回当前评分上下文快照，供测试和调试面板读取。
   * @returns {null|Object}
   */
  getScoreContext() {
    return this._scoreContext ? {
      hardGate: this._scoreContext.hardGate,
      expectedValue: this._scoreContext.expectedValue,
      goalRisk: this._scoreContext.goalRisk,
      rewardWeight: this._scoreContext.rewardWeight,
      riskWeight: this._scoreContext.riskWeight,
      scoreConfig: { ...this._scoreContext.scoreConfig },
    } : null;
  }
```

- [ ] **Step 6: Replace `score()`**

Replace `score()` with:

```js
  score() {
    if (!this._scoreContext && (!this._considerationTrace || this._considerationTrace.length === 0) && this.modulators.length === 0) {
      return this.priority;
    }

    const ctx = this._scoreContext || {
      hardGate: 1,
      expectedValue: 0,
      goalRisk: 0,
      rewardWeight: DEFAULT_SCORE_CONFIG.rewardWeight,
      riskWeight: DEFAULT_SCORE_CONFIG.riskWeight,
      scoreConfig: DEFAULT_SCORE_CONFIG,
    };

    let p = this.priority;
    let biasMult = 1;
    for (const m of this.modulators) {
      p += m.deltaPriority;
      biasMult *= m.mult;
    }

    const base = clamp01(p / 100);
    const minBias = ctx.scoreConfig.minBiasMult;
    const maxBias = ctx.scoreConfig.maxBiasMult;
    const clampedBias = clamp(biasMult, minBias, maxBias);
    const considerationMean = this._considerationMean(ctx.scoreConfig.defaultConsiderationFloor);
    const rewardMult = 1 + ctx.rewardWeight * ctx.expectedValue;
    const riskMult = 1 / (1 + ctx.riskWeight * ctx.goalRisk);

    return ctx.hardGate * 100 * base * considerationMean * clampedBias * rewardMult * riskMult;
  }
```

- [ ] **Step 7: Update `toJSON()`**

Add `scoreContext` to the returned object:

```js
      scoreContext: this.getScoreContext() || undefined,
```

- [ ] **Step 8: Add `floor` support in `consideration.js`**

In the `Consideration` constructor, replace:

```js
    this.weight = config.weight ?? 1;
```

with:

```js
    const weight = Number(config.weight ?? 1);
    this.weight = Number.isFinite(weight) && weight >= 0 ? weight : 1;
    const floor = config.floor == null ? null : Number(config.floor);
    this.floor = Number.isFinite(floor) ? clamp01(floor) : null;
```

- [ ] **Step 9: Run focused test**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-utility.mjs
```

Expected: tests still fail in sections that depend on `npc-utility` risk/reward context, but section `2) Goal.score 新公式语义` passes.

- [ ] **Step 10: Commit Goal / Consideration implementation**

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git add -- apps/game/js/engine/abstract/goal.js apps/game/js/engine/abstract/consideration.js apps/game/tools/test-utility.mjs; git commit -m "feat: add weighted utility goal scoring"
```

Expected: commit succeeds.

---

### Task 3: 让 npc-utility 注入收益风险评分上下文

**Files:**
- Modify: `apps/game/js/engine/npc/npc-utility.js`
- Modify: `apps/game/tools/test-utility.mjs`
- Test: `apps/game/tools/test-utility.mjs`

- [ ] **Step 1: Add risk/reward context assertions to test-utility**

In `apps/game/tools/test-utility.mjs`, add imports near the existing imports:

```js
import { readFileSync } from 'node:fs';
```

Add a helper after `const imp = ...`:

```js
const load = (p) => JSON.parse(readFileSync(resolve(GAME_ROOT, p), 'utf-8'));
```

Replace section `console.log('5) 情绪修正风险厌恶');` through the end of that block with:

```js
console.log('5) npc-utility 评分上下文：风险、情绪、收益');
{
  const riskJson = load('data/balance/risk.json');
  const rewardJson = load('data/balance/reward.json');

  function makeEntity({ caution = 50, anger = 0, fear = 0, obsessions = null } = {}) {
    return {
      state: { get: () => null, set: () => {} },
      staticData: { personality: { caution } },
      emotions: { get: (t) => t === 'anger' ? anger : t === 'fear' ? fear : 0 },
      obsessions,
    };
  }

  const cfg = {
    enabled: true,
    score: {
      minBiasMult: 0.25,
      maxBiasMult: 3,
      defaultConsiderationFloor: 0.05,
      rewardWeight: 0.5,
      riskWeight: 1,
    },
    riskAversion: { enabled: true, weight: 0.3 },
    emotionRisk: { enabled: true, angerFactor: 1.0, fearFactor: 1.0 },
    reward: { ...rewardJson, enabled: true },
  };
  const worldContext = { balanceConfig: { risk: riskJson } };

  const calmGoal = new Goal({ id: 'gCalm', sourceId: 'obsession_revenge', priority: 80 });
  decorateGoalConsiderations(makeEntity({ caution: 50, anger: 0, fear: 0 }), calmGoal, worldContext, cfg);
  const calmCtx = calmGoal.getScoreContext();
  assert(calmCtx?.goalRisk > 0, '高风险复仇目标写入 goalRisk');
  assert(calmCtx?.riskWeight > 0, '高风险复仇目标写入 riskWeight');
  assert(!calmGoal.modulators.some(m => m.label === 'riskAversion'), 'riskAversion 不再作为 modulator 重复扣分');

  const angryGoal = new Goal({ id: 'gAngry', sourceId: 'obsession_revenge', priority: 80 });
  decorateGoalConsiderations(makeEntity({ caution: 50, anger: 100, fear: 0 }), angryGoal, worldContext, cfg);
  assert(angryGoal.getScoreContext().riskWeight < calmCtx.riskWeight, '愤怒降低风险权重');

  const scaredGoal = new Goal({ id: 'gScared', sourceId: 'obsession_revenge', priority: 80 });
  decorateGoalConsiderations(makeEntity({ caution: 50, anger: 0, fear: 100 }), scaredGoal, worldContext, cfg);
  assert(scaredGoal.getScoreContext().riskWeight > calmCtx.riskWeight, '恐惧提高风险权重');

  const plunderGoal = new Goal({ id: 'gPlunder', sourceId: 'obsession_plunder', priority: 60 });
  decorateGoalConsiderations(makeEntity({ caution: 10 }), plunderGoal, worldContext, cfg);
  const plunderCtx = plunderGoal.getScoreContext();
  assert(plunderCtx?.expectedValue > 0, '夺宝目标写入 expectedValue');
  assert(plunderGoal.score() > 0, '收益风险上下文参与评分后仍得到正分');
}
```

- [ ] **Step 2: Run test to verify it fails**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-utility.mjs
```

Expected: FAIL because `npc-utility` still adds `riskAversion` as a modulator and does not call `goal.setScoreContext()`.

- [ ] **Step 3: Add helper functions to npc-utility**

In `apps/game/js/engine/npc/npc-utility.js`, add these helpers after `deriveTimeValue`:

```js
function positiveNumber(value, fallback) {
  const n = Number(value);
  return Number.isFinite(n) && n >= 0 ? n : fallback;
}

function computeRiskWeight(entity, goalRisk, utilityConfig) {
  if (goalRisk <= 0) return 0;

  const riskCfg = utilityConfig.riskAversion || {};
  if (riskCfg.enabled === false) return 0;

  const scoreCfg = utilityConfig.score || {};
  const baseWeight = positiveNumber(scoreCfg.riskWeight, positiveNumber(riskCfg.weight, 1));
  const caution = entity.staticData?.personality?.caution ?? 50;
  let riskWeight = baseWeight * (caution / 50);

  const emotionCfg = utilityConfig.emotionRisk || {};
  if (emotionCfg.enabled !== false && entity.emotions) {
    const anger = entity.emotions.get('anger') ?? 0;
    const fear = entity.emotions.get('fear') ?? 0;
    const angerFactor = emotionCfg.angerFactor ?? 1.0;
    const fearFactor = emotionCfg.fearFactor ?? 1.0;
    riskWeight *= (1 - (anger / 100) * angerFactor) * (1 + (fear / 100) * fearFactor);
  }

  return Math.max(0, riskWeight);
}
```

- [ ] **Step 4: Set score context after deriving expectedValue**

In `decorateGoalConsiderations`, immediately after `expectedValue` is computed, insert:

```js
  const scoreCfg = utilityConfig.score || {};
  const riskWeight = computeRiskWeight(entity, goalRisk, utilityConfig);
  const rewardWeight = positiveNumber(scoreCfg.rewardWeight, 0.5);
  if (typeof goal.setScoreContext === 'function') {
    goal.setScoreContext({
      hardGate: 1,
      expectedValue,
      goalRisk,
      rewardWeight,
      riskWeight,
      scoreConfig: scoreCfg,
    });
  }
```

- [ ] **Step 5: Remove old riskAversion modulator block**

Delete the block beginning with:

```js
  // ── D. 风险厌恶（情绪修正版，ADR-021 迁入）。
```

and ending just before:

```js
  // ── E. 随机扰动（上头，ADR-021 迁入）：
```

Then rename the remaining section comments so the sequence is:

```js
  // ── D. 随机扰动（上头，ADR-021 迁入）：
```

and:

```js
  // ── E. 路径偏好（ADR-021 迁入）：
```

- [ ] **Step 6: Run focused test**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-utility.mjs
```

Expected: PASS and output ends with `Utility 考量层单元测试全部通过`.

- [ ] **Step 7: Commit npc-utility wiring**

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git add -- apps/game/js/engine/npc/npc-utility.js apps/game/tools/test-utility.mjs; git commit -m "feat: wire npc utility score context"
```

Expected: commit succeeds.

---

### Task 4: 添加评分参数配置并更新激活态分化测试

**Files:**
- Modify: `apps/game/data/config/ai-config.json`
- Modify: `apps/game/tools/test-utility-divergence.mjs`
- Test: `apps/game/tools/test-utility-divergence.mjs`

- [ ] **Step 1: Add score config to ai-config**

In `apps/game/data/config/ai-config.json`, inside `npc.utility`, insert this object before `lambdaRisk`:

```json
      "score": {
        "_comment": "NPC 效用评分公式升级：控制 Goal.score 的偏置夹取、consideration floor、收益放大和风险压制。utility.json enabled=false 时不会被 npc-utility 注入评分上下文，默认关闭态仍按原 priority 路径运行。",
        "minBiasMult": 0.25,
        "maxBiasMult": 3.0,
        "defaultConsiderationFloor": 0.05,
        "rewardWeight": 0.5,
        "riskWeight": 1.0
      },
```

- [ ] **Step 2: Update test-utility-divergence expectation for reward off**

In `apps/game/tools/test-utility-divergence.mjs`, replace the reward-off assertion:

```js
  assert(gNoReward.score() < sReckless,
    `关闭期望收益后夺宝目标分数下降(${gNoReward.score().toFixed(1)} < ${sReckless.toFixed(1)})，证明 EV 提供了吸引力`);
```

with:

```js
  assert(gNoReward.score() < sReckless,
    `关闭期望收益后夺宝目标分数下降(${gNoReward.score().toFixed(1)} < ${sReckless.toFixed(1)})，证明 rewardMult 提供了收益吸引力`);
```

- [ ] **Step 3: Add explicit score context check in divergence test**

After `const sReckless = scoreGoal(reckless, 'obsession_plunder', 60);`, insert:

```js
  const gTrace = new Goal({ id: 'gPlunderTrace', sourceId: 'obsession_plunder', priority: 60 });
  decorateGoalConsiderations(reckless, gTrace, worldCtx, activeCfg);
  const traceCtx = gTrace.getScoreContext();
  assert(traceCtx?.expectedValue > 0, `夺宝目标 expectedValue 写入评分上下文(${traceCtx?.expectedValue?.toFixed(3)})`);
  assert(traceCtx?.goalRisk > 0, `夺宝目标 goalRisk 写入评分上下文(${traceCtx?.goalRisk?.toFixed(3)})`);
```

- [ ] **Step 4: Run config JSON parse check**

Run from repository root:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node -e "JSON.parse(require('fs').readFileSync('apps/game/data/config/ai-config.json','utf8')); console.log('ai-config json ok')"
```

Expected: `ai-config json ok`.

- [ ] **Step 5: Run divergence test**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-utility-divergence.mjs
```

Expected: PASS and output ends with `Utility 流派分化测试全部通过`.

- [ ] **Step 6: Commit config and divergence test**

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git add -- apps/game/data/config/ai-config.json apps/game/tools/test-utility-divergence.mjs; git commit -m "config: add npc utility score parameters"
```

Expected: commit succeeds.

---

### Task 5: 跑兼容测试

**Files:**
- Test: listed commands

- [ ] **Step 1: Run utility and obsession tests**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-utility.mjs; node tools/test-utility-divergence.mjs; node tools/test-obsession.mjs
```

Expected: all three commands exit 0.

- [ ] **Step 2: Run dynamic goal and interrupt tests**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-dynamic-goals.mjs; node tools/test-interrupt-policy.mjs
```

Expected: both commands exit 0.

- [ ] **Step 3: Inspect changed files**

Run from repository root:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git -c core.quotePath=false diff -- apps/game/tools/test-obsession.mjs apps/game/tools/test-dynamic-goals.mjs apps/game/tools/test-interrupt-policy.mjs
```

Expected: no output. This confirms Task 5 only verified compatibility and did not edit files.

---

### Task 6: 真实模拟验证与报告

**Files:**
- Create: `docs/superpowers/reports/2026-06-07-NPC效用评分公式升级验证.md`
- Modify: `docs/README.md`
- Test: `apps/game/tools/simulate-analysis.mjs`

- [ ] **Step 1: Run default-state simulation**

Run from `apps/game` and capture the output:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/simulate-analysis.mjs 300 2>&1 | Tee-Object -FilePath ..\..\docs\superpowers\reports\npc-utility-score-default.log
```

Expected: simulation completes without uncaught exception and writes `docs/superpowers/reports/npc-utility-score-default.log`.

- [ ] **Step 2: Run utility-active simulation**

Run from `apps/game` and capture the output:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; $env:UTILITY_ACTIVE='1'; node tools/simulate-analysis.mjs 500 2>&1 | Tee-Object -FilePath ..\..\docs\superpowers\reports\npc-utility-score-active.log; Remove-Item Env:UTILITY_ACTIVE
```

Expected: simulation completes without uncaught exception, prints `[激活态] UTILITY_ACTIVE=1`, and writes `docs/superpowers/reports/npc-utility-score-active.log`.

- [ ] **Step 3: Create validation report**

Run from repository root:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; $defaultLog = Get-Content -LiteralPath 'docs\superpowers\reports\npc-utility-score-default.log' -Encoding UTF8; $activeLog = Get-Content -LiteralPath 'docs\superpowers\reports\npc-utility-score-active.log' -Encoding UTF8; $content = @"
# NPC 效用评分公式升级验证

> 最后更新：2026-06-07  
> 范围：验证 Goal.score 加权几何平均、收益风险评分上下文、默认关闭态兼容与 Utility 激活态行为分化。

## 验证命令

````powershell
node tools/test-utility.mjs
node tools/test-utility-divergence.mjs
node tools/test-obsession.mjs
node tools/test-dynamic-goals.mjs
node tools/test-interrupt-policy.mjs
node tools/simulate-analysis.mjs 300
`$env:UTILITY_ACTIVE='1'; node tools/simulate-analysis.mjs 500; Remove-Item Env:UTILITY_ACTIVE
````

## 单元测试结果

- `test-utility.mjs`：已在实施任务中通过。
- `test-utility-divergence.mjs`：已在实施任务中通过。
- `test-obsession.mjs`：已在兼容验证中通过。
- `test-dynamic-goals.mjs`：已在兼容验证中通过。
- `test-interrupt-policy.mjs`：已在兼容验证中通过。

## 默认关闭态模拟输出摘录

````text
$($defaultLog -join "`n")
````

## Utility 激活态模拟输出摘录

````text
$($activeLog -join "`n")
````

## 判断

本次验证以真实行为统计、测试断言和模拟日志为依据。固定摘要或哈希一致性不作为正确性证据。
"@; Set-Content -LiteralPath 'docs\superpowers\reports\2026-06-07-NPC效用评分公式升级验证.md' -Value $content -Encoding UTF8
```

Expected: `docs/superpowers/reports/2026-06-07-NPC效用评分公式升级验证.md` exists and contains both captured simulation logs.

- [ ] **Step 4: Remove raw log files**

Run from repository root:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Remove-Item -LiteralPath 'docs\superpowers\reports\npc-utility-score-default.log','docs\superpowers\reports\npc-utility-score-active.log'
```

Expected: raw `.log` files are removed; the Markdown report remains.

- [ ] **Step 5: Update docs README**

In `docs/README.md`, under `superpowers/reports/`, add:

```text
│       ├── 2026-06-07-NPC效用评分公式升级验证.md
```

In the “关键文档” table, update the NPC score row to:

```markdown
| NPC 效用评分公式升级 | `superpowers/specs/2026-06-07-NPC效用评分公式升级设计.md`、`superpowers/plans/2026-06-07-NPC效用评分公式升级实施计划.md`、`superpowers/reports/2026-06-07-NPC效用评分公式升级验证.md` |
```

- [ ] **Step 6: Commit validation report**

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git add -- docs/README.md docs/superpowers/reports/2026-06-07-NPC效用评分公式升级验证.md; git commit -m "docs: report npc utility score validation"
```

Expected: commit succeeds.

---

### Task 7: Final verification and cleanup

**Files:**
- Verify only: repository status and recent commits

- [ ] **Step 1: Run final focused test suite**

Run from `apps/game`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-utility.mjs; node tools/test-utility-divergence.mjs; node tools/test-obsession.mjs; node tools/test-dynamic-goals.mjs; node tools/test-interrupt-policy.mjs
```

Expected: all commands exit 0.

- [ ] **Step 2: Check working tree**

Run from repository root:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git -c core.quotePath=false status --short
```

Expected: no output.

- [ ] **Step 3: Review recent commits**

Run from repository root:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; git -c core.quotePath=false log --oneline -6
```

Expected: recent commits include:

```text
test: specify npc utility score formula
feat: add weighted utility goal scoring
feat: wire npc utility score context
config: add npc utility score parameters
docs: report npc utility score validation
```

---

## Self-Review

- Spec coverage: The plan covers the scoring formula, weighted geometric mean, consideration floor, bias clamp, reward/risk separation, config fields, default Utility gating, tests, and real simulation validation.
- Placeholder scan: The plan contains concrete file paths, code snippets, commands, expected results, and commit commands for each task.
- Type consistency: The plan consistently uses `Goal.setScoreContext()`, `Goal.getScoreContext()`, `scoreConfig`, `expectedValue`, `goalRisk`, `rewardWeight`, `riskWeight`, `floor`, and `weight`.
