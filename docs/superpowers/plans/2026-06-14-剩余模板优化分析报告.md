# 剩余世界观参考模板 · 优化分析报告

> 最后更新：2026-06-14
> 范围：除 10 个已修好模板外的 27 个待修模板，仅分析、不改内容。
> 配套计划：[实施计划](2026-06-14-剩余模板优化-实施计划.md)

## 1. 评审基准 rubric

| 维度 | 合格标准（来自已修模板的共性） |
|---|---|
| 章节构成 | 必含五块固定骨架：①头部元数据（`最后更新` 日期 + `状态：模板` + `类型` + `目标详细程度：L3`）；②`## 适用范围`（说明收录什么、并划清与其他模板的边界，如"材料只在丹方中提及，材料本体放材料分析模板"）；③范围约束段（物品类用 `## 前置声明`"只整理原文出现/可支撑、缺据写'原文未说明'、不为补齐梯度而虚构"；职业类可用 `## 不要写什么`）；④字段规格块（见"字段定义"维）；⑤`## 推荐结构` 含一个可直接套用的产出文档骨架。缺任意一块即不合格。 |
| 字段定义 | 必含一份逐字段规格，且对每个字段给出"写法要求/含义"而非只列字段名。两种合格形态：物品类用 `## 必须写什么` 表格（`\| 字段 \| 写法要求 \|`，逐字段说明取值口径，如"稀有度=按作品自己的描述填写"）或逐字段 bullet（功法/秘境）；职业类用 `\| 模块 \| 必写内容 \| 项目用途 \|` 三列表 + 多个 `### 子模块` 段落释义。所有"无据"字段须明确允许写 `原文未说明`。只罗列字段名、无释义/取值口径者不合格。 |
| 数据来源标注 | 推荐结构骨架的头部必须固定带 `资料来源：` 行，指向 `docs/世界观参考/作品名/作品名.txt` 与 `.storygraph/storygraph.db`；并带 `对应模板：docs/世界观参考/模板/<本模板>.md` 行做溯源；产出文档 `状态` 须给"草案 / 原文已复核 / 待复核"枚举。缺资料来源行或对应模板溯源行即不合格。 |
| 与项目对接 | 须指明本类条目落到项目的去向。最佳做法（符师/阵法师）：在字段表设 `项目用途`/`项目映射` 列，并显式点名 `items`/`abilities`/`effects`/逃生 GA/任务奖励/建筑/地图/动态事件等 `apps/game/data/` 资产类型，符师另设 `## 游戏化抽象（我的判断）` 表。多数物品类（丹药/法宝/材料/武器/功法/秘境）当前以"用途"字段隐式对接、未单列项目映射列——按本 rubric 视为"对接偏弱"，评审时应建议补一句对应 `apps/game/data/` 资产或 wiki 条目类别。 |
| 示例 | 必须给出至少 1 个填好的样例，且样例要把每个字段/模块都填满（含示范何时写 `原文未说明`）。合格形态：物品类在推荐结构里给填好的"总表"样例行（丹药 5 行、武器 5 行等）或填好的卡片（功法"基础吐纳法/御兽诀/傀儡术"、秘境"宗门试炼秘境/古修洞府"）；职业类（炼丹师/炼器师）以逐个 `###` 子模块的释义段充当填写范例。只给空表头、空卡片占位而无任何填好内容者不合格。 |

> 说明：后续每张速评卡的"主要问题"均按本 rubric 逐维对照得出。

## 2. 项目代码 ↔ 模板对照表与缺口

> 说明：左列为项目实际维度。代码侧覆盖 `apps/game/data/` 下全部 18 个子目录（abilities / actions / balance / behavior-trees / config / definitions / economy / effects / entities / goals / items / jobs / needs / quests / relationships / tags / toils / world），外加 `docs/worldbuilding/wiki/` 四类与 ADR-058「分层时间域 / 月压缩」里程碑维度。模板名取自 `docs/世界观参考/模板/`。

| 项目维度（data 目录 / wiki 类 / 里程碑） | 已对应模板 | 缺口判断 |
|---|---|---|
| abilities（GA：战斗/逃生/神通等技能定义） | 功法术法神通、战斗与保命机制 | 已覆盖（技能数值/触发由两模板的"项目用途"列对接，符合 GAS 资产分工） |
| actions（NPC/势力行动集、反应、世界规则原子动作） | 角色AI行为参考、动态事件与机会点 | 部分（行动是 AI 行为的原子单元，AI 行为参考偏决策层，原子动作集本身无专门模板） |
| balance（combat/cultivation/economy/emotion/memory/obsession/personality/reaction/relationship/social/risk/utility 等平衡参数） | 境界提升与功法分析、记忆情绪与执念、NPC性格与代表事件、物资产出与消耗、拍卖坊市与交易 | 部分（多数子表有对应主题模板承载数值口径；utility/reaction/risk 决策权重无叙事侧模板，但属纯调参，可不立模板） |
| behavior-trees（NPC/势力/妖兽分层行为树） | 角色AI行为参考、妖兽与修士关系分析 | 部分（行为树是 AI 编排结构，AI 行为参考覆盖意图层，但"行为树/决策编排"这一结构维度无专门模板） |
| config（ai-config / data-manifest / game-config 运行时配置） | 无 | 缺（纯工程配置，世界观侧本不需模板——记录为"无需模板"而非待补） |
| definitions（境界 ranks / 功法 techniques / 武器 weapons / 妖兽 monsters / 地形 terrains / 名字 names / 门派组织 sect-organization / 战斗基表 等静态定义） | 境界提升与功法分析、武器分析、功法术法神通、妖兽分析、势力设定、世界观设定 | 部分（terrains 地形定义、names 命名、combat-base-table 战斗基表无对应模板；地形/命名属世界观可立模板，战斗基表属调参） |
| economy（交易场景/托管/债务/抽象拍卖底座） | 拍卖坊市与交易、物资产出与消耗 | 已覆盖 |
| effects（GE：combat/core 通用效果原语） | 战斗与保命机制、丹药分析 | 已覆盖（GE 为机制原语，数值由 items 提供，模板的"项目用途"列已点名 effects） |
| entities（factions / npcs 初始实体种子） | 势力设定、NPC性格与代表事件、人物关系与事件分析 | 已覆盖 |
| goals（动态目标：事件感知后临时产出 Goal） | 动态事件与机会点、角色AI行为参考 | 已覆盖 |
| items（artifact / pill / material / talisman / technique / currency 物品实例数值） | 丹药分析、法宝分析、材料分析、符师、功法术法神通、拍卖坊市与交易 | 已覆盖（六类物品各有对应分析模板承载数值口径） |
| jobs（combat/cultivation/economy/quest/social/dynamic-event 六类 NPC 长流程作业） | 角色修炼历程、宗门任务体系、散修生存方式、时间行动与事件耗时 | 部分（Job 是多日长流程编排单元，时间行动模板覆盖耗时维，但"Job/Toil 流程编排"作为机制结构无专门模板） |
| needs（NPC/势力需求驱动） | 角色AI行为参考、记忆情绪与执念 | 部分（需求驱动是 AI 动机层，AI 行为参考覆盖，但"需求体系"本身无独立模板） |
| quests（任务模板与难度阶梯） | 宗门任务体系 | 已覆盖 |
| relationships（关系图谱：marks/groups/impact-rules/signal-rules/projections/ledgers/通缉链） | 人物关系与事件分析、信息传播与情报、冲突事件分析 | 部分（通缉链 wanted-chain、信号规则 signal-rules 属"声誉/通缉/情报传播"机制，信息传播模板偏新闻扩散，通缉/声誉链对接偏弱） |
| tags（GameplayTag 层级登记表） | 无 | 缺（GAS 工程登记表，世界观侧无需独立模板——记录为"无需模板"） |
| toils（core + 六类 NPC 原子工序，Job 的最小执行步） | 时间行动与事件耗时、出门游历流程分析 | 部分（Toil 为最小耗时工序，时间行动/游历流程覆盖耗时与流程，但工序编排结构无专门模板） |
| world（dynamic-events / map / modifiers / news / opportunities 世界态与事件） | 世界状态与灾变、动态事件与机会点、信息传播与情报、秘境遗迹与机缘 | 已覆盖 |
| wiki/artifacts（rare-treasures / techniques） | 法宝分析、功法术法神通、丹药分析、材料分析、武器分析、符师 | 已覆盖 |
| wiki/characters（relationship-types） | 人物关系与事件分析、NPC性格与代表事件、角色修炼历程、记忆情绪与执念 | 已覆盖 |
| wiki/creatures（monsters） | 妖兽分析、妖兽与修士关系分析 | 已覆盖 |
| wiki/rules（境界/战斗/逃生/门派/继承/死亡/性格/体质/灵根/游历/暴富/执念 等规则条目） | 境界提升与功法分析、战斗与保命机制、灵根体质血脉、出门游历流程分析、角色AI行为参考、记忆情绪与执念、世界状态与灾变、宗门任务体系、邪修分析、夺舍设定分析 | 部分（leader-succession 门派继承、faction-crisis-defection 势力危机叛逃、wealth-exposed 暴富露财 这几条规则无精确对口模板，势力设定模板偏静态结构，缺"势力运营/继承/危机"动态规则模板） |
| 里程碑 · ADR-058 / 月压缩事件因果图（事件因果链） | 冲突事件分析、动态事件与机会点 | 部分（现有事件类模板按单事件组织，缺"事件因果链 / 长程因果图"组织视角） |
| 里程碑 · ADR-058 / 月度 episode 与地点轨迹 | 时间行动与事件耗时、出门游历流程分析 | 缺（月压缩 episode 与地点轨迹是"压缩态经历摘要"，现有模板按逐日流程/单次游历组织，无"月度经历摘要 / 轨迹"维度） |
| 里程碑 · ADR-058 / NPC 长程演化（修为·境界·关系·背包·死亡的月度累积） | 角色修炼历程、人物关系与事件分析、物资产出与消耗 | 部分（各分项有模板，但缺"远处 NPC 长程离线演化"这一跨维聚合视角的模板） |
| 里程碑 · ADR-058 / 相遇与剧情对话 DSL（对话候选/选项/副作用命令/剧情包状态机） | 有限视角与叙事日志、NPC性格与代表事件 | 缺（叙事日志偏事后记录，无"对话 DSL / 剧情包状态机 / 相遇剧情"模板） |
| 里程碑 · ADR-058 / 保真等级与时间域分层（Hot/Warm/Cold） | 时间行动与事件耗时 | 缺（属架构机制，世界观侧无对口模板；如需玩家侧叙事感知可考虑，但本质工程层） |

**缺口小结：** 以下条目为"有系统无模板"或"覆盖不足"，作为后续新增模板清单的输入，逐条点名：

- **势力运营 / 继承 / 危机动态规则**：`wiki/rules` 下 `leader-succession`（掌门继承）、`faction-crisis-defection`（势力危机与叛逃）、`wealth-exposed`（暴富露财）已有规则落地，但「势力设定」模板偏静态结构（势力类型/资源），缺承载"势力运营、掌门更替、危机叛逃"这类**动态势力规则**的模板。建议新增「势力运营与权力更替」类模板。
- **ADR-058 · 事件因果链 / 长程因果图**：现有「冲突事件分析」「动态事件与机会点」按单个事件组织，月压缩里程碑要求的"事件因果链 / 多月因果图"组织视角无模板。建议新增「事件因果链」模板。
- **ADR-058 · 月度 episode 与地点轨迹**：远处 NPC 的月压缩产出是"压缩态经历摘要 + 地点轨迹"，与「时间行动与事件耗时」（逐日耗时）、「出门游历流程分析」（单次游历）粒度不同，无对口模板。建议新增「月度经历摘要与地点轨迹」模板。
- **ADR-058 · 相遇与剧情对话 DSL**：对话候选/选项/副作用命令/剧情包状态机为里程碑核心，现有「有限视角与叙事日志」偏事后日志，缺"对话 DSL / 剧情包状态机 / 相遇剧情"模板。建议新增「相遇剧情与对话设计」模板。
- **ADR-058 · NPC 长程离线演化聚合**：「角色修炼历程」「人物关系与事件分析」「物资产出与消耗」分别覆盖单维，缺"远处 NPC 一个月一次跨维（修为·境界·关系·背包·死亡）离线演化"的聚合视角模板（可作为前一条月度 episode 模板的子项，优先级次于上四条）。
- **覆盖不足但暂判无需新模板（仅记录，不进新增清单）**：`config`/`tags`（纯工程配置/登记表，世界观侧无需模板）；`balance` 的 utility/reaction/risk、`definitions` 的 combat-base-table（纯数值调参）；`actions`/`toils`/`jobs`/`behavior-trees`/`needs`（AI 行为/流程编排的机制结构维，已由「角色AI行为参考」「时间行动与事件耗时」从意图与耗时两侧间接覆盖，是否单立"机制编排"模板可在第 5 节讨论）。
- **对接偏弱（rubric 第 4 维已记，非缺模板）**：`relationships` 的 `wanted-chain`（通缉链）/`signal-rules`（声誉信号）与「信息传播与情报」模板的对接偏弱——后者偏新闻扩散，通缉/声誉链需要在该模板补一句对应 `apps/game/data/relationships/` 资产，或在第 3 节速评卡建议补"项目映射"列。

## 3. 逐模板速评卡
### 3.1 修炼体系类

#### 修炼流派模板
- **当前状态：** 五块骨架齐全（头部元数据 / `## 适用范围`+边界 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 / `## 推荐结构`），结构成熟，是职业类风格的完整模板。
- **主要问题：** （章节构成）合格，五块齐备。（字段定义）`## 必须写什么` 用 `模块/必写内容/项目用途` 三列表，但卡片字段（定义、触发条件、典型行动链、不可照搬点等）只在 `### 流派名` 表里列了字段名，**无逐字段写法/取值口径释义**，属"列字段名无释义"边缘不合格。（示例）最关键缺陷——`## 流派总览` 表、`### 流派名` 卡片、`## 游戏化抽象` 表全是空表头**无任何填好的样例行**，未示范"长生苟道/夺宝冒险"等任一流派填法，按 rubric 示例维不合格。（数据来源）推荐结构头部 `资料来源/对应模板/状态枚举` 三行齐全，合格。（与项目对接）三列表已点名"执念、Utility、GOAP、Job/Toil、GAS"，对接较强。
- **对接的项目代码：** `apps/game/data/balance/obsession.json`（执念）、`balance/utility.json`（Utility 决策权重）、`balance/personality.json`（personality 区间）、`apps/game/data/goals`（goalState/动态目标）、`apps/game/data/jobs` 与 `apps/game/data/toils`（行为链 Job/Toil）；wiki 侧落 `docs/worldbuilding/wiki/` NPC 原型条目。
- **建议涵盖方面（草案）：**
  - 为 `## 必须写什么` 各模块补一句取值口径（如"资源偏好=按作品优先级排序，无据写'原文未说明'"）。
  - 在 `## 流派总览` 给 1 行填好的样例（如"长生苟道"全字段填满）。
  - 在 `### 流派名` 卡片给 1 张填好的范例卡，含示范何时写"原文未说明"。
  - `游戏化抽象` 表补 1 行示范（流派原型 → `obsession`/`motiveWeights` 建议值 + 边界）。
  - "可映射系统"字段显式点名 `apps/game/data/` 具体目录而非泛称"GAS"。
- **优先级：** 中 —— 结构成熟、对接强，乱点集中在"缺填好样例"；与月压缩里程碑（NPC 长程演化需流派原型驱动动机）有间接相关，但非阻塞核心。

#### 境界提升与功法分析模板
- **当前状态：** 五块骨架齐全，且字段块最丰富（必须写什么 + 尽量写全清单 + 详细程度 + 推荐结构含 5 张子表），是修炼数值口径的主承载模板。
- **主要问题：** （字段定义）`## 必须写什么` 三列表 + `## 尽量写全的内容清单` 释义充分，合格。（示例）核心缺陷——`境界体系总览`/`境界突破卡片`/`突破条件表`/`功法体系`/`游戏化抽象`/`数据候选` 六张表**全为空表头，无任一填好行**（如未给"练气→筑基"示范），示例维不合格。（与项目对接）已点名 `ranks.json`/`techniques.json`/效果，但用的是裸文件名，**未带 `apps/game/data/definitions/` 全路径**，对接定位偏弱。（章节构成 / 数据来源）均合格。（一致性）数据候选表的"文件"列应约束到真实路径以免误填。
- **对接的项目代码：** `apps/game/data/definitions/ranks.json`（境界层级/order/寿元）、`apps/game/data/definitions/techniques.json`（功法品级/效果）、`apps/game/data/balance/cultivation.json`（修炼速度/突破概率单一真相源）、`apps/game/data/effects`（功法 GE 效果）、`apps/game/data/tags/tags.json`（功法/体系 Trait Tag）。
- **建议涵盖方面（草案）：**
  - 六张表各补至少 1 行填好样例（境界、突破、功法、数据候选各一）。
  - 项目映射列与"数据候选-文件"列统一补全 `apps/game/data/definitions/...` 路径。
  - 明确数值单一真相源在 `balance/cultivation.json`，模板只承载机制口径（呼应 ADR-042）。
  - 寿元桶/突破概率字段给取值范围与"无据写原文未说明"示范。
  - 功法 effect 字段示范如何引用 `ge_` 前缀效果而非写死数值。
- **优先级：** 高 —— 直接对接 ranks/techniques/cultivation 三个核心数值文件，代码相关度最高；境界/寿元是 NPC 长程演化（月压缩）的底座维度，里程碑相关度高。

#### 灵根体质血脉模板
- **当前状态：** 头部元数据 + 适用范围 + `## 必须写什么` + `## 不要写什么` + 推荐结构齐全，对接 GAS Tag/Effect 描述清晰。
- **主要问题：** （章节构成）相比同组模板**缺 `## 详细程度` 段**（境界/历程模板都有），骨架不一致。（示例）`资质总览`/`特质卡片`/`游戏化抽象` 三表**均为空表头，无填好样例**（未给"天灵根/先天道体"任一示范），示例维不合格。（字段定义）卡片字段只列名（修炼影响、社会关系影响等）无写法口径，边缘不合格。（与项目对接）已点名 `Trait.SpiritRoot.*`/`Trait.Physique.*`/Effect/Ability，对接较强，但未点名这些 Tag 登记在哪个文件。（数据来源）三行齐全，合格。
- **对接的项目代码：** `apps/game/data/tags/tags.json`（`Trait.SpiritRoot.*`/`Trait.Physique.*` 层级登记）、`apps/game/data/entities/npcs.json`（NPC 初始 trait/initial roll）、`apps/game/data/balance/cultivation.json`（修炼速度倍率/突破 bonus）、`apps/game/data/effects`（资质 GE 效果）、`apps/game/data/abilities`（资质赋予的 GA）。
- **建议涵盖方面（草案）：**
  - 补 `## 详细程度` 段，与同组模板对齐（每个资质至少 2 证据点 / 倍率属"我的判断"）。
  - 三表各补 1 行填好样例，含示范"无据倍率写到游戏化抽象"。
  - 卡片字段补取值口径（如"修炼影响=倍率或定性，无据写原文未说明"）。
  - 显式点名 `Trait.*` Tag 须先在 `tags/tags.json` 登记。
  - 区分先天/觉醒 trait 在 npcs.json initial roll 的落法。
- **优先级：** 高 —— 直接对接 tags/npcs/cultivation，资质是 NPC 差异化与长程演化的初始条件，里程碑相关度高；且自身存在骨架缺段，乱度中上。

#### 角色修炼历程模板
- **当前状态：** 五块骨架齐全（含 `## 详细程度`），定位为 L2/标杆 L3 的角色纵向成长时间线模板。
- **主要问题：** （示例）`修炼历程总览`/`阶段卡片`/`节奏抽象` 三表**全空，无任一角色阶段填好样例**，示例维不合格。（与项目对接）"可映射系统"只写"修炼、游历、任务、机会点、动态事件"等**泛称，未点名 `apps/game/data/` 具体目录**，且本模板天然贴合月压缩"NPC 长程演化"但模板内**完全没提月度/压缩态粒度**，与里程碑脱节。（字段定义）卡片字段列名为主、口径偏少。（章节构成 / 数据来源）合格。
- **对接的项目代码：** `apps/game/data/jobs`（cultivation/quest 等六类长流程作业，成长行为链）、`apps/game/data/definitions/ranks.json`（境界变化）、`apps/game/data/balance/cultivation.json`（修炼节奏/突破概率）、`apps/game/data/economy` 与 `apps/game/data/items`（资源消耗曲线）、`apps/game/data/goals`（长期目标切换）；与 ADR-058 月压缩"NPC 长程离线演化"直接相关。
- **建议涵盖方面（草案）：**
  - 三表各补 1 行填好的角色阶段样例。
  - "可映射系统"列改为点名具体 data 目录。
  - 增加"月度/压缩态阶段"粒度说明，对接月压缩 episode（呼应缺口小结第 3、5 条）。
  - 资源消耗曲线给可数据化字段示范（与 economy/items 对齐）。
  - 明确"相对阶段"如何转成长程演化的时间桶。
- **优先级：** 高 —— 与月压缩"NPC 长程演化"里程碑最贴合，但当前完全未对接月度粒度，是承上启下的关键模板；代码相关度高、需补里程碑视角。

#### 战斗与保命机制模板
- **当前状态：** 骨架基本齐全，对接 GAS（GE/GA/DamageSpec/State.Dying）描述最具体，但推荐结构末尾的 `## 游戏化抽象（我的判断）` 段**只有标题、内容被截断为空**。
- **主要问题：** （章节构成）`## 游戏化抽象（我的判断）` 标题下**无任何表格/正文，骨架不完整**（对比同组模板该段应有抽象规则表），是明确的结构残缺。（示例）`战斗机制总览`/`保命手段卡片` 两表**全空，无填好样例**（未给"遁地符/锁血"任一示范），示例维不合格。（字段定义）"保命手段卡片"实为表格非卡片，字段列名清晰但缺逐字段口径。（与项目对接）项目映射点名最到位（`applyDamage`/`Trigger.LethalDamage`/`State.Dying`/`Immune.Crush`/`ability_lock_hp`），但 `ability_lock_hp` 命名与 ADR-042 规范的 `ga_`/`ge_` 前缀不符，**对接示例用了过时/不合规 id**。（数据来源）三行齐全，合格。
- **对接的项目代码：** `apps/game/data/abilities/combat-abilities.json`（保命/逃生 GA）、`apps/game/data/effects/combat-effects.json` 与 `effects/core-effects.json`（锁血/伤害 GE）、`apps/game/data/balance/combat.json` 与 `balance/risk.json`（战斗/风险结算）、`apps/game/data/tags/tags.json`（`State.Dying`/`Immune.Crush`/`Trigger.LethalDamage` 登记）、`apps/game/data/definitions/combat-base-table.json`（战斗基表）。
- **建议涵盖方面（草案）：**
  - 补全 `## 游戏化抽象（我的判断）` 表（抽象规则/依据/建议落点/边界），修复结构残缺。
  - 两表各补 1 行填好样例（如"锁血保命"映射 `ge_lock_hp`/`ga_lock_hp`）。
  - 将示例 id 更新为 ADR-042 规范的 `ga_`/`ge_` 前缀，删除 `ability_lock_hp` 旧写法。
  - 显式点名 Tag 须在 `tags/tags.json` 登记。
  - 保命手段卡片补逐字段取值口径与"被克制条件无据写原文未说明"。
- **优先级：** 高 —— 自身存在骨架残缺（游戏化抽象段空）+ 对接 id 不合 ADR-042，乱度最高；战斗/逃生是 GAS 核心资产，代码相关度高，应优先修。
### 3.2 角色 / NPC / AI 类

#### NPC性格与代表事件模板
- **当前状态：** 五块骨架齐全（头部元数据含 L3 / `## 适用范围`+不只记主角的边界 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 / `## 推荐结构`），是性格维度 → personality 数值的主承载模板。
- **主要问题：** （示例）核心缺陷——推荐结构里 `性格维度定义`/`角色性格表`/`代表事件卡片(### 事件名)`/`参数建议` 四表**全为空表头，无任一填好样例**（未给任何一个角色的"野心/谨慎"打分或一张填好的事件卡），按 rubric 示例维不合格。（字段定义）`## 必须写什么` 三列表 + `## 尽量写全的内容清单`+`## 详细程度`（每角色≥3 事件、数值给区间标"我的判断"）释义较充分，合格。（与项目对接）只在三列表写"`personality` 字段 / Utility/GOAP/Reaction"等**裸字段名，未点名 `apps/game/data/balance/` 全路径**，对接定位偏弱；且"数据来源"行齐全，章节构成合格。
- **对接的项目代码：** `apps/game/data/balance/personality.json`（六维区间）、`apps/game/data/balance/utility.json` 与 `balance/reaction.json`（决策/反应权重）、`apps/game/data/balance/obsession.json`（执念倾向）、`apps/game/data/entities/npcs.json`（NPC 初始 personality/trait）、`apps/game/data/actions/reaction-actions.json`（代表事件 → Reaction 行动）。
- **建议涵盖方面（草案）：**
  - 四表各补至少 1 行/张填好样例（一个角色全六维打分 + 一张填好的代表事件卡，含示范何时写"原文未说明"）。
  - "可映射项目字段"列补全 `apps/game/data/balance/personality.json` 等真实路径。
  - 明确数值口径以 `balance/personality.json` 为单一真相源，模板只承载证据与区间判断。
  - "参数建议"表区间须绑定具体证据事件，呼应"每个性格判断回到事件"。
  - 补一句月压缩相关：性格作为 NPC 长程离线演化的稳定动机底色。
- **优先级：** 高 —— 直接对接 personality/utility/obsession 三个 AI 核心平衡文件，代码相关度高；性格是 NPC 长程演化的动机底座，里程碑相关度高，且自身缺全部填好样例，乱度中上。

#### 角色AI行为参考模板
- **当前状态：** 五块骨架齐全（含 `## 游戏化抽象（我的判断）` 段），且明确对齐当前 Job/Toil 而非旧 BT/GOAP 术语，是角色行为 → AI 架构映射的主模板。
- **主要问题：** （示例）核心缺陷——`行为原型总览`/`行为链卡片(### 行为原型名)`/`游戏化抽象` 三表**全为空表头，无任一填好样例**（未给"谨慎求生型/赌命夺宝型"任一原型的完整填法），示例维不合格。（与项目对接）"可映射文件"行列了 `dynamic-goals.json`/`npc-job-actions.json`/`jobs/`/`toils/`/`balance/*.json` 但**用的是裸文件名/通配，未带 `apps/game/data/` 前缀**，定位偏弱（这些文件均真实存在于 `goals/`、`actions/`、`jobs/`、`toils/`）。（字段定义 / 章节构成 / 数据来源）`## 必须写什么`+`## 详细程度`（每原型须含输入条件/目标状态/行动链/成败/映射）充分，三块齐全，合格。
- **对接的项目代码：** `apps/game/data/goals/dynamic-goals.json`（动态目标）、`apps/game/data/actions/npc-job-actions.json` 与 `actions/npc-action-sets.json`（JobAction/行动集）、`apps/game/data/jobs` 与 `apps/game/data/toils`（行动链编排）、`apps/game/data/behavior-trees/npc-default.json`（NPC 行为树编排）、`apps/game/data/balance/utility.json`/`balance/reaction.json`/`balance/risk.json`（权重/打断/风险键）、`apps/game/data/needs/npc-needs.json`（需求驱动）。
- **建议涵盖方面（草案）：**
  - 三表各补至少 1 行/张填好样例（如"谨慎求生型"全字段填满 + 一条游戏化抽象规则）。
  - "可映射文件"统一补 `apps/game/data/...` 全路径，区分 SimpleAction/JobAction/Toil/Utility 权重四类落点。
  - 显式串联 `behavior-trees/` 编排层与 `goals/`+`actions/` 原子层的关系。
  - 打断规则映射到 `balance/reaction.json` 与 `balance/risk.json` 的风险键示范。
  - 补月压缩视角：远处 NPC 离线时行为原型如何退化为压缩态动机标签。
- **优先级：** 高 —— 是 AI 决策层的总纲模板，横跨 goals/actions/jobs/toils/behavior-trees/needs 多目录，代码相关度最高；NPC 长程演化依赖行为原型驱动，里程碑相关度高，缺全部填好样例需补。

#### 记忆情绪与执念模板
- **当前状态：** 头部元数据 + `## 适用范围` + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（含 `## 游戏化抽象`）齐全，直接点名 memory/emotion/obsession 三个平衡表。
- **主要问题：** （章节构成）相比同组 NPC性格/AI 模板**缺独立 `## 详细程度` 段**（要求散落在"不要写什么"里，无"每执念≥N 证据 / 数值标我的判断"的明确量化标准），骨架不一致。（示例）`触发事件总览`/`记忆/执念卡片(### 事件/执念名)`/`游戏化抽象` 三表**全为空表头，无任一填好样例**（未给"亲友死亡→复仇"任一链条填法），示例维不合格。（字段定义）卡片"消退或转化条件"等字段只列名、无取值口径（如衰减是否给 decay 数值范围），边缘不合格。（与项目对接）三列表点名 memory/emotion/obsession/decay 但**未带 `apps/game/data/balance/` 全路径**，对接偏弱；数据来源行齐全合格。
- **对接的项目代码：** `apps/game/data/balance/memory.json`（记忆事件/强度）、`apps/game/data/balance/emotion.json`（情绪 delta）、`apps/game/data/balance/obsession.json`（执念类型/目标倍率）、`apps/game/data/goals/dynamic-goals.json`（执念 → 动态目标）、`apps/game/data/relationships/impact-rules`（记忆/情绪反哺关系边）、`apps/game/data/balance/covet.json`（贪念/夺宝执念）。
- **建议涵盖方面（草案）：**
  - 补独立 `## 详细程度` 段，与同组模板对齐（每执念至少 2 证据点、衰减/强化给定性或区间）。
  - 三表各补 1 行/张填好样例（一条"羞辱→愤怒→复仇"完整链，含消退条件示范）。
  - 字段补取值口径：情绪 delta、obsession 倍率、decay 是否给区间与"无据写原文未说明"。
  - "项目映射"补全 `apps/game/data/balance/memory.json` 等真实路径。
  - 强化月压缩对接：记忆/执念在 NPC 离线长程演化中的衰减与被新事件加强机制（直接喂 Cold 月压缩动机）。
- **优先级：** 高 —— 直接对接 memory/emotion/obsession 三表，且记忆衰减/执念加强正是月压缩里程碑"NPC 长程离线演化"最核心的动机驱动机制，里程碑相关度最高；自身缺详细程度段 + 全部填好样例，乱度中上。

#### 人物关系与事件分析模板
- **当前状态：** 五块骨架最齐全（头部元数据 / 适用范围+边界 / `## 不要写什么` / `## 必须写什么` 三列表 + `## 详细程度` / 推荐结构含 4 张表：关系总览/关键关系边/**事件因果链**/卡片+游戏化抽象），且已内置"事件因果链"表，是本组结构最成熟的模板。
- **主要问题：** （示例）唯一突出缺陷——`关系类型总览`/`关键关系边`/`事件因果链`/`关系/事件卡片`/`游戏化抽象` 五表**全为空表头，无任一填好样例**（未给"师徒/拜师建边/复仇因果"任一填法），示例维不合格。（与项目对接）三列表写 `relationship edge types`/`eventBindings`/`RelationType` 等**裸字段名，未点名 `apps/game/data/relationships/` 下 dictionaries/event-hooks/impact-rules 等真实子目录**，对接定位偏弱。（章节构成 / 字段定义 / 数据来源）`## 详细程度`（须写 from/to/type/触发/状态/证据）+ 三列表释义充分、三行来源齐全，均合格。
- **对接的项目代码：** `apps/game/data/relationships/dictionaries`（关系类型/RelationType 词表）、`apps/game/data/relationships/event-hooks`（建边事件 eventBindings）、`apps/game/data/relationships/impact-rules`（行为后果/强度变化规则）、`apps/game/data/relationships/groups` 与 `relationships/projections`（势力/群体关系投影）、`apps/game/data/entities/npcs.json`/`entities/factions.json`（实体初始关系边）、`apps/game/data/goals/dynamic-goals.json`（关系驱动复仇/报恩目标）。
- **建议涵盖方面（草案）：**
  - 五表各补至少 1 行/张填好样例（一类师徒关系 + 一条拜师建边 + 一条"救命→报恩→继承遗志"因果链）。
  - "项目映射"列补全到 `apps/game/data/relationships/dictionaries`、`event-hooks`、`impact-rules` 具体子目录。
  - 关系方向/对称性须落到 RelationType 词表字段示范（呼应"恩人/师徒非对称"）。
  - 事件因果链补"多月/跨事件"粒度，直接对接月压缩"事件因果图"里程碑（呼应缺口小结第 2 条）。
  - 公开/秘密状态映射到 relationships projections/signal-rules 的可见性。
- **优先级：** 高 —— 直接对接 relationships 全套子目录 + entities，代码相关度高；内置"事件因果链"表与月压缩"事件因果图"里程碑天然贴合，里程碑相关度高，仅缺填好样例与路径点名，是性价比高的优先修对象。
### 3.3 势力 / 组织类

#### 势力设定模板
- **当前状态：** 五块骨架齐全（头部元数据含 L3 / `## 适用范围` 含服务对象 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 详细程度` / `## 推荐结构` 含势力总览/势力卡片/关系网络/游戏化抽象/数据候选五张表），是势力静态结构的主承载模板，结构成熟。
- **主要问题：** （示例）核心缺陷——`势力总览`/`### 势力名` 卡片/`势力关系网络`/`游戏化抽象`/`可转化数据候选` 五表**全为空表头，无任一填好样例**（未给"某正道宗门/某商会"任一全字段填法），按 rubric 示例维不合格。（与项目对接）`## 必须写什么` 三列表点名 `faction.type/subtype`、`role/roleQuota`、势力 AI 等**裸字段名，未点名 `apps/game/data/entities/factions.json`、`definitions/sect-organization.json`、`behavior-trees/faction-default.json` 等真实路径**，对接定位偏弱；且模板偏静态结构（类型/资源/关系），与 wiki/rules 已落地的 `leader-succession`（掌门继承）、`faction-crisis-defection`（势力危机叛逃）、`sect-operation`（势力运营）等**动态势力规则脱节，未指引这些规则去向**。（字段定义）卡片字段（组织结构、核心资源、内部矛盾等）只列字段名、无逐字段取值口径，边缘不合格。（章节构成 / 数据来源）合格。
- **对接的项目代码：** `apps/game/data/entities/factions.json`（势力初始种子：type/subtype/resources/relations）、`apps/game/data/definitions/sect-organization.json` 与 `definitions/sect-seed-profiles.json`（门派组织结构/职位/种子档案）、`apps/game/data/behavior-trees/faction-default.json` 与 `actions/faction-actions.json`（势力 AI 行为树/势力行动集）、`apps/game/data/relationships/groups` 与 `relationships/projections`（势力/群体关系投影）；wiki 侧落 `docs/worldbuilding/wiki/rules/leader-succession.md`、`faction-crisis-defection.md`、`sect-operation.md`、`wealth-exposed.md`（势力运营/继承/危机动态规则）。
- **建议涵盖方面（草案）：**
  - 五表各补至少 1 行/张填好样例（一个正道宗门全字段填满 + 一条势力关系边 + 一行数据候选）。
  - "可映射字段"列补全到 `apps/game/data/entities/factions.json`、`definitions/sect-organization.json` 等真实路径。
  - 卡片字段补取值口径（如"核心资源=按作品列灵脉/坊市/库存，无据写'原文未说明'"；"规模/战力须标'我的判断'"）。
  - 显式串联势力静态结构与 wiki/rules 的 `leader-succession`/`faction-crisis-defection`/`sect-operation` 动态规则去向（呼应缺口小结第 1 条"势力运营/继承/危机"）。
  - 行为倾向映射到 `behavior-trees/faction-default.json` 与 `actions/faction-actions.json` 的势力 AI 策略示范。
- **优先级：** 高 —— 直接对接 factions/sect-organization/faction 行为树多个核心势力资产，代码相关度高；势力是 NPC 长程演化的归属与冲突来源（月压缩中势力兴衰、继承、叛逃驱动远程剧情），里程碑相关度高，且缺全部填好样例 + 与动态规则脱节，乱度中上，是缺口小结点名的优先修对象。

#### 宗门任务体系模板
- **当前状态：** 五块骨架齐全（头部元数据含 L3 / `## 适用范围` 含服务对象 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 详细程度` / `## 推荐结构` 含任务类型总览/任务卡片/制度闭环三块），定位为宗门任务→quest/job 的主承载模板。
- **主要问题：** （示例）核心缺陷——`任务类型总览`/`### 任务类型名` 卡片/`制度闭环` 三表**全为空表头，无任一填好样例**（未给"采药/猎妖/秘境试炼"任一全字段填法），按 rubric 示例维不合格。（与项目对接）卡片"可映射文件"行写 `quests/`、`actions/`、`jobs/`、`toils/` 等**裸目录名/通配，未带 `apps/game/data/` 前缀且未点名具体文件**（实际落点为 `quests/quest-templates.json` 与 `jobs/npc-quest-jobs.json`、`toils/npc-quest-toils.json`），对接定位偏弱。（字段定义）卡片字段（任务目标/接取条件/行动链等）只列字段名、缺逐字段取值口径（如奖励是否只写相对梯度的字段约束未落到卡片字段说明），边缘不合格。（章节构成 / 数据来源）`## 详细程度`（每类任务须写发布者/目标/条件/奖励/风险/证据）+ 三列表释义较充分，三行来源齐全，合格。
- **对接的项目代码：** `apps/game/data/quests/quest-templates.json`（任务模板与难度阶梯）、`apps/game/data/jobs/npc-quest-jobs.json`（quest 类长流程作业）、`apps/game/data/toils/npc-quest-toils.json`（任务原子工序）、`apps/game/data/actions/npc-job-actions.json`（JobAction）、`apps/game/data/economy/transaction-scenarios.json`（奖励灵石/贡献的经济结算）；制度闭环侧关联 `definitions/sect-organization.json`（晋升/职位）与 wiki `docs/worldbuilding/wiki/rules/sect-operation.md`（宗门运营反哺）。
- **建议涵盖方面（草案）：**
  - 三表各补至少 1 行/张填好样例（如"采药任务"全字段填满 + 一条制度闭环规则）。
  - "可映射文件"补全 `apps/game/data/quests/quest-templates.json` 等真实文件路径，区分 Quest 模板/Job 编排/Toil 工序三层落点。
  - 卡片字段补取值口径（奖励只写相对梯度、接取条件给境界/贡献阈值、"无据写原文未说明"）。
  - 制度闭环表显式串联 `definitions/sect-organization.json` 晋升制度与 wiki `sect-operation` 运营规则。
  - 补一句月压缩相关：宗门任务作为远程 NPC 月度作业链的来源之一，喂入压缩态经历摘要。
- **优先级：** 中 —— 直接对接 quest-templates/quest-jobs/quest-toils，代码相关度高，是缺口表中 quests 维"已覆盖"的唯一模板；但任务体系偏宗门内部循环，与月压缩"NPC 长程演化/事件因果"里程碑相关度中等，乱点集中在缺填好样例与路径点名，结构本身成熟。

#### 散修生存方式模板
- **当前状态：** 五块骨架齐全（头部元数据含 L3 / `## 适用范围` 含服务对象 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 详细程度` / `## 推荐结构` 含生存方式总览/行为卡片/游戏化抽象三块），定位为无宗门/弱组织修士生存→wanderer NPC 行为的主承载模板。
- **主要问题：** （示例）核心缺陷——`生存方式总览`/`### 行为名` 卡片/`游戏化抽象` 三表**全为空表头，无任一填好样例**（未给"采药谋生/赏金猎妖/怀璧其罪"任一全字段填法），按 rubric 示例维不合格。（与项目对接）`## 详细程度` 与 `尽量写全清单` 已点名 SimpleAction/JobAction/Quest/Opportunity/怀璧其罪等落点，但卡片"可映射文件"行写 `actions/`、`jobs/`、`quests/`、`world/opportunities.json` **未统一带 `apps/game/data/` 前缀且未点名 wanderer 行为具体落在哪个 jobs/behavior-trees 文件**，对接偏弱。（字段定义）卡片字段（触发条件/行动链/产出/消耗/风险等）只列字段名、缺逐字段取值口径，边缘不合格。（章节构成 / 数据来源）`## 详细程度`（每条写收入/成本/风险/适用境界/证据、至少 3 类失败后果）较充分，三行来源齐全，合格。
- **对接的项目代码：** `apps/game/data/world/opportunities.json`（机会点/怀璧其罪机缘）、`apps/game/data/quests/quest-templates.json`（悬赏任务）、`apps/game/data/economy/transaction-scenarios.json`（坊市交易/倒卖套利）、`apps/game/data/jobs/npc-economy-jobs.json` 与 `jobs/npc-dynamic-event-jobs.json`（散修谋生/机会响应作业）、`apps/game/data/behavior-trees/npc-default.json`（wanderer NPC 行为编排）、`apps/game/data/goals/dynamic-goals.json`（生存/上升动态目标）；风险侧关联 wiki `docs/worldbuilding/wiki/rules/wealth-exposed.md`（怀璧其罪/暴富露财）与 `travel-and-risk.md`（游历风险）。
- **建议涵盖方面（草案）：**
  - 三表各补至少 1 行/张填好样例（如"赏金猎妖"全字段填满 + 一条散修原型游戏化抽象规则，含示范何时写"原文未说明"）。
  - "可映射文件"统一补 `apps/game/data/...` 全路径，点名 wanderer 行为落在 `behavior-trees/npc-default.json` 与 `jobs/` 哪类作业。
  - 卡片字段补取值口径（产出/消耗给相对量、风险绑定具体来源、失败后续对应动态事件）。
  - 风险/怀璧其罪显式对接 `world/opportunities.json` 与 wiki `wealth-exposed.md`/`travel-and-risk.md`。
  - 补月压缩相关：散修长程谋生/上升/失败路径作为远程 NPC 离线演化的低组织度轨迹来源（喂 Cold 月压缩 episode）。
- **优先级：** 中 —— 横跨 opportunities/quests/economy/jobs/goals 多目录，代码相关度高，且散修是远程 NPC 离线演化的典型低组织度群体，与月压缩里程碑有相关度；但乱点集中在缺填好样例与路径点名，结构与详细程度本身较成熟，故定中。
### 3.4 妖兽 / 邪修 / 夺舍类

#### 妖兽分析模板
- **当前状态：** 五块骨架齐全（头部元数据含 L3 / `## 适用范围`+与「妖兽与修士关系」模板划界 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 详细程度` / `## 推荐结构` 含六张表），是妖兽条目→怪物库数值的主承载模板，结构最厚。
- **主要问题：** （示例）核心缺陷——推荐结构里 `品阶体系`/`妖兽条目清单`/`生态行为`/`资源产出`/`驯养关系`/`### 妖兽名` 卡片六表/卡**全为空表头，无任一填好样例**（未给"低阶/化形/真灵"任一妖兽条目或一张填满的资产卡），按 rubric 示例维不合格。（与项目对接）`## 必须写什么` 与 `项目映射` 行写 `definitions/monsters.json`、`items/material.json`、`behavior-trees/monster-*.json`、`balance/monster-spawn.json` 等，**未带 `apps/game/data/` 全路径，且漏掉项目已存在的 `monster-attribute-templates.json`/`monster-combat.json`/`monster-resource-rules.json` 三张妖兽专表**，对接定位偏弱、不完整。（字段定义）资产卡字段（特点与习性、战斗能力、掉落/资源等）只列字段名、缺逐字段取值口径（如掉落是否给相对量、战力是否标"我的判断"），边缘不合格。（章节构成 / 数据来源）合格。
- **对接的项目代码：** `apps/game/data/definitions/monsters.json`（妖兽定义/family/grade/terrain）、`apps/game/data/definitions/monster-attribute-templates.json` 与 `definitions/monster-combat.json`（妖兽属性模板/战斗基表）、`apps/game/data/definitions/monster-resource-rules.json`（掉落资源规则）、`apps/game/data/balance/monster-spawn.json`（刷新/兽潮平衡）、`apps/game/data/behavior-trees/monster-tier1.json`/`monster-tier2.json`/`monster-tier3.json`（分层妖兽行为树）、`apps/game/data/items/material.json`（妖丹/兽骨/精血等掉落材料实例）。
- **建议涵盖方面（草案）：**
  - 六表/卡各补至少 1 行/张填好样例（一条低阶可猎妖兽 + 一条化形/真灵 + 一张填满资产卡，含示范何时写"原文未说明"）。
  - `项目映射` 列统一补 `apps/game/data/...` 全路径，并补点名 `monster-attribute-templates.json`/`monster-combat.json`/`monster-resource-rules.json` 三张妖兽专表。
  - 资产卡字段补取值口径（掉落给相对量、战力/等级映射标"我的判断"、属性引用 `ge_` 而非写死）。
  - 行为/兽潮显式对接 `behavior-trees/monster-tier*.json` 分层与 `balance/monster-spawn.json` 刷新规则。
  - 补一句月压缩相关：妖兽兽潮/领地冲突作为远程世界态事件源，喂入 Cold 月压缩事件因果链。
- **优先级：** 高 —— 直接对接 monsters/monster-* 多张专表 + monster-spawn + 分层行为树 + material，代码相关度最高（项目妖兽子系统最完整）；妖兽是世界态灾变与资源闭环的核心，与月压缩事件因果有相关度；缺全部填好样例 + 漏点名三张专表，乱度中上。

#### 妖兽与修士关系分析模板
- **当前状态：** 头部元数据含 L3 + `## 适用范围` + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + `## 详细程度` + 推荐结构（含妖兽类型总览/关系类型/妖兽卡片/游戏化抽象四块），定位为人妖长期社会关系/猎妖资源闭环模板，与「妖兽分析模板」侧重条目收集形成分工。
- **主要问题：** （示例）核心缺陷——`妖兽类型总览`/`关系类型`/`### 妖兽名` 卡片/`游戏化抽象` 四表/卡**全为空表头，无任一填好样例**（未给"猎杀/守药冲突/契约灵宠"任一关系填法），示例维不合格。（与项目对接）三列表与卡片"可映射字段"写 `family`/`grade`/`behavior`/`drops`/`home`/`swarmBehavior`/"关系边"等**裸字段名，未点名 `apps/game/data/definitions/monsters.json`、`relationships/` 子目录等真实路径**，对接定位偏弱。（章节构成 / 字段定义 / 数据来源）`## 详细程度`（每类写等阶/行为/资源/互动/证据、候选须写 family/grade/terrain/drops）较充分，三行来源齐全，合格。（一致性）与「妖兽分析模板」的"妖兽条目"存在重叠风险，适用范围虽已划界但卡片字段口径需保持互补不重复。
- **对接的项目代码：** `apps/game/data/definitions/monsters.json`（family/grade/terrain/drops/swarmBehavior 字段）、`apps/game/data/behavior-trees/monster-tier1.json`/`monster-tier2.json`/`monster-tier3.json`（巡逻/守巢/护群行为编排）、`apps/game/data/relationships/dictionaries` 与 `relationships/impact-rules`（人妖关系类型/仇怨/契约关系边）、`apps/game/data/quests/quest-templates.json`（猎妖/赏金任务）、`apps/game/data/world/opportunities.json` 与 `world/dynamic-events.json`（妖兽尸骸机会点/兽潮动态事件）、`apps/game/data/balance/monster-spawn.json`（兽潮/刷新）。
- **建议涵盖方面（草案）：**
  - 四表/卡各补至少 1 行/张填好样例（一条"猎杀→掉落"闭环 + 一条"契约灵宠"关系边 + 一张妖兽关系卡）。
  - "可映射字段"补全到 `apps/game/data/definitions/monsters.json` 与 `relationships/` 具体子目录真实路径。
  - 关系方向/对称性落到 `relationships/dictionaries` 词表字段示范（猎杀/契约非对称）。
  - 兽潮/尸骸机会点显式对接 `world/dynamic-events.json`/`world/opportunities.json` 与 `balance/monster-spawn.json`。
  - 与「妖兽分析模板」明确分工边界：本模板只写关系/事件闭环，妖兽本体条目去妖兽分析模板，避免字段重复。
- **优先级：** 中 —— 横跨 monsters/relationships/quests/world/monster-spawn 多目录，代码相关度高，人妖冲突/兽潮是月压缩远程事件源；但条目本体由「妖兽分析模板」承载、本模板偏关系闭环，且与前者存在重叠需先理清分工，故定中。

#### 邪修分析模板
- **当前状态：** 头部元数据含 L3 + `## 适用范围` + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + `## 详细程度` + 推荐结构（含邪修类型总览/行为卡片/游戏化抽象三块），并含敏感桥段"只写机制摘要"的约束，定位为邪派势力/高风险高收益行为主模板。
- **主要问题：** （示例）核心缺陷——`邪修类型总览`/`### 行为名` 卡片/`游戏化抽象` 三表/卡**全为空表头，无任一填好样例**（未给"血祭/炉鼎采补/夺舍"任一全字段填法），示例维不合格。（与项目对接）三列表与卡片"可映射系统"写 `evil faction traits`/`covet`/`quest`/`dynamic event`/`obsession`/`risk`/`relationship grudge` 等**裸概念名/英文短语，未点名 `apps/game/data/entities/factions.json`、`balance/covet.json`、`relationships/signal-rules/wanted-chain.json` 等真实路径**，对接定位偏弱；"通缉/围剿"社会反应未点名项目已落地的 `relationships/signal-rules/wanted-chain.json` 通缉链。（字段定义）行为卡字段（收益/代价/被发现后果等）只列字段名、缺逐字段取值口径（如收益是否给相对梯度、代价是否绑定 risk 键），边缘不合格。（章节构成 / 数据来源）`## 详细程度`（每类写触发/收益/代价/被发现后果）+ 敏感约束齐全，三行来源齐全，合格。
- **对接的项目代码：** `apps/game/data/entities/factions.json`（邪派/魔宗势力种子 type/subtype/traits）、`apps/game/data/balance/covet.json`（贪念/夺宝掠夺驱动）、`apps/game/data/balance/obsession.json`（邪修执念倾向）、`apps/game/data/balance/risk.json`（高风险行为风险键）、`apps/game/data/relationships/signal-rules/wanted-chain.json` 与 `relationships/impact-rules`（通缉链/仇怨关系边）、`apps/game/data/world/dynamic-events.json`（围剿/血祭动态事件）、`apps/game/data/quests/quest-templates.json`（悬赏/执法堂追杀任务）；血食/禁丹等掠夺物资关联 `items/pill.json`、`items/material.json`。
- **建议涵盖方面（草案）：**
  - 三表/卡各补至少 1 行/张填好样例（如"炉鼎采补"全字段填满 + 一条邪修原型游戏化抽象规则，含敏感桥段只写机制摘要的示范）。
  - "可映射系统"补全到 `apps/game/data/entities/factions.json`、`balance/covet.json`、`relationships/signal-rules/wanted-chain.json` 等真实路径，区分势力 trait/执念/风险/通缉四类落点。
  - 行为卡字段补取值口径（收益只写相对梯度、代价绑定 `balance/risk.json` 风险键、被发现后果对应 `world/dynamic-events.json` 围剿事件）。
  - 通缉/围剿显式对接 `relationships/signal-rules/wanted-chain.json` 通缉链与执法堂任务（呼应缺口小结"通缉/声誉链对接偏弱"）。
  - 补月压缩相关：邪修掠夺→暴露→被通缉围剿作为远程 NPC 离线高风险轨迹与事件因果链来源。
- **优先级：** 高 —— 直接对接 factions/covet/obsession/risk/wanted-chain/dynamic-events 多个核心资产，代码相关度高；邪修是正邪冲突与月压缩远程剧情的高张力来源，里程碑相关度高；缺全部填好样例 + 漏点名通缉链，乱度中上。

#### 夺舍设定分析模板
- **当前状态：** 头部元数据含 L3 + `## 适用范围` + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + `## 详细程度` + 推荐结构（含机制总览/事件卡片/游戏化抽象三块），定位为夺舍/附身/转世等身体接管机制模板，与邪修/死亡/师徒风险交叉。
- **主要问题：** （示例）核心缺陷——`机制总览`/`### 事件名` 卡片/`游戏化抽象` 三表/卡**全为空表头，无任一填好样例**（未给"残魂夺舍/器灵附体/转世重生"任一全字段填法），示例维不合格。（与项目对接）三列表与卡片"可映射系统"写 `Obsession`/`Relationship`/`GAS`/`DynamicEvent`/"GAS Tag"等**裸概念名，未点名 `apps/game/data/tags/tags.json`、`abilities/`、`effects/`、`goals/dynamic-goals.json` 真实路径**，且"GAS 状态"未点名项目已有的 `State.Dying` 等 Tag 登记位置，对接定位偏弱。（字段定义）事件卡字段（前置条件/行动链/失败后果/关系后果等）只列字段名、缺逐字段取值口径（如境界差/资质阈值如何取值、失败是否落 GE），边缘不合格。（章节构成 / 数据来源）`## 详细程度`（每种写主体/目标/媒介/条件/成功/失败）齐全，三行来源齐全，合格。
- **对接的项目代码：** `apps/game/data/tags/tags.json`（`State.Dying` 及夺舍/附身相关 State/Trait Tag 登记位）、`apps/game/data/effects`（魂灭/反噬/记忆继承 GE 效果）、`apps/game/data/abilities`（夺舍/附身行动 GA）、`apps/game/data/goals/dynamic-goals.json`（夺舍执念→动态目标）、`apps/game/data/balance/obsession.json`（夺舍/复活执念）、`apps/game/data/relationships/impact-rules` 与 `relationships/signal-rules/wanted-chain.json`（师徒背叛/仇怨/被追杀关系边）、`apps/game/data/world/dynamic-events.json`（残魂夺舍/复活动态事件）、`apps/game/data/jobs` 与 `apps/game/data/toils`（潜伏/削弱/接管行动链编排）。
- **建议涵盖方面（草案）：**
  - 三表/卡各补至少 1 行/张填好样例（如"残魂夺舍弱境界目标"全字段填满 + 一条游戏化抽象规则，含示范何时写"原文未说明"）。
  - "可映射系统"补全到 `apps/game/data/tags/tags.json`、`abilities/`、`effects/`、`goals/dynamic-goals.json` 真实路径，并点名新增 Tag 须先在 `tags/tags.json` 登记（参 `State.Dying`）。
  - 事件卡字段补取值口径（境界差/资质阈值给区间标"我的判断"、失败后果落 GE/GA 而非纯叙事）。
  - 行动链显式对接 `jobs`/`toils` 编排（潜伏→削弱→接管→融合/失败为多步 Toil）。
  - 补月压缩相关：高阶 NPC 夺舍/转世导致的身份变更作为远程 NPC 长程离线演化的稀有突变事件（喂 Cold 月压缩 episode 与事件因果链）。
- **优先级：** 高 —— 横跨 tags/effects/abilities/goals/obsession/relationships/world/jobs/toils 多目录，代码相关度高，且夺舍是 NPC 身份变更/死亡突变的稀有但高影响机制，与月压缩"NPC 长程离线演化"里程碑相关度高；缺全部填好样例 + 对接全为裸概念名未落地，乱度中上。
### 3.5 经济 / 场所 / 物资类

#### 拍卖坊市与交易模板
- **当前状态：** 头部元数据齐全（L3）+ `## 适用范围`+边界（明市/黑市/宗门兑换分开） + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（交易场景总览 + 场景卡片），定位为交易场所/制度→经济底座的主承载模板。
- **主要问题：** （示例）核心缺陷——`交易场景总览` 表与 `### 场景名` 卡片**全为空表头，无任一填好样例**（未给"坊市拍卖会/黑市/宗门兑换殿"任一全字段填法），按 rubric 示例维不合格。（与项目对接）`## 必须写什么` 与 `尽量写全清单` 的项目映射写 `org_market`、`org_auction_house`、`auction dynamic event`、`item value`、`covet expose` 等**裸 id/英文短语，未带 `apps/game/data/` 路径**；且 `org_market`/`org_auction_house` 当前并不是独立文件——拍卖已被抽象进 `economy/transaction-scenarios.json` 的场景倍率/托管/抽象拍卖参数，坊市/兑换对应其中 `formal_market`/`faction_exchange` 场景，对接点名既缺路径又指向不存在的资产形态。（字段定义）卡片字段（准入条件/定价竞价方式/信息传播等）只列字段名、缺逐字段取值口径（如保证金/手续费是否给相对比例、定价是否标"我的判断"），边缘不合格。（章节构成 / 数据来源）合格。
- **对接的项目代码：** `apps/game/data/economy/transaction-scenarios.json`（统一交易底座：`formal_market`/`faction_exchange`/`private_trade` 场景倍率、escrow 托管、debt 债务、抽象拍卖参数）、`apps/game/data/balance/economy.json`（价格/价值平衡单一真相源）、`apps/game/data/items/currency.json`（灵石货币）与 `items/pill.json`/`material.json`/`artifact.json`（交易品类 value）、`apps/game/data/world/dynamic-events.json`（拍卖/截杀动态事件）、`apps/game/data/world/news.json` 与 `world/opportunities.json`（拍卖预告/价差情报、怀璧机会点）、`apps/game/data/balance/covet.json` 与 `balance/risk.json`（财富暴露/截杀风险）。
- **建议涵盖方面（草案）：**
  - 总览表与场景卡片各补至少 1 行/张填好样例（一个坊市拍卖会全字段 + 一个黑市场景，含示范何时写"原文未说明"）。
  - 项目映射改为点名 `economy/transaction-scenarios.json` 的真实场景键（`formal_market`/`faction_exchange`/`private_trade`/抽象拍卖参数），删除 `org_market`/`org_auction_house` 这类不存在的资产名。
  - 卡片字段补取值口径（保证金/手续费给相对比例或区间标"我的判断"、定价方式绑定 `priceMultiplier`/escrow 语义）。
  - 风险/情报显式对接 `balance/covet.json`、`world/news.json`、`world/opportunities.json` 真实路径。
  - 补一句月压缩相关：大型拍卖/黑市截杀作为远程世界态事件源，喂 Cold 月压缩事件因果链。
- **优先级：** 高 —— 直接对接 economy 统一交易底座 + balance/economy + items value + covet/risk 多个核心经济资产，代码相关度高，是缺口表 economy 维"已覆盖"的两模板之一；且对接点名指向不存在的 `org_*` 资产，乱度中上，需先校正映射。

#### 建筑设施与场所功能模板
- **当前状态：** 头部元数据齐全（L3）+ `## 适用范围`（强调"场所提供功能/消耗资源/触发行为"而非纯地图点） + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（设施总览 + 设施卡片），定位为功能性场所→地图/建筑/Action 落点的主承载模板。
- **主要问题：** （示例）核心缺陷——`设施总览` 表与 `### 设施名` 卡片**全为空表头，无任一填好样例**（未给"藏经阁/任务殿/秘境入口"任一全字段填法），按 rubric 示例维不合格。（与项目对接）项目映射写 `地图地点`、`dynamic event pos`、`quest location`、`building function`、`Action targetResolver` 等**裸概念名/英文短语，未带 `apps/game/data/` 路径**；且项目当前并无独立"建筑/设施定义文件"——设施以 `world/map.json` 每个 tile 的 `buildings[]` 数组承载，模板未指明这一真实落点，导致"设施去哪"指引悬空（rubric 第 4 维与项目对接维不合格）。（字段定义）卡片字段（使用条件/消耗/产出/维护风险等）只列字段名、缺逐字段取值口径（如使用条件给境界/贡献阈值口径、消耗是否给相对量），边缘不合格。（章节构成 / 数据来源）合格。
- **对接的项目代码：** `apps/game/data/world/map.json`（tile `buildings[]` 数组承载设施实例；当前无独立 building 定义文件，设施挂在地图格上）、`apps/game/data/definitions/sect-organization.json` 与 `definitions/sect-seed-profiles.json`（宗门设施/职位/兑换殿结构）、`apps/game/data/quests/quest-templates.json`（任务地点/任务殿发布）、`apps/game/data/actions/npc-actions.json`/`npc-job-actions.json`（修炼/兑换/疗伤等 Action 的 targetResolver 落点）、`apps/game/data/world/dynamic-events.json` 与 `world/opportunities.json`（古修洞府/秘境入口/试炼塔定位的事件与机会点）、`apps/game/data/economy/transaction-scenarios.json`（坊市/兑换设施的交易结算）。
- **建议涵盖方面（草案）：**
  - 总览表与设施卡片各补至少 1 行/张填好样例（一个宗门藏经阁全字段 + 一个野外秘境入口，含示范何时写"原文未说明"）。
  - 明确设施当前落点是 `world/map.json` 的 tile `buildings[]`（暂无独立建筑定义文件），并建议是否需要新增 `definitions/buildings.json` 之类的建筑定义维度（讨论起点）。
  - 项目映射补 `apps/game/data/...` 全路径，区分"地图建筑实例 / 任务地点 / Action targetResolver / 动态事件定位"四类落点。
  - 卡片字段补取值口径（使用条件给境界/贡献/令牌阈值、消耗/产出给相对量、维护成本绑定势力资源）。
  - 补月压缩相关：远程 NPC 月度作业的发生地点（任务殿/坊市/秘境）作为地点轨迹的锚点，喂月度 episode 地点轨迹。
- **优先级：** 中 —— 对接横跨 map/sect-organization/quests/actions/world 多目录，代码相关度较高，但项目侧设施仅以 map tile `buildings[]` 隐式承载、无独立定义文件，落点本身偏薄；与月压缩"地点轨迹"有间接相关；乱点集中在缺样例 + 映射悬空，故定中。

#### 物资产出与消耗模板
- **当前状态：** 五块骨架最齐全（头部元数据 L3 / `## 适用范围`+服务对象 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 详细程度`（每资源写来源/消耗/持有者/流通/保值/证据，价格用相对梯度标"我的判断"） / 推荐结构含资源循环总览/资源卡片/产出渠道/消耗去向/经济抽象五块），结构本组最成熟、含完整 `## 详细程度` 段。
- **主要问题：** （示例）核心缺陷——`资源循环总览`/`### 资源名` 卡片/`产出渠道`/`消耗去向`/`经济抽象` 五表/卡**全为空表头，无任一填好样例**（未给"灵石/妖丹/贡献"任一资源的产出-消耗闭环填法），按 rubric 示例维不合格。（与项目对接）项目映射写 `economy.json`、`reward.json`、`covet.json`、`items/`、`quests/`、`opportunities.json`、`势力资源` 等**裸文件名/通配，未带 `apps/game/data/` 前缀**；其中 `economy.json`/`reward.json`/`covet.json` 实际在 `balance/` 下（`balance/economy.json` 等），裸名易误导填顶层路径；"势力资源"对应的 `definitions/macro-resources.json`（food/disciples 等 faction_state 资源）未点名，对接定位偏弱、不完整。（字段定义 / 章节构成 / 数据来源）`## 详细程度` 释义充分、五块骨架与三行来源齐全，均合格。
- **对接的项目代码：** `apps/game/data/balance/economy.json`（经济平衡/价格单一真相源）、`apps/game/data/balance/reward.json`（任务/事件奖励结算）、`apps/game/data/balance/covet.json`（财富暴露/掠夺驱动）、`apps/game/data/economy/transaction-scenarios.json`（坊市/兑换/拍卖流通结算）、`apps/game/data/items/currency.json`/`pill.json`/`material.json`/`artifact.json`（灵石/丹药/材料/法宝资源 value）、`apps/game/data/definitions/macro-resources.json`（粮食/弟子等势力库存 faction_state 资源）、`apps/game/data/quests/quest-templates.json`（任务奖励产出）、`apps/game/data/world/opportunities.json`（猎妖掉落/秘境产出机会点）、`apps/game/data/definitions/monster-resource-rules.json`（妖兽掉落产出规则）。
- **建议涵盖方面（草案）：**
  - 五表/卡各补至少 1 行/张填好样例（一条"猎妖→妖丹→炼丹消耗"闭环 + 一条"任务→贡献→晋升兑换"闭环 + 一张灵石资源卡）。
  - 项目映射补全 `apps/game/data/...` 全路径，并改正 `economy.json`/`reward.json`/`covet.json` 实为 `balance/` 下文件，补点名 `definitions/macro-resources.json` 势力资源。
  - 强调数值单一真相源在 `balance/economy.json`/`balance/reward.json`，模板只承载产出-消耗闭环口径（呼应 ADR-042）。
  - 闭环表显式串联产出（quests/opportunities/monster-resource-rules）→流通（transaction-scenarios）→消耗（cultivation/突破/法宝）。
  - 强化月压缩对接：资源产出-消耗闭环是远程 NPC 离线长程演化"背包/资源累积"维度的直接喂料（Cold 月压缩 NPC 长程演化）。
- **优先级：** 高 —— 直接对接 balance/economy + reward + covet + economy 底座 + 多类 items value + macro-resources 势力库存，代码相关度最高，是长程模拟经济闭环的核心口径承载；资源累积是月压缩"NPC 长程离线演化"背包维度的直接来源，里程碑相关度高；缺全部填好样例 + 映射路径不准，乱度中上。
### 3.6 世界 / 时间 / 事件 / 叙事类

#### 世界观设定模板
- **当前状态：** 五块骨架齐全（头部元数据 L2/涉项目升 L3 / `## 适用范围` / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 详细程度` / `## 推荐结构` 含世界层级/底层规则/社会分层/游戏化抽象/可转化条目五张表），是单作品宏观世界观的总纲模板，结构成熟。
- **主要问题：** （示例）核心缺陷——`世界层级`/`底层规则`/`社会分层`/`游戏化抽象`/`可转化条目` 五张表**全为空表头，无任一填好样例**（未给"凡界→灵界层级"或"寿元约束"任一填法），按 rubric 示例维不合格。（与项目对接）`## 必须写什么` 的"项目用途"列写"世界修正器、境界、寿元、NPC 初始身份、动态事件"等**泛称，未点名 `apps/game/data/` 具体目录**（如世界修正器实为 `world/modifiers.json`、境界为 `definitions/ranks.json`），定位偏弱。（字段定义）三列表 + `## 详细程度`（每规则≥1 证据、可转化规则须写作用对象/触发/结果/限制/映射）释义较充分，合格。（章节构成 / 数据来源）三行齐全，合格。
- **对接的项目代码：** `apps/game/data/world/modifiers.json`（世界修正器/灵气复苏等底层状态）、`apps/game/data/definitions/ranks.json`（境界/寿元层级）、`apps/game/data/definitions/terrains.json`（地域层级/地形）、`apps/game/data/entities/factions.json` 与 `entities/npcs.json`（社会分层 → 势力/NPC 初始身份）、`apps/game/data/world/dynamic-events.json`（天劫/灾变/飞升潮风险来源）；wiki 侧落 `docs/worldbuilding/wiki/rules/` 境界/死亡/飞升等规则条目。
- **建议涵盖方面（草案）：**
  - 五张表各补至少 1 行/张填好样例（一条世界层级 + 一条底层规则 + 一行可转化条目，含示范何时写"原文未说明"）。
  - "项目用途"列改为点名 `apps/game/data/world/modifiers.json`、`definitions/ranks.json` 等真实路径。
  - 明确本模板为宏观总纲，细化口径下沉到境界/势力/经济等专题模板，避免与其重复。
  - 社会分层显式串联 `entities/factions.json`/`npcs.json` 的初始身份落法。
  - 补 ADR-058 关联：底层世界规则（灵气强度/界面压制/寿元）作为月压缩 Cold 域"结算世界月阶段"的全局约束输入，喂月度世界态演化（ADR-058 月压缩 pipeline 第 3 步）。
- **优先级：** 中 —— 是世界观顶层总纲，对接横跨 modifiers/ranks/terrains/entities，代码相关度较高；与月压缩为全局约束的间接相关（非逐事件因果核心），结构成熟、乱点集中在缺样例与泛称映射，故定中。

#### 世界状态与灾变模板
- **当前状态：** 头部元数据 L3 + `## 适用范围`（点名服务 `world/modifiers.json`/稳定度/势力 AI） + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（状态总览 + 状态/灾变卡片 + `## 游戏化抽象`），定位为世界修正器/时代变局/大环境压力的主承载模板。
- **主要问题：** （章节构成）`## 游戏化抽象（我的判断）` 标题下**内容被截断为空，无任何表格/正文**，骨架残缺；且相比同组 L3 模板**缺独立 `## 详细程度` 段**，量化标准缺位。（示例）`状态总览` 表与 `### 状态名` 卡片**全为空表头，无任一填好样例**（未给"魔气上涨/兽潮/王朝衰败"任一全字段填法），示例维不合格。（与项目对接）`项目映射` 写 `Modifier.type`/`intensity`/`duration`/`effects`/稳定度/dynamic event 等**裸字段名，未带 `apps/game/data/world/modifiers.json` 全路径**，定位偏弱。（数据来源）三行齐全，合格。
- **对接的项目代码：** `apps/game/data/world/modifiers.json`（世界修正器实例：type/intensity/duration/effects，已有 `demon_qi_rising` 等条目）、`apps/game/data/world/dynamic-events.json`（兽潮/魔劫/宗门大战等灾变事件）、`apps/game/data/balance/monster-spawn.json`（兽潮/刷新平衡）、`apps/game/data/actions/faction-actions.json` 与 `behavior-trees/faction-default.json`（势力对灾变的防御/扩张应对）、`apps/game/data/world/news.json`（灾变消息传播）。
- **建议涵盖方面（草案）：**
  - 补全 `## 游戏化抽象（我的判断）` 表并新增独立 `## 详细程度` 段（每状态≥1 证据、强度/时长标"我的判断"），修复双重骨架残缺。
  - `状态总览` 与卡片各补 1 行/张填好样例（如"魔气上涨"映射 `modifiers.json` 的 `demon_qi_rising`，含范围/消退）。
  - `项目映射` 补全 `apps/game/data/world/modifiers.json` 真实字段路径，区分长期 modifier 与一次性 dynamic-event。
  - 灾变消息显式对接 `world/news.json` 传播参数。
  - 补 ADR-058 关联：世界状态/灾变作为月压缩 Cold 域"结算世界月阶段"的区域级 modifier 输入，跨多月持续演化并影响远程 NPC agenda（ADR-058 pipeline 第 3 步）。
- **优先级：** 高 —— 直接对接 modifiers/dynamic-events/monster-spawn 核心世界态资产，代码相关度高；区域级世界状态是月压缩远程演化的环境压力源，里程碑相关度高；自身存在游戏化抽象段空 + 缺详细程度段的骨架残缺，乱度中上。

#### 动态事件与机会点模板
- **当前状态：** 头部元数据 L3 + `## 适用范围`（点名服务 dynamic-events/dynamic-goals/opportunities/Job-Toil） + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（事件总览 + 事件卡片，卡片已含"项目映射"列把生命周期映射到 WorldNews/Job-Toil/DynamicEvent/Action-Opportunity/Reward-Risk/Relationship-Memory），是事件窗口→world 事件资产的主承载模板，卡片映射设计最到位。
- **主要问题：** （示例）核心缺陷——`事件总览` 表与 `### 事件名` 卡片**全为空表头，无任一填好样例**（未给"秘境开启/拍卖会/强者陨落"任一全字段填法），示例维不合格。（章节构成）相比同组 L3 模板**缺独立 `## 详细程度` 段**与 `## 游戏化抽象` 段，量化标准与抽象规则缺位。（与项目对接）卡片"项目映射"列已点名机制类型（WorldNews/DynamicEvent 等），但用的是**类型名而非 `apps/game/data/world/dynamic-events.json`/`world/opportunities.json`/`goals/dynamic-goals.json` 真实文件路径**，定位偏弱。（字段定义 / 数据来源）三列表释义清晰、三行来源齐全，合格。
- **对接的项目代码：** `apps/game/data/world/dynamic-events.json`（事件窗口：eventType/announceDay/startDay/endDay/scope）、`apps/game/data/world/opportunities.json`（机会点：尸骸/宝物/洞府/怀璧）、`apps/game/data/goals/dynamic-goals.json`（事件感知 → 临时 Goal）、`apps/game/data/world/news.json`（事件预告/消息传播）、`apps/game/data/jobs/npc-dynamic-event-jobs.json` 与 `toils/npc-dynamic-event-toils.json`（准备/参与作业链）、`apps/game/data/balance/reward.json` 与 `balance/risk.json`（收益/风险结算）。
- **建议涵盖方面（草案）：**
  - 总览表与事件卡片各补 1 行/张填好样例（一个秘境开启全生命周期填满，含示范何时写"原文未说明"）。
  - 补独立 `## 详细程度` 段与 `## 游戏化抽象` 段，与同组 L3 模板对齐。
  - "项目映射"列在类型名旁补 `apps/game/data/world/dynamic-events.json` 等真实文件路径，并点名事件感知落 `goals/dynamic-goals.json`、准备落 `jobs/npc-dynamic-event-jobs.json`。
  - 收益/风险绑定 `balance/reward.json`/`balance/risk.json` 风险键示范。
  - 补 ADR-058 关联：动态事件是月压缩"事件因果链"的节点单元，需补"多月/跨事件因果"组织视角（事件 A 余波触发事件 B），直接对接缺口小结第 2 条「事件因果链」与 ADR-058 pipeline 第 6 步事件结算。
- **优先级：** 高 —— 直接对接 dynamic-events/opportunities/dynamic-goals/event-jobs 多个核心 world 事件资产，代码相关度高；事件是月压缩"事件因果图"里程碑的基本节点，里程碑相关度高（缺口小结点名）；缺样例 + 缺详细程度/抽象段，乱度中上。

#### 冲突事件分析模板
- **当前状态：** 五块骨架齐全（头部元数据 L3 / `## 适用范围`+不只记主线大战的边界 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 详细程度`（含"长期后果"阶段、要求每条尽量写完整事件卡） / 推荐结构含冲突总览/冲突事件卡片/`## 游戏化抽象`），是冲突事件→战斗风险/仇怨/通缉的主承载模板，详细程度段最强调"完整事件链"。
- **主要问题：** （示例）核心缺陷——`冲突类型总览`/`### 事件名` 卡片/`游戏化抽象` 三表/卡**全为空表头，无任一填好样例**（未给"杀人夺宝/宗门战争/秘境出口截杀"任一全字段填法），示例维不合格。（与项目对接）卡片"参考用途"行写 `Combat`/`Covet`/`Relationship`/`DynamicEvent`/`Opportunity`/`Quest`/`Memory`/`WorldState` 等**类型枚举，未点名 `apps/game/data/relationships/signal-rules/wanted-chain.json`、`balance/covet.json`、`world/dynamic-events.json` 等真实路径**，尤其"通缉追杀"未点名已落地的 `relationships/signal-rules/wanted-chain.json` 通缉链，定位偏弱。（字段定义 / 章节构成 / 数据来源）`## 详细程度`（须含触发/参与者/信息来源/行动链/结果/关系资源变化/消息传播/证据）释义充分，三行齐全，合格。
- **对接的项目代码：** `apps/game/data/balance/covet.json`（怀璧其罪/资源暴露驱动冲突）、`apps/game/data/balance/combat.json` 与 `balance/risk.json`（战斗结算/风险键）、`apps/game/data/relationships/signal-rules/wanted-chain.json`（通缉链）与 `relationships/impact-rules/combat.json`（战斗仇怨关系边）、`apps/game/data/balance/memory.json`（仇怨记忆）、`apps/game/data/world/dynamic-events.json`（宗门战争/围剿冲突事件）、`apps/game/data/world/news.json`（冲突消息扩散）、`apps/game/data/world/opportunities.json`（资源暴露机会点）。
- **建议涵盖方面（草案）：**
  - 三表/卡各补 1 行/张填好样例（一条"怀璧→截杀→结仇→通缉"完整冲突链，含信息来源与长期后果）。
  - "参考用途"行在类型枚举旁补 `apps/game/data/...` 真实路径，区分战斗/掠夺/仇怨/通缉/事件五类落点。
  - 通缉追杀显式对接 `relationships/signal-rules/wanted-chain.json` 通缉链（呼应缺口小结"通缉/声誉链对接偏弱"）。
  - 冲突后果绑定 `relationships/impact-rules/combat.json` 关系边与 `balance/memory.json` 仇怨记忆。
  - 补 ADR-058 关联：冲突的"预兆→爆发→长期后果"天然是事件因果链，应补"多月跨事件因果图"组织视角，作为月压缩远程高张力剧情与因果链的核心来源（缺口小结第 2 条、ADR-058 pipeline 第 6 步）。
- **优先级：** 高 —— 直接对接 covet/combat/risk/wanted-chain/impact-rules/dynamic-events 多个核心资产，代码相关度高；冲突因果链是月压缩"事件因果图"里程碑最核心的内容来源，里程碑相关度最高；缺全部填好样例 + 漏点名通缉链，乱度中上。

#### 时间行动与事件耗时模板
- **当前状态：** 头部元数据 L3 + `## 适用范围`（点名"玩家忙这边那边已变天"体验、服务行动点/world Tick/Job wait toil） + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（耗时总览 + 耗时/行动卡片 + `## 游戏化抽象`），定位为行动耗时/时间尺度/世界同步的主承载模板，是分层时间域最贴近的世界观侧模板。
- **主要问题：** （章节构成）`## 游戏化抽象（我的判断）` 标题下**内容被截断为空**，骨架残缺；且**缺独立 `## 详细程度` 段**。（示例）`耗时总览` 表与 `### 行动/事件名` 卡片**全为空表头，无任一填好样例**（未给"闭关突破/赶路/炼丹"任一耗时填法），示例维不合格。（与项目对接）`项目映射` 写 `行动点`/`daysPerYear`/`durationDays`/`Job wait toil`/`world tick` 等**裸字段名/概念名，未点名 `apps/game/data/toils/core-toils.json`、`config/game-config.json`、`balance/movement.json` 等真实路径**，定位偏弱；且模板按逐日/逐行动耗时组织，**未提月压缩 episode 的"压缩态时间桶"粒度**，与 ADR-058 Cold 域脱节。（数据来源）三行齐全，合格。
- **对接的项目代码：** `apps/game/data/toils/core-toils.json`（含 wait/移动等基础耗时工序）、`apps/game/data/toils/npc-cultivation-toils.json`（闭关/突破耗时）、`apps/game/data/balance/movement.json`（移动/赶路耗时）、`apps/game/data/balance/cultivation.json`（修炼/突破周期）、`apps/game/data/world/dynamic-events.json`（announceDay/startDay/endDay 事件窗口）、`apps/game/data/config/game-config.json`（daysPerYear 等时间尺度配置）、`apps/game/data/jobs`（多日长流程作业的总时长编排）。
- **建议涵盖方面（草案）：**
  - 补全 `## 游戏化抽象（我的判断）` 表并新增独立 `## 详细程度` 段（每行动给耗时尺度/期间外界变化/机会成本、无精确天数写相对时长），修复骨架残缺。
  - 总览表与卡片各补 1 行/张填好样例（如"闭关突破"映射 `toils/npc-cultivation-toils.json`，含期间外界变化）。
  - `项目映射` 补 `apps/game/data/toils/core-toils.json`、`config/game-config.json` 等真实路径，区分行动点/Toil 耗时/事件窗口/时间尺度四类。
  - 时间窗口字段对接 `world/dynamic-events.json` 的 announceDay/startDay/endDay 语义。
  - 补 ADR-058 关联（核心）：新增"压缩态时间桶/月度耗时聚合"粒度说明，对接 ADR-058 三层时间域（Hot 逐帧/Warm 日域/Cold 月压缩）与月度 episode 的耗时折叠，直接呼应缺口小结第 3 条「月度 episode 与地点轨迹」与保真等级分层。
- **优先级：** 高 —— 是 ADR-058 分层时间域在世界观侧最贴近的对口模板（时间尺度/世界同步/月压缩耗时折叠），里程碑相关度最高；对接 toils/movement/cultivation/game-config 代码相关度高；自身游戏化抽象段空 + 缺详细程度段 + 缺月压缩粒度，乱度中上，应优先补里程碑视角。

#### 出门游历流程分析模板
- **当前状态：** 五块骨架齐全（头部元数据 L3 / `## 适用范围`+不只记主角远行的边界 / `## 不要写什么` 约束段 / `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 详细程度`（要求写原因/准备/同行/目的地/途中/收获/撤退，可转 Job/Toil 须拆步骤） / 推荐结构含游历总览/游历流程卡片（含"可映射系统"列）/`## 游戏化抽象`），是单次游历全流程→移动/风险/Job-Toil 的主承载模板，结构成熟。
- **主要问题：** （示例）核心缺陷——`游历案例总览`/`### 案例名` 卡片/`游戏化抽象` 三表/卡**全为空表头，无任一填好样例**（未给"赴秘境/坊市采买/外海历练"任一全流程填法），示例维不合格。（与项目对接）卡片"可映射系统"列写 `WorldNews`/`Job-Toil`/`Movement`/`Risk-Opportunity`/`Dynamic Event-Action`/`Reward-Inventory`/`Memory-Relationship` 等**类型名，未点名 `apps/game/data/balance/movement.json`、`jobs/npc-dynamic-event-jobs.json`、`world/opportunities.json` 真实路径**，定位偏弱；且按"单次游历"组织，**与月压缩远程 NPC 的"地点轨迹"聚合粒度不同**，未提月度轨迹折叠。（字段定义 / 章节构成 / 数据来源）`## 详细程度`释义充分，三行齐全，合格。
- **对接的项目代码：** `apps/game/data/balance/movement.json`（移动/赶路/旅行风险）、`apps/game/data/balance/risk.json`（途中风险键）、`apps/game/data/world/opportunities.json`（途中/目的地机会点）、`apps/game/data/world/dynamic-events.json`（目的地事件窗口）、`apps/game/data/jobs/npc-dynamic-event-jobs.json` 与 `toils/npc-dynamic-event-toils.json`（游历准备/探索作业链）、`apps/game/data/world/news.json`（信息获取）、`apps/game/data/world/map.json`（路线/目的地 tile）。
- **建议涵盖方面（草案）：**
  - 三表/卡各补 1 行/张填好样例（一条"寻药赴秘境"全流程：信息获取→准备→移动→途中事件→探索→收获→撤退）。
  - "可映射系统"列在类型名旁补 `apps/game/data/...` 真实路径，准备/探索显式拆到 `jobs/npc-dynamic-event-jobs.json` 与 `toils/` 步骤。
  - 移动/风险绑定 `balance/movement.json`/`balance/risk.json`，途中机会点对接 `world/opportunities.json`。
  - 路线/目的地落 `world/map.json` 的 tile 定位。
  - 补 ADR-058 关联：单次游历的"目的地序列"应可聚合为月压缩远程 NPC 的"地点轨迹"，需补月度轨迹折叠粒度，直接对接缺口小结第 3 条「月度 episode 与地点轨迹」与 ADR-058 pipeline 第 5 步生成地点轨迹/episode。
- **优先级：** 高 —— 横跨 movement/risk/opportunities/dynamic-events/jobs/map 多目录，代码相关度高；游历目的地序列是月压缩"地点轨迹"里程碑的直接素材来源，里程碑相关度高（缺口小结点名）；缺全部填好样例 + 缺月度轨迹粒度，乱度中上。

#### 信息传播与情报模板
- **当前状态：** 头部元数据 L3 + `## 适用范围`（点名服务 `info-sense.md`/`news.json`/InfoPropagationSystem/knownNews） + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（信息机制总览 + 信息机制卡片），定位为感知/传闻/可信度/信息不对称的主承载模板。
- **主要问题：** （章节构成）相比同组 L3 模板**缺独立 `## 详细程度` 段与 `## 游戏化抽象` 段**，量化标准与抽象规则缺位。（示例）`信息机制总览` 表与 `### 信息机制名` 卡片**全为空表头，无任一填好样例**（未给"宗门公告/坊市传闻/通缉令"任一全字段填法），示例维不合格。（与项目对接）`项目映射` 写 `WorldNews`/`spreadRadius`/`reliability`/`knownNews`/`opportunityId` 等**裸字段名，未带 `apps/game/data/world/news.json` 全路径**；且"悬赏/通缉/情报组织"未点名已落地的 `relationships/signal-rules/wanted-chain.json` 通缉链与 `signal-keys.json` 信号键（rubric 第 2 节点名"通缉/声誉链对接偏弱"），定位偏弱、不完整。（字段定义 / 数据来源）三列表释义清晰、三行齐全，合格。
- **对接的项目代码：** `apps/game/data/world/news.json`（WorldNews 传播：importance/spreadSpeed/maxRadius/baseReliability/decayRate/ttlDays，已有 `tribulation` 等 newsTypes）、`apps/game/data/relationships/signal-rules/wanted-chain.json`（通缉链传播）、`apps/game/data/relationships/dictionaries/signal-keys.json`（声誉/通缉信号键）、`apps/game/data/world/opportunities.json`（情报 → 机会点 opportunityId）、`apps/game/data/goals/dynamic-goals.json`（情报驱动赶赴/逃亡目标）、`apps/game/data/balance/utility.json`（情报对决策权重的影响）。
- **建议涵盖方面（草案）：**
  - 补独立 `## 详细程度` 段与 `## 游戏化抽象` 段，与同组 L3 模板对齐（每机制≥1 证据、可信度/衰减标"我的判断"）。
  - 总览表与卡片各补 1 行/张填好样例（如"坊市传闻"映射 `news.json` 的 spreadSpeed/reliability，含失真衰减）。
  - `项目映射` 补 `apps/game/data/world/news.json` 真实字段路径，并点名 `relationships/signal-rules/wanted-chain.json` 通缉链与 `dictionaries/signal-keys.json` 信号键（呼应缺口小结"通缉/声誉链对接偏弱"）。
  - 情报 → 机会点对接 `world/opportunities.json` 的 opportunityId 与 `goals/dynamic-goals.json`。
  - 补 ADR-058 关联：信息可信度/距离衰减/滞后是月压缩对话上下文"玩家已知信息"的前置（ADR-058 对话 DSL 读取玩家已知信息），且 NPC 月度 knownNews 累积喂入 Cold 域 agenda 决策。
- **优先级：** 高 —— 直接对接 news/wanted-chain/signal-keys/opportunities 多个信息资产，代码相关度高；NPC 已知信息是月压缩对话 DSL 与 agenda 决策的输入，里程碑相关度高；自身缺详细程度/抽象段 + 漏点名通缉/信号链，乱度中上。

#### 有限视角与叙事日志模板
- **当前状态：** 头部元数据 L2/涉 UI 升 L3 + `## 适用范围`（点名服务玩家日志/确认-消息-传闻分级/LogPanel） + `## 必须写什么` 三列表 + `## 尽量写全的内容清单` + `## 不要写什么` + 推荐结构（信息呈现总览 + 视角/日志卡片 + `## 文案模式（我的判断）` 表），定位为玩家有限视角/日志文本/UI 叙事反馈的主承载模板，是面向呈现层而非机制层的模板。
- **主要问题：** （示例）核心缺陷——`信息呈现总览`/`### 信息呈现名` 卡片/`文案模式` 三表/卡**全为空表头，无任一填好样例**（未给"突破/死亡/拍卖"任一确认/消息/传闻三级文本填法），示例维不合格。（章节构成）相比同组 L3 模板**缺独立 `## 详细程度` 段**，量化标准缺位。（与项目对接）`项目映射` 写 `LogPanel`/`WorldNews content`/`INFO_RECEIVED`/`事件弹窗`/`地图迷雾` 等**代码符号/UI 概念，多属前端/运行时而非 `apps/game/data/` 数据资产**——本模板本质对接呈现层与 ADR-058 对话/剧情包文本，纯 data 目录落点偏少，应明确"暂无直接 data 对接、主要对接日志/对话 DSL"。（字段定义 / 数据来源）三列表释义清晰、三行齐全，合格。
- **对接的项目代码：** 偏弱直接对接——主要服务运行时呈现层（LogPanel/事件弹窗/地图迷雾，属前端而非 `apps/game/data/`）。数据侧间接对接：`apps/game/data/world/news.json`（WorldNews content/reliability → 日志可信度文本）、`apps/game/data/relationships/dictionaries/signal-keys.json`（声誉/通缉信号 → 榜单文本）；其余"确认/消息/传闻"文案与对话/剧情包文本对接 ADR-058 规划中的对话 DSL 配置（见 `docs/systems/player-encounter-dialogue.md`、`docs/data-models/player-encounter-dialogue.md`，对应数据目录待 ADR-058 落地）。
- **建议涵盖方面（草案）：**
  - 三表/卡各补 1 行/张填好样例（如"突破"事件的确认/消息/传闻三级文本 + 一张视角卡，含示范何时写"误判或缺失信息"）。
  - 补独立 `## 详细程度` 段，与同组模板对齐（每事件给三级文本与可见性边界）。
  - 明确本模板对接呈现层（LogPanel/迷雾属前端）+ ADR-058 对话/剧情包文本，data 侧仅间接经 `world/news.json` 可信度字段，不强行虚构 data 落点。
  - 可信度分级文本对接 `world/news.json` 的 baseReliability/decayRate 语义。
  - 补 ADR-058 关联（核心）：有限视角的"确认/消息/传闻"分级与可见性边界正是月压缩对话 DSL 与剧情包状态机的呈现规范——对话候选/选项文本须遵守玩家已知信息边界（ADR-058 对话上下文读取玩家已知信息），是缺口小结第 4 条「相遇剧情与对话设计」的呈现侧前置（参 `docs/systems/player-encounter-dialogue.md`）。
- **优先级：** 中 —— 本模板偏呈现层、直接 `apps/game/data/` 落点最少（多为前端/对话 DSL），代码相关度中等；但其有限视角/分级文本是月压缩对话 DSL 与剧情包的呈现规范前置，里程碑相关度高；自身缺样例 + 缺详细程度段，乱度中等，故综合定中。

## 4. 建议新增模板清单

> 说明：候选来源全部取自第 2 节「缺口小结」逐条点名项与第 3 节速评卡的"建议涵盖方面"，未凭空发明需求。每行"对接的项目代码"指向第 2 节缺口中的真实存在路径或 ADR-058 里程碑维度。已核对：`docs/decisions/adr-058-layered-time-domain-and-monthly-compressed-simulation.md`、`docs/worldbuilding/wiki/rules/{leader-succession,faction-crisis-defection,sect-operation,wealth-exposed}.md`、`docs/systems/player-encounter-dialogue.md`、`docs/data-models/player-encounter-dialogue.md`、`apps/game/data/relationships/signal-rules/wanted-chain.json` 均真实存在。

| 建议新增模板 | 解决什么问题 | 对接的项目代码 | 优先级 |
|---|---|---|---|
| 势力运营与权力更替 | 「势力设定」模板偏静态结构（势力类型/资源/关系），缺承载"势力运营、掌门更替、危机叛逃、暴富露财"这类**动态势力规则**的去向（缺口小结第 1 条）；让已落地的运营/继承/危机规则有对口的世界观侧采集模板。 | `docs/worldbuilding/wiki/rules/leader-succession.md`、`faction-crisis-defection.md`、`sect-operation.md`、`wealth-exposed.md`；`apps/game/data/entities/factions.json`、`apps/game/data/definitions/sect-organization.json`、`apps/game/data/behavior-trees/faction-default.json`、`apps/game/data/actions/faction-actions.json` | 高 |
| 事件因果链（长程因果图） | 现有「冲突事件分析」「动态事件与机会点」按**单个事件**组织，月压缩里程碑要求的"事件因果链 / 多月因果图"（事件 A 余波触发事件 B）组织视角无模板（缺口小结第 2 条，ADR-058 pipeline 第 6 步事件结算）。 | ADR-058 维度（`docs/decisions/adr-058-layered-time-domain-and-monthly-compressed-simulation.md`）；`apps/game/data/world/dynamic-events.json`、`apps/game/data/relationships/signal-rules/wanted-chain.json`、`apps/game/data/relationships/impact-rules/`、`apps/game/data/balance/memory.json` | 高 |
| 月度经历摘要与地点轨迹 | 远处 NPC 月压缩产出是"压缩态经历摘要 + 地点轨迹"，与「时间行动与事件耗时」（逐日耗时）、「出门游历流程分析」（单次游历）粒度不同，无对口模板（缺口小结第 3 条，ADR-058 pipeline 第 5 步生成地点轨迹/episode）。同时收纳 NPC 长程离线演化（修为·境界·关系·背包·死亡的月度跨维聚合，缺口小结第 5 条并入项）。 | ADR-058 维度（`docs/decisions/adr-058-layered-time-domain-and-monthly-compressed-simulation.md`）；`apps/game/data/world/map.json`（地点轨迹锚点）、`apps/game/data/definitions/ranks.json`、`apps/game/data/balance/cultivation.json`、`apps/game/data/items/`（背包累积）、`apps/game/data/relationships/` | 高 |
| 相遇剧情与对话设计 | 对话候选/选项/副作用命令/剧情包状态机为月压缩里程碑核心，「有限视角与叙事日志」偏事后日志、无"对话 DSL / 剧情包状态机 / 相遇剧情"组织视角（缺口小结第 4 条）。 | ADR-058 维度；`docs/systems/player-encounter-dialogue.md`、`docs/data-models/player-encounter-dialogue.md`（对话 DSL 数据模型，对应 data 目录待 ADR-058 落地）；输入侧关联 `apps/game/data/world/news.json`（玩家已知信息边界） | 高 |

**判为"并入现有模板而非新增"的缺口：**

- **NPC 长程离线演化聚合（缺口小结第 5 条）**：缺口小结本身已注明"可作为前一条月度 episode 模板的子项，优先级次于上四条"，故并入上表「月度经历摘要与地点轨迹」模板，不单列新模板。
- **`relationships` 通缉链 / 声誉信号（对接偏弱项）**：第 2 节已定性为 rubric 第 4 维"对接偏弱"而非缺模板，建议在「信息传播与情报」「冲突事件分析」「邪修分析」三模板补点名 `apps/game/data/relationships/signal-rules/wanted-chain.json` 与 `dictionaries/signal-keys.json`，不新增独立通缉链模板。
- **保真等级与时间域分层（Hot/Warm/Cold）**：第 2 节判为"属架构机制，世界观侧无对口模板"，其世界观侧的时间粒度诉求由「时间行动与事件耗时」补"压缩态时间桶"粒度承载，不单列新模板。
- **`config`/`tags`/`balance` 调参子表 / `actions`·`toils`·`jobs`·`behavior-trees`·`needs` 机制编排**：第 2 节缺口小结已明确归为"覆盖不足但暂判无需新模板"（纯工程配置、纯数值调参、AI 行为/流程编排机制结构），均不进新增清单，是否单立"机制编排"模板留待第 5 节讨论。


## 5. 推荐讨论顺序

> 排序依据：`乱的程度 × 代码相关度 × 月压缩（ADR-058）里程碑相关度`——高优先 + 与里程碑强相关 + 结构残缺严重者排前。
> 分批建议：**第 1 批（第 1–5 项）**集中讨论与月压缩里程碑最强相关且自身结构残缺/缺月度粒度的 5 个模板，它们决定 NPC 长程离线演化的底座口径，应优先达成共识；**第 2 批（第 6–9 项）**为同样高里程碑相关、但乱点偏结构残缺/缺段、且能为后续因果链与对话设计铺路的模板；**第 3 批（第 10–23 项）**为其余高优先模板（先里程碑、再对接校正类）；**第 4 批（第 24–27 项）**为中优先模板；**第 5 批（第 28–31 项，全为新增模板）**放在最后，待前述被修模板的字段口径与里程碑粒度达成共识后再设计，避免新模板与既有模板边界重复返工。

1. 时间行动与事件耗时 —— 乱：游戏化抽象段空 + 缺详细程度段 + 缺月压缩粒度；代码：toils/movement/cultivation/game-config；里程碑：ADR-058 分层时间域世界观侧最贴近模板，相关度最高，需补"压缩态时间桶/月度耗时折叠"。
2. 冲突事件分析 —— 乱：缺全部填好样例 + 漏点名通缉链；代码：covet/combat/risk/wanted-chain/impact-rules/dynamic-events；里程碑：冲突因果链是"事件因果图"最核心内容来源，相关度最高。
3. 记忆情绪与执念 —— 乱：缺详细程度段 + 全部填好样例；代码：memory/emotion/obsession 三表；里程碑：记忆衰减/执念加强是"NPC 长程离线演化"最核心动机驱动，相关度最高。
4. 角色修炼历程 —— 乱：三表全空 + 完全未对接月度粒度；代码：jobs/ranks/cultivation/economy/items/goals；里程碑：与"NPC 长程演化"最贴合，承上启下需补月度 episode 粒度。
5. 战斗与保命机制 —— 乱：游戏化抽象段残缺 + 对接 id 用旧 `ability_lock_hp` 不合 ADR-042，乱度最高；代码：abilities/effects/combat/risk/tags（GAS 核心资产）；里程碑：战斗逃生为基础机制，需先校正合规。
6. 世界状态与灾变 —— 乱：游戏化抽象段空 + 缺详细程度段双重残缺；代码：modifiers/dynamic-events/monster-spawn；里程碑：区域级世界状态是月压缩远程演化的环境压力源（pipeline 第 3 步）。
7. 动态事件与机会点 —— 乱：缺样例 + 缺详细程度/游戏化抽象段；代码：dynamic-events/opportunities/dynamic-goals/event-jobs；里程碑：事件是"事件因果图"基本节点，需补多月跨事件因果视角。
8. 人物关系与事件分析 —— 乱：仅缺填好样例与路径点名（结构最成熟，性价比高）；代码：relationships 全套子目录 + entities；里程碑：内置"事件因果链"表，与"事件因果图"天然贴合。
9. 角色AI行为参考 —— 乱：三表全空 + 裸文件名缺前缀；代码：横跨 goals/actions/jobs/toils/behavior-trees/needs，相关度最高；里程碑：长程演化依赖行为原型驱动，需补离线压缩态动机标签。
10. 境界提升与功法分析 —— 乱：六表全空 + 裸文件名缺全路径；代码：ranks/techniques/cultivation 三核心数值文件，相关度最高；里程碑：境界/寿元是 NPC 长程演化底座维度。
11. 灵根体质血脉 —— 乱：缺详细程度段 + 三表全空；代码：tags/npcs/cultivation；里程碑：资质是 NPC 差异化与长程演化的初始条件。
12. NPC性格与代表事件 —— 乱：四表全空 + 裸字段名缺路径；代码：personality/utility/obsession 三 AI 核心平衡文件；里程碑：性格是长程演化的动机底色。
13. 信息传播与情报 —— 乱：缺详细程度/游戏化抽象段 + 漏点名通缉/信号链；代码：news/wanted-chain/signal-keys/opportunities；里程碑：NPC 已知信息是对话 DSL 与 agenda 决策输入。
14. 物资产出与消耗 —— 乱：五表全空 + 映射路径不准（economy 实在 balance/）；代码：balance/economy + reward + economy 底座 + items value + macro-resources，相关度最高；里程碑：资源累积是长程演化背包维度直接来源。
15. 出门游历流程分析 —— 乱：三表全空 + 缺月度轨迹粒度；代码：movement/risk/opportunities/dynamic-events/jobs/map；里程碑：游历目的地序列是"地点轨迹"直接素材。
16. 势力设定 —— 乱：五表全空 + 与 wiki 动态规则（继承/危机/运营）脱节；代码：factions/sect-organization/faction 行为树；里程碑：势力兴衰/继承/叛逃驱动远程剧情。
17. 拍卖坊市与交易 —— 乱：对接点名指向不存在的 `org_market`/`org_auction_house`，需先校正映射；代码：economy 交易底座 + balance/economy + items value + covet/risk；里程碑：大型拍卖/黑市截杀是远程世界态事件源。
18. 邪修分析 —— 乱：三表全空 + 漏点名通缉链；代码：factions/covet/obsession/risk/wanted-chain/dynamic-events；里程碑：邪修掠夺→暴露→围剿是远程高张力剧情与因果链来源。
19. 夺舍设定分析 —— 乱：对接全为裸概念名未落地 + 三表全空；代码：横跨 tags/effects/abilities/goals/obsession/relationships/world/jobs/toils；里程碑：身份变更是 NPC 长程演化的稀有高影响突变事件。
20. 妖兽分析 —— 乱：六表全空 + 漏点名 monster-attribute-templates/monster-combat/monster-resource-rules 三专表；代码：monsters/monster-* + monster-spawn + 分层行为树 + material，相关度最高；里程碑：兽潮/领地冲突是远程世界态事件源。
21. 有限视角与叙事日志 —— 乱：三表全空 + 缺详细程度段；代码：偏呈现层、data 落点最少（间接 news）；里程碑：分级文本是对话 DSL 与剧情包的呈现规范前置，相关度高。
22. 建筑设施与场所功能 —— 乱：五表全空 + 映射悬空（设施仅以 map tile `buildings[]` 隐式承载）；代码：map/sect-organization/quests/actions/world；里程碑：场所是月度作业地点轨迹的锚点（间接）。
23. 妖兽与修士关系分析 —— 乱：四表全空 + 与「妖兽分析」存在重叠需先理清分工；代码：monsters/relationships/quests/world/monster-spawn；里程碑：人妖冲突/兽潮是月压缩远程事件源。
24. 宗门任务体系 —— 乱：三表全空 + 裸目录名缺路径（结构成熟）；代码：quest-templates/quest-jobs/quest-toils；里程碑：宗门任务是远程月度作业链来源之一，相关度中等。
25. 散修生存方式 —— 乱：三表全空 + 裸路径（结构与详细程度成熟）；代码：opportunities/quests/economy/jobs/goals；里程碑：散修是远程离线演化典型低组织度轨迹来源。
26. 修炼流派 —— 乱：缺填好样例（结构成熟、对接强）；代码：obsession/utility/personality/goals/jobs/toils；里程碑：流派原型间接驱动长程演化动机，非阻塞。
27. 世界观设定 —— 乱：五表全空 + 泛称映射（顶层总纲、结构成熟）；代码：modifiers/ranks/terrains/entities；里程碑：底层世界规则是月压缩 Cold 域全局约束（间接，非逐事件因果）。
28. 事件因果链（长程因果图）（新增） —— 月压缩里程碑核心组织视角；需先在第 2、7、8 项（冲突/动态事件/人物关系）达成"多月跨事件因果"口径共识后设计，避免与既有事件类模板边界重复。
29. 月度经历摘要与地点轨迹（新增） —— 月压缩 pipeline 第 5 步直接产物，并入 NPC 长程离线演化聚合；需先在第 1、4、15 项（时间耗时/修炼历程/游历）确定月度粒度后设计。
30. 相遇剧情与对话设计（新增） —— 对话 DSL/剧情包状态机里程碑核心；需先在第 13、21 项（信息传播/有限视角叙事）确定玩家已知信息边界与分级文本后设计。
31. 势力运营与权力更替（新增） —— 承载势力运营/继承/危机叛逃动态规则；需先在第 16 项（势力设定）厘清静态结构与动态规则边界后设计，避免重复。
