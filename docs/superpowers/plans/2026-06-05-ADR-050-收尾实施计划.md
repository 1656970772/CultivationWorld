# Job/Toil 正式启用前收尾 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Job/Toil 动态目标链路从“可通过环境变量开启验证”推进到“默认配置可正式启用、可回退、可用真实长程模拟验收”的状态。

**Architecture:** 保留现有 `Reaction -> Utility / Intent -> GOAP -> Job / Toil -> Execution` 分层；本计划不重写 Job/Toil 基础设施，只补默认启用前的严格验收门、默认开关、回退路径测试和文档状态。动态事件内容库扩展与 PvP 真实扣血拆为后续独立计划，不进入本计划实现范围。

**Tech Stack:** 原生 ES Module JavaScript、JSON 数据配置、PowerShell、Node.js 工具脚本、真实多种子长程模拟验证。

---

## Sources

- Spec: `docs/superpowers/specs/2026-06-05-Job-Toil正式启用前收尾规格.md`
- System spec: `docs/systems/job-toil-ai-spec.md`
- ADR: `docs/decisions/adr-050-goap-job-toil-layered-ai.md`
- Data rules: `docs/data/data-config-rules.md`
- Existing verification: `apps/game/tools/verify-dynamic-goals.mjs`
- Current configs: `apps/game/data/config/ai-config.json`, `apps/game/data/world/dynamic-events.json`, `apps/game/data/goals/dynamic-goals.json`, `apps/game/data/actions/npc-action-sets.json`

## Current Baseline

当前代码已经完成以下收尾项，本计划不重复实现：

- `apps/game/tools/test-job-config-load.mjs` 已递归校验 Job 配置中的 `itemId`、`priceItemId`、`currencyItemId` 引用真实物品 ID。
- `apps/game/tools/test-job-action-planning.mjs` 已覆盖秘境目标只能规划秘境 JobAction、大比目标只能规划大比 JobAction。
- `apps/game/js/engine/abstract/behavior-system.js` 已从动态目标上下文注入 `targetDynamicEventType`，并派生 `dynamicEventIsSecretRealm`、`dynamicEventIsSectTournament`、`dynamicEventUsesGenericPreparation`。
- `docs/systems/job-toil-ai-spec.md` 已记录一轮 900 天、3 种子、`JOBS_ACTIVE=1` 的真实观察：JobAction 规划 84375 次，Job 启动 21707 次，Job 完成 4351 次，失败/中止 0 次，动态行动后恢复率 96.6%。

剩余缺口：

- `verify-dynamic-goals.mjs` 当前恢复率断言仍是 50%，低于规格要求的 90%。
- `verify-dynamic-goals.mjs` 当前没有“失败/abort 必须为 0”的可选硬门。
- `verify-dynamic-goals.mjs` 当前默认会在内存中强制打开 dynamic events/goals，缺少“不依赖环境变量、不依赖内存覆盖”的默认配置路径验证。
- `ai-config.npc.jobs.enabled`、`dynamic-events.enabled`、`dynamic-goals.enabled` 仍为 `false`。
- 默认启用后的文档状态、数据规则和导航还需要同步。

## File Structure

- Create: `apps/game/tools/verify-dynamic-goals-gates.mjs`
  - 纯函数模块：解析验证门参数、计算恢复率、检查启用门、生成 Markdown 报告。
- Create: `apps/game/tools/test-dynamic-goals-gates.mjs`
  - 覆盖严格启用门：恢复率、失败/abort、默认配置参数解析、报告渲染。
- Modify: `apps/game/tools/verify-dynamic-goals.mjs`
  - 接入 gate 模块。
  - 增加 `--min-recovery=0.9`、`--require-zero-job-failures`、`--use-config-defaults`、`--report=<path>`。
  - 支持默认配置路径验证，不再强制内存覆盖开关。
- Modify: `apps/game/tools/test-job-config-load.mjs`
  - 将默认开关断言从“默认关闭”改为“正式启用后默认开启”。
  - 增加 `jobs.enabled=false` 回退路径仍不加入 JobAction 的测试。
- Modify: `apps/game/data/config/ai-config.json`
  - 将 `npc.jobs.enabled` 改为 `true`。
- Modify: `apps/game/data/world/dynamic-events.json`
  - 将 `enabled` 改为 `true`。
- Modify: `apps/game/data/goals/dynamic-goals.json`
  - 将 `enabled` 改为 `true`。
- Create: `docs/superpowers/reports/2026-06-05-Job-Toil默认启用验证.md`
  - 由验证脚本生成的真实模拟报告。
- Modify: `docs/systems/job-toil-ai-spec.md`
  - 同步默认启用状态、验证报告路径、启用/回退边界。
- Modify: `docs/decisions/adr-050-goap-job-toil-layered-ai.md`
  - 状态改为“首批默认启用”，记录启用范围。
- Modify: `docs/data/data-config-rules.md`
  - 将 Job/Toil 默认开关规则从“默认保持 false”更新为“正式启用后默认为 true，但 false 仍是回退开关”。
- Modify: `docs/README.md`
  - 更新最后更新说明、文档结构和关键文档导航。

---

### Task 1: 严格启用门纯函数

**Files:**
- Create: `apps/game/tools/test-dynamic-goals-gates.mjs`
- Create: `apps/game/tools/verify-dynamic-goals-gates.mjs`

- [ ] **Step 1: 写失败测试**

Create `apps/game/tools/test-dynamic-goals-gates.mjs`:

```js
#!/usr/bin/env node
import {
  evaluateDefaultEnableGate,
  parseGateArgs,
  renderGateReport,
  recoveryRatioOf,
} from './verify-dynamic-goals-gates.mjs';

let failed = 0;
function assert(cond, msg) {
  if (!cond) {
    console.error('  FAIL:', msg);
    failed++;
  } else {
    console.log('  OK:', msg);
  }
}

function stats(overrides = {}) {
  return {
    phaseChanges: 3,
    phaseByType: { secret_realm: 2, sect_tournament: 1 },
    awarenessObservations: 10,
    awareNpcs: new Set(['npc_a']),
    candidateGoalCount: 8,
    uniqueCandidateKeys: new Set(['candidate_a']),
    candidateBySource: { prepare_tournament: 2 },
    candidateByKind: { preparation: 4, window: 4 },
    dynamicPlanCount: 6,
    dynamicPlanByKind: { preparation: 3, window: 3 },
    dynamicPlanBySource: { prepare_secret_realm: 3, join_secret_realm: 3 },
    interruptCount: 2,
    interruptByDecision: { after_step: 1, ignore: 1 },
    dynamicActions: { prepare: 2, join: 2, prepareSucceeded: 2, joinSucceeded: 2 },
    jobActions: {
      planned: 6,
      started: 4,
      completed: 4,
      failed: 0,
      aborted: 0,
      byJobId: { job_npc_prepare_secret_realm: 2, job_npc_join_dynamic_event: 2 },
      byToilId: { bind_event: 4, mark_prepared: 2, mark_participant: 2 },
      failureReasons: {},
    },
    dynamicActionNpcs: new Set(['npc_a', 'npc_b']),
    recoveredNpcs: new Set(['npc_a', 'npc_b']),
    normalPlanCount: 10,
    normalActionCount: 9,
    normalPlanAfterDynamic: 5,
    normalActionAfterDynamic: 4,
    ...overrides,
  };
}

console.log('1) parse strict gate args');
const parsed = parseGateArgs([
  '--min-recovery=0.9',
  '--require-zero-job-failures',
  '--use-config-defaults',
  '--report=docs/superpowers/reports/out.md',
]);
assert(parsed.minRecoveryRatio === 0.9, 'min recovery parsed as ratio');
assert(parsed.requireZeroJobFailures === true, 'require-zero-job-failures parsed');
assert(parsed.useConfigDefaults === true, 'use-config-defaults parsed');
assert(parsed.reportPath === 'docs/superpowers/reports/out.md', 'report path parsed');

console.log('2) recovery ratio handles empty dynamic action set');
assert(recoveryRatioOf(stats({ dynamicActionNpcs: new Set(), recoveredNpcs: new Set() })) === 1, 'empty dynamic action set is fully recovered');
assert(recoveryRatioOf(stats({ dynamicActionNpcs: new Set(['a', 'b']), recoveredNpcs: new Set(['a']) })) === 0.5, 'partial recovery ratio is calculated');

console.log('3) strict gate passes healthy stats');
const healthy = evaluateDefaultEnableGate(stats(), {
  minRecoveryRatio: 0.9,
  requireZeroJobFailures: true,
});
assert(healthy.ok === true, 'healthy stats pass strict gate');
assert(healthy.recoveryRatio === 1, 'healthy gate exposes recovery ratio');

console.log('4) strict gate fails low recovery');
const lowRecovery = evaluateDefaultEnableGate(stats({
  dynamicActionNpcs: new Set(['a', 'b', 'c']),
  recoveredNpcs: new Set(['a']),
}), { minRecoveryRatio: 0.9, requireZeroJobFailures: true });
assert(lowRecovery.ok === false, 'low recovery fails strict gate');
assert(lowRecovery.checks.some(check => check.key === 'recovery_ratio' && check.ok === false), 'low recovery failure is named');

console.log('5) strict gate fails job failures and aborts');
const failedJobs = evaluateDefaultEnableGate(stats({
  jobActions: {
    planned: 6,
    started: 4,
    completed: 2,
    failed: 1,
    aborted: 1,
    byJobId: { job_npc_prepare_secret_realm: 2 },
    byToilId: {},
    failureReasons: { dynamic_event_missing: 1, replan: 1 },
  },
}), { minRecoveryRatio: 0.9, requireZeroJobFailures: true });
assert(failedJobs.ok === false, 'job failure stats fail strict gate');
assert(failedJobs.checks.some(check => check.key === 'zero_job_failures' && check.ok === false), 'job failure gate is named');

console.log('6) report includes source numbers and verification method');
const report = renderGateReport({
  stats: stats(),
  days: 900,
  seeds: [12345, 67890, 24680],
  options: { minRecoveryRatio: 0.9, requireZeroJobFailures: true, useConfigDefaults: true },
});
assert(report.includes('# Job/Toil 默认启用验证报告'), 'report has Chinese title');
assert(report.includes('恢复率：100.0%'), 'report prints recovery ratio');
assert(report.includes('真实多种子长程模拟输出'), 'report names direct simulation observation source');

if (failed > 0) {
  console.error(`\nDynamic goal gate tests failed: ${failed}`);
  process.exit(1);
}
console.log('\nDynamic goal gate tests passed');
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
node tools/test-dynamic-goals-gates.mjs
```

Expected:

- FAIL with `Cannot find module ... verify-dynamic-goals-gates.mjs`.

- [ ] **Step 3: 实现 gate 模块**

Create `apps/game/tools/verify-dynamic-goals-gates.mjs`:

```js
export function parseGateArgs(args = []) {
  const options = {
    minRecoveryRatio: 0.5,
    requireZeroJobFailures: false,
    useConfigDefaults: false,
    reportPath: null,
  };
  for (const arg of args) {
    let match;
    if ((match = /^--min-recovery=(0(?:\.\d+)?|1(?:\.0+)?)$/.exec(arg))) {
      options.minRecoveryRatio = Number(match[1]);
    } else if (arg === '--require-zero-job-failures') {
      options.requireZeroJobFailures = true;
    } else if (arg === '--use-config-defaults') {
      options.useConfigDefaults = true;
    } else if ((match = /^--report=(.+)$/.exec(arg))) {
      options.reportPath = match[1];
    }
  }
  return options;
}

export function recoveryRatioOf(stats) {
  const total = stats.dynamicActionNpcs?.size || 0;
  if (total === 0) return 1;
  return (stats.recoveredNpcs?.size || 0) / total;
}

function count(stats, path, fallback = 0) {
  let value = stats;
  for (const key of path.split('.')) value = value?.[key];
  return Number(value ?? fallback) || 0;
}

function hasAnyJob(stats, ids) {
  const byJobId = stats.jobActions?.byJobId || {};
  return ids.some(id => count(byJobId, id) > 0);
}

export function evaluateDefaultEnableGate(stats, options = {}) {
  const minRecoveryRatio = Number(options.minRecoveryRatio ?? 0.5);
  const recoveryRatio = recoveryRatioOf(stats);
  const nonImmediateInterrupts = count(stats, 'interruptByDecision.after_step')
    + count(stats, 'interruptByDecision.keep_current_queue')
    + count(stats, 'interruptByDecision.ignore');
  const totalDynamicActions = count(stats, 'dynamicActions.prepare') + count(stats, 'dynamicActions.join');
  const checks = [
    { key: 'phase_changes', ok: count(stats, 'phaseChanges') > 0, message: `动态事件阶段真实推进（阶段变化 ${count(stats, 'phaseChanges')} 次）` },
    { key: 'secret_realm_window', ok: count(stats, 'phaseByType.secret_realm') > 0, message: `秘境事件窗口有阶段变化（${count(stats, 'phaseByType.secret_realm')} 次）` },
    { key: 'sect_tournament_window', ok: count(stats, 'phaseByType.sect_tournament') > 0, message: `宗门大比事件窗口有阶段变化（${count(stats, 'phaseByType.sect_tournament')} 次）` },
    { key: 'awareness', ok: (stats.awareNpcs?.size || 0) > 0, message: `NPC 真实知晓动态事件（${stats.awareNpcs?.size || 0} 人，观察 ${count(stats, 'awarenessObservations')} 次）` },
    { key: 'candidate_goals', ok: count(stats, 'candidateGoalCount') > 0 && (stats.uniqueCandidateKeys?.size || 0) > 0, message: `DynamicGoalProvider 真实产出候选目标（观察 ${count(stats, 'candidateGoalCount')} 次，唯一 ${stats.uniqueCandidateKeys?.size || 0} 个）` },
    { key: 'preparation_candidates', ok: count(stats, 'candidateByKind.preparation') > 0, message: `PreparationGoal 候选真实产出（${count(stats, 'candidateByKind.preparation')} 次）` },
    { key: 'window_candidates', ok: count(stats, 'candidateByKind.window') > 0, message: `WindowGoal 候选真实产出（${count(stats, 'candidateByKind.window')} 次）` },
    { key: 'dynamic_plan', ok: count(stats, 'dynamicPlanCount') > 0, message: `动态 Goal 真实进入 planResult（${count(stats, 'dynamicPlanCount')} 次）` },
    { key: 'prepare_and_join_plan', ok: count(stats, 'dynamicPlanBySource.prepare_secret_realm') > 0 && count(stats, 'dynamicPlanBySource.join_secret_realm') > 0, message: `秘境准备和进入目标都能进入 planResult（prepare=${count(stats, 'dynamicPlanBySource.prepare_secret_realm')}, join=${count(stats, 'dynamicPlanBySource.join_secret_realm')}）` },
    { key: 'interrupt_policy', ok: count(stats, 'interruptCount') > 0 && nonImmediateInterrupts > 0, message: `InterruptPolicy 有真实决策且不是无脑立即打断（总数 ${count(stats, 'interruptCount')}，非立即 ${nonImmediateInterrupts}）` },
    { key: 'dynamic_actions', ok: totalDynamicActions > 0 && count(stats, 'dynamicActions.prepare') > 0 && count(stats, 'dynamicActions.join') > 0, message: `准备/参与动态事件行为真实执行（准备 ${count(stats, 'dynamicActions.prepare')}，参与 ${count(stats, 'dynamicActions.join')}）` },
    { key: 'dynamic_action_effects', ok: count(stats, 'dynamicActions.prepareSucceeded') > 0 && count(stats, 'dynamicActions.joinSucceeded') > 0, message: `准备/参与副作用真实落地（准备成功 ${count(stats, 'dynamicActions.prepareSucceeded')}，参与成功 ${count(stats, 'dynamicActions.joinSucceeded')}）` },
    { key: 'job_planned', ok: count(stats, 'jobActions.planned') > 0, message: `JobAction 真实进入规划（${count(stats, 'jobActions.planned')} 次）` },
    { key: 'job_started', ok: count(stats, 'jobActions.started') > 0, message: `Job 真实启动（${count(stats, 'jobActions.started')} 次）` },
    { key: 'dynamic_job_executed', ok: hasAnyJob(stats, ['job_npc_prepare_dynamic_event', 'job_npc_prepare_secret_realm', 'job_npc_join_dynamic_event']), message: '动态事件准备或参与 Job 真实执行' },
    { key: 'job_completed', ok: count(stats, 'jobActions.completed') > 0, message: `至少有 Job 完成（${count(stats, 'jobActions.completed')} 次）` },
    { key: 'normal_behavior', ok: count(stats, 'normalPlanCount') > 0 && count(stats, 'normalActionCount') > 0, message: `动态目标开启后普通 plan/普通行为仍持续发生（plan=${count(stats, 'normalPlanCount')}，action=${count(stats, 'normalActionCount')}）` },
    { key: 'normal_behavior_after_dynamic', ok: count(stats, 'normalPlanAfterDynamic') > 0 && count(stats, 'normalActionAfterDynamic') > 0, message: `首次动态行动后普通 plan/普通行为仍持续发生（plan=${count(stats, 'normalPlanAfterDynamic')}，action=${count(stats, 'normalActionAfterDynamic')}）` },
    { key: 'recovery_ratio', ok: recoveryRatio >= minRecoveryRatio, message: `动态行动后普通行为恢复率不低于 ${(minRecoveryRatio * 100).toFixed(1)}%（恢复 ${stats.recoveredNpcs?.size || 0}/${stats.dynamicActionNpcs?.size || 0} 人，${(recoveryRatio * 100).toFixed(1)}%）` },
  ];
  if (options.requireZeroJobFailures === true) {
    checks.push({
      key: 'zero_job_failures',
      ok: count(stats, 'jobActions.failed') === 0 && count(stats, 'jobActions.aborted') === 0 && Object.keys(stats.jobActions?.failureReasons || {}).length === 0,
      message: `Job 失败/abort 为 0（failed=${count(stats, 'jobActions.failed')}，aborted=${count(stats, 'jobActions.aborted')}，failureReasons=${JSON.stringify(stats.jobActions?.failureReasons || {})}）`,
    });
  }
  return { ok: checks.every(check => check.ok), checks, recoveryRatio };
}

export function renderGateReport({ stats, days, seeds, options }) {
  const gate = evaluateDefaultEnableGate(stats, options);
  const totalDynamicActions = count(stats, 'dynamicActions.prepare') + count(stats, 'dynamicActions.join');
  const lines = [
    '# Job/Toil 默认启用验证报告',
    '',
    '> 最后更新：2026-06-05',
    '> 来源：`apps/game/tools/verify-dynamic-goals.mjs` 真实多种子长程模拟输出。',
    '',
    '## 验证参数',
    '',
    `- 天数：${days}`,
    `- 种子：${seeds.join(', ')}`,
    `- 使用默认配置：${options.useConfigDefaults === true ? '是' : '否'}`,
    `- 最低恢复率门槛：${((options.minRecoveryRatio ?? 0.5) * 100).toFixed(1)}%`,
    `- Job 失败/abort 零容忍：${options.requireZeroJobFailures === true ? '是' : '否'}`,
    '',
    '## 观察结果',
    '',
    `- 动态事件阶段变化：${count(stats, 'phaseChanges')}`,
    `- 动态 Goal 候选观察：${count(stats, 'candidateGoalCount')}，唯一候选：${stats.uniqueCandidateKeys?.size || 0}`,
    `- 动态 Goal plan 次数：${count(stats, 'dynamicPlanCount')}`,
    `- 动态行动：准备 ${count(stats, 'dynamicActions.prepare')}（成功 ${count(stats, 'dynamicActions.prepareSucceeded')}），参与 ${count(stats, 'dynamicActions.join')}（成功 ${count(stats, 'dynamicActions.joinSucceeded')}），合计 ${totalDynamicActions}`,
    `- JobAction：planned=${count(stats, 'jobActions.planned')}，started=${count(stats, 'jobActions.started')}，completed=${count(stats, 'jobActions.completed')}，failed=${count(stats, 'jobActions.failed')}，aborted=${count(stats, 'jobActions.aborted')}`,
    `- Job 分布：${JSON.stringify(stats.jobActions?.byJobId || {})}`,
    `- Toil 分布：${JSON.stringify(stats.jobActions?.byToilId || {})}`,
    `- failureReasons：${JSON.stringify(stats.jobActions?.failureReasons || {})}`,
    `- 发生过动态行动 NPC：${stats.dynamicActionNpcs?.size || 0}`,
    `- 后续恢复普通行为 NPC：${stats.recoveredNpcs?.size || 0}`,
    `- 恢复率：${(gate.recoveryRatio * 100).toFixed(1)}%`,
    '',
    '## Gate Checks',
    '',
    ...gate.checks.map(check => `- ${check.ok ? 'PASS' : 'FAIL'} ${check.key}：${check.message}`),
    '',
    `## 结论`,
    '',
    gate.ok ? '通过默认启用门。' : '未通过默认启用门，必须先修复失败项。',
    '',
  ];
  return `${lines.join('\n')}\n`;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
node tools/test-dynamic-goals-gates.mjs
```

Expected:

- PASS: `Dynamic goal gate tests passed`.

- [ ] **Step 5: Commit**

```powershell
git add -- apps/game/tools/test-dynamic-goals-gates.mjs apps/game/tools/verify-dynamic-goals-gates.mjs
git commit -m "test: cover job toil default enable gate"
```

---

### Task 2: 接入长程验证脚本

**Files:**
- Modify: `apps/game/tools/verify-dynamic-goals.mjs`
- Test: `apps/game/tools/test-dynamic-goals-gates.mjs`

- [ ] **Step 1: 写脚本接入前失败测试**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
$env:JOBS_ACTIVE = "1"
node tools/verify-dynamic-goals.mjs --days=1 --seeds=12345 --min-recovery=0.9 --require-zero-job-failures --report=docs/superpowers/reports/test-report.md
```

Expected:

- FAIL or no `docs/superpowers/reports/test-report.md` is written, because `verify-dynamic-goals.mjs` has not yet wired the new options.

- [ ] **Step 2: 修改 fs import 与 gate import**

In `apps/game/tools/verify-dynamic-goals.mjs`, replace:

```js
import { readFileSync } from 'node:fs';
```

with:

```js
import { mkdirSync, readFileSync, writeFileSync } from 'node:fs';
```

After Node path imports, add:

```js
import {
  evaluateDefaultEnableGate,
  parseGateArgs,
  renderGateReport,
} from './verify-dynamic-goals-gates.mjs';
```

- [ ] **Step 3: 修改 enabledConfigs 支持默认配置路径**

Replace the current `enabledConfigs(seed)` function with:

```js
function enabledConfigs(seed, options = {}) {
  const configs = { ...baseConfigs(), seed };
  const useConfigDefaults = options.useConfigDefaults === true;

  if (!useConfigDefaults) {
    configs.dynamicEvents = {
      ...clone(configs.dynamicEvents),
      enabled: process.env.DYNAMIC_EVENTS_ACTIVE === '0' ? false : true,
    };
    configs.dynamicGoals = {
      ...clone(configs.dynamicGoals),
      enabled: process.env.DYNAMIC_GOALS_ACTIVE === '0' ? false : true,
    };
    configs.aiConfig = clone(configs.aiConfig);
    configs.aiConfig.npc = { ...(configs.aiConfig.npc || {}) };
    configs.aiConfig.npc.jobs = { ...(configs.aiConfig.npc.jobs || {}) };
    if (process.env.JOBS_ACTIVE === '1') {
      configs.aiConfig.npc.jobs.enabled = true;
    }
    configs.worldNews = { ...clone(configs.worldNews), enabled: true };
    configs.worldOpportunities = { ...clone(configs.worldOpportunities), enabled: true };
    configs.balanceReward = { ...clone(configs.balanceReward), enabled: true };
    return configs;
  }

  configs.dynamicEvents = clone(configs.dynamicEvents);
  configs.dynamicGoals = clone(configs.dynamicGoals);
  configs.aiConfig = clone(configs.aiConfig);
  configs.worldNews = clone(configs.worldNews);
  configs.worldOpportunities = clone(configs.worldOpportunities);
  configs.balanceReward = clone(configs.balanceReward);
  return configs;
}
```

- [ ] **Step 4: 修改参数读取和启动日志**

Replace:

```js
const { days, seeds } = parseArgs();
console.log(`[verify-dynamic-goals] seeds=${seeds.join(',')} days=${days}`);
console.log(`  环境开关：DYNAMIC_EVENTS_ACTIVE=${process.env.DYNAMIC_EVENTS_ACTIVE || '(default-on)'} DYNAMIC_GOALS_ACTIVE=${process.env.DYNAMIC_GOALS_ACTIVE || '(default-on)'} JOBS_ACTIVE=${process.env.JOBS_ACTIVE || '(config-default)'}`);
console.log('  内存覆盖开关：dynamicEvents/dynamicGoals/worldNews/worldOpportunities/balanceReward.enabled=true；jobs 仅在 JOBS_ACTIVE=1 时启用');
```

with:

```js
const { days, seeds } = parseArgs();
const gateOptions = parseGateArgs(process.argv.slice(2));
console.log(`[verify-dynamic-goals] seeds=${seeds.join(',')} days=${days}`);
console.log(`  环境开关：DYNAMIC_EVENTS_ACTIVE=${process.env.DYNAMIC_EVENTS_ACTIVE || '(default-on)'} DYNAMIC_GOALS_ACTIVE=${process.env.DYNAMIC_GOALS_ACTIVE || '(default-on)'} JOBS_ACTIVE=${process.env.JOBS_ACTIVE || '(config-default)'}`);
console.log(`  默认配置路径：${gateOptions.useConfigDefaults === true ? '是' : '否'}`);
console.log(`  启用门：minRecovery=${gateOptions.minRecoveryRatio} requireZeroJobFailures=${gateOptions.requireZeroJobFailures}`);
console.log(gateOptions.useConfigDefaults === true
  ? '  内存覆盖开关：关闭；完全使用配置文件默认 enabled 状态'
  : '  内存覆盖开关：dynamicEvents/dynamicGoals/worldNews/worldOpportunities/balanceReward.enabled=true；jobs 仅在 JOBS_ACTIVE=1 时启用');
```

Replace:

```js
const configs = enabledConfigs(seed);
```

with:

```js
const configs = enabledConfigs(seed, gateOptions);
```

- [ ] **Step 5: 用严格 gate 替换恢复率断言并写报告**

Replace the final recovery assertion:

```js
assert(
  agg.dynamicActionNpcs.size === 0 || recoveryRatio >= 0.5,
  `动态行动后大部分 NPC 可恢复普通行为（恢复 ${agg.recoveredNpcs.size}/${agg.dynamicActionNpcs.size} 人，${(recoveryRatio * 100).toFixed(1)}%）`,
);
```

with:

```js
const gate = evaluateDefaultEnableGate(agg, gateOptions);
for (const check of gate.checks) {
  assert(check.ok, check.message);
}

if (gateOptions.reportPath) {
  const reportPath = resolve(GAME_ROOT, gateOptions.reportPath);
  mkdirSync(dirname(reportPath), { recursive: true });
  writeFileSync(reportPath, renderGateReport({
    stats: agg,
    days,
    seeds,
    options: gateOptions,
  }), 'utf-8');
  console.log(`\n验证报告已写入：${gateOptions.reportPath}`);
}
```

- [ ] **Step 6: 运行局部测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
$env:JOBS_ACTIVE = "1"
node tools/test-dynamic-goals-gates.mjs
node tools/verify-dynamic-goals.mjs --days=900 --seeds=12345 --min-recovery=0.9 --require-zero-job-failures --report=docs/superpowers/reports/test-report.md
Remove-Item -LiteralPath 'docs\superpowers\reports\test-report.md' -ErrorAction SilentlyContinue
```

Expected:

- `test-dynamic-goals-gates.mjs` exits 0.
- The one-seed 900-day verification exits 0.
- `docs/superpowers/reports/test-report.md` is created during the run and removed before the step finishes.

- [ ] **Step 7: Commit**

```powershell
git add -- apps/game/tools/verify-dynamic-goals.mjs apps/game/tools/test-dynamic-goals-gates.mjs apps/game/tools/verify-dynamic-goals-gates.mjs
git commit -m "feat: add strict job toil verification gate"
```

---

### Task 3: 开启前严格长程验证

**Files:**
- Create: `docs/superpowers/reports/2026-06-05-Job-Toil启用前验证.md`
- Modify: none

- [ ] **Step 1: 运行启用前严格验证**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
$env:DYNAMIC_EVENTS_ACTIVE = "1"
$env:DYNAMIC_GOALS_ACTIVE = "1"
$env:JOBS_ACTIVE = "1"
node tools/verify-dynamic-goals.mjs --days=900 --seeds=12345,67890,24680 --min-recovery=0.9 --require-zero-job-failures --report=docs/superpowers/reports/2026-06-05-Job-Toil启用前验证.md
```

Expected:

- Exit 0.
- Output ends with `动态世界事件 / 动态 Goal 真实模拟验证通过`.
- `JobAction planned > 0`.
- `JobAction started > 0`.
- `JobAction completed > 0`.
- `failed=0`.
- `aborted=0`.
- `failureReasons={}`.
- `恢复率` is at least `90.0%`.
- Report file exists at `docs/superpowers/reports/2026-06-05-Job-Toil启用前验证.md`.

- [ ] **Step 2: 如果严格验证失败则停止**

If the command fails, stop this plan and create a focused bugfix plan from the failed gate check key. Do not flip default config switches while any strict gate fails.

- [ ] **Step 3: Commit**

```powershell
git add -- docs/superpowers/reports/2026-06-05-Job-Toil启用前验证.md
git commit -m "docs: record job toil pre-enable verification"
```

---

### Task 4: 默认启用配置与回退测试

**Files:**
- Modify: `apps/game/tools/test-job-config-load.mjs`
- Modify: `apps/game/data/config/ai-config.json`
- Modify: `apps/game/data/world/dynamic-events.json`
- Modify: `apps/game/data/goals/dynamic-goals.json`

- [ ] **Step 1: 写默认启用失败测试**

In `apps/game/tools/test-job-config-load.mjs`, add these loads after `const aiConfig = load('data/config/ai-config.json');`:

```js
const dynamicEventsConfig = load('data/world/dynamic-events.json');
const dynamicGoalsConfig = load('data/goals/dynamic-goals.json');
```

Replace:

```js
console.log('4) jobs config defaults to disabled in ai-config');
assert(aiConfig.npc.jobs.enabled === false, 'npc.jobs.enabled defaults false');
assert(aiConfig.npc.jobs.maxActiveJobsPerNpc === 1, 'maxActiveJobsPerNpc is 1');
assert(aiConfig.npc.jobs.logToilEvents === true, 'logToilEvents defaults true');
```

with:

```js
console.log('4) Job/Toil dynamic chain defaults enabled after formal launch');
assert(aiConfig.npc.jobs.enabled === true, 'npc.jobs.enabled defaults true after formal launch');
assert(dynamicEventsConfig.enabled === true, 'dynamic-events.enabled defaults true after formal launch');
assert(dynamicGoalsConfig.enabled === true, 'dynamic-goals.enabled defaults true after formal launch');
assert(aiConfig.npc.jobs.maxActiveJobsPerNpc === 1, 'maxActiveJobsPerNpc is 1');
assert(aiConfig.npc.jobs.logToilEvents === true, 'logToilEvents defaults true');
```

In section `6) WorldEngine can initialize Job/Toil configs twice in one process`, after:

```js
assert(ToilPool.getExecutor('toil_resolve_target'), 'first WorldEngine init registers resolve-target executor');
```

add:

```js
const firstNpc = firstEngine.entityRegistry.getAliveByType('npc')[0];
const enabledActionIds = new Set(firstNpc.behaviorSystem.availableActions.map(action => action.id));
assert(firstNpc.state.get('jobsEnabled') === true, 'default NPC state records jobsEnabled=true');
assert(enabledActionIds.has('act_npc_prepare_secret_realm'), 'default NPC action set includes dynamic JobAction when jobs enabled');
assert(enabledActionIds.has('act_npc_acquire_artifact'), 'default NPC action set includes economy JobAction when jobs enabled');
```

Before the final failure check, add:

```js
console.log('7) jobs.enabled=false remains a runtime rollback switch');
const disabledConfigs = JSON.parse(JSON.stringify(configs));
disabledConfigs.aiConfig.npc.jobs.enabled = false;
const disabledEngine = new WorldEngine();
disabledEngine.init(disabledConfigs);
const disabledNpc = disabledEngine.entityRegistry.getAliveByType('npc')[0];
const disabledActionIds = new Set(disabledNpc.behaviorSystem.availableActions.map(action => action.id));
assert(disabledNpc.state.get('jobsEnabled') === false, 'disabled NPC state records jobsEnabled=false');
assert(!disabledActionIds.has('act_npc_prepare_secret_realm'), 'disabled NPC action set excludes dynamic JobAction');
assert(!disabledActionIds.has('act_npc_acquire_artifact'), 'disabled NPC action set excludes economy JobAction');
assert(disabledActionIds.has('act_npc_cultivate'), 'disabled NPC action set keeps SimpleAction behavior');
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
node tools/test-job-config-load.mjs
```

Expected:

- FAIL: `npc.jobs.enabled defaults true after formal launch`.
- FAIL: `dynamic-events.enabled defaults true after formal launch`.
- FAIL: `dynamic-goals.enabled defaults true after formal launch`.

- [ ] **Step 3: 修改默认开关**

In `apps/game/data/config/ai-config.json`, change:

```json
"_comment": "Job/Toil 复杂行动编排层。默认关闭；开启后 NPC 默认行为集加入 npc-job-actions.json 中的 JobAction。",
"enabled": false,
```

to:

```json
"_comment": "Job/Toil 复杂行动编排层。正式启用后默认开启；如需回退可改为 false，关闭后 NPC 默认行为集不加入 npc-job-actions.json 中的 JobAction。",
"enabled": true,
```

In `apps/game/data/world/dynamic-events.json`, change:

```json
"_description": "动态世界事件配置：事件有预告、开启、结算窗口，可产生 NPC 动态目标。默认 enabled=false，关闭时不改变现有行为。",
"enabled": false,
```

to:

```json
"_description": "动态世界事件配置：事件有预告、开启、结算窗口，可产生 NPC 动态目标。正式启用后默认 enabled=true；如需回退可改为 false。",
"enabled": true,
```

In `apps/game/data/goals/dynamic-goals.json`, change:

```json
"_description": "动态目标配置：事件被 NPC 感知后，在指定阶段临时产出 Goal。默认 enabled=false，关闭时不改变现有行为。",
"enabled": false,
```

to:

```json
"_description": "动态目标配置：事件被 NPC 感知后，在指定阶段临时产出 Goal。正式启用后默认 enabled=true；如需回退可改为 false。",
"enabled": true,
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
node tools/test-job-config-load.mjs
```

Expected:

- PASS: `Job config load tests passed`.

- [ ] **Step 5: Commit**

```powershell
git add -- apps/game/tools/test-job-config-load.mjs apps/game/data/config/ai-config.json apps/game/data/world/dynamic-events.json apps/game/data/goals/dynamic-goals.json
git commit -m "feat: enable job toil dynamic chain by default"
```

---

### Task 5: 默认配置路径长程验证

**Files:**
- Create: `docs/superpowers/reports/2026-06-05-Job-Toil默认启用验证.md`

- [ ] **Step 1: 运行默认配置路径验证**

Run without `JOBS_ACTIVE`, `DYNAMIC_EVENTS_ACTIVE`, or `DYNAMIC_GOALS_ACTIVE`:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
Remove-Item Env:JOBS_ACTIVE -ErrorAction SilentlyContinue
Remove-Item Env:DYNAMIC_EVENTS_ACTIVE -ErrorAction SilentlyContinue
Remove-Item Env:DYNAMIC_GOALS_ACTIVE -ErrorAction SilentlyContinue
node tools/verify-dynamic-goals.mjs --days=900 --seeds=12345,67890,24680 --use-config-defaults --min-recovery=0.9 --require-zero-job-failures --report=docs/superpowers/reports/2026-06-05-Job-Toil默认启用验证.md
```

Expected:

- Exit 0.
- Output says `默认配置路径：是`.
- Output ends with `动态世界事件 / 动态 Goal 真实模拟验证通过`.
- `failed=0`.
- `aborted=0`.
- `failureReasons={}`.
- Recovery ratio is at least `90.0%`.
- Report file exists at `docs/superpowers/reports/2026-06-05-Job-Toil默认启用验证.md`.

- [ ] **Step 2: Commit**

```powershell
git add -- docs/superpowers/reports/2026-06-05-Job-Toil默认启用验证.md
git commit -m "docs: record job toil default verification"
```

---

### Task 6: 文档状态同步

**Files:**
- Modify: `docs/systems/job-toil-ai-spec.md`
- Modify: `docs/decisions/adr-050-goap-job-toil-layered-ai.md`
- Modify: `docs/data/data-config-rules.md`
- Modify: `docs/README.md`

- [ ] **Step 1: 更新系统规格状态**

In `docs/systems/job-toil-ai-spec.md`, replace the status line:

```markdown
> 状态：首批 Job/Toil 运行时已实现；`ai-config.npc.jobs.enabled=true` 时启用  
```

with:

```markdown
> 状态：首批 Job/Toil 动态目标链路已正式默认启用；`ai-config.npc.jobs.enabled=false` 可作为回退开关  
```

Replace the config switch paragraph:

```markdown
默认 `enabled=false`。关闭时：
```

with:

```markdown
正式启用后默认 `enabled=true`。回退为 `false` 时：
```

Under the verification section, add:

```markdown
2026-06-05 默认配置路径验证：

- 验证报告：`docs/superpowers/reports/2026-06-05-Job-Toil默认启用验证.md`。
- 命令不依赖 `JOBS_ACTIVE`、`DYNAMIC_EVENTS_ACTIVE`、`DYNAMIC_GOALS_ACTIVE` 环境变量。
- 验收门槛：900 天、3 种子、Job 失败/abort 为 0、动态行动后普通行为恢复率不低于 90%。
- 验证方式仍是完整模拟行为观察，不使用固定摘要一致性证明。
```

- [ ] **Step 2: 更新 ADR-050 状态**

In `docs/decisions/adr-050-goap-job-toil-layered-ai.md`, replace:

```markdown
> 状态：首批 Job/Toil 分层已实施，进入真实模拟验证与后续扩展阶段  
```

with:

```markdown
> 状态：首批 Job/Toil 动态目标链路已正式默认启用（2026-06-05）  
```

In `## 实施状态`, add:

```markdown
2026-06-05 默认启用收尾已覆盖：

- `ai-config.npc.jobs.enabled=true`、`dynamic-events.enabled=true`、`dynamic-goals.enabled=true` 组成默认体验链路。
- `jobs.enabled=false` 仍保留为运行时回退开关；关闭后默认 NPC 行为集不追加 JobAction。
- 默认配置路径已通过 900 天、3 种子长程验证；报告见 `docs/superpowers/reports/2026-06-05-Job-Toil默认启用验证.md`。
- 动态事件内容库扩展、PvP 真实扣血和反击触发仍是 ADR-050 之后的独立后续阶段，不阻塞本 ADR 的默认启用。
```

- [ ] **Step 3: 更新数据配置规则**

In `docs/data/data-config-rules.md`, replace:

```markdown
4. `ai-config.npc.jobs.enabled` 默认保持 `false`；开启后 NPC 默认行为集才追加 `defaultNpcJobActionIds`。
```

with:

```markdown
4. 正式启用后 `ai-config.npc.jobs.enabled` 默认保持 `true`；如需回退可改为 `false`，关闭后 NPC 默认行为集不追加 `defaultNpcJobActionIds`。
```

Add after the dynamic event Job rules:

```markdown
9. `dynamic-events.enabled`、`dynamic-goals.enabled` 与 `ai-config.npc.jobs.enabled` 共同决定 Job/Toil 动态目标链路是否进入默认体验；正式启用后三者默认均为 `true`，回退时应在验证报告或 ADR 中说明关闭范围。
```

- [ ] **Step 4: 更新文档导航**

In `docs/README.md`, update the last updated line to mention default enable verification:

```markdown
> 最后更新：2026-06-05（Job/Toil 动态目标链路默认启用验证；新增默认启用验证报告；补充 StoryGraph 小说图谱设计规格、单作品本地索引库规格与单作品 MVP 实施计划）
```

In the document tree under `docs/superpowers/`, add:

```text
│   └── reports/
│       ├── 2026-06-05-Job-Toil启用前验证.md
│       └── 2026-06-05-Job-Toil默认启用验证.md
```

In key documents, replace the Job/Toil line with:

```markdown
| Job/Toil 正式启用 | `superpowers/specs/2026-06-05-Job-Toil正式启用前收尾规格.md`、`superpowers/plans/2026-06-05-ADR-050-收尾实施计划.md`、`superpowers/reports/2026-06-05-Job-Toil默认启用验证.md` |
```

- [ ] **Step 5: Commit**

```powershell
git add -- docs/systems/job-toil-ai-spec.md docs/decisions/adr-050-goap-job-toil-layered-ai.md docs/data/data-config-rules.md docs/README.md
git commit -m "docs: mark job toil chain default enabled"
```

---

### Task 7: 完整回归验证

**Files:**
- Modify: none

- [ ] **Step 1: 运行 Job/Toil 局部测试**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
node tools/test-job-pool.mjs
node tools/test-toil-pool.mjs
node tools/test-job-system.mjs
node tools/test-job-config-load.mjs
node tools/test-economy-toils.mjs
node tools/test-job-action-planning.mjs
node tools/test-dynamic-event-jobs.mjs
node tools/test-job-interrupt-resume.mjs
node tools/test-dynamic-goals-gates.mjs
```

Expected:

- Every script exits 0.

- [ ] **Step 2: 运行动态目标默认配置验证**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
Remove-Item Env:JOBS_ACTIVE -ErrorAction SilentlyContinue
Remove-Item Env:DYNAMIC_EVENTS_ACTIVE -ErrorAction SilentlyContinue
Remove-Item Env:DYNAMIC_GOALS_ACTIVE -ErrorAction SilentlyContinue
node tools/verify-dynamic-goals.mjs --days=900 --seeds=12345,67890,24680 --use-config-defaults --min-recovery=0.9 --require-zero-job-failures
```

Expected:

- Exit 0.
- Output ends with `动态世界事件 / 动态 Goal 真实模拟验证通过`.

- [ ] **Step 3: 搜索禁止项**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null
rg -n "item_heal_pill|item_artifact_low" apps docs
```

Expected:

- No matches in active runtime files.
- Historical ADR or old superseded plans may still mention old IDs only as explicit history. If matches appear in active configs, tests, or current system docs, fix them before completion.

- [ ] **Step 4: Commit final verification metadata only if files changed**

If no files changed, skip this commit. If report timestamps or docs changed, run:

```powershell
git add -- docs/superpowers/reports docs/systems/job-toil-ai-spec.md docs/decisions/adr-050-goap-job-toil-layered-ai.md docs/data/data-config-rules.md docs/README.md
git commit -m "docs: finalize job toil enable verification"
```

---

## Out Of Scope Follow-Ups

Do not implement these in this plan:

- Dynamic event content library expansion: auction, companion exploration, alchemy, treasure birth, sect task chains.
- PvP real HP damage and counterattack source propagation.

After this plan is complete, create separate specs and plans for those two areas. Each follow-up must have its own tests and long-run observation metrics.

## Self-Review

- Spec coverage：覆盖物品引用和事件类型绑定的当前已完成基线，剩余计划聚焦严格启用门、默认开关、默认路径验证和文档同步。
- Spec coverage：默认启用不是只改 `ai-config.npc.jobs.enabled`；计划同时处理 `dynamic-events.enabled`、`dynamic-goals.enabled` 和 JobAction 默认行为集。
- Spec coverage：内容库扩展与 PvP 真实扣血明确拆出，不阻塞本计划。
- 占位内容扫描：没有空章节或需要手填的统计值；执行者观察的统计值由 `--report` 自动生成。
- Type consistency：统一使用 `dynamicEventIsSecretRealm`、`dynamicEventIsSectTournament`、`dynamicEventUsesGenericPreparation`、`preparedForSecretRealm`、`preparedForSectTournament`、`preparedForGenericDynamicEvent`。
