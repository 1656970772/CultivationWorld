# 统一经济交易底座 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立第一版统一经济交易底座，让资产读写、估价、托管、原子交割、账本、债务和经济信号成为任务、贡献兑换、材料上交、直接买卖和抽象拍卖的共同结算入口。

**Architecture:** 新增 `engine/economy/` 平台模块，采用门面 + 适配器 + 服务组合：`EconomicSystem` 暴露统一端口，`AssetAdapter` 处理 NPC inventory、势力 state、组织点数三类资产，`SettlementService` 原子交割，`EscrowService` 托管真实资产，`TransactionLedger` 和 `DebtService` 保存证据与逾期状态。现有 NPC Job/Toil、任务交付和抽象拍卖只调用经济端口，不直接互相耦合。

**Tech Stack:** JavaScript ES modules、JSON 数据配置、Node.js 工具测试、现有 WorldEngine / Inventory / ItemRegistry / Job-Toil / DynamicEvent。

**最后更新:** 2026-06-07

---

## 文件结构

新增文件：

- `apps/game/js/engine/economy/asset-adapter.js`：统一资产读写端口，处理 `item`、`faction_state_resource`、`organization_point`，并提供可回滚 delta。
- `apps/game/js/engine/economy/price-service.js`：读取物品 `value` 和场景倍率，计算成交参考价、以物易物估值和私人交易偏差。
- `apps/game/js/engine/economy/transaction-ledger.js`：保存 `TransactionRecord`，按系统真相层、机构/公开层、个人认知层过滤。
- `apps/game/js/engine/economy/settlement-service.js`：校验资产、按顺序移出和移入、失败回滚，返回每个资产 delta。
- `apps/game/js/engine/economy/escrow-service.js`：创建、锁定、释放、罚没和交割托管资产。
- `apps/game/js/engine/economy/debt-service.js`：创建、偿还、逾期、豁免、争议和 `converted_to_quest` 债务状态。
- `apps/game/js/engine/economy/economic-signal-provider.js`：输出 `canAfford`、`canEscrow`、`estimatedPrice`、`hasOverdueDebt`、`hasEscrowPending` 等 AI 可消费信号。
- `apps/game/js/engine/economy/transaction-engine.js`：统一门面，组合 price/escrow/settlement/ledger/debt/signal，暴露 `quote`、`settle`、`openEscrow`、`settleEscrow`、`createDebt`。
- `apps/game/js/engine/economy/auction-service.js`：无玩家到场抽象拍卖结算，生成拍品、参与者、赢家、成交/流拍、财富曝光和账本。
- `apps/game/data/economy/transaction-scenarios.json`：场景倍率、托管默认值、正式/私人交易规则、拍卖参数和债务逾期参数。
- `apps/game/tools/test-economic-asset-adapter.mjs`：资产适配器与势力 state 单一真相源测试。
- `apps/game/tools/test-economic-transaction-platform.mjs`：估价、原子交割、账本、托管、债务、信号单元测试。
- `apps/game/tools/test-economic-integration.mjs`：材料上交、贡献兑换、直接购买、任务奖励接入测试。
- `apps/game/tools/test-economic-auction-abstract.mjs`：无玩家参与抽象拍卖结算测试。
- `docs/systems/economic-transaction-system.md`：系统设计文档。
- `docs/data-models/economic-transaction.md`：`TransactionRecord`、`EscrowEntry`、`DebtRecord`、`AssetSpec` 数据模型。
- `docs/superpowers/reports/2026-06-07-统一经济交易底座验证.md`：实现完成后的验证记录。

修改文件：

- `apps/game/js/core/config-loader.js`：加载 `data/economy/transaction-scenarios.json` 为 `economicTransactionConfig`。
- `apps/game/js/engine/world-engine.js`：初始化 `EconomicSystem`，注入 `TickManager` 和快照。
- `apps/game/js/engine/world/services/world-context-builder.js`：在 `worldContext` 暴露 `economicSystem`、`settleTransaction`、`economicSignalsFor`。
- `apps/game/js/engine/npc/npc-economy.js`：材料上交、贡献兑换改用经济底座；保留丹药使用入口。
- `apps/game/js/engine/npc/toils/economy-toils.js`：直接购买、贡献兑换 Toil 改用 `worldContext.settleTransaction`，输出交易记录 ID。
- `apps/game/js/engine/npc/services/quest-service.js`：任务交付奖励、悬赏阁付款、宗门收益和贡献发放通过经济底座结算。
- `apps/game/js/engine/world/world-event.js`：允许拍卖事件在到期时交给 `AuctionService` 抽象结算。
- `apps/game/data/world/dynamic-events.json`：新增一个拍卖会预设事件，使用 `type:"auction"`。
- `apps/game/js/engine/world/tick-manager.js`：每日推进债务逾期和抽象拍卖到期结算。
- `apps/game/js/engine/world/services/faction-ai-service.js`：势力贸易调用经济底座时保持 `state` 真相源，不再直接写普通 inventory。
- `docs/data/data-config-rules.md`：新增 `data/economy/` 目录与字段规范。
- `docs/architecture/file-structure.md`：补充 `engine/economy/` 和 `data/economy/`。
- `docs/README.md`：新增实施计划、系统文档、数据模型和验证报告导航。

---

### Task 1: 写经济底座 RED 测试

**Files:**
- Create: `apps/game/tools/test-economic-asset-adapter.mjs`
- Create: `apps/game/tools/test-economic-transaction-platform.mjs`
- Create: `apps/game/tools/test-economic-integration.mjs`
- Create: `apps/game/tools/test-economic-auction-abstract.mjs`

- [x] **Step 1: 写资产适配器失败测试**

```javascript
#!/usr/bin/env node
import { fileURLToPath, pathToFileURL } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const imp = (p) => import(pathToFileURL(resolve(GAME_ROOT, p)).href);

const { Inventory } = await imp('js/engine/abstract/inventory.js');
const { RuntimeState } = await imp('js/engine/abstract/runtime-state.js');
const { AssetAdapter } = await imp('js/engine/economy/asset-adapter.js');

let failed = 0;
function ok(condition, message) {
  console.log(`  ${condition ? 'OK' : 'FAIL'}: ${message}`);
  if (!condition) failed++;
}

function entity(id, inventory = {}, state = {}) {
  const e = {
    id,
    name: id,
    inventory: new Inventory(),
    state: new RuntimeState(state),
    alive: true,
  };
  e.inventory.loadFrom(inventory);
  return e;
}

console.log('1) NPC inventory item asset');
{
  const npc = entity('npc_a', { low_spirit_stone: 100, mat_century_ginseng: 2 });
  const adapter = new AssetAdapter();
  ok(adapter.amountOf(npc, { kind: 'item', itemId: 'low_spirit_stone' }) === 100, '读取 NPC 背包灵石');
  const delta = adapter.withdraw(npc, { kind: 'item', itemId: 'low_spirit_stone', quantity: 30 }, { reason: 'test' });
  ok(delta.success === true, 'NPC 背包可扣除资产');
  ok(npc.inventory.getAmount('low_spirit_stone') === 70, '扣除后数量减少');
  adapter.rollback(delta);
  ok(npc.inventory.getAmount('low_spirit_stone') === 100, 'rollback 恢复 NPC 背包');
}

console.log('2) faction state resource uses state as truth source');
{
  const faction = entity('sect_a', { low_spirit_stone: 5000 }, { low_spirit_stone: 4200, food: 800, disciples: 100 });
  const adapter = new AssetAdapter();
  ok(adapter.amountOf(faction, { kind: 'faction_state_resource', itemId: 'low_spirit_stone' }) === 4200, '势力灵石读取 state');
  const delta = adapter.withdraw(faction, { kind: 'faction_state_resource', itemId: 'low_spirit_stone', quantity: 300 }, { reason: 'trade' });
  ok(delta.success === true, '势力 state 资源可扣除');
  ok(faction.state.get('low_spirit_stone') === 3900, '扣除写 state');
  ok(faction.inventory.getAmount('low_spirit_stone') === 5000, '扣除不直接改 inventory，避免双轨');
  adapter.deposit(faction, { kind: 'faction_state_resource', itemId: 'food', quantity: 120 }, { reason: 'trade' });
  ok(faction.state.get('food') === 920, '势力粮食收入写 state');
  adapter.rollback(delta);
  ok(faction.state.get('low_spirit_stone') === 4200, 'rollback 恢复势力 state');
}

console.log('3) organization point cannot be transferred privately');
{
  const npc = entity('npc_b', {}, { contribution: 20 });
  const adapter = new AssetAdapter();
  ok(adapter.amountOf(npc, { kind: 'organization_point', pointKey: 'contribution' }) === 20, '读取贡献点');
  ok(adapter.isPrivatelyTransferable({ kind: 'organization_point', pointKey: 'contribution' }) === false, '贡献点不可私下转让');
}

if (failed > 0) {
  console.error(`\n经济资产适配器测试失败：${failed} 项`);
  process.exit(1);
}
console.log('\n经济资产适配器测试通过');
```

- [x] **Step 2: 写交易平台失败测试**

```javascript
#!/usr/bin/env node
import { readFileSync } from 'node:fs';
import { fileURLToPath, pathToFileURL } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const load = (p) => JSON.parse(readFileSync(resolve(GAME_ROOT, p), 'utf-8'));
const imp = (p) => import(pathToFileURL(resolve(GAME_ROOT, p)).href);

const { Inventory } = await imp('js/engine/abstract/inventory.js');
const { RuntimeState } = await imp('js/engine/abstract/runtime-state.js');
const { ItemRegistry } = await imp('js/engine/items/item-registry.js');
const { EconomicSystem } = await imp('js/engine/economy/transaction-engine.js');

ItemRegistry.clear();
ItemRegistry.loadFromArray(load('data/definitions/macro-resources.json'));
ItemRegistry.loadFromArray(['currency', 'material', 'pill', 'artifact', 'talisman', 'technique'].flatMap(c => load(`data/items/${c}.json`).items));

const config = load('data/economy/transaction-scenarios.json');
let failed = 0;
function ok(condition, message) {
  console.log(`  ${condition ? 'OK' : 'FAIL'}: ${message}`);
  if (!condition) failed++;
}

function actor(id, inventory = {}, state = {}) {
  const entity = { id, name: id, inventory: new Inventory(), state: new RuntimeState(state), alive: true };
  entity.inventory.loadFrom(inventory);
  return entity;
}

console.log('1) formal direct sale is atomic and writes ledger');
{
  const buyer = actor('npc_buyer', { low_spirit_stone: 200 });
  const seller = actor('org_market', { item_qi_pill: 2 });
  const economy = new EconomicSystem({ config });
  const result = economy.settle({
    type: 'direct_purchase',
    scenarioId: 'formal_market',
    day: 10,
    parties: [
      { role: 'buyer', entity: buyer },
      { role: 'seller', entity: seller },
    ],
    transfers: [
      { from: 'buyer', to: 'seller', asset: { kind: 'item', itemId: 'low_spirit_stone', quantity: 30 } },
      { from: 'seller', to: 'buyer', asset: { kind: 'item', itemId: 'item_qi_pill', quantity: 1 } },
    ],
    source: { type: 'test', id: 'direct_sale' },
    visibility: 'institution',
  });

  ok(result.success === true, '正式买卖结算成功');
  ok(buyer.inventory.getAmount('low_spirit_stone') === 170, '买方扣灵石');
  ok(buyer.inventory.getAmount('item_qi_pill') === 1, '买方获得丹药');
  ok(seller.inventory.getAmount('low_spirit_stone') === 30, '卖方获得灵石');
  ok(economy.ledger.all().length === 1, '写入一条交易账本');
  ok(economy.ledger.all()[0].status === 'settled', '账本状态 settled');
}

console.log('2) failed settlement rolls back every prior delta');
{
  const buyer = actor('npc_buyer', { low_spirit_stone: 200 });
  const seller = actor('org_market', { item_qi_pill: 0 });
  const economy = new EconomicSystem({ config });
  const result = economy.settle({
    type: 'direct_purchase',
    scenarioId: 'formal_market',
    day: 10,
    parties: [{ role: 'buyer', entity: buyer }, { role: 'seller', entity: seller }],
    transfers: [
      { from: 'buyer', to: 'seller', asset: { kind: 'item', itemId: 'low_spirit_stone', quantity: 30 } },
      { from: 'seller', to: 'buyer', asset: { kind: 'item', itemId: 'item_qi_pill', quantity: 1 } },
    ],
    source: { type: 'test', id: 'rollback' },
  });
  ok(result.success === false, '卖方资产不足时失败');
  ok(buyer.inventory.getAmount('low_spirit_stone') === 200, '失败后买方灵石恢复');
  ok(seller.inventory.getAmount('low_spirit_stone') === 0, '失败后卖方未收到灵石');
  ok(economy.ledger.all()[0].status === 'failed', '失败也写入证据账本');
}

console.log('3) escrow can release, forfeit and generate debt');
{
  const payer = actor('npc_payer', { low_spirit_stone: 100 });
  const receiver = actor('npc_receiver', {});
  const hall = actor('org_quest_hall', {});
  const economy = new EconomicSystem({ config });
  const escrow = economy.openEscrow({
    day: 1,
    purpose: 'quest_reward',
    sourceEntity: payer,
    escrowHolder: hall,
    nominalOwnerId: payer.id,
    assets: [{ kind: 'item', itemId: 'low_spirit_stone', quantity: 60 }],
    source: { type: 'quest', id: 'quest_1' },
  });
  ok(escrow.success === true, '托管创建成功');
  ok(payer.inventory.getAmount('low_spirit_stone') === 40, '托管资产真实移出付款方');
  ok(hall.inventory.getAmount('low_spirit_stone') === 60, '托管资产进入托管方');
  const release = economy.settleEscrow({
    day: 2,
    escrowId: escrow.escrowId,
    destination: receiver,
    status: 'released',
    source: { type: 'quest', id: 'quest_1' },
  });
  ok(release.success === true, '托管释放成功');
  ok(receiver.inventory.getAmount('low_spirit_stone') === 60, '收款方获得托管资产');
  ok(hall.inventory.getAmount('low_spirit_stone') === 0, '托管方资产移出');

  const debt = economy.createDebt({
    day: 3,
    debtorId: payer.id,
    creditorId: receiver.id,
    origin: { type: 'test', id: 'debt_1' },
    dueDay: 5,
    assetsDue: [{ kind: 'item', itemId: 'low_spirit_stone', quantity: 20 }],
    visibility: 'personal',
  });
  ok(debt.status === 'active', '债务创建为 active');
  economy.advanceDay(6);
  ok(economy.debts.byId(debt.id).status === 'overdue', '到期未还变为 overdue');
  ok(economy.signalsFor({ actor: payer }).facts.hasOverdueDebt === true, '逾期债务输出经济信号');
}

if (failed > 0) {
  console.error(`\n经济交易平台测试失败：${failed} 项`);
  process.exit(1);
}
console.log('\n经济交易平台测试通过');
```

- [x] **Step 3: 写现有经济链路接入失败测试**

```javascript
#!/usr/bin/env node
import { readFileSync } from 'node:fs';
import { fileURLToPath, pathToFileURL } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const load = (p) => JSON.parse(readFileSync(resolve(GAME_ROOT, p), 'utf-8'));
const imp = (p) => import(pathToFileURL(resolve(GAME_ROOT, p)).href);

const { Inventory } = await imp('js/engine/abstract/inventory.js');
const { RuntimeState } = await imp('js/engine/abstract/runtime-state.js');
const { ItemRegistry } = await imp('js/engine/items/item-registry.js');
const { EconomicSystem } = await imp('js/engine/economy/transaction-engine.js');
const { donateMaterials, redeemExchangeItem } = await imp('js/engine/npc/npc-economy.js');

ItemRegistry.clear();
ItemRegistry.loadFromArray(load('data/definitions/macro-resources.json'));
ItemRegistry.loadFromArray(['currency', 'material', 'pill', 'artifact', 'talisman', 'technique'].flatMap(c => load(`data/items/${c}.json`).items));

const economyConfig = load('data/balance/economy.json');
const economicTransactionConfig = load('data/economy/transaction-scenarios.json');
let failed = 0;
function ok(condition, message) {
  console.log(`  ${condition ? 'OK' : 'FAIL'}: ${message}`);
  if (!condition) failed++;
}

function entity(id, inventory = {}, state = {}) {
  const e = { id, name: id, inventory: new Inventory(), state: new RuntimeState(state), alive: true };
  e.inventory.loadFrom(inventory);
  return e;
}

function world(faction, economicSystem) {
  return {
    currentDay: 20,
    balanceConfig: { economy: economyConfig },
    economicSystem,
    settleTransaction(input) { return economicSystem.settle(input); },
    economicSignalsFor(input) { return economicSystem.signalsFor(input); },
    entityRegistry: { getById(id) { return id === faction.id ? faction : null; } },
  };
}

console.log('1) material donation writes transaction ledger');
{
  const npc = entity('npc_donor', { mat_century_ginseng: 2 }, { factionId: 'sect_test', hasFaction: true, contribution: 0, monthlyContribution: 0 });
  const faction = entity('sect_test', {}, { low_spirit_stone: 1000, food: 100, disciples: 10 });
  const economicSystem = new EconomicSystem({ config: economicTransactionConfig });
  const result = donateMaterials(npc, world(faction, economicSystem));
  ok(result.success === true, '材料上交成功');
  ok(result.transactionId, '材料上交返回交易记录 id');
  ok(economicSystem.ledger.all().some(r => r.type === 'material_donation'), '账本记录材料上交');
  ok(npc.state.get('contribution') > 0, '贡献由组织发放');
}

console.log('2) faction exchange writes point and item transfer ledger');
{
  const npc = entity('npc_redeem', { low_spirit_stone: 100 }, { factionId: 'sect_test', hasFaction: true, contribution: 20 });
  const faction = entity('sect_test', { mat_century_ginseng: 1 }, {});
  const economicSystem = new EconomicSystem({ config: economicTransactionConfig });
  const result = redeemExchangeItem(npc, world(faction, economicSystem), 'qi_pill');
  ok(result.success === true, '贡献兑换成功');
  ok(result.transactionId, '贡献兑换返回交易记录 id');
  ok(economicSystem.ledger.all().some(r => r.type === 'contribution_exchange'), '账本记录贡献兑换');
  ok(npc.inventory.getAmount('item_qi_pill') === 1, 'NPC 获得兑换物品');
  ok(npc.state.get('contribution') === 12, '贡献点扣除');
}

if (failed > 0) {
  console.error(`\n经济接入测试失败：${failed} 项`);
  process.exit(1);
}
console.log('\n经济接入测试通过');
```

- [x] **Step 4: 写抽象拍卖失败测试**

```javascript
#!/usr/bin/env node
import { readFileSync } from 'node:fs';
import { fileURLToPath, pathToFileURL } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const GAME_ROOT = resolve(__dirname, '..');
const load = (p) => JSON.parse(readFileSync(resolve(GAME_ROOT, p), 'utf-8'));
const imp = (p) => import(pathToFileURL(resolve(GAME_ROOT, p)).href);

const { Inventory } = await imp('js/engine/abstract/inventory.js');
const { RuntimeState } = await imp('js/engine/abstract/runtime-state.js');
const { ItemRegistry } = await imp('js/engine/items/item-registry.js');
const { EconomicSystem } = await imp('js/engine/economy/transaction-engine.js');
const { AuctionService } = await imp('js/engine/economy/auction-service.js');

ItemRegistry.clear();
ItemRegistry.loadFromArray(load('data/definitions/macro-resources.json'));
ItemRegistry.loadFromArray(['currency', 'material', 'pill', 'artifact', 'talisman', 'technique'].flatMap(c => load(`data/items/${c}.json`).items));

const config = load('data/economy/transaction-scenarios.json');
let failed = 0;
function ok(condition, message) {
  console.log(`  ${condition ? 'OK' : 'FAIL'}: ${message}`);
  if (!condition) failed++;
}

function actor(id, inventory = {}, state = {}) {
  const e = { id, name: id, inventory: new Inventory(), state: new RuntimeState(state), alive: true };
  e.inventory.loadFrom(inventory);
  return e;
}

console.log('1) no-player auction resolves by abstract bidding and ledger');
{
  const house = actor('org_auction', { item_breakthrough_pill: 1 });
  const rich = actor('npc_rich', { low_spirit_stone: 1000 }, { ambition: 80 });
  const poor = actor('npc_poor', { low_spirit_stone: 50 }, { ambition: 100 });
  const economy = new EconomicSystem({ config });
  const auction = new AuctionService({ economicSystem: economy, config });
  const result = auction.resolveAbstractAuction({
    day: 30,
    event: { id: 'evt_auction_test', name: '测试拍卖会', type: 'auction' },
    auctionHouse: house,
    lots: [{ itemId: 'item_breakthrough_pill', quantity: 1, reservePrice: 100 }],
    participants: [rich, poor],
    rng: { next: () => 0.2 },
  });
  ok(result.success === true, '抽象拍卖结算成功');
  ok(result.lots[0].status === 'sold', '拍品成交');
  ok(result.lots[0].winnerId === 'npc_rich', '有支付能力的参与者胜出');
  ok(rich.inventory.getAmount('item_breakthrough_pill') === 1, '赢家获得拍品');
  ok(rich.inventory.getAmount('low_spirit_stone') < 1000, '赢家支付灵石');
  ok(economy.ledger.all().some(r => r.type === 'auction_sale'), '账本记录拍卖成交');
  ok(result.wealthExposureEvents.length >= 1, '高价成交产生财富曝光事件');
}

if (failed > 0) {
  console.error(`\n抽象拍卖测试失败：${failed} 项`);
  process.exit(1);
}
console.log('\n抽象拍卖测试通过');
```

- [x] **Step 5: 运行 RED 测试并确认失败原因**

Run:

```powershell
cd apps/game
node tools/test-economic-asset-adapter.mjs
node tools/test-economic-transaction-platform.mjs
node tools/test-economic-integration.mjs
node tools/test-economic-auction-abstract.mjs
```

Expected:

```text
ERR_MODULE_NOT_FOUND ... js/engine/economy/asset-adapter.js
```

---

### Task 2: 增加经济交易数据配置与加载

**Files:**
- Create: `apps/game/data/economy/transaction-scenarios.json`
- Modify: `apps/game/js/core/config-loader.js`
- Modify: `docs/data/data-config-rules.md`

- [x] **Step 1: 新增交易场景配置**

Create `apps/game/data/economy/transaction-scenarios.json`:

```json
{
  "_description": "统一经济交易底座 V1 配置：场景倍率、托管规则、债务规则和抽象拍卖参数。",
  "currencyItemId": "low_spirit_stone",
  "scenarios": {
    "formal_market": {
      "name": "正规坊市买卖",
      "kind": "formal",
      "priceMultiplier": 1.0,
      "escrowRequired": false,
      "allowDefault": false,
      "visibility": "institution"
    },
    "faction_exchange": {
      "name": "宗门贡献兑换",
      "kind": "formal",
      "priceMultiplier": 0.85,
      "escrowRequired": false,
      "allowDefault": false,
      "visibility": "institution"
    },
    "material_donation": {
      "name": "材料上交",
      "kind": "formal",
      "priceMultiplier": 1.0,
      "escrowRequired": false,
      "allowDefault": false,
      "visibility": "institution"
    },
    "quest_contract": {
      "name": "任务契约",
      "kind": "formal",
      "priceMultiplier": 1.0,
      "escrowRequired": true,
      "allowDefault": false,
      "visibility": "institution"
    },
    "private_trade": {
      "name": "私人交易",
      "kind": "private",
      "priceMultiplier": 1.0,
      "escrowRequired": false,
      "allowDefault": true,
      "visibility": "personal",
      "privateVariance": { "min": 0.75, "max": 1.45 }
    },
    "auction": {
      "name": "拍卖",
      "kind": "formal",
      "priceMultiplier": 1.25,
      "escrowRequired": true,
      "allowDefault": true,
      "visibility": "public"
    }
  },
  "assets": {
    "factionStateResourceIds": ["low_spirit_stone", "food", "disciples"],
    "organizationPointKeys": ["contribution", "warMerit", "sectCredit"],
    "privateForbiddenKinds": ["organization_point"],
    "collateralAllowedItemCategories": ["currency", "material", "pill", "artifact", "technique"]
  },
  "escrow": {
    "defaultHolderByScenario": {
      "quest_contract": "org_bounty",
      "auction": "org_auction"
    }
  },
  "debt": {
    "defaultDueDays": 30,
    "overdueSignalWeight": 1.0
  },
  "auction": {
    "abstractBid": {
      "minParticipants": 2,
      "maxParticipants": 8,
      "startingMultiplier": 0.85,
      "winnerMultiplierMin": 1.05,
      "winnerMultiplierMax": 1.65,
      "wealthExposureThreshold": 300,
      "reservePriceMultiplier": 0.8
    },
    "defaultLots": [
      { "itemId": "item_qi_pill", "quantity": 2, "reservePrice": 30 },
      { "itemId": "item_breakthrough_pill", "quantity": 1, "reservePrice": 180 },
      { "itemId": "artifact_green_sword", "quantity": 1, "reservePrice": 280 }
    ]
  }
}
```

- [x] **Step 2: 在 ConfigLoader 中加载配置**

Modify `apps/game/js/core/config-loader.js`:

```javascript
// Promise.all 解构列表中，在 balanceCovet 之后追加：
economicTransactionConfig,

// Promise.all 路径列表中，在 data/balance/covet.json 之后追加：
loadJSON('data/economy/transaction-scenarios.json'),

// return 对象中追加：
economicTransactionConfig,

// GameConfigs typedef 中追加：
* @property {Object} economicTransactionConfig 统一经济交易底座配置
```

- [x] **Step 3: 更新数据规范**

Modify `docs/data/data-config-rules.md`:

```markdown
├── economy/
│   └── transaction-scenarios.json
```

在 `## relationships/` 之前新增：

```markdown
## economy/

`economy/transaction-scenarios.json` 是统一经济交易底座的运行时规则目录。代码只负责执行资产校验、托管、结算、账本、债务和信号；场景倍率、正式/私人规则、拍卖参数和可抵押资产范围由本文件提供。

| 字段 | 说明 |
|------|------|
| `currencyItemId` | 第一版基础计价货币，当前为 `low_spirit_stone` |
| `scenarios` | 交易场景定义；key 使用 snake_case |
| `scenarios.*.kind` | `formal` 或 `private`；正式交易默认稳定，私人交易允许违约 |
| `scenarios.*.priceMultiplier` | 基础 `value` 的场景倍率 |
| `scenarios.*.escrowRequired` | 是否默认要求托管 |
| `assets.factionStateResourceIds` | 以势力 `state` 为真相源的资源键 |
| `assets.organizationPointKeys` | 贡献、战功、宗门信用等组织点数 |
| `escrow.defaultHolderByScenario` | 场景默认托管机构 |
| `debt.defaultDueDays` | 债务默认到期天数 |
| `auction.defaultLots` | 无玩家抽象拍卖的默认拍品池 |
```

- [x] **Step 4: 运行配置加载验证**

Run:

```powershell
cd apps/game
node tools/test-job-config-load.mjs
```

Expected: 当前测试通过；新增配置不会破坏既有加载。经济 RED 测试仍因模块未实现失败。

---

### Task 3: 实现资产适配、估价和原子交割

**Files:**
- Create: `apps/game/js/engine/economy/asset-adapter.js`
- Create: `apps/game/js/engine/economy/price-service.js`
- Create: `apps/game/js/engine/economy/settlement-service.js`
- Modify: `apps/game/tools/test-economic-asset-adapter.mjs`
- Modify: `apps/game/tools/test-economic-transaction-platform.mjs`

- [x] **Step 1: 实现 AssetAdapter**

Create `apps/game/js/engine/economy/asset-adapter.js`:

```javascript
const FACTION_STATE_RESOURCE_IDS = new Set(['low_spirit_stone', 'food', 'disciples']);
const ORGANIZATION_POINT_KEYS = new Set(['contribution', 'warMerit', 'sectCredit']);

function quantityOf(asset) {
  const quantity = Number(asset?.quantity ?? asset?.amount ?? 0);
  return Number.isFinite(quantity) ? Math.max(0, quantity) : 0;
}

function itemIdOf(asset) {
  return asset?.itemId || asset?.id || null;
}

function stateKeyOf(asset) {
  return asset?.pointKey || asset?.itemId || asset?.key || null;
}

function readState(entity, key) {
  return Number(entity?.state?.get?.(key) || 0);
}

function writeState(entity, key, value) {
  entity?.state?.set?.(key, Math.max(0, Number(value) || 0));
}

export class AssetAdapter {
  constructor(config = {}) {
    this.factionStateResourceIds = new Set(config?.assets?.factionStateResourceIds || [...FACTION_STATE_RESOURCE_IDS]);
    this.organizationPointKeys = new Set(config?.assets?.organizationPointKeys || [...ORGANIZATION_POINT_KEYS]);
  }

  normalize(asset) {
    const kind = asset?.kind || 'item';
    const quantity = quantityOf(asset);
    if (kind === 'item') return { kind, itemId: itemIdOf(asset), quantity };
    if (kind === 'faction_state_resource') return { kind, itemId: itemIdOf(asset), quantity };
    if (kind === 'organization_point') return { kind, pointKey: stateKeyOf(asset), quantity };
    return { kind, itemId: itemIdOf(asset), quantity };
  }

  isPrivatelyTransferable(asset) {
    const spec = this.normalize(asset);
    return spec.kind !== 'organization_point';
  }

  amountOf(entity, asset) {
    const spec = this.normalize(asset);
    if (!entity || spec.quantity < 0) return 0;
    if (spec.kind === 'faction_state_resource') return readState(entity, spec.itemId);
    if (spec.kind === 'organization_point') return readState(entity, spec.pointKey);
    return Number(entity.inventory?.getAmount?.(spec.itemId) || 0);
  }

  canWithdraw(entity, asset) {
    const spec = this.normalize(asset);
    if (!entity || spec.quantity < 0) return false;
    if (spec.quantity === 0) return true;
    if (spec.kind === 'organization_point' && !this.organizationPointKeys.has(spec.pointKey)) return false;
    if (spec.kind === 'faction_state_resource' && !this.factionStateResourceIds.has(spec.itemId)) return false;
    return this.amountOf(entity, spec) >= spec.quantity;
  }

  withdraw(entity, asset, meta = {}) {
    const spec = this.normalize(asset);
    if (!this.canWithdraw(entity, spec)) return { success: false, op: 'withdraw', entity, asset: spec, meta };
    if (spec.kind === 'faction_state_resource') {
      writeState(entity, spec.itemId, readState(entity, spec.itemId) - spec.quantity);
    } else if (spec.kind === 'organization_point') {
      writeState(entity, spec.pointKey, readState(entity, spec.pointKey) - spec.quantity);
    } else if (spec.quantity > 0) {
      entity.inventory?.remove?.(spec.itemId, spec.quantity);
    }
    return { success: true, op: 'withdraw', entity, asset: spec, meta };
  }

  deposit(entity, asset, meta = {}) {
    const spec = this.normalize(asset);
    if (!entity) return { success: false, op: 'deposit', entity, asset: spec, meta };
    if (spec.kind === 'faction_state_resource') {
      writeState(entity, spec.itemId, readState(entity, spec.itemId) + spec.quantity);
    } else if (spec.kind === 'organization_point') {
      writeState(entity, spec.pointKey, readState(entity, spec.pointKey) + spec.quantity);
    } else if (spec.quantity > 0) {
      entity.inventory?.add?.(spec.itemId, spec.quantity);
    }
    return { success: true, op: 'deposit', entity, asset: spec, meta };
  }

  rollback(delta) {
    if (!delta?.success) return false;
    if (delta.op === 'withdraw') return this.deposit(delta.entity, delta.asset, { rollbackOf: delta.meta });
    if (delta.op === 'deposit') return this.withdraw(delta.entity, delta.asset, { rollbackOf: delta.meta });
    return false;
  }
}
```

- [x] **Step 2: 实现 PriceService**

Create `apps/game/js/engine/economy/price-service.js`:

```javascript
import { ItemRegistry } from '../items/item-registry.js';

function clamp(value, min, max) {
  return Math.max(min, Math.min(max, value));
}

export class PriceService {
  constructor(config = {}) {
    this.config = config || {};
  }

  itemValue(itemId) {
    const def = ItemRegistry.get(itemId);
    const value = Number(def?.properties?.value ?? def?.value ?? 0);
    return Number.isFinite(value) ? Math.max(0, value) : 0;
  }

  assetValue(asset) {
    const quantity = Math.max(0, Number(asset?.quantity ?? asset?.amount ?? 1) || 0);
    if (asset?.kind === 'organization_point') return 0;
    return this.itemValue(asset?.itemId) * quantity;
  }

  scenario(scenarioId = 'formal_market') {
    return this.config?.scenarios?.[scenarioId] || this.config?.scenarios?.formal_market || {};
  }

  quoteAsset(asset, scenarioId = 'formal_market', privateAdjustment = 1) {
    const scenario = this.scenario(scenarioId);
    const base = this.assetValue(asset);
    const multiplier = Number(scenario.priceMultiplier ?? 1) || 1;
    return Math.max(0, Math.round(base * multiplier * privateAdjustment));
  }

  privateAdjustment({ relationship = 0, urgency = 0, informationAdvantage = 0, rng = null } = {}) {
    const variance = this.scenario('private_trade').privateVariance || { min: 0.75, max: 1.45 };
    const random = rng?.next ? rng.next() : 0.5;
    const spread = variance.min + random * (variance.max - variance.min);
    const social = 1 - clamp(relationship, -100, 100) / 400;
    const urgent = 1 + clamp(urgency, 0, 100) / 250;
    const info = 1 + clamp(informationAdvantage, -100, 100) / 300;
    return clamp(spread * social * urgent * info, 0.5, 2.5);
  }
}
```

- [x] **Step 3: 实现 SettlementService**

Create `apps/game/js/engine/economy/settlement-service.js`:

```javascript
export class SettlementService {
  constructor({ assetAdapter }) {
    this.assetAdapter = assetAdapter;
  }

  settle({ parties = [], transfers = [], type = 'transaction', source = null } = {}) {
    const roleMap = new Map(parties.map(p => [p.role, p.entity]));
    const deltas = [];
    for (const transfer of transfers) {
      const from = typeof transfer.from === 'string' ? roleMap.get(transfer.from) : transfer.from;
      const to = typeof transfer.to === 'string' ? roleMap.get(transfer.to) : transfer.to;
      const asset = transfer.asset;

      const out = this.assetAdapter.withdraw(from, asset, { type, source, transfer });
      if (!out.success) {
        for (let i = deltas.length - 1; i >= 0; i--) this.assetAdapter.rollback(deltas[i]);
        return { success: false, reason: 'asset_unavailable', failedTransfer: transfer, deltas: [] };
      }
      deltas.push(out);

      const incoming = this.assetAdapter.deposit(to, asset, { type, source, transfer });
      if (!incoming.success) {
        for (let i = deltas.length - 1; i >= 0; i--) this.assetAdapter.rollback(deltas[i]);
        this.assetAdapter.rollback(out);
        return { success: false, reason: 'asset_deposit_failed', failedTransfer: transfer, deltas: [] };
      }
      deltas.push(incoming);
    }
    return { success: true, deltas };
  }
}
```

- [x] **Step 4: 运行资产适配器测试**

Run:

```powershell
cd apps/game
node tools/test-economic-asset-adapter.mjs
```

Expected:

```text
经济资产适配器测试通过
```

---

### Task 4: 实现账本、托管、债务、信号和交易门面

**Files:**
- Create: `apps/game/js/engine/economy/transaction-ledger.js`
- Create: `apps/game/js/engine/economy/escrow-service.js`
- Create: `apps/game/js/engine/economy/debt-service.js`
- Create: `apps/game/js/engine/economy/economic-signal-provider.js`
- Create: `apps/game/js/engine/economy/transaction-engine.js`
- Modify: `apps/game/tools/test-economic-transaction-platform.mjs`

- [x] **Step 1: 实现 TransactionLedger**

Create `apps/game/js/engine/economy/transaction-ledger.js`:

```javascript
export class TransactionLedger {
  constructor() {
    this.records = [];
    this.nextId = 1;
  }

  append(input) {
    const record = {
      id: input.id || `tx_${this.nextId++}`,
      type: input.type || 'transaction',
      status: input.status || 'pending',
      day: input.day ?? 0,
      parties: input.parties || [],
      assets: input.assets || [],
      escrowRefs: input.escrowRefs || [],
      debtRefs: input.debtRefs || [],
      visibility: input.visibility || 'system',
      source: input.source || null,
      evidence: input.evidence || [],
      reason: input.reason || null,
    };
    this.records.push(record);
    return record;
  }

  all() {
    return [...this.records];
  }

  byId(id) {
    return this.records.find(record => record.id === id) || null;
  }

  visibleTo(viewerId, scope = 'system') {
    if (scope === 'system') return this.all();
    return this.records.filter(record => {
      if (record.visibility === 'public') return true;
      if (record.visibility === 'institution') {
        return record.parties.some(p => p.entityId === viewerId || p.role === 'institution');
      }
      if (record.visibility === 'personal') {
        return record.parties.some(p => p.entityId === viewerId);
      }
      return false;
    });
  }

  snapshot() {
    return { nextId: this.nextId, records: this.all() };
  }

  loadFrom(data = {}) {
    this.nextId = Number(data.nextId || 1);
    this.records = Array.isArray(data.records) ? data.records.map(r => ({ ...r })) : [];
  }
}
```

- [x] **Step 2: 实现 EscrowService**

Create `apps/game/js/engine/economy/escrow-service.js`:

```javascript
export class EscrowService {
  constructor({ assetAdapter, ledger }) {
    this.assetAdapter = assetAdapter;
    this.ledger = ledger;
    this.entries = new Map();
    this.nextId = 1;
  }

  open({ day = 0, purpose, sourceEntity, escrowHolder, nominalOwnerId, assets = [], source = null } = {}) {
    const deltas = [];
    const escrowId = `escrow_${this.nextId++}`;
    for (const asset of assets) {
      const out = this.assetAdapter.withdraw(sourceEntity, asset, { purpose, escrowId, source });
      if (!out.success) {
        for (let i = deltas.length - 1; i >= 0; i--) this.assetAdapter.rollback(deltas[i]);
        return { success: false, reason: 'escrow_asset_unavailable', escrowId: null };
      }
      deltas.push(out);
      const incoming = this.assetAdapter.deposit(escrowHolder, asset, { purpose, escrowId, source });
      if (!incoming.success) {
        for (let i = deltas.length - 1; i >= 0; i--) this.assetAdapter.rollback(deltas[i]);
        this.assetAdapter.rollback(out);
        return { success: false, reason: 'escrow_deposit_failed', escrowId: null };
      }
      deltas.push(incoming);
    }

    const entry = {
      id: escrowId,
      day,
      purpose,
      itemizedAssets: assets.map(asset => ({ ...asset })),
      nominalOwnerId,
      sourceEntityId: sourceEntity?.id || null,
      escrowHolderId: escrowHolder?.id || null,
      status: 'locked',
      source,
    };
    this.entries.set(escrowId, entry);
    this.ledger.append({
      type: 'escrow_lock',
      status: 'settled',
      day,
      parties: [
        { role: 'source', entityId: sourceEntity?.id || null },
        { role: 'escrow_holder', entityId: escrowHolder?.id || null },
      ],
      assets,
      escrowRefs: [escrowId],
      visibility: 'institution',
      source,
    });
    return { success: true, escrowId, entry };
  }

  settle({ day = 0, escrowId, holder, destination, status = 'released', source = null } = {}) {
    const entry = this.entries.get(escrowId);
    if (!entry || entry.status !== 'locked') return { success: false, reason: 'escrow_not_locked' };
    const deltas = [];
    for (const asset of entry.itemizedAssets) {
      const out = this.assetAdapter.withdraw(holder, asset, { escrowId, source });
      if (!out.success) {
        for (let i = deltas.length - 1; i >= 0; i--) this.assetAdapter.rollback(deltas[i]);
        return { success: false, reason: 'escrow_holder_asset_missing' };
      }
      deltas.push(out);
      const incoming = this.assetAdapter.deposit(destination, asset, { escrowId, source });
      if (!incoming.success) {
        for (let i = deltas.length - 1; i >= 0; i--) this.assetAdapter.rollback(deltas[i]);
        this.assetAdapter.rollback(out);
        return { success: false, reason: 'escrow_destination_failed' };
      }
      deltas.push(incoming);
    }
    entry.status = status;
    entry.closedDay = day;
    this.ledger.append({
      type: status === 'forfeited' ? 'escrow_forfeit' : 'escrow_release',
      status: 'settled',
      day,
      parties: [
        { role: 'escrow_holder', entityId: holder?.id || entry.escrowHolderId },
        { role: 'destination', entityId: destination?.id || null },
      ],
      assets: entry.itemizedAssets,
      escrowRefs: [escrowId],
      visibility: 'institution',
      source,
    });
    return { success: true, escrowId, entry };
  }

  byId(id) {
    return this.entries.get(id) || null;
  }

  pendingFor(entityId) {
    return [...this.entries.values()].filter(entry =>
      entry.status === 'locked' &&
      (entry.nominalOwnerId === entityId || entry.sourceEntityId === entityId || entry.escrowHolderId === entityId)
    );
  }
}
```

- [x] **Step 3: 实现 DebtService 和 EconomicSignalProvider**

Create `apps/game/js/engine/economy/debt-service.js`:

```javascript
export class DebtService {
  constructor({ ledger, config = {} }) {
    this.ledger = ledger;
    this.config = config;
    this.records = new Map();
    this.nextId = 1;
  }

  create({ day = 0, debtorId, creditorId, origin, dueDay = null, assetsDue = [], guarantorIds = [], collateralRefs = [], visibility = 'personal' } = {}) {
    const id = `debt_${this.nextId++}`;
    const record = {
      id,
      debtorId,
      creditorId,
      origin: origin || null,
      day,
      dueDay: dueDay ?? (day + (this.config?.debt?.defaultDueDays ?? 30)),
      assetsDue: assetsDue.map(asset => ({ ...asset })),
      guarantorIds: [...guarantorIds],
      collateralRefs: [...collateralRefs],
      status: 'active',
      visibility,
    };
    this.records.set(id, record);
    this.ledger.append({
      type: 'debt_created',
      status: 'settled',
      day,
      parties: [{ role: 'debtor', entityId: debtorId }, { role: 'creditor', entityId: creditorId }],
      assets: assetsDue,
      debtRefs: [id],
      visibility,
      source: origin,
    });
    return record;
  }

  byId(id) {
    return this.records.get(id) || null;
  }

  forEntity(entityId) {
    return [...this.records.values()].filter(record => record.debtorId === entityId || record.creditorId === entityId);
  }

  advanceDay(day) {
    for (const record of this.records.values()) {
      if (record.status === 'active' && day > record.dueDay) record.status = 'overdue';
    }
  }
}
```

Create `apps/game/js/engine/economy/economic-signal-provider.js`:

```javascript
export class EconomicSignalProvider {
  constructor({ assetAdapter, priceService, escrowService, debtService }) {
    this.assetAdapter = assetAdapter;
    this.priceService = priceService;
    this.escrowService = escrowService;
    this.debtService = debtService;
  }

  signalsFor({ actor, target = null, asset = null, scenarioId = 'formal_market' } = {}) {
    const estimatedPrice = asset ? this.priceService.quoteAsset(asset, scenarioId) : 0;
    const currencyAsset = { kind: 'item', itemId: 'low_spirit_stone', quantity: estimatedPrice };
    const debts = actor?.id ? this.debtService.forEntity(actor.id) : [];
    const pendingEscrow = actor?.id ? this.escrowService.pendingFor(actor.id) : [];
    return {
      facts: {
        canAfford: actor ? this.assetAdapter.canWithdraw(actor, currencyAsset) : false,
        canEscrow: actor ? this.assetAdapter.canWithdraw(actor, currencyAsset) : false,
        estimatedPrice,
        hasOverdueDebt: debts.some(debt => debt.debtorId === actor?.id && debt.status === 'overdue'),
        hasEscrowPending: pendingEscrow.length > 0,
      },
      gates: {
        privateTransferAllowed: asset ? this.assetAdapter.isPrivatelyTransferable(asset) : true,
      },
      modifiers: {},
      traces: [],
    };
  }
}
```

- [x] **Step 4: 实现 EconomicSystem 门面**

Create `apps/game/js/engine/economy/transaction-engine.js`:

```javascript
import { AssetAdapter } from './asset-adapter.js';
import { PriceService } from './price-service.js';
import { SettlementService } from './settlement-service.js';
import { TransactionLedger } from './transaction-ledger.js';
import { EscrowService } from './escrow-service.js';
import { DebtService } from './debt-service.js';
import { EconomicSignalProvider } from './economic-signal-provider.js';

export class EconomicSystem {
  constructor({ config = {} } = {}) {
    this.config = config;
    this.assetAdapter = new AssetAdapter(config);
    this.price = new PriceService(config);
    this.ledger = new TransactionLedger();
    this.settlement = new SettlementService({ assetAdapter: this.assetAdapter });
    this.escrow = new EscrowService({ assetAdapter: this.assetAdapter, ledger: this.ledger });
    this.debts = new DebtService({ ledger: this.ledger, config });
    this.signals = new EconomicSignalProvider({
      assetAdapter: this.assetAdapter,
      priceService: this.price,
      escrowService: this.escrow,
      debtService: this.debts,
    });
  }

  quote(input = {}) {
    return this.price.quoteAsset(input.asset, input.scenarioId, input.privateAdjustment ?? 1);
  }

  settle(input = {}) {
    const result = this.settlement.settle(input);
    const record = this.ledger.append({
      type: input.type || 'transaction',
      status: result.success ? 'settled' : 'failed',
      day: input.day ?? 0,
      parties: (input.parties || []).map(p => ({ role: p.role, entityId: p.entity?.id || p.entityId || null })),
      assets: (input.transfers || []).map(t => ({ from: t.from, to: t.to, asset: t.asset })),
      visibility: input.visibility || this.config?.scenarios?.[input.scenarioId]?.visibility || 'system',
      source: input.source || null,
      reason: result.reason || null,
    });
    return { ...result, transactionId: record.id, record };
  }

  openEscrow(input = {}) {
    return this.escrow.open(input);
  }

  settleEscrow(input = {}) {
    const entry = this.escrow.byId(input.escrowId);
    const holder = input.holder || null;
    return this.escrow.settle({
      ...input,
      holder,
      destination: input.destination,
      source: input.source || entry?.source || null,
    });
  }

  createDebt(input = {}) {
    return this.debts.create(input);
  }

  advanceDay(day) {
    this.debts.advanceDay(day);
  }

  signalsFor(input = {}) {
    return this.signals.signalsFor(input);
  }

  snapshot() {
    return {
      ledger: this.ledger.snapshot(),
      debts: [...this.debts.records.values()],
      escrows: [...this.escrow.entries.values()],
    };
  }
}
```

- [x] **Step 5: 运行交易平台测试**

Run:

```powershell
cd apps/game
node tools/test-economic-transaction-platform.mjs
```

Expected:

```text
经济交易平台测试通过
```

---

### Task 5: 接入 WorldEngine 和 worldContext

**Files:**
- Modify: `apps/game/js/engine/world-engine.js`
- Modify: `apps/game/js/engine/world/services/world-context-builder.js`
- Modify: `apps/game/js/engine/world/tick-manager.js`
- Modify: `apps/game/tools/test-job-config-load.mjs`

- [x] **Step 1: WorldEngine 初始化 EconomicSystem**

Modify `apps/game/js/engine/world-engine.js`:

```javascript
import { EconomicSystem } from './economy/transaction-engine.js';

// init(configs) 中，在 this._itemDefs 后追加：
this._economicTransactionConfig = configs.economicTransactionConfig || {};
this.economicSystem = new EconomicSystem({ config: this._economicTransactionConfig });

// _createTickManager() 参数中追加：
economicSystem: this.economicSystem,
economicTransactionConfig: this._economicTransactionConfig,

// getWorldSnapshot() 返回对象中追加：
economic: this.economicSystem ? this.economicSystem.snapshot() : null,
```

- [x] **Step 2: TickManager 保存经济系统并推进债务**

Modify `apps/game/js/engine/world/tick-manager.js` constructor:

```javascript
this.economicSystem = deps.economicSystem || null;
this.economicTransactionConfig = deps.economicTransactionConfig || {};
```

Modify 每日 tick 完成世界日推进后：

```javascript
if (this.economicSystem && this.worldEntity?.currentDay != null) {
  this.economicSystem.advanceDay(this.worldEntity.currentDay);
}
```

- [x] **Step 3: worldContext 暴露经济端口**

Modify `apps/game/js/engine/world/services/world-context-builder.js`:

```javascript
economicSystem: host.economicSystem || null,
economicTransactionConfig: host.economicTransactionConfig || {},

settleTransaction(input) {
  return host.economicSystem?.settle?.({
    day: host.worldEntity.currentDay,
    ...input,
  }) || { success: false, reason: 'economic_system_missing' };
},

openEscrow(input) {
  return host.economicSystem?.openEscrow?.({
    day: host.worldEntity.currentDay,
    ...input,
  }) || { success: false, reason: 'economic_system_missing' };
},

economicSignalsFor(input) {
  return host.economicSystem?.signalsFor?.(input) || {
    facts: {},
    gates: {},
    modifiers: {},
    traces: [],
  };
},
```

- [x] **Step 4: 扩展配置加载测试**

Modify `apps/game/tools/test-job-config-load.mjs`:

```javascript
assert(configs.economicTransactionConfig?.scenarios?.formal_market, 'loadGameConfigs loads economic transaction scenarios');

const economicSnapshot = firstEngine.getWorldSnapshot().economic;
assert(economicSnapshot?.ledger?.records, 'WorldEngine snapshot includes economic ledger');

const ctx = firstEngine.tickManager.contextBuilder.build();
assert(typeof ctx.settleTransaction === 'function', 'worldContext exposes settleTransaction');
assert(typeof ctx.economicSignalsFor === 'function', 'worldContext exposes economicSignalsFor');
```

- [x] **Step 5: 运行加载与初始化验证**

Run:

```powershell
cd apps/game
node tools/test-job-config-load.mjs
```

Expected:

```text
Job config load tests passed
```

---

### Task 6: 迁移材料上交、贡献兑换和直接购买

**Files:**
- Modify: `apps/game/js/engine/npc/npc-economy.js`
- Modify: `apps/game/js/engine/npc/toils/economy-toils.js`
- Modify: `apps/game/tools/test-economic-integration.mjs`
- Modify: `apps/game/tools/test-economy-toils.mjs`
- Modify: `apps/game/tools/test-quest-reward-economy.mjs`

- [x] **Step 1: 材料上交改用经济底座**

Modify `donateMaterials` in `apps/game/js/engine/npc/npc-economy.js`:

```javascript
const economicSystem = worldContext?.economicSystem || null;
if (economicSystem && options.useEconomicSystem !== false) {
  const transaction = economicSystem.settle({
    type: 'material_donation',
    scenarioId: 'material_donation',
    day: worldContext?.currentDay ?? 0,
    parties: [
      { role: 'donor', entity },
      { role: 'faction', entity: faction },
    ],
    transfers: [
      { from: 'donor', to: 'faction', asset: { kind: 'item', itemId: rule.itemId, quantity: qty } },
      { from: 'faction', to: 'donor', asset: { kind: 'organization_point', pointKey: 'contribution', quantity: contribution } },
      { from: 'faction', to: 'donor', asset: { kind: 'organization_point', pointKey: 'monthlyContribution', quantity: monthlyContribution } },
    ],
    source: { type: 'npc_material_donation', itemId: rule.itemId },
    visibility: 'institution',
  });
  if (!transaction.success) return { success: false, outcome: transaction.reason || 'transaction_failed', eventType: 'material_donate', transactionId: transaction.transactionId };
  return {
    success: true,
    outcome: 'donated',
    eventType: 'material_donate',
    itemId: rule.itemId,
    qty,
    contribution,
    monthlyContribution,
    transactionId: transaction.transactionId,
    description: `${entity.name || entity.staticData?.name || entity.id} 上交${name}x${qty}，换得${contribution}贡献`,
  };
}
```

Keep the existing direct mutation branch when `economicSystem` is missing so older isolated tests can still construct `mkWorld` without economy.

- [x] **Step 2: 贡献兑换改用经济底座**

Modify `redeemExchangeItem`:

```javascript
const economicSystem = worldContext?.economicSystem || null;
if (economicSystem && options.useEconomicSystem !== false) {
  const transfers = [];
  if (contributionCost > 0) {
    transfers.push({ from: 'npc', to: 'faction', asset: { kind: 'organization_point', pointKey: 'contribution', quantity: contributionCost } });
  }
  if (stoneCost > 0) {
    transfers.push({ from: 'npc', to: 'faction', asset: { kind: 'item', itemId: 'low_spirit_stone', quantity: stoneCost } });
  }
  for (const req of required) {
    transfers.push({ from: 'faction', to: 'system_sink', asset: { kind: 'item', itemId: req.itemId, quantity: Math.max(0, Math.floor(req.qty ?? 1)) } });
  }
  transfers.push({ from: 'faction', to: 'npc', asset: { kind: 'item', itemId: option.itemId, quantity: qty } });

  const sink = { id: 'system_sink', name: '系统消耗', inventory: { add() {}, remove() { return true; }, getAmount() { return 999999; } }, state: { get() { return 0; }, set() {} } };
  const transaction = economicSystem.settle({
    type: 'contribution_exchange',
    scenarioId: 'faction_exchange',
    day: worldContext?.currentDay ?? 0,
    parties: [
      { role: 'npc', entity },
      { role: 'faction', entity: faction },
      { role: 'system_sink', entity: sink },
    ],
    transfers,
    source: { type: 'npc_exchange', optionKey },
    visibility: 'institution',
  });
  if (!transaction.success) return { success: false, outcome: transaction.reason || 'transaction_failed', optionKey, eventType: `redeem_${optionKey}`, transactionId: transaction.transactionId };
  const grant = isArtifact(option.itemId) ? equipBestArtifact(entity) : { changed: false, equippedArtifactId: entity.state?.get?.('equippedArtifactId') || null };
  return {
    success: true,
    outcome: 'redeemed',
    eventType: `redeem_${optionKey}`,
    optionKey,
    itemId: option.itemId,
    qty,
    contributionCost,
    stoneCost,
    requiredFactionItems: required,
    equippedArtifactId: grant?.equippedArtifactId || entity.state?.get?.('equippedArtifactId') || null,
    transactionId: transaction.transactionId,
    description: `${entity.name || entity.staticData?.name || entity.id} 兑换${name}x${qty}`,
  };
}
```

- [x] **Step 3: 直接购买 Toil 改用 worldContext.settleTransaction**

Modify `buyItem` in `apps/game/js/engine/npc/toils/economy-toils.js` to accept `worldContext` and `entity`:

```javascript
function buyItem(entity, worldContext, params, successReason, invalidResult = invalidPurchase()) {
  const inventory = inventoryOf(entity);
  if (!inventory) return missingInventory();
  const purchase = validatePurchaseParams(params, invalidResult);
  if (!purchase.ok) return purchase.result;
  const { itemId, amount, priceItemId, priceAmount } = purchase;

  if (worldContext?.settleTransaction) {
    const vendor = {
      id: params.vendorId || 'org_market',
      name: params.vendorName || '坊市',
      inventory: {
        getAmount(id) { return id === itemId ? Number.MAX_SAFE_INTEGER : 0; },
        remove() { return true; },
        add() {},
      },
      state: { get() { return 0; }, set() {} },
    };
    const result = worldContext.settleTransaction({
      type: 'direct_purchase',
      scenarioId: 'formal_market',
      parties: [{ role: 'buyer', entity }, { role: 'seller', entity: vendor }],
      transfers: [
        { from: 'buyer', to: 'seller', asset: { kind: 'item', itemId: priceItemId, quantity: priceAmount } },
        { from: 'seller', to: 'buyer', asset: { kind: 'item', itemId, quantity: amount } },
      ],
      source: { type: 'toil_buy_item', itemId },
      visibility: 'institution',
    });
    return result.success
      ? { status: ToilResultStatus.SUCCESS, reason: successReason, contextPatch: { transactionId: result.transactionId } }
      : { status: ToilResultStatus.FAILED, reason: result.reason || 'transaction_failed', contextPatch: { transactionId: result.transactionId } };
  }

  if (amountOf(inventory, priceItemId) < priceAmount) return { status: ToilResultStatus.FAILED, reason: 'insufficient_currency' };
  if (priceAmount > 0 && !removeItem(inventory, priceItemId, priceAmount)) return { status: ToilResultStatus.FAILED, reason: 'insufficient_currency' };
  if (!addItem(inventory, itemId, amount)) return { status: ToilResultStatus.FAILED, reason: 'inventory_add_failed' };
  return { status: ToilResultStatus.SUCCESS, reason: successReason };
}
```

Update callers:

```javascript
return buyItem(entity, worldContext, { ...params, itemId, amount: minAmount - current }, 'item_purchased');
return buyItem(entity, worldContext, paramsOf(toil), 'item_purchased');
```

- [x] **Step 4: 运行经济接入验证**

Run:

```powershell
cd apps/game
node tools/test-economic-integration.mjs
node tools/test-economy-toils.mjs
node tools/test-quest-reward-economy.mjs
```

Expected:

```text
经济接入测试通过
...
V1 任务奖励与 NPC 消费闭环测试通过
```

---

### Task 7: 接入任务交付结算和债务信号

**Files:**
- Modify: `apps/game/js/engine/npc/services/quest-service.js`
- Modify: `apps/game/tools/test-economic-integration.mjs`
- Modify: `apps/game/tools/test-monster-hunt-job.mjs`

- [x] **Step 1: 任务交付奖励改用经济底座**

Modify `turnInQuest` in `apps/game/js/engine/npc/services/quest-service.js`:

```javascript
const economicSystem = worldContext?.economicSystem || null;
if (economicSystem) {
  const payer = isWanderer ? (worldContext._resolveBountyOrgFor?.(entity) || null) : faction;
  const rewardTransfers = [];
  if (payer && rewardStones > 0) {
    rewardTransfers.push({ from: 'payer', to: 'receiver', asset: { kind: 'item', itemId: 'low_spirit_stone', quantity: rewardStones } });
  }
  if (!isWanderer && rewardContribution > 0) {
    rewardTransfers.push({ from: 'payer', to: 'receiver', asset: { kind: 'organization_point', pointKey: 'contribution', quantity: rewardContribution } });
    rewardTransfers.push({ from: 'payer', to: 'receiver', asset: { kind: 'organization_point', pointKey: 'monthlyContribution', quantity: rewardContribution } });
  }
  if (!isWanderer && factionStones > 0) {
    rewardTransfers.push({ from: 'system_source', to: 'payer', asset: { kind: 'faction_state_resource', itemId: 'low_spirit_stone', quantity: factionStones } });
  }

  const systemSource = { id: 'system_source', name: '任务系统收益源', inventory: { getAmount() { return Number.MAX_SAFE_INTEGER; }, remove() { return true; }, add() {} }, state: { get() { return Number.MAX_SAFE_INTEGER; }, set() {} } };
  const transaction = economicSystem.settle({
    type: 'quest_reward',
    scenarioId: 'quest_contract',
    day: worldContext?.currentDay ?? 0,
    parties: [
      { role: 'receiver', entity },
      { role: 'payer', entity: payer },
      { role: 'system_source', entity: systemSource },
    ],
    transfers: rewardTransfers,
    source: { type: 'quest_turn_in', questTypeId },
    visibility: 'institution',
  });
  if (!transaction.success && payer) {
    economicSystem.createDebt({
      day: worldContext?.currentDay ?? 0,
      debtorId: payer.id,
      creditorId: entity.id,
      origin: { type: 'quest_reward_shortfall', questTypeId },
      assetsDue: [{ kind: 'item', itemId: 'low_spirit_stone', quantity: rewardStones }],
      visibility: 'institution',
    });
  }
}
```

Keep `applyQuestRewardProfile` after the money/points settlement so extra item rewards continue to work.

- [x] **Step 2: 扩展经济接入测试覆盖任务奖励账本**

Append to `apps/game/tools/test-economic-integration.mjs`:

```javascript
console.log('3) quest reward creates ledger and debt on payer shortfall');
{
  const npc = entity('npc_quester', {}, {
    factionId: null,
    hasActiveQuest: true,
    activeQuestDifficulty: 1,
    activeQuestTypeId: 'qt_slay_monster',
    activeQuestTypeName: '斩妖',
    activeQuestDiffName: '低阶',
    questComplete: true,
  });
  const bounty = entity('org_bounty', { low_spirit_stone: 0 }, {});
  const economicSystem = new EconomicSystem({ config: economicTransactionConfig });
  const questTemplates = load('data/quests/quest-templates.json');
  const { turnInQuest } = await imp('js/engine/npc/services/quest-service.js');
  const result = turnInQuest(npc, {
    currentDay: 40,
    questTemplates,
    balanceConfig: { economy: economyConfig, cultivation: load('data/balance/cultivation.json') },
    economicSystem,
    _resolveBountyOrgFor() { return bounty; },
  });
  ok(result.success === true, '任务交付仍成功');
  ok(economicSystem.ledger.all().some(r => r.type === 'quest_reward'), '任务奖励写入经济账本');
  ok([...economicSystem.debts.records.values()].some(d => d.status === 'active'), '付款方不足时生成债务');
}
```

- [x] **Step 3: 运行任务验证**

Run:

```powershell
cd apps/game
node tools/test-economic-integration.mjs
node tools/test-monster-hunt-job.mjs
```

Expected: 两个脚本通过；斩妖任务仍绑定真实妖兽死亡和交付状态。

---

### Task 8: 实现无玩家抽象拍卖

**Files:**
- Create: `apps/game/js/engine/economy/auction-service.js`
- Modify: `apps/game/js/engine/world/world-event.js`
- Modify: `apps/game/js/engine/world/tick-manager.js`
- Modify: `apps/game/data/world/dynamic-events.json`
- Modify: `apps/game/tools/test-economic-auction-abstract.mjs`
- Modify: `apps/game/tools/test-dynamic-event-system.mjs`

- [x] **Step 1: 实现 AuctionService**

Create `apps/game/js/engine/economy/auction-service.js`:

```javascript
import { ItemRegistry } from '../items/item-registry.js';

function amount(entity, itemId) {
  return Number(entity?.inventory?.getAmount?.(itemId) || 0);
}

function pickBidder(participants, price, rng) {
  const affordable = participants.filter(entity => amount(entity, 'low_spirit_stone') >= price);
  if (affordable.length === 0) return null;
  affordable.sort((a, b) => amount(b, 'low_spirit_stone') - amount(a, 'low_spirit_stone'));
  const top = affordable.slice(0, Math.min(3, affordable.length));
  const idx = Math.floor((rng?.next?.() ?? 0) * top.length);
  return top[Math.max(0, Math.min(top.length - 1, idx))];
}

export class AuctionService {
  constructor({ economicSystem, config = {} }) {
    this.economicSystem = economicSystem;
    this.config = config;
  }

  lotName(lot) {
    return ItemRegistry.get(lot.itemId)?.name || lot.itemId;
  }

  resolveAbstractAuction({ day = 0, event, auctionHouse, lots = [], participants = [], rng = null } = {}) {
    const cfg = this.config?.auction?.abstractBid || {};
    const exposureThreshold = cfg.wealthExposureThreshold ?? 300;
    const results = [];
    const wealthExposureEvents = [];
    for (const lot of lots) {
      const base = Number(lot.reservePrice ?? this.economicSystem.quote({ asset: { kind: 'item', itemId: lot.itemId, quantity: lot.quantity || 1 }, scenarioId: 'auction' })) || 0;
      const min = cfg.winnerMultiplierMin ?? 1.05;
      const max = cfg.winnerMultiplierMax ?? 1.65;
      const price = Math.max(1, Math.round(base * (min + (rng?.next?.() ?? 0.5) * (max - min))));
      const winner = pickBidder(participants, price, rng);
      if (!winner) {
        results.push({ itemId: lot.itemId, quantity: lot.quantity || 1, status: 'unsold', price: 0, winnerId: null });
        this.economicSystem.ledger.append({
          type: 'auction_unsold',
          status: 'settled',
          day,
          parties: [{ role: 'auction_house', entityId: auctionHouse?.id || null }],
          assets: [{ kind: 'item', itemId: lot.itemId, quantity: lot.quantity || 1 }],
          visibility: 'public',
          source: { type: 'auction', id: event?.id || null },
        });
        continue;
      }
      const tx = this.economicSystem.settle({
        type: 'auction_sale',
        scenarioId: 'auction',
        day,
        parties: [{ role: 'buyer', entity: winner }, { role: 'seller', entity: auctionHouse }],
        transfers: [
          { from: 'buyer', to: 'seller', asset: { kind: 'item', itemId: 'low_spirit_stone', quantity: price } },
          { from: 'seller', to: 'buyer', asset: { kind: 'item', itemId: lot.itemId, quantity: lot.quantity || 1 } },
        ],
        visibility: 'public',
        source: { type: 'auction', id: event?.id || null, lotName: this.lotName(lot) },
      });
      const status = tx.success ? 'sold' : 'failed';
      results.push({ itemId: lot.itemId, quantity: lot.quantity || 1, status, price, winnerId: winner.id, transactionId: tx.transactionId });
      if (tx.success && price >= exposureThreshold) {
        wealthExposureEvents.push({
          type: 'wealth_exposure',
          day,
          eventId: event?.id || null,
          npcId: winner.id,
          value: price,
          description: `${winner.name || winner.id} 在${event?.name || '拍卖会'}高价拍下${this.lotName(lot)}，财富外露`,
        });
      }
    }
    return { success: true, eventId: event?.id || null, lots: results, wealthExposureEvents };
  }
}
```

- [x] **Step 2: dynamic-events 新增拍卖会**

Modify `apps/game/data/world/dynamic-events.json` events array:

```json
{
  "id": "evt_auction_tianbao_001",
  "type": "auction",
  "name": "天宝楼拍卖会",
  "announceDay": 120,
  "startDay": 150,
  "endDay": 151,
  "expireDay": 170,
  "value": 400,
  "riskKey": null,
  "scope": "public",
  "opportunityType": "auction",
  "pos": { "resolver": "faction_hq", "factionId": "org_auction" }
}
```

- [x] **Step 3: TickManager 到期抽象结算拍卖**

Modify `apps/game/js/engine/world/tick-manager.js`:

```javascript
import { AuctionService } from '../economy/auction-service.js';

// constructor
this.auctionService = this.economicSystem
  ? new AuctionService({ economicSystem: this.economicSystem, config: this.economicTransactionConfig })
  : null;

// 每日事件推进后调用
_resolveAbstractAuctions() {
  if (!this.auctionService || !this.worldEventSystem) return [];
  const day = this.worldEntity.currentDay;
  const events = this.worldEventSystem.snapshot?.().events || [];
  const resolved = [];
  for (const event of events) {
    if (event.type !== 'auction' || event.phase !== 'active' || event._abstractResolved === true) continue;
    const auctionHouse = this.entityRegistry.getById('org_auction');
    const participants = this.entityRegistry.getAliveByType('npc')
      .filter(npc => (npc.inventory?.getAmount?.('low_spirit_stone') || 0) > 0)
      .slice(0, this.economicTransactionConfig?.auction?.abstractBid?.maxParticipants ?? 8);
    const defaultLots = this.economicTransactionConfig?.auction?.defaultLots || [];
    for (const lot of defaultLots) auctionHouse?.inventory?.add?.(lot.itemId, lot.quantity || 1);
    const result = this.auctionService.resolveAbstractAuction({ day, event, auctionHouse, lots: defaultLots, participants, rng: this.rng });
    event._abstractResolved = true;
    resolved.push(result);
    for (const info of result.wealthExposureEvents) this.infoEvents?.push?.(info);
  }
  return resolved;
}
```

Call `_resolveAbstractAuctions()` once per tick after dynamic event phase update and before `infoEvents` are flushed.

- [x] **Step 4: 运行拍卖验证**

Run:

```powershell
cd apps/game
node tools/test-economic-auction-abstract.mjs
node tools/test-dynamic-event-system.mjs
```

Expected: 抽象拍卖测试通过；动态事件系统仍能推进秘境、宗门大比和拍卖事件。

---

### Task 9: 文档、导航和真实模拟验证

**Files:**
- Create: `docs/systems/economic-transaction-system.md`
- Create: `docs/data-models/economic-transaction.md`
- Create: `docs/superpowers/reports/2026-06-07-统一经济交易底座验证.md`
- Modify: `docs/architecture/file-structure.md`
- Modify: `docs/data/data-config-rules.md`
- Modify: `docs/README.md`

- [x] **Step 1: 写系统文档**

Create `docs/systems/economic-transaction-system.md`:

```markdown
# 统一经济交易底座

> 最后更新：2026-06-07

统一经济交易底座负责价值校验、估价、托管、原子交割、账本、债务和经济信号。交易动机仍由 AI、任务、动态事件、机会点和玩家行为产生。

## 模块

| 模块 | 职责 |
|------|------|
| `EconomicSystem` | 统一门面，组合估价、托管、交割、账本、债务和信号 |
| `AssetAdapter` | 读写 NPC inventory、势力 state 资源和组织点数 |
| `PriceService` | 按物品 `value` 与场景倍率估价 |
| `SettlementService` | 原子交割并回滚失败 delta |
| `EscrowService` | 托管、释放、返还和罚没资产 |
| `TransactionLedger` | 保存分层可见的交易证据 |
| `DebtService` | 维护长期债务与逾期状态 |
| `EconomicSignalProvider` | 输出 AI 可消费经济信号 |
| `AuctionService` | 无玩家到场时抽象结算拍卖 |

## 资产规则

势力 `low_spirit_stone`、`food`、`disciples` 仍遵守 ADR-015，以 `state` 为单一真相源。NPC 可持有物品读写 `inventory`。贡献、战功、宗门信用是组织点数，不可私下转让或抵押。

## 验证

经济底座验证观察真实资产变化、账本记录、债务状态和模拟行为，不使用指纹或哈希作为正确性证据。
```

- [x] **Step 2: 写数据模型文档**

Create `docs/data-models/economic-transaction.md`:

```markdown
# 经济交易数据模型

> 最后更新：2026-06-07

## AssetSpec

| 字段 | 类型 | 说明 |
|------|------|------|
| `kind` | string | `item` / `faction_state_resource` / `organization_point` |
| `itemId` | string? | 物品或势力 state 资源 ID |
| `pointKey` | string? | 组织点数 key，如 `contribution` |
| `quantity` | number | 数量 |

## TransactionRecord

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | `tx_` 前缀运行时记录 ID |
| `type` | string | `direct_purchase`、`material_donation`、`contribution_exchange`、`quest_reward`、`auction_sale` 等 |
| `status` | string | `pending` / `settled` / `failed` |
| `day` | number | 发生日 |
| `parties` | array | 参与方角色和实体 ID |
| `assets` | array | 资产转移摘要 |
| `escrowRefs` | array | 关联托管记录 |
| `debtRefs` | array | 关联债务记录 |
| `visibility` | string | `system` / `institution` / `public` / `personal` |
| `source` | object | 来源系统和来源 ID |
| `evidence` | array | 调查、审判和追责可用证据 |

## EscrowEntry

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | `escrow_` 前缀运行时记录 ID |
| `purpose` | string | `quest_reward`、`auction_lot`、`collateral` 等 |
| `itemizedAssets` | array | 被托管资产 |
| `nominalOwnerId` | string | 名义所有人 |
| `sourceEntityId` | string | 原资产持有人 |
| `escrowHolderId` | string | 托管方 |
| `status` | string | `locked` / `released` / `refunded` / `forfeited` |

## DebtRecord

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | `debt_` 前缀运行时记录 ID |
| `debtorId` | string | 债务人 |
| `creditorId` | string | 债权人 |
| `origin` | object | 来源交易、任务或赔偿 |
| `dueDay` | number | 到期日 |
| `assetsDue` | array | 应偿还资产 |
| `guarantorIds` | array | 担保人 |
| `collateralRefs` | array | 抵押托管引用 |
| `status` | string | `active` / `paid` / `overdue` / `forgiven` / `disputed` / `converted_to_quest` |
| `visibility` | string | 可见性 |
```

- [x] **Step 3: 写验证报告**

Create `docs/superpowers/reports/2026-06-07-统一经济交易底座验证.md`:

````markdown
# 统一经济交易底座验证

> 最后更新：2026-06-07

本报告只在下列命令全部通过后写入。任一命令失败时，先修复实现并重跑对应命令。

## 验证命令

```powershell
cd apps/game
node tools/test-economic-asset-adapter.mjs
node tools/test-economic-transaction-platform.mjs
node tools/test-economic-integration.mjs
node tools/test-economic-auction-abstract.mjs
node tools/test-job-config-load.mjs
node tools/test-quest-reward-economy.mjs
node tools/test-economy-toils.mjs
node tools/test-dynamic-event-system.mjs
node tools/simulate-analysis.mjs 300 --seed=1001
node tools/simulate-analysis.mjs 300 --seed=1002
node tools/simulate-analysis.mjs 300 --seed=1003
```

## 观察项

| 项 | 结果 |
|----|------|
| NPC 与组织资产读写 | 通过：`test-economic-asset-adapter.mjs` 覆盖 NPC inventory 扣除、存入与回滚 |
| 势力 state 资源单一真相源 | 通过：势力 `low_spirit_stone` / `food` 读写走 state，测试确认不直接改普通 inventory |
| 正式交易交割与账本 | 通过：`test-economic-transaction-platform.mjs` 观察到 `direct_purchase` 成功结算与 `settled` 账本 |
| 失败交易回滚 | 通过：卖方资产不足时交易失败，买方灵石和卖方收入均回滚 |
| 材料上交与贡献兑换 | 通过：`test-economic-integration.mjs` 观察到 `material_donation` 与 `contribution_exchange` 账本 |
| 任务奖励与短款债务 | 通过：悬赏阁付款不足时任务仍交付，经济底座生成 active 债务 |
| 抽象拍卖成交、流拍与财富曝光 | 通过：`test-economic-auction-abstract.mjs` 观察到 `auction_sale` 账本与财富曝光事件 |
| 长程模拟异常 | 通过：seed 1001 / 1002 / 1003 各 300 天真实模拟无运行时异常 |
```
````

- [x] **Step 4: 更新导航**

Modify `docs/architecture/file-structure.md`:

```markdown
| `apps/game/js/engine/economy/` | 统一经济交易底座：资产适配、估价、托管、原子交割、账本、债务、经济信号和抽象拍卖 |
```

Modify `docs/README.md` key docs row:

```markdown
| 统一经济交易底座 | `superpowers/specs/2026-06-07-统一经济交易底座设计.md`、`superpowers/plans/2026-06-07-统一经济交易底座实施计划.md`、`systems/economic-transaction-system.md`、`data-models/economic-transaction.md`、`superpowers/reports/2026-06-07-统一经济交易底座验证.md` |
```

- [x] **Step 5: 运行完整验证**

Run:

```powershell
cd apps/game
node tools/test-economic-asset-adapter.mjs
node tools/test-economic-transaction-platform.mjs
node tools/test-economic-integration.mjs
node tools/test-economic-auction-abstract.mjs
node tools/test-job-config-load.mjs
node tools/test-quest-reward-economy.mjs
node tools/test-economy-toils.mjs
node tools/test-dynamic-event-system.mjs
node tools/simulate-analysis.mjs 300 --seed=1001
node tools/simulate-analysis.mjs 300 --seed=1002
node tools/simulate-analysis.mjs 300 --seed=1003
```

Expected:

```text
全部经济单元和接入脚本通过。
三组 300 天真实模拟无运行时异常。
交易账本能观察到 material_donation / contribution_exchange / quest_reward / auction_sale。
势力 state 资源没有重新出现 inventory 覆盖丢失。
```

---

## Self-Review

**Spec coverage:** 本计划覆盖规格中的资产范围、价格场景倍率、正式/私人边界、托管、账本、债务、任务结算、贡献兑换、材料上交、直接买卖、无玩家抽象拍卖、经济信号和真实模拟验证。玩家到场完整拍卖流程、区域动态供需、真假货、鉴宝职业、物流押运和经济治理不纳入第一版实现。

**Placeholder scan:** 本计划没有保留未落地的占位步骤。每个代码步骤都给出具体路径、接口和验证命令。

**Type consistency:** 核心类型统一为 `AssetSpec.kind`、`TransactionRecord`、`EscrowEntry`、`DebtRecord`。模块名统一为 `EconomicSystem`、`AssetAdapter`、`PriceService`、`SettlementService`、`EscrowService`、`TransactionLedger`、`DebtService`、`EconomicSignalProvider`、`AuctionService`。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-07-统一经济交易底座实施计划.md`. Two execution options:

1. **Subagent-Driven (recommended)** - Dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints.

