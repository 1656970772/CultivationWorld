# 跟随角色状态面板 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在大世界模拟器渲染视图中，为当前跟随角色提供左下角默认折叠的分类状态面板，并在右侧实体列表显示存活/死亡与当前行为。

**Architecture:** 新增一个纯 UI 状态格式化模块，负责把 `engine.getWorldSnapshot()` 里的 NPC/妖兽/势力快照转成稳定展示模型；`simulation-main.js` 只负责调用该模块并刷新 DOM。左下浮层挂在 `#render-canvas-wrap` 内，右侧列表沿用现有 `entity-item` 结构扩展为名称、元信息、生命状态、行为状态四块。

**Tech Stack:** 原生 ES modules、浏览器 DOM、现有静态页面 `apps/game/simulation.html`、现有样式 `apps/game/css/simulation.css`、Node `.mjs` 工具测试。

---

## 文件结构

- Create: `apps/game/js/ui/follow-entity-status.js`
  - 纯函数模块，不直接访问 DOM。
  - 导出 `getLifeStatus(entity, kind)`、`getActionStatus(entity, kind)`、`buildTrackedStatusModel(entity, kind, snapshot)`、`statusModelToHtml(model, escapeHtml)`。
- Create: `apps/game/tools/test-follow-entity-status-ui.mjs`
  - 覆盖 NPC、死亡 NPC、妖兽、势力和空跟随对象的状态格式化。
- Modify: `apps/game/js/simulation-main.js`
  - 导入状态格式化模块。
  - 在跟随对象变化、tick、定时刷新时同步左下角状态面板。
  - 右侧实体列表加入生命状态与行为状态。
- Modify: `apps/game/simulation.html`
  - 在 `#render-canvas-wrap` 内加入默认折叠的 `#follow-status-card`。
- Modify: `apps/game/css/simulation.css`
  - 新增左下浮层、分类区块、右侧列表状态标签样式。
- Modify: `docs/README.md`
  - 导航新增本实施计划。

---

### Task 1: 状态格式化模块 TDD

**Files:**
- Create: `apps/game/tools/test-follow-entity-status-ui.mjs`
- Create: `apps/game/js/ui/follow-entity-status.js`

- [ ] **Step 1: Write the failing test**

Create `apps/game/tools/test-follow-entity-status-ui.mjs`:

```js
import assert from 'node:assert/strict';
import {
  buildTrackedStatusModel,
  getActionStatus,
  getLifeStatus,
  statusModelToHtml,
} from '../js/ui/follow-entity-status.js';

function hasText(html, text) {
  assert.ok(html.includes(text), `expected html to include ${text}`);
}

console.log('1) NPC life/action status');
{
  const npc = {
    name: '鬼面',
    alive: true,
    rankName: '元婴',
    factionId: 'sect_001',
    ageYears: 128,
    maxAgeYears: 800,
    actionStatus: 'executing',
    spatial: { tileX: 22, tileY: 147 },
    qi: 80,
    cultivationProgress: 42,
  };
  assert.deepEqual(getLifeStatus(npc, 'npc'), { label: '存活', tone: 'alive' });
  assert.deepEqual(getActionStatus(npc, 'npc'), { label: 'executing', tone: 'busy' });
  const model = buildTrackedStatusModel(npc, 'npc', {
    factions: { sect_001: { name: '青云宗' } },
    day: 1738,
  });
  assert.equal(model.title, '鬼面');
  assert.equal(model.subtitle, 'NPC · 元婴 · 青云宗');
  assert.equal(model.sections.length, 4);
  hasText(statusModelToHtml(model), '生命');
  hasText(statusModelToHtml(model), '行为');
  hasText(statusModelToHtml(model), '位置');
}

console.log('2) dead NPC shows dead and no action');
{
  const npc = { name: '赵信', alive: false, rankName: '金丹', actionStatus: 'executing' };
  assert.deepEqual(getLifeStatus(npc, 'npc'), { label: '死亡', tone: 'dead' });
  assert.deepEqual(getActionStatus(npc, 'npc'), { label: '无行动', tone: 'idle' });
}

console.log('3) monster and faction status');
{
  const monster = { name: '赤鳞兽', alive: true, gradeName: '三阶', family: '鳞兽', behaviorState: 'hunt', spatial: { tileX: 9, tileY: 12 } };
  assert.deepEqual(getActionStatus(monster, 'monster'), { label: 'hunt', tone: 'busy' });
  const faction = { name: '血河门', alive: false, isDestroyed: true, type: 'evil', resources: { disciples: 0 } };
  assert.deepEqual(getLifeStatus(faction, 'faction'), { label: '覆灭', tone: 'dead' });
  assert.deepEqual(getActionStatus(faction, 'faction'), { label: '无行动', tone: 'idle' });
}

console.log('4) empty model is safe');
{
  const model = buildTrackedStatusModel(null, 'npc', {});
  assert.equal(model.title, '未跟随');
  assert.equal(model.subtitle, '选择 NPC、妖兽或势力后显示状态');
  hasText(statusModelToHtml(model), '未跟随');
}

console.log('Follow entity status UI tests passed');
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-follow-entity-status-ui.mjs
```

Expected: FAIL with module-not-found for `../js/ui/follow-entity-status.js`.

- [ ] **Step 3: Write minimal implementation**

Create `apps/game/js/ui/follow-entity-status.js` with these exports:

```js
function text(value, fallback = '-') {
  const s = value == null ? '' : String(value).trim();
  return s || fallback;
}

function posText(spatial) {
  if (!spatial || typeof spatial.tileX !== 'number' || typeof spatial.tileY !== 'number') return '-';
  return `(${spatial.tileX},${spatial.tileY})`;
}

function factionName(snapshot, factionId) {
  return factionId ? (snapshot?.factions?.[factionId]?.name || factionId) : '散修';
}

export function getLifeStatus(entity, kind) {
  if (!entity) return { label: '未跟随', tone: 'idle' };
  if (kind === 'faction' && (entity.isDestroyed || entity.alive === false)) return { label: '覆灭', tone: 'dead' };
  if (entity.alive === false) return { label: '死亡', tone: 'dead' };
  return { label: '存活', tone: 'alive' };
}

export function getActionStatus(entity, kind) {
  if (!entity) return { label: '未选择', tone: 'idle' };
  if (getLifeStatus(entity, kind).tone === 'dead') return { label: '无行动', tone: 'idle' };
  const raw = kind === 'monster'
    ? entity.behaviorState
    : kind === 'npc'
      ? entity.actionStatus
      : null;
  const label = text(raw, 'idle');
  return { label, tone: label === 'idle' ? 'idle' : 'busy' };
}

export function buildTrackedStatusModel(entity, kind, snapshot = {}) {
  if (!entity) {
    return {
      title: '未跟随',
      subtitle: '选择 NPC、妖兽或势力后显示状态',
      life: getLifeStatus(null, kind),
      action: getActionStatus(null, kind),
      sections: [],
    };
  }

  const life = getLifeStatus(entity, kind);
  const action = getActionStatus(entity, kind);
  const title = text(entity.name, entity.id || '未知实体');

  if (kind === 'monster') {
    return {
      title,
      subtitle: `妖兽 · ${text(entity.gradeName || (entity.grade ? `${entity.grade}阶` : ''), '未知阶位')} · ${text(entity.family, '未知族类')}`,
      life,
      action,
      sections: [
        { title: '生命', rows: [['状态', life.label], ['气血', `${text(entity.hp, '?')}/${text(entity.maxHp, '?')}`]] },
        { title: '行为', rows: [['当前行为', action.label]] },
        { title: '位置', rows: [['坐标', posText(entity.spatial)]] },
      ],
    };
  }

  if (kind === 'faction') {
    return {
      title,
      subtitle: `势力 · ${text(entity.type, '未知类型')}`,
      life,
      action,
      sections: [
        { title: '生命', rows: [['状态', life.label], ['稳定度', text(entity.stability)]] },
        { title: '资源', rows: [['弟子', text(entity.resources?.disciples, '0')], ['领地', text(entity.territoryCount, '0')]] },
      ],
    };
  }

  return {
    title,
    subtitle: `NPC · ${text(entity.rankName, '未知境界')} · ${factionName(snapshot, entity.factionId)}`,
    life,
    action,
    sections: [
      { title: '生命', rows: [['状态', life.label], ['年龄', `${text(entity.ageYears, '?')}/${text(entity.maxAgeYears, '?')}岁`]] },
      { title: '行为', rows: [['当前行为', action.label], ['角色', text(entity.role, '无')]] },
      { title: '修炼', rows: [['境界', text(entity.rankName, '未知')], ['真气', text(entity.qi, '0')], ['进度', `${text(entity.cultivationProgress, '0')}%`]] },
      { title: '位置', rows: [['坐标', posText(entity.spatial)]] },
    ],
  };
}

export function statusModelToHtml(model, escapeHtml = (s) => String(s)) {
  const badge = `<span class="follow-life ${model.life.tone}">${escapeHtml(model.life.label)}</span>`;
  const action = `<span class="follow-action ${model.action.tone}">${escapeHtml(model.action.label)}</span>`;
  const sections = (model.sections || []).map(section => {
    const rows = section.rows.map(([k, v]) =>
      `<div class="follow-status-row"><span>${escapeHtml(k)}</span><b>${escapeHtml(v)}</b></div>`
    ).join('');
    return `<section class="follow-status-section"><h4>${escapeHtml(section.title)}</h4>${rows}</section>`;
  }).join('');
  return `<div class="follow-status-summary"><div><strong>${escapeHtml(model.title)}</strong><small>${escapeHtml(model.subtitle)}</small></div><div class="follow-status-badges">${badge}${action}</div></div>${sections}`;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run the same command. Expected: `Follow entity status UI tests passed`.

- [ ] **Step 5: Commit**

```powershell
git add -- apps/game/tools/test-follow-entity-status-ui.mjs apps/game/js/ui/follow-entity-status.js
git commit -m "test: add followed entity status model"
```

---

### Task 2: 左下角默认折叠状态面板

**Files:**
- Modify: `apps/game/simulation.html`
- Modify: `apps/game/js/simulation-main.js`
- Modify: `apps/game/css/simulation.css`

- [ ] **Step 1: Insert panel DOM**

In `apps/game/simulation.html`, inside `#render-canvas-wrap` after `#zoom-controls`, add:

```html
<div id="follow-status-card" class="follow-status-card collapsed">
  <div id="follow-status-card-header" class="follow-status-card-header">
    <span class="follow-status-caret"></span>
    <div>
      <strong id="follow-status-card-title">跟随状态</strong>
      <small id="follow-status-card-subtitle">默认折叠</small>
    </div>
  </div>
  <div id="follow-status-card-body" class="follow-status-card-body"></div>
</div>
```

- [ ] **Step 2: Wire refresh logic**

In `apps/game/js/simulation-main.js`, add import:

```js
import { buildTrackedStatusModel, getActionStatus, getLifeStatus, statusModelToHtml } from './ui/follow-entity-status.js';
```

Add instance field in constructor:

```js
this._followStatusCollapsed = true;
```

Add click binding in `bindUI()`:

```js
const followStatusHeader = document.getElementById('follow-status-card-header');
if (followStatusHeader) {
  followStatusHeader.onclick = () => {
    this._followStatusCollapsed = !this._followStatusCollapsed;
    this.refreshFollowStatusCard();
  };
}
```

Add method:

```js
refreshFollowStatusCard() {
  const card = document.getElementById('follow-status-card');
  const titleEl = document.getElementById('follow-status-card-title');
  const subtitleEl = document.getElementById('follow-status-card-subtitle');
  const bodyEl = document.getElementById('follow-status-card-body');
  if (!card || !titleEl || !subtitleEl || !bodyEl) return;

  card.classList.toggle('collapsed', this._followStatusCollapsed);
  const snap = this.engine.getWorldSnapshot();
  const followId = this.renderer ? this.renderer.getFollowId() : null;
  const kind = followId && snap.npcs[followId] ? 'npc'
    : followId && snap.monsters[followId] ? 'monster'
      : followId && snap.factions[followId] ? 'faction'
        : 'npc';
  const entity = followId ? (snap.npcs[followId] || snap.monsters[followId] || snap.factions[followId] || null) : null;
  const model = buildTrackedStatusModel(entity, kind, snap);

  titleEl.textContent = model.title === '未跟随' ? '跟随状态' : model.title;
  subtitleEl.textContent = `${model.life.label} · ${model.action.label}`;
  bodyEl.innerHTML = statusModelToHtml(model, (s) => this._escapeHtml(s));
}
```

Call `this.refreshFollowStatusCard()` at the end of `refreshEntityList()` and after renderer snapshot update in `doTick()`.

- [ ] **Step 3: Style floating panel**

Append to `apps/game/css/simulation.css`:

```css
.follow-status-card {
  position: absolute;
  left: 12px;
  bottom: 12px;
  z-index: 6;
  width: min(360px, calc(100% - 24px));
  max-height: min(52vh, 460px);
  background: rgba(15, 25, 35, 0.88);
  border: 1px solid var(--border);
  border-radius: 6px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.28);
  overflow: hidden;
  backdrop-filter: blur(6px);
}
.follow-status-card-header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 10px;
  cursor: pointer;
  user-select: none;
}
.follow-status-card-header:hover { background: rgba(37, 61, 82, 0.72); }
.follow-status-card-header strong {
  display: block;
  color: var(--accent);
  font-size: 13px;
  line-height: 1.25;
}
.follow-status-card-header small {
  display: block;
  color: var(--text-secondary);
  font-size: 11px;
  line-height: 1.25;
}
.follow-status-caret {
  width: 0;
  height: 0;
  border-left: 5px solid var(--text-secondary);
  border-top: 4px solid transparent;
  border-bottom: 4px solid transparent;
  transform: rotate(90deg);
  transition: transform 0.15s ease;
}
.follow-status-card.collapsed .follow-status-caret { transform: rotate(0deg); }
.follow-status-card.collapsed .follow-status-card-body { display: none; }
.follow-status-card-body {
  max-height: calc(min(52vh, 460px) - 48px);
  overflow-y: auto;
  padding: 0 10px 10px;
}
.follow-status-summary {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  padding: 8px 0;
  border-top: 1px solid rgba(255,255,255,0.06);
}
.follow-status-summary strong,
.follow-status-summary small {
  display: block;
}
.follow-status-summary small { color: var(--text-secondary); font-size: 11px; margin-top: 2px; }
.follow-status-badges {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 4px;
}
.follow-life,
.follow-action {
  display: inline-flex;
  align-items: center;
  min-height: 18px;
  padding: 1px 6px;
  border-radius: 3px;
  font-size: 11px;
  white-space: nowrap;
}
.follow-life.alive { color: var(--success); background: rgba(102,187,106,0.16); }
.follow-life.dead { color: var(--danger); background: rgba(239,83,80,0.16); }
.follow-life.idle { color: var(--text-secondary); background: rgba(136,153,170,0.12); }
.follow-action.busy { color: var(--accent); background: rgba(79,195,247,0.14); }
.follow-action.idle { color: var(--text-secondary); background: rgba(136,153,170,0.12); }
.follow-status-section {
  padding: 8px 0;
  border-top: 1px solid rgba(255,255,255,0.06);
}
.follow-status-section h4 {
  margin: 0 0 6px;
  color: var(--text-secondary);
  font-size: 11px;
  letter-spacing: 1px;
}
.follow-status-row {
  display: grid;
  grid-template-columns: 72px minmax(0, 1fr);
  gap: 8px;
  font-size: 12px;
  line-height: 1.6;
}
.follow-status-row span { color: var(--text-muted); }
.follow-status-row b {
  color: var(--text-primary);
  font-weight: 500;
  overflow-wrap: anywhere;
}
```

- [ ] **Step 4: Manual browser check**

Open `http://127.0.0.1:8888/simulation.html`, enable render, click an NPC. Expected: lower-left card is visible and collapsed; clicking header expands categorized state rows.

- [ ] **Step 5: Commit**

```powershell
git add -- apps/game/simulation.html apps/game/js/simulation-main.js apps/game/css/simulation.css
git commit -m "feat: add followed entity status panel"
```

---

### Task 3: 右侧实体列表生命/行为状态

**Files:**
- Modify: `apps/game/js/simulation-main.js`
- Modify: `apps/game/css/simulation.css`
- Test: `apps/game/tools/test-follow-entity-status-ui.mjs`

- [ ] **Step 1: Extend tests for list status**

Append to `apps/game/tools/test-follow-entity-status-ui.mjs`:

```js
console.log('5) entity list status uses life and action together');
{
  const npc = { name: '李妙真', alive: true, actionStatus: 'idle' };
  assert.deepEqual(getLifeStatus(npc, 'npc'), { label: '存活', tone: 'alive' });
  assert.deepEqual(getActionStatus(npc, 'npc'), { label: 'idle', tone: 'idle' });
  const deadNpc = { name: '李妙真', alive: false, actionStatus: 'executing' };
  assert.deepEqual(getLifeStatus(deadNpc, 'npc'), { label: '死亡', tone: 'dead' });
  assert.deepEqual(getActionStatus(deadNpc, 'npc'), { label: '无行动', tone: 'idle' });
}
```

- [ ] **Step 2: Run test to verify it passes with existing helper**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-follow-entity-status-ui.mjs
```

Expected: PASS because Task 1 helper already supports these states.

- [ ] **Step 3: Update entity list rendering**

In `refreshEntityList()`, stop filtering out dead NPCs so dead tracked/listed NPCs can show their state:

```js
const entries = Object.entries(source)
  .filter(([, e]) => e.spatial || e.alive === false)
  .slice(0, 200);
```

Replace the entity item inner HTML with:

```js
const kind = this.entityTab === 'monster' ? 'monster' : 'npc';
const life = getLifeStatus(e, kind);
const action = getActionStatus(e, kind);
div.className = `entity-item${id === followId ? ' following' : ''}${life.tone === 'dead' ? ' dead' : ''}`;
div.innerHTML = `
  <div class="entity-main">
    <div class="e-name">${this._escapeHtml(e.name)}</div>
    <div class="e-meta">${this._escapeHtml(meta)}</div>
  </div>
  <div class="entity-status-stack">
    <span class="entity-life ${life.tone}">${this._escapeHtml(life.label)}</span>
    <span class="e-state ${action.tone}">${this._escapeHtml(action.label)}</span>
  </div>
`;
```

Apply the same life/action stack to `refreshFactionList()` using `kind = 'faction'`.

- [ ] **Step 4: Update list CSS**

Append or modify:

```css
.entity-item.dead { opacity: 0.58; }
.entity-main {
  min-width: 0;
}
.entity-main .e-name,
.entity-main .e-meta {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.entity-status-stack {
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 3px;
  min-width: 76px;
}
.entity-life {
  font-size: 11px;
  line-height: 1.2;
  padding: 1px 5px;
  border-radius: 3px;
}
.entity-life.alive { color: var(--success); background: rgba(102,187,106,0.14); }
.entity-life.dead { color: var(--danger); background: rgba(239,83,80,0.14); }
.entity-life.idle { color: var(--text-secondary); background: rgba(136,153,170,0.12); }
.entity-item .e-state.busy { color: var(--accent); }
.entity-item .e-state.idle { color: var(--text-secondary); }
```

- [ ] **Step 5: Browser check**

Open render view, verify right-side list rows show life label and action label in the red-marked right column; dead NPC rows show `死亡` and `无行动`.

- [ ] **Step 6: Commit**

```powershell
git add -- apps/game/js/simulation-main.js apps/game/css/simulation.css apps/game/tools/test-follow-entity-status-ui.mjs
git commit -m "feat: show entity life and action status"
```

---

### Task 4: Final verification and documentation

**Files:**
- Modify: `docs/README.md`
- Test: browser verification and targeted Node tests

- [ ] **Step 1: Update docs navigation**

In `docs/README.md`:

- Update the “最后更新” note to include `跟随角色状态面板实施计划`。
- Add `2026-06-05-跟随角色状态面板实施计划.md` under `superpowers/plans/` in the tree.
- Add a key-doc row:

```markdown
| 跟随角色状态面板 | `superpowers/plans/2026-06-05-跟随角色状态面板实施计划.md` |
```

- [ ] **Step 2: Run targeted tests**

Run:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; node tools/test-follow-entity-status-ui.mjs; node tools/test-dynamic-event-system.mjs
```

Expected: both scripts pass.

- [ ] **Step 3: Browser verification**

Ensure server is running:

```powershell
$env:PYTHONIOENCODING = "utf-8"; [Console]::OutputEncoding = [Text.Encoding]::UTF8; $OutputEncoding = [Text.Encoding]::UTF8; chcp 65001 > $null; Invoke-WebRequest -Uri 'http://127.0.0.1:8888/simulation.html' -UseBasicParsing -TimeoutSec 5
```

Expected: HTTP 200. Then visually verify in browser:

- render view opens without console-breaking blank screen;
- lower-left follow status card defaults collapsed;
- expanding card shows classified sections;
- right list shows life and action labels;
- panel does not overlap zoom controls or right entity panel.

- [ ] **Step 4: Commit docs and final changes**

```powershell
git add -- docs/README.md docs/superpowers/plans/2026-06-05-跟随角色状态面板实施计划.md
git commit -m "docs: add followed entity status panel plan"
```

If `docs/README.md` was already committed with the plan before implementation, skip staging it here and only verify it remains accurate.

---

## Self-Review

- Spec coverage: covers lower-left categorized default-collapsed panel, right-side life/action labels, data fallbacks, tests, and browser verification.
- Placeholder scan: no banned placeholder markers remain.
- Type consistency: all plan tasks use the same helper names: `getLifeStatus`, `getActionStatus`, `buildTrackedStatusModel`, `statusModelToHtml`.
